# Drift Report — 2026-07-04

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (identical to 2026-07-03 snapshot)
- Field references: 0 dead, 0 orphaned, 53 chronic rogue (unchanged; 0 new; all valid)
- SOP drift: 0 new, 10 standing version gaps, existing CFV task current
- Health checks: 2 pass, 3 not-applicable (headless/unauthenticated run)
- Auto-fixed: 0 items (nothing met the safe unattended auto-fix bar)
- Human review: 0 new items — existing task `rec9ihFqDDSY8DMLZ` already open, current, due today; not duplicated

## 1. Schema Changes
Compared today's snapshot against `monitoring/schema-2026-07-03.json` (most recent prior).

- New tables: none
- Removed tables: none
- New/removed/renamed fields: none
- Type changes: none

Both snapshots hold 112 tables and 4,809 fields, byte-for-byte identical structure. No code breakage risk. INFO only.

## 2. Field Reference Issues
Verified all 369 field IDs + 31 table IDs from `monitoring/reference-map.json` against today's schema.

- **DEAD:** 0. Every config.js field ID and table ID resolves in Airtable.
- **TYPE_MISMATCH:** 0. Schema-to-schema diff shows no type changes on referenced fields.
- **ORPHAN:** 0. config.js field/table counts (369 / 31) match the reference-map exactly; no newly-unused constants.
- **ROGUE (hardcoded IDs in feature js, bypassing config.js):** 53 unique IDs across 11 files, all valid Airtable fields. Identical to the 2026-07-03 chronic set (0 added, 0 removed). Holders: js/dashboard.js (22), js/skills-data.js (18), js/sitemap.js (6), js/arrears.js (6), js/fintable.js (3), js/costs.js (3), js/transactions.js (2), js/reconciliation.js (2), js/pnl.js (2), js/wealth.js (1), js/cashflow.js (1).

reference-map.json is current (config.js field/table counts unchanged since generation on 2026-07-01) — no update needed this run.

No field-reference item met the auto-fix bar. The rogue IDs are a chronic clean-up, not a break; centralising 53 IDs across 11 feature files is a multi-file refactor, out of safe unattended scope.

## 3. SOP Drift

### No new drift this run
The only code changed since the 2026-07-03 report (commits `0d25b9c`, `f8388b6`, `f79c60e`) touched `os/systemisation/index.html` and `workers/agent-runner/worker.js`. Neither maps to any SOP pair in the audit table (os/systemisation has no dedicated SOP entry), so no new SOP-vs-code behaviour drift was introduced on a documented page.

### Standing — CFV SOP drift (already tracked)
The 2026-07-03 CFV finding (early-payment tolerance `CFV_EARLY_PAY_DAYS = 5`; live days-overdue derivation) remains open in Airtable task `rec9ihFqDDSY8DMLZ` (status "To do", due 2026-07-04). Its notes are still accurate. Not re-escalated or duplicated.

### Standing — VERSION_GAP (all SOP-backed pages)
Every page with a SOP still has pageVer ahead of sopVer. Worst offenders unchanged: Tasks-OS (pageVer 1.108 vs sopVer 1.1), Inbound Comms (2.52 vs 2.4), P&L (2.24 vs 2.12), Overview (2.39 vs 2.9). Long-running documentation-lag item folded into the same standing task above.

Note: pageVer and sopVer schemes are not directly numerically comparable; the substantive signal remains the CFV behaviour drift already captured in the task.

## 4. Health Check Results
Ran in a headless, unauthenticated scheduled context (no user login → no PAT, no data load). Expected for an automated run.

- **4a Badge/data consistency:** N/A — `window.allTransactions` undefined (no authenticated data load without PAT).
- **4b Token/auth health:** N/A — `window.PAT` absent (no login in headless run; expected).
- **4c Stale data:** N/A — no dashboard load occurred.
- **4d localStorage dismissal JSON:** PASS — no corrupt `dismissed_`/`cfv_` keys (store empty in fresh context).
- **Bonus — load-time JS errors:** PASS — page served on :8951, zero console errors on load of index.html, confirming no syntax/script-load breakage in the current JS bundle.

## 5. Actions Taken

### Auto-fixed
None. No dead references to comment out, no new rogue IDs to centralise, no simple renames. Nothing met the safe unattended auto-fix bar.

### Escalated to Human Review
None new. The single standing item (SOP docs behind code, incl. CFV logic + 10-page version gap) is already tracked in open task `rec9ihFqDDSY8DMLZ`, due today, with accurate notes. No duplicate created, no update needed.

### Notes
- Schema snapshot saved: `monitoring/schema-2026-07-04.json` (112 tables, 4,809 fields).
- reference-map.json unchanged (config.js stable since 2026-07-01; 369 fields / 31 tables verified against it).
- Quietest run to date: zero schema deltas, zero new field-reference issues, zero new SOP drift, zero new escalations.
