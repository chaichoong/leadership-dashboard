# Drift Report — 2026-07-11

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes, 0 renames (identical to 2026-07-10 snapshot; 114 tables both days)
- Field references: 0 dead, 0 dead tables, 0 orphaned, 54 rogue (chronic, unchanged vs 07-10; all valid/live)
- SOP drift: 0 new stale, 0 new undocumented, 10 standing version gaps (no SOP-mapped feature file changed this cycle)
- Health checks: N/A — headless scheduled run, no authenticated browser
- Auto-fixed: 0 items (nothing met the safe auto-fix bar; reference-map already set-equal to config.js)
- Human review: 0 new items (no new drift; existing CFV-SOP behavioural gap already tracked in `rec9ihFqDDSY8DMLZ`, not re-raised)

Net: clean cycle. The only commit since the last run was the `chore: e2e sweep 2026-07-10` report; no SOP-mapped source file changed, so no field-reference or SOP-behaviour drift was introduced on any documented page.

---

## 1. Schema Changes
Compared today's snapshot (`schema-2026-07-11.json`) against yesterday's (`schema-2026-07-10.json`).

- New tables: **0**
- Removed tables: **0**
- New fields: **0**
- Removed fields: **0**
- Type changes: **0**
- Field renames: **0**

Base holds 114 tables / 4,825 fields, unchanged from 07-10. No schema drift this cycle.

## 2. Field Reference Issues
Verified all 372 mapped field IDs and 31 mapped table IDs in `reference-map.json` against today's schema.

- **DEAD (in code, not in Airtable): 0.** Every mapped field ID resolves to a live field.
- **DEAD tables: 0.** Every mapped table ID resolves to a live table.
- **TYPE_MISMATCH: 0.** No referenced field changed type (corroborated by the zero type changes in §1). Note: `reference-map.json` stores JS-constant names only, not expected types, so type verification is bounded by the day-over-day schema diff, which is clean.
- **ORPHAN (mapped but genuinely unreferenced across js/): 0.** Feature files reference config field IDs by named `F.*` constant, not by raw `fld…` string, which is the intended architecture. Set-equality check confirms `reference-map.json` fields == `config.js` `fld…` IDs exactly (372 == 372, no adds or drops), so no reference-map update was required this run.
- **ROGUE (hardcoded `fld…` in js/ not in the central config map): 54.** Unchanged from the 54 standing at 07-10. All 54 resolve to real, live Airtable fields; none dead. This is a chronic, stable condition, not new drift. Centralising 54 hardcoded IDs across 9 feature files is a multi-file refactor that touches shared `config.js`, not a safe unattended auto-fix, so they are left standing (same call as every prior cycle). Distribution by file:
  - `js/dashboard.js` — Projects/KPI cluster, Tasks, Systemisation Workflows (largest group, ~22 IDs)
  - `js/skills-data.js` — Tenancies, Tenants, Transactions, Costs, Tasks reference set (~20 IDs)
  - `js/sitemap.js` — SOP Update Requests table (6 IDs)
  - `js/arrears.js`, `js/costs.js`, `js/fintable.js`, `js/pnl.js`, `js/reconciliation.js`, `js/transactions.js`, `js/cashflow.js`, `js/wealth.js`, `js/wealth-ratios.js` — Chart of Accounts category/sub-category names + Accounts fields (shared across files)

No rogue reference is dead; every one is live. No new rogue references appeared this cycle.

## 3. SOP Drift
No SOP-mapped feature file changed between the 07-10 snapshot commit (`6149589`) and HEAD (`b7e2d58`). The only change was `monitoring/e2e-sweep-2026-07-10.md`. Therefore no new STALE_SOP or UNDOCUMENTED behaviour could have been introduced on any documented page this cycle.

**VERSION_GAP (pageVer ahead of sopVer)** — 10 standing gaps across the SOP-mapped pages, all pre-existing and consistent with prior cycles:

| SOP file | Page key | sopVer | pageVer |
|----------|----------|--------|---------|
| sop.html | overview | 2.9 | 2.41 |
| sop-cfvs.html | cfv | 1.6 | 1.29 |
| sop-invoices.html | invoices | 2.2 | 2.18 |
| sop-pnl.html | pnl | 2.12 | 2.25 |
| sop-compliance.html | compliance | 1.1 | 1.11 |
| sop-sitemap.html | sitemap | 1.2 | 1.17 |
| inbound-comms-sop.html | comms | 2.4 | 2.52 |
| os/strategy/sop.html | os-strategy | 1.0 | 1.15 |
| os/tasks/sop.html | tasks | 1.1 | 1.108 |
| os/business-plan-builder/sop.html | os-bplan | 1.3 | 1.14 |

These gaps reflect the normal cadence: `pageVer` auto-bumps on every source push, `sopVer` is bumped manually only when the SOP is rewritten. The gaps are a documentation-lag signal, not a code fault. The one gap with confirmed behavioural drift (CFV detection SOP) is already tracked in Airtable task `rec9ihFqDDSY8DMLZ` and is not re-raised to avoid duplicate noise.

## 4. Health Check Results
- 4a. Badge/data consistency — **N/A** (no authenticated browser session)
- 4b. Token/auth health (`window.PAT`) — **N/A** (interactive Airtable auth not available in headless run)
- 4c. Stale-data timestamp — **N/A**
- 4d. localStorage dismissal integrity — **N/A**

This is a scheduled headless run. The dashboard requires interactive PAT auth to populate `window.PAT`, `window.allTransactions`, etc., which cannot happen without a logged-in browser. All four checks skipped, consistent with every prior scheduled cycle.

## 5. Actions Taken
### Auto-fixed
None. No dead references, no dead tables, no type mismatches, no simple rename mismatches. No rogue IDs are safe to centralise unattended (multi-file refactor of shared `config.js`). `reference-map.json` is already set-equal to `config.js`, so no adds were needed. No PR created.

### Escalated to Human Review
None new. No schema changes, no type mismatches, no new SOP behaviour drift, no health-check failures. The chronic 54 rogue references and 10 SOP version gaps are pre-existing and consistent with prior cycles; the one behaviourally-caused gap (CFV SOP) is already tracked in `rec9ihFqDDSY8DMLZ`. No new Airtable task raised to avoid duplicate noise.

### Committed
- `monitoring/schema-2026-07-11.json` (compact snapshot, 114 tables / 4,825 fields)
- `monitoring/drift-2026-07-11.md` (this report)
