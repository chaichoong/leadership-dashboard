# Drift Report — 2026-06-29

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (identical to 2026-06-28 snapshot)
- Field references: 0 dead, 0 orphaned (suppressed, see below), 0 new rogue
- SOP drift: 0 new behavioural (no feature code changed in 24h), 10 chronic version gaps, 13 SOP-coverage gaps
- Health checks: 1 pass (shell), 4 not-verifiable (auth-gated headless)
- Auto-fixed: 0 items (nothing in the safe auto-fix scope)
- Human review: 0 new tasks (existing SOP-drift task `rec9ihFqDDSY8DMLZ` still "To do")

No new drift. Clean run.

## 1. Schema Changes

Live base `appnqjDpqDniH3IRl`: **110 tables, 4,788 fields.** Compared field-by-field against `monitoring/schema-2026-06-28.json`.

- New tables: **none**
- Removed tables: **none**
- Added fields: **none**
- Removed fields: **none**
- Type changes: **none**
- Rename changes: **none**

Snapshot saved to `monitoring/schema-2026-06-29.json`. Schema is byte-for-byte stable day-over-day.

## 2. Field Reference Issues

Source of truth: `monitoring/reference-map.json` (368 fields, 31 tables, 20 records, 11 select choices). Live `config.js` ID counts match the map exactly (368 / 31 / 20 / 11), so the map is current — no regeneration needed.

- **DEAD (referenced in code, missing in Airtable): none.** All 368 mapped field IDs and 31 table IDs resolve to live Airtable fields.
- **TYPE_MISMATCH: none.** Schema is identical to yesterday, so no type drift is possible.
- **ORPHAN: none reported.** Feature files read fields via the `F.*` constants, not raw `fldXXX` literals, so a raw-ID grep produces false orphans. Suppressed, consistent with prior runs.

### ROGUE (hardcoded IDs in feature js, bypassing config.js)
A raw `fld/tbl/rec/sel` grep across `js/*.js` (excluding `config.js`) surfaced 79 candidate IDs. After classification:

- **55 fld/tbl IDs resolve to live fields/tables** — the pre-existing local field-map pattern (used via `getField()` in `dashboard.js`, doc-string template literals in `skills-data.js`, link-field PATCH payloads in `arrears.js`, etc.). All valid, zero broken references.
- **18 genuine record IDs** — 15 in `js/admin-repair-splits.js` (an intentional one-shot console remediation tool for the IMMO LTD May 2026 split linkages, present since 2026-05-07), plus 3 in `cashflow.js` / `arrears.js` / `skills.js`. Expected hardcodes, not drift.
- **3 select-choice IDs in `skills-data.js`** — inside markdown doc-string template literals, not live reads.
- **3 false positives** (`reconForecastKeys`, `reconciledInflows`, `selectedProjectId`) — JS variable names that happen to match the `rec…`/`sel…` regex. Not Airtable IDs.

No NEW rogue files or IDs since 2026-06-28. This is the chronic local-field-map pattern logged INFO on every prior run (06-11, 06-12, 06-25 → 06-28). Not auto-fixed: consolidating into `config.js` would touch ~10 feature files plus shared `config.js` in one pass — beyond the safe unattended auto-fix scope and against the concurrent-edit rule in `CLAUDE.md`.

No rogue IDs in `js/*.js` that fail to resolve against the live schema.

## 3. SOP Drift

**No behavioural re-audit this run.** Zero feature code changed in the last 24h — the only commit on `main` since the 2026-06-28 run is that run's own drift report (`HEAD = 920867e`). The last real feature commit was the meetings merge on 2026-06-26. Behaviour cannot have drifted with no code change.

### Version gaps (10, chronic)
Every page carrying a SOP file shows `pageVer` ahead of `sopVer`. Structural, not new: the auto-bump GitHub Action increments `pageVer` on every source push, while `sopVer` is bumped manually only when the SOP itself is rewritten.

| Page | pageVer | sopVer |
|------|---------|--------|
| overview (Leadership Dashboard) | 2.36 | 2.9 |
| os-strategy | 1.14 | 1.0 |
| tasks | 1.103 | 1.1 |
| cfv | 1.27 | 1.6 |
| invoices | 2.16 | 2.2 |
| pnl | 2.22 | 2.12 |
| comms | 2.51 | 2.4 |
| compliance | 1.11 | 1.1 |
| os-bplan | 1.12 | 1.3 |
| sitemap | 1.16 | 1.1 |

Identical to the 2026-06-27 and 2026-06-28 runs (no pushes since, so no auto-bumps either).

The deep behavioural audit (~30 stale/undocumented items across overview, cfv, invoices, pnl, comms, tasks) was performed 2026-06-11 and is tracked by open Airtable task `rec9ihFqDDSY8DMLZ` ("Drift Monitor: [WARNING] SOP docs behind code on 10 pages"), re-confirmed **still "To do"** this run via Airtable. Not re-listed here to avoid noise.

### SOP coverage gap (INFO, no new task)
13 tabs carry no SOP at all (`sopFile: ''` in PAGE_REGISTRY): money, wealth, income (AR Fixed), ar-variable, costs (AP Fixed), transactions, operations, fintable, systemisation, os-team, content-machine, skills, how-it-works. Whether each needs a published SOP is Kevin's call. Folded under the existing SOP-drift task rather than spawning a duplicate.

## 4. Health Check Results

Static server (`ops-director`, port 8811) launched via preview; `index.html` loaded headless.

| Check | Result | Detail |
|-------|--------|--------|
| App shell load | **PASS** | Title "Operations Director", sidebar, and `PAGE_REGISTRY` (23 entries) all present. `config.js` + `shared.js` parse and execute (`escHtml`, `switchTab` both defined) with **zero console errors** |
| 4a Badge / `allTransactions` consistency | NOT VERIFIABLE | `allTransactions` empty (0) headless — data load is PAT-gated, no token in headless context |
| 4b Token / auth health | NOT VERIFIABLE | `window.PAT` empty headless (expected); real token only exists in an authenticated browser session |
| 4c Stale-data timestamp | NOT VERIFIABLE | No data loaded, so no refresh timestamp to read |
| 4d localStorage dismissal integrity | NOT VERIFIABLE | Dismissal keys only populate in an authenticated session |

Clean, error-free shell load confirms the codebase still parses and the registry is intact. The four data-level checks remain auth-gated headless, identical to prior runs.

## 5. Actions Taken

### Auto-fixed
None. No dead references, no type mismatches, no new rogue IDs, no simple renames — nothing fell inside the safe unattended auto-fix scope (comments / config refs / simple naming). No PR created.

### Escalated to Human Review
No new Airtable tasks created. The only open drift item — SOP documentation lagging code on 10 pages — is already tracked by `rec9ihFqDDSY8DMLZ` (status "To do"), confirmed live this run. Creating a second task would duplicate it.

### Notes
- `STRUCTURE.md` carried an uncommitted local modification at run start (pre-existing, not produced by this monitor). Left untouched.
- `monitoring/reference-map.json` unchanged — live `config.js` ID counts still match the map exactly (368 / 31 / 20 / 11).
