# Drift Report — 2026-07-15

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (vs 2026-07-14)
- Field references: 0 dead, 0 type-mismatch, 0 orphan, 60 rogue (36 `fld` + 5 `tbl` + 19 `rec`, all live, chronic)
- SOP drift: 0 new stale, 0 new undocumented, 11 standing version gaps (was 10 — `prospecting` added)
- Health checks: 1 pass (live site serves clean, zero console errors), 3 N/A (auth-gated, headless run)
- Auto-fixed: 0 items (no PR — nothing safely auto-fixable this cycle; `reference-map.json` already in sync)
- Human review: 0 new items (2 prior drift items remain tracked in Airtable, not re-raised)

**Verdict: clean cycle. No new drift. No action required.**

## 1. Schema Changes
Fetched full base schema via Airtable MCP (`appnqjDpqDniH3IRl`): **117 tables, 4,865 fields**. Saved compact snapshot to `monitoring/schema-2026-07-15.json`.

Diffed against yesterday's snapshot (`schema-2026-07-14.json`):
- **New tables: 0**
- **Removed tables: 0**
- **Field additions: 0**
- **Field removals: 0**
- **Type changes: 0**
- **Renames: 0**

No schema movement in the last 24 hours.

## 2. Field Reference Issues
Verified all mapped IDs in `reference-map.json` (399 fields, 33 tables, 20 records, 11 select choices) against today's schema snapshot.

- **DEAD (in code, not in Airtable): 0.** Every mapped field/table ID resolves to a live field/table.
- **TYPE_MISMATCH: 0.** No referenced field changed type (corroborated by zero type changes in §1). `reference-map.json` stores JS-constant names only, not expected types, so type verification is bounded by the day-over-day schema diff, which is clean.
- **ORPHAN: 0.** Feature files reference config fields by named `F.*` constant, not raw `fld…` string — the intended architecture.
- **ROGUE (quoted Airtable IDs in `js/*.js` not present in `js/config.js`): 60 — chronic, all resolve live.**
  - **36 `fld…` IDs**, all live. Clusters: `js/dashboard.js` (Projects/KPI/Tasks cluster, 23 fld + 1 tbl), `js/sitemap.js` (SOP Update Requests, 6 fld + 1 tbl), `js/arrears.js` (6 fld), and the shared Chart-of-Accounts trio (`fldO4BTJhFv5EsN6i`, `fldii4oUzSfmplihO`, `fldqr09KqLGGYCYkC`) spread across the finance files (`costs`, `pnl`, `reconciliation`, `transactions`, `wealth`, `wealth-ratios`, `cashflow`, `fintable`).
  - **5 `tbl…` IDs**, all live: `tbl5MY9FbImFQfvez` + `tblZ75JgE1wzDP0ps` (`js/ai-assistant.js`), `tblHGNzDmOs59r9QD` (`js/skills.js`), `tblJ3GFnAAoXf99e9` (`js/dashboard.js`), `tbltuZz5Omrpo7t1x` (`js/sitemap.js`).
  - **19 `rec…` IDs**, all live. 15 sit in `js/admin-repair-splits.js` — a one-off data-repair script with hardcoded record targets (expected for that file type). The remaining 4 are single-record anchors in `arrears`, `cashflow`, `skills`, `skills-data`.

Centralising these into `config.js` is a multi-file refactor touching shared feature code — **not a safe unattended auto-fix**. No rogue reference is dead; no new rogue reference appeared this cycle. Note: a naive `(fld|tbl|rec|sel)[A-Za-z0-9]{14}` scan also matches JS variable names (`reconForecastKeys`, `selectedProjectId`, etc.); this report counts quoted string literals only.

**`reference-map.json`:** fully in sync with `js/config.js` (399 fld / 33 tbl / 20 rec / 11 sel — exact match). No update needed; the prospecting field/table IDs were added in the 07-14 run.

## 3. SOP Drift
Compared `PAGE_REGISTRY` `pageVer` vs `sopVer` for all 11 pages that carry a SOP file.

- **STALE_SOP: 0 new.** No SOP found describing behaviour that has been removed from code.
- **UNDOCUMENTED: 0 new.** (The Tasks Activity trail flagged 07-14 remains the standing item — see below.)
- **VERSION_GAP: 11 (up from 10).**

| Page | pageVer | sopVer | SOP file |
|------|---------|--------|----------|
| overview | 2.41 | 2.9 | sop.html |
| os-strategy | 1.15 | 1.0 | os/strategy/sop.html |
| tasks | 1.111 | 1.1 | os/tasks/sop.html |
| cfv | 1.29 | 1.6 | sop-cfvs.html |
| invoices | 2.18 | 2.2 | sop-invoices.html |
| pnl | 2.25 | 2.12 | sop-pnl.html |
| comms | 2.52 | 2.4 | inbound-comms-sop.html |
| compliance | 1.11 | 1.1 | sop-compliance.html |
| os-bplan | 1.14 | 1.3 | os/business-plan-builder/sop.html |
| **prospecting** | **1.4** | **1.3** | **sop-prospecting.html** |
| sitemap | 1.17 | 1.2 | sop-sitemap.html |

`prospecting` is the new 11th gap: a single minor bump (1.3 → 1.4) with no evidence of material behavioural drift. `pageVer` auto-bumps on every code push; `sopVer` only bumps on a manual SOP rewrite, so a persistent gap is the expected steady state, not by itself a defect. Not raised as a task (would be noise for a one-point bump).

**Already-tracked behavioural drift (not re-raised):**
- CFV SOP drift — Airtable task `rec9ihFqDDSY8DMLZ`.
- Tasks Activity trail undocumented in SOP — Airtable task `recesmr9BNH2curGY` (created 07-14).

## 4. Health Check Results
Loaded the live GitHub Pages site (`https://chaichoong.github.io/leadership-dashboard/`) in a headless browser.

- **Page load:** PASS — serves cleanly, `document.title` = "Operations Director", **zero console errors**, auth gate renders correctly.
- **4a Badge count consistency:** N/A — no data loaded (unauthenticated; `window.allTransactions` undefined).
- **4b Token/auth health:** N/A — `window.PAT` absent by design (no interactive login in a background run; not real drift).
- **4c Stale data (>30 min):** N/A — no data session to time.
- **4d localStorage dismissal persistence:** N/A — no authenticated session.

The data/auth checks require interactive PAT login, which is unavailable in a scheduled headless run — consistent N/A across all prior cycles. The one runnable signal (clean page serve, no load-time JS errors) passed.

## 5. Actions Taken
### Auto-fixed
None. Nothing was safely auto-fixable this cycle:
- No dead references to comment out.
- Rogue references are a chronic multi-file refactor, not an unattended fix, and all resolve live.
- `reference-map.json` already in sync with `config.js`.

### Escalated to Human Review
None new. Two prior behavioural-drift items remain open in Airtable (`rec9ihFqDDSY8DMLZ`, `recesmr9BNH2curGY`) and were not duplicated.

### Committed
- `monitoring/schema-2026-07-15.json` (compact snapshot)
- `monitoring/drift-2026-07-15.md` (this report)

---
*Note: A SessionStart hook surfaced a Knowledge OS weekly-sort item (2 files awaiting a filing decision). That needs Kevin present to answer "where does each go" and is out of scope for this autonomous drift run — left for an interactive session.*
