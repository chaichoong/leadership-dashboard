# Drift Report — 2026-06-26

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes, 0 renames
- Field references: 0 dead, 0 type mismatches, 53 rogue (code-level, all valid live fields)
- SOP drift: 10 version gaps (chronic, unchanged); 13 tabs without a SOP (unchanged)
- Health checks: 1 structural pass (clean shell load, no JS errors), 4 data checks not-verifiable (auth-gated headless)
- Auto-fixed: 0 items, no PR. reference-map.json already current (no config.js ID changes)
- Human review: 0 new tasks — existing task `rec9ihFqDDSY8DMLZ` (still "To do") covers SOP drift

**Headline:** No drift. `HEAD` sits at the previous drift-report commit (`4253cf6`); the only change since the 2026-06-25 run was that run's own `pageVer` auto-bump in `config.js`. No feature code, no schema work, no new IDs entered the system in the last 24 hours. The live Airtable schema is byte-for-byte identical to yesterday (110 tables, 4,788 fields). Every one of the 368 field IDs and 31 table IDs in `config.js` resolves to a live field. Zero dead references, zero type mismatches, zero removed tables. The system is clean.

---

## 1. Schema Changes

Compared `schema-2026-06-26.json` against prior snapshot `schema-2026-06-25.json`.

- Tables: 110 (unchanged). Fields: 4,788 (unchanged).
- New tables: none. Removed tables: none. Type changes: none. Renames: none.

A field-by-field diff across all 110 shared tables produced zero differences. Nothing changed upstream in Airtable since the last run.

---

## 2. Field Reference Issues

Source of truth: `monitoring/reference-map.json` (368 fields, 31 tables, 20 records, 11 select choices). config.js ID counts match the map exactly (368 / 31 / 20 / 11), so the map is current — no regeneration needed this run.

- **DEAD (referenced in code, missing in Airtable): none.** All 368 mapped field IDs and 31 table IDs resolve to live Airtable fields.
- **TYPE_MISMATCH: none.** Schema is identical to yesterday, so no type drift is possible.
- **ORPHAN: none reported.** Feature files read fields via `F.constName` (and the other constant objects), not raw `fldXXX` literals, so a raw-ID grep produces false orphans. Suppressed, consistent with prior runs.

### ROGUE (hardcoded field IDs in feature js, bypassing config.js)
53 distinct IDs across feature files. **All resolve to valid live fields — zero broken references.** This is the pre-existing local field-map pattern, not new drift:

| File | Rogue IDs | Notes |
|------|-----------|-------|
| js/dashboard.js | 22 | local Projects/Business field-ID map used via `getField()` |
| js/skills-data.js | 18 | mostly doc-string IDs inside markdown template literals (not code reads) |
| js/arrears.js | 6 | link-field IDs in PATCH payloads (Tasks ↔ Units/Tenancies/Tenants/Properties) |
| js/sitemap.js | 6 | SOP Update Requests field-ID map |
| js/costs.js | 3 | Category / Sub-Category name IDs |
| js/fintable.js | 3 | Accounts field-ID map |
| js/pnl.js | 2 | shared Category / Sub-Category IDs |
| js/reconciliation.js | 2 | shared Category / Sub-Category IDs |
| js/transactions.js | 2 | shared Category / Sub-Category IDs |
| js/cashflow.js | 1 | single link/field ID |

**Why not auto-fixed:** consolidating these into `config.js` would touch ~10 feature files plus the shared `config.js` in one pass — exactly the concurrent-edit / shared-file risk `CLAUDE.md` warns against, and beyond the "comments / config refs / simple naming" scope allowed for unattended auto-fixes. Logged as INFO, consistent with the 2026-06-11, 06-12, and 06-25 decisions. A dedicated single-file-at-a-time session should consolidate the `dashboard.js` and `skills-data.js` maps if Kevin wants to close this.

---

## 3. SOP Drift

No deep behavioural re-audit this run: zero feature code changed since the last drift run, so behaviour cannot have drifted. The mechanical version-gap and coverage checks below are unchanged from 2026-06-25.

### Version gaps (10, chronic)
Every page with a SOP file shows `pageVer` ahead of `sopVer`. Structural, not new: the auto-bump GitHub Action increments `pageVer` on every source push, while `sopVer` is bumped manually only when the SOP itself is rewritten.

| Page | pageVer | sopVer |
|------|---------|--------|
| overview (Leadership Dashboard) | 2.36 | 2.9 |
| os-strategy | 1.14 | 1.0 |
| tasks | 1.103 | 1.1 |
| cfv | 1.27 | 1.6 |
| invoices | 2.16 | 2.2 |
| pnl | 2.22 | 2.12 |
| comms | 2.51 | 2.4 |
| compliance | 1.11 | 1.1 |
| os-bplan | 1.12 | 1.3 |
| sitemap | 1.16 | 1.1 |

(Only change vs last run: `tasks` pageVer 1.102 → 1.103 from the auto-bump. Still ahead of sop 1.1.)

The deep behavioural audit (≈30 stale/undocumented items across overview, cfv, invoices, pnl, comms, tasks) was performed 2026-06-11 and is tracked by open Airtable task `rec9ihFqDDSY8DMLZ` ("To do"). Not re-listed here to avoid noise.

### SOP coverage gap (INFO, no new task)
13 tabs carry no SOP at all (`sopFile: ''` in PAGE_REGISTRY): money, wealth, income (AR Fixed), ar-variable, costs (AP Fixed), transactions, operations, fintable, systemisation, os-team, content-machine, skills, how-it-works. These are mostly recent builds; whether each needs a published SOP is Kevin's call. Flagged as INFO and folded under the existing SOP-drift task rather than spawning a duplicate.

---

## 4. Health Check Results

Static server (`python3 -m http.server 8811`) launched via preview; `index.html` loaded headless.

| Check | Result | Detail |
|-------|--------|--------|
| App shell load | **PASS** | Title "Operations Director", sidebar, and `PAGE_REGISTRY` (23 entries) all present. `config.js` + `shared.js` parse and execute (`escHtml`, `switchTab` defined) with **zero console errors** |
| 4a Badge / `allTransactions` consistency | NOT VERIFIABLE | `allTransactions` empty headless — data load is PAT-gated, no token in headless context |
| 4b Token / auth health | NOT VERIFIABLE | `window.PAT` empty headless (expected); real token only exists in an authenticated browser session |
| 4c Stale-data timestamp | NOT VERIFIABLE | No data loaded, so no refresh timestamp to read |
| 4d localStorage dismissal integrity | NOT VERIFIABLE | Dismissal keys only populate in an authenticated session |

The clean, error-free shell load confirms the `pageVer` auto-bump did not break page parsing or the registry. The four data-level checks remain auth-gated headless, identical to prior runs.

---

## 5. Actions Taken

### Auto-fixed
- **No code changes.** No dead references, type mismatches, or simple rename mismatches existed to fix. No `fix/drift-2026-06-26` branch or PR created.
- **`monitoring/reference-map.json` not regenerated** — config.js ID counts (368 / 31 / 20 / 11) already match the map exactly, so it is current.
- **`monitoring/schema-2026-06-26.json`** written (compact snapshot of all 110 tables / 4,788 fields).

### Escalated to Human Review
- **No new Airtable tasks created.** The only open drift theme (SOP docs behind code) is tracked by `rec9ihFqDDSY8DMLZ`, verified still "To do" this run. A second task would duplicate it. All other findings are healthy or chronic-INFO.

### Open items carried forward
1. SOP behavioural drift + 10 version gaps — task `rec9ihFqDDSY8DMLZ` (open).
2. Rogue field-ID consolidation into config.js (53 IDs, all valid) — INFO, needs a dedicated single-file-at-a-time session if Kevin wants it closed.
3. 13 tabs without a published SOP — Kevin to decide which warrant one.
