# Drift Report — 2026-08-26

Run as phase 3 of `daily-ops`. Queue lock NOT taken (Rule 1). Read-only with respect to code
(Rule 2, reinforced by the caller's override): no repo file edited, no `git add`, commit, pull,
branch, push or PR. Report written to `monitoring/` and left uncommitted.

## Summary

- **Schema changes:** **1 new table** (Creditor Plans), 1 new field, 0 removed, 0 type changes, 0 renames — 122 tables (was 121)
- **Field references:** 0 dead of 422 mapped fields, 0 dead of 35 mapped tables, 0 dead of 10 mapped select choices
- **Rogue IDs:** 949 unique Airtable-shaped candidates across 163 files; **2 unresolvable `tbl` IDs**, both in one superseded scratch script (filed)
- **Coverage gap:** 48 IDs in `config.js` absent from `reference-map.json` — unchanged from yesterday, already open as `20260814-drift-monitor-128`; not refiled
- **SOP drift:** pair audited was `os/tasks/sop.html` ↔ `os/tasks/index.html` — 1 stale claim, 2 undocumented areas, all filed
- **Health checks:** SKIPPED — no browser in an unattended session. Static substitute run instead (§4)
- **Auto-fixed:** 0 — prohibited
- **Filed to the findings queue:** 4 — `20260826-drift-monitor-366` to `-369`
- **Airtable tasks created:** 0 — nothing met the human-review bar

---

## 1. Schema Changes

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat`. The token was never printed. Snapshot saved to
`monitoring/schema-2026-08-26.json` (gitignored) and to
`~/knowledge-os/logs/inbound-triage/scratch/schema-2026-08-26.json`, where the recent series
lives. Compared against yesterday's snapshot in that scratch directory.

| Compared against | Tables | Fields added | Removed | Type changes | Renames |
|---|---|---|---|---|---|
| `schema-2026-08-25.json` | 121 → **122** | 1 | 0 | 0 | 0 |

**New table — expected:**

| Table | ID | Origin |
|---|---|---|
| Creditor Plans | `tbljyVlkq1BXzny2G` | Creditor Management agent (LIVE 25 Aug). Referenced in `~/.claude/agents/creditor-management.md` and in the 25 Aug agent-dispatch logs |

Fields: Creditor, Status, Monthly Amount, Entity, Lane, Agreed Date, Last Contact, Notes, Tasks.
**Zero consumers in this repo** — the agent works through Airtable directly, so that is expected
for now, but the next platform surface that reports creditor plans must add its IDs to
`js/config.js`, not hardcode them.

**New field:** `Tasks → Creditor Plans` (`fldN1DTm15qJW3izY`, multipleRecordLinks) — the reverse
side of the new table's Tasks link. Nothing to reconcile.

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (`reference-map.json` vs schema) | **0 of 422** |
| DEAD tables | **0 of 35** |
| DEAD select choices | **0 of 10** (choices re-derived from the raw schema) |
| TYPE_MISMATCH | still cannot fire — the map stores no expected types (`20260814-drift-monitor-127`, open). Proxy: 0 type changes vs yesterday |
| **Unresolvable `fld`/`tbl` IDs, whole repo** | **2** — both in `scripts/_dispatch_markpeters.py` (filed, see below) |
| IDs in `config.js` NOT covered by the map | **48** (40 `fld`, 3 `sel`, 3 `rec`, 2 `tbl`) — all resolve; unchanged, already filed |

Scan covered `.js`, `.mjs`, `.html` and `.py` across the whole repo, excluding `node_modules`,
`.git`, `monitoring/`, `test-results/`, `playwright-report/` and `.claude/`. Boundary-anchored
regex per the 2026-08-04 correction. Every candidate was resolved against today's snapshot;
nothing was filtered by name.

**The 2 unresolvable IDs are real dead references.** `scripts/_dispatch_markpeters.py:17` and
`:21` query base `appnqjDpqDniH3IRl` with `tblSHUoiqKYOhFP3M` ("tenancies") and
`tblttEkNlh7XQfTj8` ("costs"). Neither exists — the real tables are `tblN51a88qTDB6iMH`
(Tenancies) and `tblx5kvhzNEI5TFlS` (Costs). The IDs were invented, both queries 404, and the
script prints nothing: a silent zero. The follow-ups `_dispatch_markpeters2-5.py` use the
correct IDs, so this is a superseded first attempt — but it sits untracked in `scripts/` where
a future session could rerun it and trust the empty result. Filed as
`20260826-drift-monitor-369` (low, touches code: delete or correct).

Unresolved `sel`-prefixed residue is the known set: `selypOeFtsBePQG1E`, `selwuotKAoizHJl6z`,
`selZazCz6gUJJ8Pl8` are deliberately commented out at `js/config.js:999-1001` (the 2026-08-02
drift fix, correctly left in place), and `selectedProjectId` / `selectRecordAsync` are ordinary
JavaScript identifiers.

Heaviest files by unique IDs: `js/config.js` (534), `scripts/sync-airtable-mirror.py` (163),
`os/team/index-supabase.html` (150), `os/tasks/index.html` (125), `os/tasks/index-supabase.html`
(90), `dashboard-shim.js` (87), `os/tasks/supabase-shim.js` (86), `os/agents/index.html` (80).

## 3. SOP Drift

**Pair audited this run: `os/tasks/sop.html` (v1.4, 19 Aug) ↔ `os/tasks/index.html`.**
Chosen because it is the highest-churn pair since its SOP was last corrected: PR #128 (Post
Deadline enforcement chain, 25 Aug), #123 and #125 (agent data sync + AI Agents page, 24 Aug)
all landed after v1.4. Recent audits covered compliance (18th), pnl (20th), strategy (21st),
skills (24th) and ai-agents (25th).

**What held up.** The 19 Aug rewrite is broadly accurate: the ten views, the Contractor Tasks
tab matched on lower-cased Assignee email (`os/tasks/index.html` contractor filter), auto-
reschedule skipping Some Day AND Hard Deadline with batches of 10 checked individually, the
localStorage key table (all nine keys verified present in code), the approval-gate description
(emoji approve/reject, reply-as-amendment, staleness guard), and the Airtable fetch formula.

**What has drifted — 3 filed:**

- `20260826-drift-monitor-366` (medium, STALE_SOP): SOP §5 still says "The Task Manager does
  not provide a create-task form — it is a read and update interface only. Tasks are created
  directly in Airtable." The page has had `openCreateTaskModal` since 17 Apr 2026 (commit
  `9294d6f` — the same day the SOP was first written), plus quick-add inline and the
  maintenance-task creator (`:8673`). The claim has been false for the SOP's entire life and
  survived the 19 Aug correction sweep. Overlaps still-open `20260809-drift-036`, but filed
  separately because v1.4 re-asserted the claim rather than merely omitting the feature.
- `20260826-drift-monitor-367` (low, UNDOCUMENTED): the §11 field table has no row for Hard
  Deadline (`fldZKzIxgyrQ8CG8a`) even though the flag is load-bearing in auto-reschedule, the
  create modal, the bulk hard/soft toggle (`:542`), the hard/soft filter (`:5488`) and the new
  deadline alarm.
- `20260826-drift-monitor-368` (low, UNDOCUMENTED): §12 predates PR #128. The Approvals view
  now renders a stall/deadline alarm chain (`computeApprovalLoop`, `:9130-9230`): decide >5
  days, amended >48h, due-and-undrafted >24h, and a hard-deadline alarm firing from 3 days out
  or past — whatever the status, INCLUDING Approval — with the UC verification lane excluded
  by name prefix in step with `scripts/loop-health.py`. None of it is in the SOP.

### Version gaps

pageVer/sopVer from `PAGE_REGISTRY`: 13 entries with a real SOP file show a gap (overview
2.86/2.9, os-strategy 1.46/1.1, tasks 1.147/1.4, invoices 2.23/2.19, pnl 2.32/2.26, coa
1.3/1.1, comms 2.73/2.60, compliance 1.15/1.14, systemisation 1.19/1.11, crm 1.1/1.0,
prospecting 1.15/1.5, sitemap 1.24/1.19, skills 1.16/1.0). `cfv` (1.33/1.33) is aligned;
`agents` moved 1.1 → 1.9 in one day (PR #136 churn) against sopVer 1.0. As established
20-25 Aug, this metric carries no signal on its own: `pageVer` is auto-bumped by a GitHub
Action on every push while `sopVer` moves by hand.

## 4. Health Check Results

**SKIPPED — no browser.** Unattended run; starting a preview server would leave a port held
after the session ends, which the session-hygiene rule forbids.

Static substitute, run against source instead:

| Check | Result | Evidence |
|---|---|---|
| 4a badge counts | not verifiable statically | needs a live render |
| 4b PAT present | not verifiable statically | `_dlr_pat` never read; correct behaviour per the 2026-08-03 correction |
| 4c stale data | not verifiable statically | needs a live timestamp |
| 4d dismissal-key shapes | **PASS on catalogue** | Both shapes still present in `js/cfv.js` and matching the SKILL's catalogue: sentinel `'1'` at :240, :784, :941 (`_returned`, `_returnDismissed`); ISO strings at :337, :818, :867, :897 (`_chaseStart`, `cfv_dismissed_*`, `cfv_reflag_dismissed_*`) |

**Re-confirmed today:** `cfv_{id}_startDate` still has three `removeItem` calls (:246, :821,
:935) and no writer anywhere in the repo. Already open three times over as `20260821-drift-276`,
`20260810-drift-055` and `20260816-drift-165`. Not refiled.

## 5. Actions Taken

### Auto-fixed
None. Prohibited by Rule 2 and by the caller's override.

### Filed to the findings queue
| ID | Severity | Item |
|---|---|---|
| `20260826-drift-monitor-366` | medium | Tasks SOP asserts "no create-task form" while the page has three creation paths |
| `20260826-drift-monitor-367` | low | Tasks SOP field reference omits Hard Deadline (`fldZKzIxgyrQ8CG8a`) |
| `20260826-drift-monitor-368` | low | Tasks SOP §12 predates the stall/deadline alarm chain (PR #128) |
| `20260826-drift-monitor-369` | low | Dead invented table IDs in `scripts/_dispatch_markpeters.py` (only unresolvable IDs in the repo) |

Queue depth after this run: 369 findings issued, 207 open.

### Escalated to Human Review
None. The new table is expected (creditor-management agent, live 25 Aug), every mapped
reference resolves, no type mismatch, and no health-check failure, so nothing met the bar for
an Airtable task.
