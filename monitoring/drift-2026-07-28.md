# Drift Report — 2026-07-28

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (vs 2026-07-27)
- Field references: 0 dead, 0 type mismatches, 53 rogue (chronic, all resolve to live fields)
- SOP drift: 0 new stale, 0 new undocumented, **23 version gaps** (was reported as 18 — see correction below)
- Health checks: 4 static PASS (incl. sync gate now GREEN **on the working tree only**), 4 runtime NOT RUNNABLE headless
- Auto-fixed: 0 items (no PR)
- Human review: 0 NEW tasks. Existing task `rec26wc9fTpxgC6ya` updated with a material new fact.

**One line:** Zero code changed since yesterday's report, so the base and every reference are
identical. The one real finding moved: someone wrote the fix for the red pre-push gate yesterday
evening and **left it uncommitted in the main worktree**. The gate passes 62/62 with those edits in
the tree and still fails 2/61 on committed main.

---

## 1. Schema Changes

None. 117 tables, 4,867 fields, byte-identical in name and type against
`monitoring/schema-2026-07-27.json`. No new/removed/renamed tables, no new/removed/renamed fields,
no type changes.

Snapshot written to `monitoring/schema-2026-07-28.json` (fetched via the Airtable meta API with the
PAT at `~/.config/od/airtable_pat`).

---

## 2. Field Reference Issues

### DEAD (in code, not in Airtable)
**None.** All 403 field IDs and 34 table IDs in `monitoring/reference-map.json` resolve to live
fields and tables in today's snapshot.

### TYPE_MISMATCH
**None.** The day-over-day type diff shows 0 changes, so nothing referenced in code changed type.

### ORPHAN
Not escalated. Orphan = a field that exists in Airtable but is unreferenced in code. Across a
117-table / 4,867-field base that is thousands of fields, most belonging to tables the dashboard was
never meant to read. Not an actionable signal for this monitor.

### ROGUE (hardcoded field IDs in `js/` bypassing config.js) — 53 references / 52 unique, chronic

Every one resolves to a live field. Unchanged from 2026-07-27 and 2026-07-26.

| File | Rogue IDs | What they are |
|---|---|---|
| [js/dashboard.js](js/dashboard.js) | 21 | Projects table field set |
| [js/skills-data.js](js/skills-data.js) | 18 | Tenancies / Costs / Tasks fields used by skill definitions |
| [js/sitemap.js](js/sitemap.js) | 6 | SOP Update Requests table |
| [js/arrears.js](js/arrears.js) | 4 | Tasks link fields |
| [js/fintable.js](js/fintable.js) | 3 | Accounts `**Institution`, `**Fintable User` |
| [js/costs.js](js/costs.js) | 1 | Accounts `*Name` (also referenced in fintable.js — 53 refs, 52 unique IDs) |

**Not auto-fixed, per standing policy.** Moving these into `config.js` edits a shared file that
every feature depends on. That is a reviewed refactor, not an unattended one.

### reference-map.json sync
In perfect sync with `js/config.js`: fields 403/403, tables 34/34, records 19/19, selectChoices
11/11 — 0 missing, 0 stale in all four sets. The file's verification stamp was refreshed to
2026-07-28; no ID content changed.

---

## 3. SOP Drift

### STALE_SOP / UNDOCUMENTED
**None new.** `git diff 7024aa1..HEAD -- js/ *.html os/ css/` is empty — not one line of application
code has changed since yesterday's report. The last commit touching any SOP-mapped source file was
2026-07-20 (`js/dashboard.js`), already covered by prior runs. There is no new behaviour for an SOP
to have fallen behind.

### VERSION_GAP (pageVer ahead of sopVer) — 23

**Correction to prior reports.** Earlier runs counted 18 by comparing the versions as decimals. They
are not decimals. `scripts/pre-commit-action.py:97-102` bumps the minor as an integer
(`int(parts[1]) + 1`), so `1.9` → `1.10` and version ordering is tuple-based. Compared correctly,
five more pages are behind, and several gaps are far larger than the old numbers implied — the
Leadership Dashboard SOP is 34 minor versions behind its page, not "ahead".

| Page | pageVer | sopVer | Minor versions behind | New in this count |
|---|---|---|---|---|
| overview | 2.43 | 2.9 | 34 | **yes** |
| cfv | 1.30 | 1.6 | 24 | **yes** |
| invoices | 2.18 | 2.2 | 16 | **yes** |
| sitemap | 1.17 | 1.2 | 15 | **yes** |
| os-bplan | 1.15 | 1.3 | 12 | **yes** |
| tasks | 1.112 | 1.1 | 111 | |
| comms | 2.53 | 2.4 | 49 | |
| operations | 1.43 | 1.0 | 43 | |
| pnl | 2.25 | 2.12 | 13 | |
| os-strategy | 1.16 | 1.0 | 16 | |
| compliance | 1.11 | 1.1 | 10 | |
| costs | 1.8 | 1.0 | 8 | |
| fintable | 1.8 | 1.0 | 8 | |
| systemisation | 1.6 | 1.0 | 6 | |
| skills | 1.4 | 1.0 | 4 | |
| prospecting | 1.4 | 1.3 | 1 | |
| os-team | 1.3 | 1.0 | 3 | |
| wealth | 1.2 | 1.0 | 2 | |
| income | 1.2 | 1.0 | 2 | |
| how-it-works | 2.1 | 1.0 | 1 | |
| money | 1.1 | 1.0 | 1 | |
| ar-variable | 1.1 | 1.0 | 1 | |
| transactions | 1.1 | 1.0 | 1 | |

Eleven of these 23 pages have no `sopFile` at all (money, wealth, income, ar-variable, costs,
transactions, operations, fintable, systemisation, os-team, skills, how-it-works) — their `sopVer`
sits at the 1.0 default and will never move. For those the gap is not "SOP is stale", it is "there
is no SOP". Still a standing doc-maintenance backlog Kevin already knows about, not escalated
per-page.

---

## 4. Health Check Results

| Check | Result |
|---|---|
| Static: 403 config + 52 rogue field IDs all resolve to live fields | **PASS** |
| Static: schema snapshot written and diffed (0 changes) | **PASS** |
| Static: forbidden-pattern scan | **PASS\*** |
| Static: hardcoded AI model IDs (`scripts/check-model-ids.sh`) | **PASS** (exit 0) |
| Static: sync-invariant gate (`npx playwright test tests/sync-invariants/`) | **62 passed, 0 failed — but see the caveat below** |
| Runtime 4a badge/data consistency | NOT RUNNABLE headless |
| Runtime 4b token/auth health | NOT RUNNABLE headless |
| Runtime 4c stale-data timestamp | NOT RUNNABLE headless |
| Runtime 4d localStorage dismissal JSON | NOT RUNNABLE headless |

Runtime checks 4a–4d need the Airtable PAT that the in-app auth flow injects into the live page.
An unattended headless run has no way to obtain it, so they are skipped, same as every prior run.
The Playwright suite is the stronger substitute — it loads the full dashboard against a mocked
Airtable and exercises render, badge counts, auth handling and PATCH payloads across 62 tests.

\* Forbidden-pattern detail (all pre-existing, none new — no code changed since yesterday):
- `console.log` in [js/arrears.js:1130-1163](js/arrears.js:1130) — the `explainCFV()` developer
  console helper. On-demand diagnostic, not a render or data path. **Noted as INFO.** Prior reports
  missed this file; it is not new, it was there yesterday too.
- `console.log` in [js/admin-repair-splits.js](js/admin-repair-splits.js) — admin one-off repair
  utility with intentional progress logging. INFO, as previously.
- `document.write` in [js/arrears.js:1284](js/arrears.js:1284) and
  [js/costs.js:1750](js/costs.js:1750) — both write into a fresh `printWin` popup for statement
  printing, with `escHtml()` applied to interpolated values. Known, accepted pattern.
- `new Function` in [js/dashboard.js:395](js/dashboard.js:395) — the sandboxed custom-KPI formula
  evaluator, which deliberately shadows `PAT`, `fetch`, `localStorage`, `document`, `window` and
  friends as parameters to deny the formula access to them. Known, deliberate design.
- The `eval`/`console.log` strings in [js/skills-data.js](js/skills-data.js) are quoted rule text
  inside skill definitions, not executable code.

### The sync-invariant gate — status changed, and it needs a decision

**The green result above does not describe committed main.** The main worktree has uncommitted
changes to the very test files that were failing:

```
 M tests/sync-invariants/helpers.js               (adds assertByNameFetchesAllowed)
 M tests/sync-invariants/two-way-sync.spec.js
 M tests/sync-invariants/stale-data-protection.spec.js
?? tests/sync-invariants/_diag_capture.spec.js    (untracked, 1 test)
```

All four were last written on **27 Jul, 17:34–17:36** and are still uncommitted at 02:05 on 28 Jul,
roughly 8.5 hours later. The `chore: e2e sweep 2026-07-27` commit landed at 17:43, seven minutes
after the last of those edits, and committed only its own report — so whoever wrote the fix walked
away without committing it and without mentioning it in the sweep report.

The change is exactly the fix the open Airtable task asked for: it replaces the hard
`expect(withoutFieldId.length).toBeLessThanOrEqual(2)` budget with a named allowlist of known
read-only by-name feature fetches, so a genuinely new by-name fetch on any other table still fails
and names the offending URL.

- With those edits in the tree: **62 passed, 0 failed** (verified 28 Jul 02:04).
- On committed main: still **59 passed, 2 failed** — the gate that blocks pushes is still red.

Two things need a human:
1. Review and commit the allowlist change. Until then main's push gate stays red and the fix is
   exposed to the exact stash/sweep hazard `CLAUDE.md` documents for a shared worktree.
2. Decide what `_diag_capture.spec.js` is. It is untracked but sits in the test directory, so it
   runs for whoever has it locally and does not exist for anyone who pulls. The gate is not the same
   gate on two machines. Commit it or delete it.

**Drift Monitor did not commit these.** Committing another session's unattributed work is out of
scope for an unattended run and is explicitly warned against in `CLAUDE.md`.

---

## 5. Actions Taken

### Auto-fixed
None, and nothing was eligible. No dead references, no renames, no type mismatches. The rogue-ID
migration is a reviewed refactor under standing policy, and the one fix that does exist is another
session's uncommitted work.

Only non-report change: refreshed the verification stamp in `monitoring/reference-map.json` to
2026-07-28. No ID content changed.

### Escalated to Human Review

**No new task created** — raising a second record for the same gate would be duplicate spam. The
existing task was updated instead:

- **`rec26wc9fTpxgC6ya`** — "Drift Monitor: [WARNING] Sync-invariant pre-push gate red on main…"
  Status **Upcoming**, due 2026-07-27, **now overdue by 1 day**. Appended a dated note recording
  that the fix is already written and uncommitted, the exact file list, both test results
  (62/62 in-tree vs 59/61 on main), the `_diag_capture.spec.js` question, and the stash risk.
  Due date deliberately left at 2026-07-27 so the slip stays visible rather than being masked.

### Noted, not escalated
- 23 SOP version gaps (chronic doc backlog; 11 of them are pages with no SOP file at all).
- 52 rogue field IDs (chronic; all resolve).
- INFO-level `console.log` in two admin/diagnostic files.
- The prior undercount of version gaps, corrected in §3 above.
