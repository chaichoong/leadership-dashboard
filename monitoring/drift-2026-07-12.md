# Drift Report — 2026-07-12

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (vs 2026-07-11)
- Field references: 0 dead, 0 type-mismatch, 0 orphan, 59 rogue (54 `fld` + 5 `tbl`, all live, chronic)
- SOP drift: 0 new stale, 0 new undocumented, 10 standing version gaps (SOP-mapped pages)
- Health checks: 0 pass, 0 warn, 0 fail — 4/4 N/A (headless run, no interactive PAT auth)
- Auto-fixed: 0 items (no PR — nothing safely auto-fixable this cycle)
- Human review: 0 new items (CFV SOP drift already tracked in Airtable `rec9ihFqDDSY8DMLZ`, not re-raised)

**Verdict: clean cycle. No new drift. No action required.**

## 1. Schema Changes
Live base `appnqjDpqDniH3IRl` fetched via meta API: 114 tables, 4,825 fields. Snapshot saved to `monitoring/schema-2026-07-12.json` (compact format).

Day-over-day diff against `monitoring/schema-2026-07-11.json`:
- New tables: 0
- Removed tables: 0
- New fields: 0
- Removed fields: 0
- Type changes: 0
- Renames: 0

The schema is byte-stable against yesterday. No upstream Airtable structural change occurred in the last 24h.

## 2. Field Reference Issues
Verified all 372 mapped field IDs and 31 mapped table IDs in `reference-map.json` against today's schema.

- **DEAD (in code, not in Airtable): 0.** Every mapped field ID resolves to a live field. Every mapped table ID resolves to a live table.
- **TYPE_MISMATCH: 0.** No referenced field changed type (corroborated by the zero type changes in §1). Note: `reference-map.json` stores JS-constant names only, not expected types, so type verification is bounded by the day-over-day schema diff, which is clean.
- **ORPHAN (mapped but genuinely unreferenced across js/): 0.** Feature files reference config field IDs by named `F.*` constant, not by raw `fld…` string, which is the intended architecture. Set-equality check confirms `reference-map.json` == `config.js` exactly (372 == 372 fields, 31 == 31 tables, no adds or drops), so no reference-map update was required this run.
- **ROGUE (hardcoded IDs in js/ not in the central config map): 59 — unchanged and chronic.**
  - **54 `fld…` IDs** (identical count to 07-10 and 07-11). All 54 resolve to real, live Airtable fields; none dead. Distribution: `js/dashboard.js` (Projects/KPI cluster, Tasks, Systemisation Workflows, ~22 IDs); `js/skills-data.js` (Tenancies/Tenants/Transactions/Costs/Tasks reference set, ~20 IDs); `js/sitemap.js` (SOP Update Requests, 6 IDs); shared Chart-of-Accounts / Accounts fields across `js/arrears.js`, `js/costs.js`, `js/fintable.js`, `js/pnl.js`, `js/reconciliation.js`, `js/transactions.js`, `js/cashflow.js`, `js/wealth.js`, `js/wealth-ratios.js`.
  - **5 `tbl…` IDs**, all live tables: `tbl5MY9FbImFQfvez` (AI Brain Index) and `tblZ75JgE1wzDP0ps` (AI Brain Today) in `js/ai-assistant.js`; `tblHGNzDmOs59r9QD` ((Deprecated) Settings) in `js/skills.js`; `tblJ3GFnAAoXf99e9` (Agent Activity) in `js/dashboard.js`; `tbltuZz5Omrpo7t1x` (SOP Update Requests) in `js/sitemap.js`.

Centralising these hardcoded IDs is a multi-file refactor that touches shared `config.js` and is not a safe unattended auto-fix. Left standing, same call as every prior cycle. **No rogue reference is dead; no new rogue reference appeared this cycle.**

*(The raw `rec`-prefix grep hits in `js/cashflow.js`, `js/costs.js`, `js/reconciliation.js` — `reconciledInflows`, `recomputeCostFrom`, `reconForecastKeys`, etc. — are JS identifiers, not Airtable record IDs, and are excluded.)*

## 3. SOP Drift
No SOP-mapped feature file changed between yesterday's drift-report commit (`3b8480e`) and HEAD (`03af02a`). The only committed change was `monitoring/e2e-sweep-2026-07-11.md`. Working-tree changes are limited to `.claude/launch.json` and `MASTER-PLAN.md` (non-feature files). Therefore no new STALE_SOP or UNDOCUMENTED behaviour could have been introduced on any documented page this cycle.

**VERSION_GAP (pageVer ahead of sopVer)** — 10 standing gaps across the SOP-mapped pages, all pre-existing and consistent with prior cycles:

| SOP file | Page key | sopVer | pageVer |
|----------|----------|--------|---------|
| sop.html | overview | 2.9 | 2.41 |
| sop-cfvs.html | cfv | 1.6 | 1.29 |
| sop-invoices.html | invoices | 2.2 | 2.18 |
| sop-pnl.html | pnl | 2.12 | 2.25 |
| sop-compliance.html | compliance | 1.1 | 1.11 |
| sop-sitemap.html | sitemap | 1.2 | 1.17 |
| inbound-comms-sop.html | comms | 2.4 | 2.52 |
| os/strategy/sop.html | os-strategy | 1.0 | 1.15 |
| os/tasks/sop.html | tasks | 1.1 | 1.108 |
| os/business-plan-builder/sop.html | os-bplan | 1.3 | 1.14 |

These gaps reflect the normal cadence: `pageVer` auto-bumps on every source push, `sopVer` is bumped manually only when the SOP is rewritten. The gaps are a documentation-lag signal, not a code fault. The one gap with confirmed behavioural drift (CFV detection SOP) is already tracked in Airtable task `rec9ihFqDDSY8DMLZ` and is not re-raised to avoid duplicate noise.

*(A wider mechanical scan of PAGE_REGISTRY shows 22 pageVer>sopVer gaps in total, but the extra 12 are non-SOP-mapped pages carrying the default `sopVer: 1.0`, i.e. pages with no dedicated SOP file in the audit mapping. Only the 10 mapped pairs above are in scope.)*

## 4. Health Check Results
- 4a. Badge/data consistency — **N/A** (no authenticated browser session)
- 4b. Token/auth health (`window.PAT`) — **N/A** (interactive Airtable auth not available in headless run)
- 4c. Stale-data timestamp — **N/A**
- 4d. localStorage dismissal integrity — **N/A**

This is a scheduled headless run. The dashboard requires interactive PAT auth to populate `window.PAT`, `window.allTransactions`, etc., which cannot happen without a logged-in browser. All four checks skipped, consistent with every prior scheduled cycle. Not counted as failures.

## 5. Actions Taken
### Auto-fixed
None. No dead references, no simple renames, and the rogue hardcoded IDs are a chronic multi-file condition that cannot be safely centralised unattended. No PR created.

### Escalated to Human Review
None new. The only known behavioural SOP drift (CFV detection) remains tracked in Airtable task `rec9ihFqDDSY8DMLZ`; re-raising would create duplicate noise. No new Airtable task created.

### Housekeeping
- `monitoring/schema-2026-07-12.json` written (114 tables, 4,825 fields).
- `reference-map.json` verified in sync with `config.js` (372 fields, 31 tables) — no update needed.
- Drift report + schema snapshot committed and pushed to main.
