# Daily Ops — Sunday 30 August 2026

**Started** 07:06 BST. **Finished** 21:18 BST. **Wall clock 14h 12m; actual compute ~45 min.**
The Mac suspended mid-run (known clock-skew behaviour). The 09:30 guard will have seen
"started but did not finish". Everything below completed; nothing was skipped.

Because of the suspension, phase 2 read the estate as at ~07:15. Phase 5 re-read the slots
live at 21:18, so the slot lines below are current, not 14 hours stale.

---

## Phase 1 — Readiness

- `job-queue.py rantoday daily-ops` → exit 0, no end mark today, safe to start.
- `job-queue.py ready daily-ops` → ready, first attempt.
- `job-queue.py mark daily-ops` → marked running 2026-08-30T06:06:40.975Z.
- `findings.py reopen --stale` → 0 reopened.
- `check-routines.py` → **exit 0**. Only daily-ops and approved slots used the queue in the
  last 26h (263 queue events; slots seen: ceo-agent, inbound-triage, prod-sweep-weekly,
  prospecting, task-manager). 26 routines known, 13 other jobs also used the queue.
- No progress file existed at start; this was a fresh run.

## Phase 2 — Exceptions (subagent, read-only)

**1. Drift scan** — `com.kevinbrittain.drift-scan` 06:20, **exit 0, CLEAN**. 122 tables,
4,964 fields, 904 repo ids scanned. No exceptions file written. Nothing that can break.

**2. Data invariants** — `com.kevinbrittain.data-invariants` 06:40. **18 PASS, 1 FAIL,
1 WAITING.** No CONTROL_FAILED.
- WAITING: `sop-queue-not-abandoned`, empty population, field names verified live.
- FAIL: `hard-deadline-passed-still-open`, 3 violations, each checked live:
  - `recQxOwx7swgS1Vd4` Council Tax summons 148778 — 30 days past a hard deadline, still
    Upcoming, never actioned → **finding 20260830-exceptions-411 (critical, Kevin decision,
    no code change)**.
  - `recCLYVwUUUueIlOF` net worth statement — low-stakes admin, status touch only.
  - `recM60eTkHuO7fTdV` Sefton HMO fee — carries `Deferred Until` 2026-09-27, a deliberate
    park the invariant cannot see → **finding 20260830-exceptions-413 (medium, touches
    code)**. FIXED and merged today in PR #195.
- No open Airtable task already covered any of the three (dedupe query used `Task Name`,
  not `Name`).

**3. Drive auth** — `com.kevinbrittain.drive-auth` 06:50, verdict **BROKEN**,
`alert_kevin: true`, 4th consecutive morning. API HEALTHY; local mount unreadable (EDEADLK)
across 5 probes over ~10 min. **The subagent then read the same `founder-profile.md` at
07:15 successfully — 200 bytes, no error.** The mount healed between 07:00 and 07:15, so
today's BROKEN was a false alarm: the retry window is shorter than the mount's own recovery
time. Corroborated by ceo-agent, which hit the same EDEADLK at 06:45 and acquired cleanly at
07:07:29 under its 45-minute allowance. → **finding 20260830-exceptions-412 (high, touches
code)**. FIXED and merged today in PR #195.
- Not done: back-testing whether the mount was also readable by 07:15 on 27, 28 and 29 Aug.
  That replay is written into finding 412.

**4. Slot absence** — every approved slot ran in the window, all rc=0.
- inbound-triage 09:00 / 13:18 / 17:29 (yesterday), 17:56 today
- task-manager 09:23 / 13:00 / 17:04 (yesterday), 17:26 today
- prospecting 09:48 yesterday, 10:07 today
- ceo-agent 06:45 yesterday, 07:16 today
- prod-sweep-weekly 11:00 — correctly skipped Saturday; **ran today Sunday 11:03, rc=0**
- **uc-check 08:00 did not run, and this is NOT a break.**
  `~/Library/LaunchAgents/com.kevinbrittain.uc-check.plist` carries `Disabled = true` and is
  not loaded. Matches finding 20260827-phase-2-378, closed as "turned off deliberately on
  Kevin's instruction, 27 Aug estate audit". `uc-notifier-watchdog` is off the same way.
  Its log directory has been empty since 26 Aug, which is why `check-routines.py` has never
  listed it.

## Phase 3 — Calendar work

**Skipped, none due.** Today is Sunday 30 August.
- Monthly rent due-date advance: due on the 1st. Not today.
- post-manager-weekly: due Mondays. Not today.
- update-master-prompt-quarterly: due 1 Jan / Apr / Jul / Oct. Not today.

## Phase 4 — Fix

### PR #149 (27 Aug) — MERGED, and it broke main

Both stuck PRs were `CONFLICTING`/`DIRTY` — merge conflicts, **not** a red gate and not a
protected path. `fixer-merge.py check` said #149 had `protected: []`, `mayAutoMerge: true`.

Five conflicts resolved: main's newer `schema-baseline.json`; the branch's shared
`slot-postrun.sh` epilogue with main's 28 Aug KEY-form leak patterns carried into it; the
test asserting both intents. A pre-commit `pageVer` bump that touched `js/config.js` (a
protected path) had to be reverted to let the gate run. Gate reported green and merged.
3 findings landed as fixed. Merged as `2f15131`.

**It silently reverted 28–29 Aug work.** The auto-merge dropped `agent-tools.sh` sourcing,
the Gmail sentcheck pre-read, Roy's `handover-property` lane and `clear-alerts` from
`inbound-triage-run.sh`, and loosened the failure pattern to a bare `401`. **23 tests failing
across 5 files on origin/main.**

### ROOT CAUSE — the gate has never tested a PR

`fixer-merge.py` `run_gate()` calls `sh(["npx","vitest","run"])` and the Playwright suite with
`cwd=REPO`, where `REPO` is the script's own repo root — **the main checkout's working tree.**
It never checks out the PR branch and never builds the merge result.

It ran 1745 tests against a 1748-test branch: it never saw the PR's code at all. So it can
neither catch a PR that breaks main nor pass one that fixes it, and **every green it has
reported since 29 Aug proved nothing.**

Verified independently by daily-ops, not taken on the subagent's word:
- `grep` on `scripts/fixer-merge.py`: line 63 `def sh(args, cwd=REPO...)`, line 85 `run_gate()`
  uses it unchanged; no checkout, no worktree, no merge-result build anywhere in the function.
- Clean worktree at `origin/main` (`2f15131`), `npx vitest run` → **23 failed / 1722 passed
  (1745), 5 test files failed.** Main was genuinely red.

→ **finding 20260830-queue-fixer-414 (critical).** `fixer-merge.py` is itself on a protected
path, so the repair needs Kevin.

### PR #195 — the repair. Gate refused it; daily-ops verified it properly and merged

The subagent rebuilt the three slot runners from main's feature-complete `d389aa5` versions
with only #149's two intended additions re-applied, rewired `runner-failure-reporting.test.js`
to assert the contract where it now lives, and pushed the reviewed `ceo-brief-morning-check`
SKILL live. It also carried findings 412 and 413.

`fixer-merge.py merge --pr 195` went **RED — for exactly the reason above**: it was running
the red main checkout's tests, not the PR's. The subagent left it open, did not force it, did
not use `SKIP_SYNC_TESTS`, and commented the evidence on the PR. Correct behaviour.

**daily-ops then did the gate's job the way it should have been done**, on the actual merge
result rather than on the main checkout:
- `fixer-merge.py check --pr 195` → `protected: []`, `mayAutoMerge: true`. 8 files:
  `monitoring/daily-ops-2026-08-29.md`, `scripts/agent-slot-run.sh`,
  `scripts/check-data-invariants.py`, `scripts/drive-auth-check.py`,
  `scripts/inbound-triage-run.sh`, `scripts/task-manager-run.sh`,
  `tests/drive-auth-vault.test.js`, `tests/runner-failure-reporting.test.js`.
- Built a detached worktree at `origin/main` and merged `pull/195/head` into it (clean, 0
  conflicts, 486 insertions / 32 deletions).
- **`npx vitest run` → 116 files passed, 1748/1748 passed.**
- **`npx playwright test tests/sync-invariants/ --reporter=dot` → 295 passed (3.4m).**
  Both halves green, per the gate's own standard.
- Merged: `gh pr merge 195 --squash --delete-branch` → state MERGED, 2026-08-30T20:17:06Z,
  head `4e4d643`. (`gh` reported a local-branch delete failure only, because a worktree held
  the branch; the merge itself succeeded and was confirmed against the API.)
- `findings.py land --pr 195` → 2 findings landed as fixed.
- **Post-merge confirmation on a fresh worktree at the merged `origin/main` (`4e4d643`):
  116 files passed, 1748/1748.** Main is green.

### Reports committed

`monitoring/daily-ops-2026-08-29.md` committed as a new file in #195. Nothing overwritten.
`monitoring/e2e-sweep-2026-08-30.md` was written by the weekly sweep and is still uncommitted
in the main checkout — it goes in tomorrow's fixer PR.

### Phase 4 tally

- PRs merged: **2** (#149, #195). PRs open at end: **1** (#163).
- Findings: 3 landed fixed from #149, 2 landed fixed from #195, 1 new critical filed (414),
  2 new filed in phase 2 (411 critical, 413 medium — 413 now fixed).
- Open findings: **227** (6 critical, 59 high, 119 medium, 43 low). Was 228 at start.
- Cap respected: one new PR opened by the fixer this run.

### PR #163 (28 Aug) — untouched, waiting for Kevin

`fixer-merge.py check --pr 163` → `mayAutoMerge: false`, protected hit on
`scripts/slack-automation/approvals.js` (rule `scripts/slack-automation/`). It is also
`CONFLICTING` with main and will need a rebase before it can land. Left entirely alone, as
the protected list intends. This is the one legitimate "waiting for Kevin" fixer PR.

## Phase 5 — Report

`loop-health.py` → **exit 0**. 494 tasks read, 152 agent-linked. Needs Kevin 86. Done in last
7 days 188. **NOT MOVING: 12**, in the order printed:

1. INBOUND: Council Tax summons - account 850162291 - Summons 14877 — hard deadline
   2026-07-31 passed 30 days ago, still open
2. Update net worth statement — hard deadline 2026-08-01 passed 29 days ago, still open
3. INBOUND: Sefton HMO licence fee overdue 23 Viola Street Bootle — hard deadline 2026-08-28
   passed 2 days ago, still open
4. Change of Address for [name redacted] — hard deadline 2026-08-31, 1 day away
5. Change of address for [name redacted] to 6CP — hard deadline 2026-08-31, 1 day away
6. Pay Burnley Council - CT Arrears_22 Newton Street — waiting on decision 15 days
7. INBOUND: BW Legal - Our response to your query — waiting 12 days
8. INBOUND: BW Legal query resolution — waiting 12 days
9. INBOUND: Incoming SMS from +4477XXXXX077 — waiting 11 days
10. INBOUND: SMS reply from +4475XXXXX747 — waiting 8 days
11. Warm lane second touch: approve 18 close-the-loop emails — waiting 5 days
12. Prospecting deliverability check: the emails ARE landing (97.6%) — waiting 5 days

### Other live items surfaced by the slots today

- **ceo-agent**: the Slack transcript endpoint returns June 2026 data regardless of the
  timestamp requested — a live endpoint bug, not a quiet day. Drive and session transcripts
  compensated. Also flagged: the AI Time and Money Saved card shipped, but 86% of tasks use
  the hardcoded 15-minute default, so the figure is weakly grounded. No ruling yet.
- **prod-sweep-weekly**: the browser walk was **BLIND** — `mcp__Claude_Browser` tools were not
  available in that session. Last full clean walk 2026-08-26, 28/28 PASS. Production is up, so
  no DOWN alert was sent. Report at `monitoring/e2e-sweep-2026-08-30.md`.
- **prospecting**: 0 inbound replies across the 100 most recent GHL conversations. One
  approved prospect needs Kevin's own first contact — Gemma Claire Cooper, no email, no GHL
  route, signal came from Facebook/Messenger.
- **task-manager**: score written — 171 stuck against a target of 0; 284 open; 81 with Kevin.

### Housekeeping noted, not acted on

Nine git worktrees exist under `.claude/worktrees/` (queue-fixes 22–28 and 30, plus
settings-honest), against a documented working limit of three, and a stray detached worktree
at `/private/tmp/gate-bisect`. Several hold merged or abandoned branches. Not cleared this
run because `worktree.sh done` refuses where work would be lost and that needs a judgement
call per workspace.

### Run-length exception

The routine's own rule is that a run over ~40 minutes means something has gone wrong. Compute
time was about 45 minutes, which is close to normal for a run that had to resolve two
conflicted PRs and re-run the full suite three times. The 14-hour wall clock is the Mac
suspending mid-run, not work in progress.
