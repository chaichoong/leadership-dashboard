# Drift Report — 2026-07-25

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (117 tables / 4,867 fields, identical to 2026-07-24)
- Field references: 0 dead, 0 type mismatches, 52 rogue in `js/` (chronic, all resolve to real fields)
- SOP drift: 0 new stale findings, 11 version gaps, 14 pages with no SOP at all
- Health checks: static checks pass; 4 runtime browser checks not runnable headless (no live auth)
- Auto-fixed: 0 code items. `reference-map.json` still in sync with `js/config.js` (config unchanged since 2026-07-24); date stamp only
- Human review: 0 new tasks. All findings are chronic and already tracked from prior runs

Quiet run. Airtable is unchanged for a third consecutive day. Every repo change since the last
run belongs to the Supabase shadow build, the CRM (supabase) twin, a shared.js null-safe fix, or
docs/monitoring. None of it touches SOP-mapped feature behaviour.

## 1. Schema Changes
No changes against yesterday's snapshot (`schema-2026-07-24.json`).

- New tables: 0
- Removed tables: 0
- New fields: 0
- Removed / renamed fields: 0
- Type changes: 0

Snapshot saved to `monitoring/schema-2026-07-25.json` (117 tables, 4,867 fields).

## 2. Field Reference Issues

### DEAD: 0
All 403 field IDs and 34 table IDs in `js/config.js` (via `reference-map.json`) exist in today's
Airtable schema. Every rogue ID elsewhere in the codebase also resolves to a live field. Nothing
has been deleted upstream.

### TYPE_MISMATCH: 0
No field changed type between yesterday's snapshot and today's. No code path can have been
silently re-typed under it.

### ROGUE: 52 unique `fld…` in `js/` (unchanged from yesterday)
Hardcoded field IDs that bypass `js/config.js`. All 52 resolve to live fields. Concentration:

| File | Rogue IDs | What they are |
|---|---|---|
| [js/dashboard.js](js/dashboard.js) | 21 | Projects table field set |
| [js/skills-data.js](js/skills-data.js) | 18 | Tenancies/Costs/Tasks fields used by skill definitions |
| [js/sitemap.js](js/sitemap.js) | 6 | SOP Update Requests table |
| [js/arrears.js](js/arrears.js) | 4 | Tasks link fields |
| [js/fintable.js](js/fintable.js) | 3 | Accounts `**Institution`, `**Fintable User` |
| [js/costs.js](js/costs.js) | 1 | Accounts `*Name` |

Also present (not new): hardcoded record IDs in `js/admin-repair-splits.js` (15, a one-off targeted
repair script) and a handful of table IDs (`tblZ75JgE1wzDP0ps` AI Brain feed, etc.).

Not auto-fixed, consistent with standing policy: moving these IDs into `config.js` touches a shared
file across many features and is a reviewed refactor, not an unattended one.

### ORPHAN
Not separately escalated, per prior runs. 4,867 Airtable fields against 403 referenced ones makes a
raw "unreferenced field" count noise rather than signal — most of the base is driven by Airtable
interfaces and automations, not by this repo.

## 3. SOP Drift

### STALE_SOP / UNDOCUMENTED: 0 new
Changes since the last run are confined to the Supabase shadow build, the CRM (supabase) twin,
`js/shared.js` (null-safe `getField` guard), the prospect-daily skill, and docs/monitoring. None of
these change behaviour documented in an SOP-mapped feature file (dashboard, cfv, invoices, pnl,
sitemap, compliance, comms, strategy, tasks, plan builder). No new behavioural drift.

### VERSION_GAP: 11 (chronic — SOPs lag page versions)
| Page | pageVer | sopVer | SOP file |
|---|---|---|---|
| overview | 2.43 | 2.9 | sop.html |
| os-strategy | 1.16 | 1.0 | os/strategy/sop.html |
| tasks | 1.112 | 1.1 | os/tasks/sop.html |
| cfv | 1.30 | 1.6 | sop-cfvs.html |
| invoices | 2.18 | 2.2 | sop-invoices.html |
| pnl | 2.25 | 2.12 | sop-pnl.html |
| comms | 2.53 | 2.4 | inbound-comms-sop.html |
| compliance | 1.11 | 1.1 | sop-compliance.html |
| os-bplan | 1.15 | 1.3 | os/business-plan-builder/sop.html |
| prospecting | 1.4 | 1.3 | sop-prospecting.html |
| sitemap | 1.17 | 1.2 | sop-sitemap.html |

### Pages with no SOP at all: 14
`money`, `wealth`, `income`, `ar-variable`, `costs`, `transactions`, `operations`, `fintable`,
`systemisation`, `os-team`, `content-machine`, `skills`, `ai-brain`, `how-it-works`.

These are documentation debt, not runtime risk. Tracked from prior runs; not re-escalated daily.

## 4. Health Check Results

| Check | Result |
|---|---|
| 4a. Transactions data present | NOT RUNNABLE headless (needs live auth) |
| 4b. Token/auth health | NOT RUNNABLE headless |
| 4c. Stale-data timestamp | NOT RUNNABLE headless |
| 4d. localStorage dismissal JSON | NOT RUNNABLE headless |
| Static: `getField` null-safe guard present | PASS (js/shared.js:205) |
| Static: no `console.log`/`debugger` in changed js | PASS (shared.js) |
| Static: reference-map in sync with config.js | PASS (config unchanged) |
| Static: all 52 rogue + 403 config field IDs resolve to live fields | PASS |

Runtime browser checks require the Airtable PAT injected by the auth flow, which is not available in
this unattended background run. Static equivalents were run instead and all pass.

## 5. Actions Taken

### Auto-fixed
None. No dead references, no type mismatches, no new rogue IDs. The rogue-ID refactor remains a
reviewed change per standing policy, not an unattended one.

### Escalated to Human Review
No new Airtable tasks created. Every finding (52 rogue IDs, 11 version gaps, 14 SOP-less pages) is
chronic and was surfaced in prior runs; creating a duplicate task each day would be noise. The
standing SOP/version-tracking debt remains the only open item, already logged from earlier runs.
