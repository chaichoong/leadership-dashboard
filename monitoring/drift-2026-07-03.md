# Drift Report — 2026-07-03

## Summary
- Schema changes: 2 new tables, 1 new field, 0 removed, 0 type changes
- Field references: 0 dead, 0 orphaned, 53 chronic rogue (down from 55; 0 new)
- SOP drift: 1 new stale (CFVs), 10 standing version gaps, 0 new undocumented beyond CFV
- Health checks: 1 pass, 1 partial, 3 not-applicable (headless/unauthenticated run)
- Auto-fixed: 0 items (nothing met the safe unattended auto-fix bar)
- Human review: 1 item — existing task `rec9ihFqDDSY8DMLZ` updated (not duplicated)

## 1. Schema Changes
Compared today's snapshot against `monitoring/schema-2026-07-01.json` (most recent prior).

**New tables (additive, expected):**
- `tblZ75JgE1wzDP0ps` — **AI Brain Today** (10 fields). Private feed for the AI Brain module Today tab (matches known project work).
- `tblJ3GFnAAoXf99e9` — **Agent Activity** (7 fields). Backs the agent-runner worker / systemisation activity log.

**New field:**
- `Systemisation Workflows.Agent Activity` (multipleRecordLinks) `[fldBAkVbUtcaBOYyf]` — reverse link to the new Agent Activity table.

**Removed tables/fields:** none.
**Type changes:** none.
**Renames:** none.

Assessment: all additions are additive and expected from recent commits (AI Brain data bridge, agent-runner). No code breakage risk. INFO only.

## 2. Field Reference Issues
Verified all 369 field IDs + 31 table IDs from `monitoring/reference-map.json` against today's schema.

- **DEAD:** 0. Every config.js field ID and table ID resolves in Airtable.
- **TYPE_MISMATCH:** 0. Schema-to-schema diff shows no type changes on referenced fields.
- **ORPHAN:** 0 new. config.js field count unchanged (369) since the reference-map was generated 2026-07-01, so no newly-unused constants introduced.
- **ROGUE (hardcoded IDs in feature js, bypassing config.js):** 53 unique IDs across 11 files, all valid Airtable fields. This is the known chronic set (was 55 on 2026-07-01; now 53 — two centralised, none added). Top holders: js/dashboard.js (22), js/skills-data.js (18), js/sitemap.js (6), js/arrears.js (6).

reference-map.json is current (config.js field/table counts unchanged since generation) — no update needed this run.

No field-reference item met the auto-fix bar. The rogue IDs are a chronic clean-up, not a break; per prior runs they are left in place (centralising 53 IDs across 11 feature files is a multi-file refactor, out of safe unattended scope).

## 3. SOP Drift

### NEW this run — STALE_SOP: CFVs (sop-cfvs.html)
Commit `7d1006f` (2026-07-03) changed CFV detection logic in `js/cfv.js` (plus `js/arrears.js`, `js/dashboard.js`). `sop-cfvs.html` was last substantively edited 2026-04-30 and does not describe the new behaviour:
1. **Early-payment tolerance** — `CFV_EARLY_PAY_DAYS = 5`. A rent payment up to 5 days early still counts toward that cycle, so no false-positive CFV is raised.
2. **Live days-overdue** — days overdue is now derived from the actual last reconciled payment / next unpaid monthly due date, not a frozen localStorage start date captured when the CFV was first covered. The old frozen-count behaviour is removed.

→ Escalated (folded into the standing SOP-drift task, see §5).

### Standing — VERSION_GAP (all 10 SOP-backed pages)
Every page with a SOP still has pageVer ahead of sopVer. Worst offenders unchanged: Tasks-OS (pageVer 1.108 vs sopVer 1.1) and Inbound Comms (2.52 vs 2.4). This is the long-running documentation-lag item already tracked in Airtable; no new page entered the gap this run beyond the CFV logic change above.

Note: the version schemes for pageVer and sopVer are not directly comparable numerically; the substantive signal is the CFV code-vs-SOP behaviour drift called out above.

## 4. Health Check Results
Ran in a headless, unauthenticated scheduled context (no user login → no PAT, no data load). This is expected for an automated run.

- **4a Badge/data consistency:** N/A — `window.allTransactions` undefined (no authenticated data load without PAT).
- **4b Token/auth health:** N/A — `window.PAT` absent (no login in headless run; expected).
- **4c Stale data:** N/A — no dashboard load occurred.
- **4d localStorage dismissal JSON:** PASS — no corrupt `dismissed_`/`cfv_` keys (store empty in fresh context).
- **Bonus — load-time JS errors:** PASS — page served on :8951, zero console errors on load, confirming no syntax/script-load breakage in the current JS bundle.

## 5. Actions Taken

### Auto-fixed
None. No dead references to comment out, no new rogue IDs to centralise, no simple renames. Nothing met the safe unattended auto-fix bar.

### Escalated to Human Review
- **[WARNING] SOP docs behind code** — updated existing open task `rec9ihFqDDSY8DMLZ` (status "To do") rather than creating a duplicate. Added today's CFV finding (early-pay tolerance + live days-overdue), refreshed the title, and bumped the due date to 2026-07-04. Action for Kevin/team: rewrite the sop-cfvs.html detection + days-overdue sections to match `js/cfv.js`, then bump sopVer; the standing 10-page version-gap clean-up remains folded into the same task.

### Notes
- Schema snapshot saved: `monitoring/schema-2026-07-03.json` (112 tables, 4,809 fields).
- reference-map.json unchanged (config.js stable since 2026-07-01).
