# Drift Report — 2026-07-27

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (vs 2026-07-26)
- Field references: 0 dead, 0 orphaned (not escalated), 52 rogue (chronic, all resolve to live fields)
- SOP drift: 0 new stale, 0 new undocumented, 18 chronic version gaps
- Health checks: 3 static PASS, 1 static FAIL (sync-invariant gate — unchanged from yesterday), 4 runtime NOT RUNNABLE headless
- Auto-fixed: 0 items (no PR)
- Human review: 0 NEW items — the one open finding is already tracked in Airtable task `rec26wc9fTpxgC6ya` [WARNING], created 2026-07-26, still status "Upcoming", due today

**One line:** Nothing changed since yesterday. Schema is identical, every code reference resolves, and
the only real issue — the sync-invariant pre-push gate red on main — is the same one already logged and
still open. No new work created; no duplicate task raised.

## 1. Schema Changes
No changes against `monitoring/schema-2026-07-26.json`. 117 tables, all fields identical in name and
type. Snapshot written to `monitoring/schema-2026-07-27.json`.

## 2. Field Reference Issues

### DEAD (in config.js, not in Airtable)
None. All 403 field IDs and 34 table IDs in `monitoring/reference-map.json` resolve to live fields/tables
in today's schema.

### TYPE_MISMATCH
None. Day-over-day type diff shows 0 changes, so no referenced field changed type.

### ORPHAN
Not escalated. (Orphan = field exists in Airtable but unreferenced in code — thousands across a 117-table
base; not actionable for this monitor.)

### ROGUE (hardcoded field IDs in js/ bypassing config.js) — 52, chronic
All 52 exist in the schema and resolve correctly. Unchanged from 2026-07-26.

| File | Rogue IDs | What they are |
|---|---|---|
| [js/dashboard.js](js/dashboard.js) | 21 | Projects table field set |
| [js/skills-data.js](js/skills-data.js) | 18 | Tenancies / Costs / Tasks fields used by skill definitions |
| [js/sitemap.js](js/sitemap.js) | 6 | SOP Update Requests table |
| [js/arrears.js](js/arrears.js) | 4 | Tasks link fields |
| [js/fintable.js](js/fintable.js) | 3 | Accounts `**Institution`, `**Fintable User` |
| [js/fintable.js](js/fintable.js), [js/costs.js](js/costs.js) | 1 | Accounts `*Name` |

**Not auto-fixed, per standing policy.** Moving these into `config.js` edits a shared file relied on by
many features. That is a reviewed refactor, not an unattended one.

`reference-map.json` is in perfect sync with config.js (0 field IDs missing, 0 stale) — no update needed.

## 3. SOP Drift

No SOP-mapped code file changed in the last 3 days (`git log` on dashboard.js, cfv.js, invoices.js,
pnl.js, sitemap.js, compliance.html, follow-up.html, os/strategy, os/tasks, os/business-plan-builder,
kpi-sources.js → empty). So no new STALE_SOP or UNDOCUMENTED behaviour was introduced.

### VERSION_GAP (pageVer ahead of sopVer) — 18, chronic
SOP files trailing the auto-bumped `pageVer`. Documentation lag, not code drift. Most sit at sopVer 1.0
against an incrementally bumped pageVer.

| Page | pageVer | sopVer |
|---|---|---|
| tasks | 1.112 | 1.1 |
| comms | 2.53 | 2.4 |
| operations | 1.43 | 1.0 |
| pnl | 2.25 | 2.12 |
| os-strategy | 1.16 | 1.0 |
| compliance | 1.11 | 1.1 |
| costs | 1.8 | 1.0 |
| fintable | 1.8 | 1.0 |
| systemisation | 1.6 | 1.0 |
| how-it-works | 2.1 | 1.0 |
| prospecting | 1.4 | 1.3 |
| skills | 1.4 | 1.0 |
| os-team | 1.3 | 1.0 |
| wealth | 1.2 | 1.0 |
| income | 1.2 | 1.0 |
| money | 1.1 | 1.0 |
| ar-variable | 1.1 | 1.0 |
| transactions | 1.1 | 1.0 |

Not escalated individually — this is a standing doc-maintenance backlog Kevin already knows about.

## 4. Health Check Results

Runtime browser checks (4a–4d) require the Airtable PAT injected by the in-app auth flow, which is not
available in an unattended headless run. Static equivalents were run instead.

| Check | Result |
|---|---|
| Static: all 52 rogue + 403 config field IDs resolve to live fields | PASS |
| Static: schema snapshot written and diffed (0 changes) | PASS |
| Static: forbidden-pattern scan (console.log/debugger in normal render/data paths) | PASS* |
| Static: sync-invariant pre-push gate (`npm run test:sync`) | **FAIL — 2 tests (unchanged)** |
| Runtime 4a badge/data consistency | NOT RUNNABLE headless |
| Runtime 4b token/auth health | NOT RUNNABLE headless |
| Runtime 4c stale-data timestamp | NOT RUNNABLE headless |
| Runtime 4d localStorage dismissal JSON | NOT RUNNABLE headless |

*The only console.log hits are in [js/admin-repair-splits.js](js/admin-repair-splits.js) — an admin
one-off repair utility with intentional progress logging, not a normal render/data path. Noted as INFO,
not escalated.

### Sync-invariant gate FAIL — diagnosis (unchanged from 2026-07-26)
`npm run test:sync`: 59 passed, 2 failed.
- `two-way-sync.spec.js:38` — expects ≤2 on-load fetches without `returnFieldsByFieldId=true`; got 3.
- `stale-data-protection.spec.js:82` — same root cause, same count.

Per yesterday's diagnosis (still valid, nothing changed): 2 of the 3 bare fetches are legitimate feature
fetches (AI Brain badge, Agent KPI) that read the response by field NAME on purpose. This is
**test-threshold drift, not a runtime bug** — no user-facing breakage, but it blocks clean pushes to main.
The fix is a reviewed test exception allowlist, already captured in the open Airtable task.

## 5. Actions Taken

### Auto-fixed
None. No dead references, no renames, and rogue-to-config migration is a reviewed refactor under standing
policy — nothing was safe to auto-correct unattended.

### Escalated to Human Review
No NEW task created. The single open finding (sync-invariant gate red on main) is already tracked:

- Airtable task `rec26wc9fTpxgC6ya` — "Drift Monitor: [WARNING] Sync-invariant pre-push gate red on
  main..." — status **Upcoming**, due 2026-07-27. Left as-is to avoid duplicate task spam. Needs a
  reviewed test-exception allowlist (or a config move for the two by-name feature fetches).
