# Daily Ops — Friday 28 August 2026

**Run window:** 07:05:56 – 07:23 BST (~17 minutes). Within the ~40 minute budget.
**Reporting window:** 27 Aug 07:00 → 28 Aug 07:00 (yesterday's three slot rounds + this morning's scripts).
**Phases run:** 1, 2, 3 (skipped, not due), 4, 5. None skipped silently.

---

## Phase 1 — Readiness

| Check | Result |
|---|---|
| `job-queue.py rantoday daily-ops` | exit 0 — no end mark today, safe to start |
| `job-queue.py ready daily-ops` | exit 0 — ready first attempt, no wait needed |
| `job-queue.py mark daily-ops` | marked running 2026-08-28T06:06:06.212Z |
| `findings.py reopen --stale` | 0 findings reopened |
| `check-routines.py` | **exit 0** — only daily-ops and approved slots ran in the last 26h (251 queue events; 26 routines known; 15 other jobs also used the queue). No unapproved routine. |

Progress file written after every phase at `~/knowledge-os/logs/daily-ops-progress.json`.

---

## Phase 2 — Exceptions (subagent, read-only)

### 1. Drift scan (06:20) — exit 1, DRIFT, benign
122 tables / 4,963 fields / 903 repo IDs scanned. **One new field only:** `Tasks.Verdict Reason` (the approval-gate wave 2 work, PR #160). Zero removed, zero renamed, zero retyped. 0 dead mapped IDs of 457. No action needed. Result committed as `monitoring/drift-exceptions-2026-08-28.json`.

### 2. Data invariants (06:40) — exit 1
**17 PASS, 1 WAITING, 1 FAIL.** No CONTROL_FAILED anywhere, so every check is itself sound.

- WAITING: `sop-queue-not-abandoned` — empty population, field names verified live against the table. Genuine empty, not a broken query.
- **FAIL: `hard-deadline-passed-still-open`, 3 violations.** All three are Kevin/task-manager items, not code:
  - `recEBIINzDkEgVEyF` — INBOUND: Council Tax reminder, Ref 23242360, Flat 1 Duckworth. Due 31 Jul, still "Upcoming", 28 days past a hard deadline on a summonsed balance.
  - `recQxOwx7swgS1Vd4` — INBOUND: Council Tax summons, account 850162291, Summons 148778. Due 31 Jul, same position.
  - `recCLYVwUUUueIlOF` — Update net worth statement. Due 1 Aug, 27 days past.

### 3. Drive auth (06:50) — BROKEN, then self-recovered
Mount unreadable at 06:50 (`Errno 11`); API itself healthy. Re-read `founder-profile.md` directly at 07:10 — readable. The 06:50 failure blocked `feed-brain`, `compound-brain` and `publish-brain` overnight; all three are opted into `retryWhenDeferred` and re-fired. No Kevin action.

### 4. Slot absence — the half an arrivals list cannot show

| Slot | Expected | What happened |
|---|---|---|
| `inbound-triage` | 09:00 / 13:00 / 17:00 | **3/3 clean**, rc=0 at 09:44, 14:43, 17:29. 29–104 min late, all queue wait. |
| `task-manager` | 09:00 / 13:00 / 17:00 | **2/3.** rc=0 where it ran; 80 board moves, 129 stuck. The 09:00 round was killed. |
| `ceo-agent` | 06:45 | **Produced nothing on 27 Aug.** |
| `uc-check` | 08:00 | **Correctly off** — plist `Disabled`, `job-schedule.json` `enabled: false`, turned off 27 Aug on Kevin's instruction in the estate audit. Not a gap. |
| `prospecting` | 09:15 | **Killed mid-run.** Start header written, no `done rc=`. |
| `prod-sweep-weekly` | Sundays 11:00 | Not due (Friday). |

**Root cause of both losses: the Mac slept.** `mac-guard` logged hourly to 09:42 BST on 27 Aug then not again until 11:16 — a 94-minute suspension. Both slots in flight were killed. **Neither wrote any row to `job-status.jsonl`, so no surface anywhere shows a failure.** Filed `20260828-exceptions-385` (high).

**ceo-agent:** deferred on the Drive precondition, waited 45 min, gave up. `retry-deferred` then reported "NOT WIRED" hourly for eight runs, because ceo-agent had no `retryWhenDeferred` flag while feed-brain, compound-brain and publish-brain — which share the identical precondition — all do. Filed `20260828-exceptions-386` (high). **Fixed in PR #163 this morning.**

**Live at time of writing:** ceo-agent fired at 06:45 today, held through the precondition wait, and took the lock at 07:06 once the mount woke. Today's brief has its input; yesterday's did not.

---

## Phase 3 — Calendar work

**Not due — skipped, announced.** Today is Friday 28 August.

- Monthly rent due dates: 1st of month only. Not today.
- `post-manager-weekly`: Mondays only. Not today.
- `update-master-prompt-quarterly`: 1 Jan / Apr / Jul / Oct only. Not today.

---

## Phase 4 — Fix (the only phase that writes code)

**PR #163 opened** — `fix/queue-fixes-2026-08-28`, three findings. **Not merged; Kevin reviews.**
Full suite green: **1,496 tests, 0 failures**, no `SKIP_SYNC_TESTS`. Every new test back-tested against the broken behaviour (the approvals set: all 7 fail on `origin/main`). Three existing tests asserted the old behaviour and were updated.

| Finding | Fix | Test |
|---|---|---|
| `20260828-exceptions-386` | `ceo-agent` opted in to `retryWhenDeferred`; `daily-ops` declares an explicit `false` with a reason; `retry-deferred.py` now FAILS on any drive-gated job with no declaration | `tests/retry-deferred-wiring.test.js` |
| `20260814-daily-ops-146` | Approval cards `@mention` the routed approver in the first block and in both `text` fallbacks (the push preview) | `tests/approvals-mention.test.js` |
| `20260820-queue-fixer-266` (part b) | `findings.py close --outcome fixed` now demands `--evidence`; a commit SHA is verified as an ancestor of `origin/main`, so a branch-only SHA is refused | `tests/findings-close-evidence.test.js` |

All three closed `--outcome pending --pr 163`. **Nothing closed as `fixed`, because nothing has landed.**

**Already closed, verified against `origin/main` rather than the ledger:** `20260825-task-manager-board-365` (CONTROL_FIELDS false positive — fixed and live) and `20260820-agent-dispatch-262` (`strip_carry_out_line` is live; remainder deferred).

**Left open, decisions not code (all critical):**
- `20260818-post-manager-weekly-213` — two council tax court hearings, plus a £72,891 final charging order served.
- `20260821-queue-fixer-300` — third-party personal data in the PUBLIC repo's git history.
- `20260823-agent-dispatch-326` — Agile Estates confirmation statement 84 days overdue.
- `20260825-prospect-daily-run-362` — 150 cold emails, zero replies, deliverability unverified.

**Could not fix:** `-377` / `-373` / `-312` live in `~/.claude/`, outside the repo. `-385` needs an orphaned-event reconciler, not an exit trap. `-290` / `-338` would be half a fix in-repo.

**206 findings still open.**

**Reports committed:** `monitoring/daily-ops-2026-08-27.md` and `monitoring/drift-exceptions-2026-08-28.json` (both new files — nothing overwritten). The four 26-Aug reports and `schema-baseline.json` were left to PR #149 to avoid a merge conflict. No `_tmp_*` files and no third-party personal data committed.

### Open fixer PRs — the drain rate

| PR | Opened | Age |
|---|---|---|
| #149 — Queue fixer 2026-08-27, 4 findings | 27 Aug 06:24Z | 1 day |
| #163 — Queue fixer 2026-08-28, 3 findings | 28 Aug 06:22Z | today |

Two open, below the three-PR escalation threshold, so the fixer was allowed to run. **#149 has been unmerged for a full day.** If it is still open tomorrow the count reaches three and the fixer stops writing code entirely.

---

## Phase 5 — Approval loop (`loop-health.py`, exit 0)

**468 tasks read, 137 agent-linked.** Needs Kevin: 50. Done in 7 days: 147.

**NOT MOVING: 10** (order as printed — deliberately not re-sorted by age):

1. INBOUND: RE: Council Tax reminder, Ref 23242360, Flat 1 Duckworth — hard deadline 2026-07-31 passed 28 days ago, still open
2. INBOUND: Council Tax summons, account 850162291, Summons 148778 — hard deadline 2026-07-31 passed 28 days ago, still open
3. Update net worth statement — hard deadline 2026-08-01 passed 27 days ago, still open
4. Change of Address for [name redacted] — hard deadline 2026-08-31, 3 days away
5. Change of address for [name redacted] to 6CP — hard deadline 2026-08-31, 3 days away
6. Pay Burnley Council, CT Arrears, 22 Newton Street — waiting on Kevin's decision 12 days
7. INBOUND: BW Legal, our response to your query — waiting 9 days
8. INBOUND: BW Legal query resolution — waiting 9 days
9. INBOUND: Incoming SMS from +4477XXXXX077 — waiting 8 days
10. INBOUND: SMS reply from +4475XXXXX747 — waiting 5 days

Items 1–3 are the same three records the `hard-deadline-passed-still-open` invariant failed on. Two independent surfaces agree, which is the point of running both.

---

## Summary

- Run took ~17 minutes against a ~40 minute budget.
- Nothing unapproved ran.
- Two slots were killed by a 94-minute Mac sleep on 27 Aug and left no failure trace anywhere.
- ceo-agent lost a full day to a missing retry flag; fixed in PR #163, not yet live.
- Three fixes written today; none in production until Kevin merges #149 and #163.

---

## Postscript — the Mac slept again during this run

The end mark stamped at 06:24:28Z (07:24 BST). The next command in the same session returned at **08:37 BST** — a ~73 minute gap with no work in between. This is the same suspension behaviour that killed `task-manager`'s 09:00 round and `prospecting` on 27 Aug, observed live today, and it is direct evidence for finding `20260828-exceptions-385`.

No impact on this run: the report and the Slack DM were both written and sent before the sleep, and the phase timings above are measured against the end mark, not wall clock. The point is that the sleep is not a one-off from yesterday morning; it happened again today, inside a routine, roughly an hour after the previous instance.

Also filed this morning: `20260828-daily-ops-387` (low) — `job-queue.py mark --note end` writes the end event correctly but prints "marked as running", so an end stamp reads on screen as a start.

---

## Task Manager — 17:00 slot (counts only)

Board: 274 open | 192 stuck | 65 waiting on Kevin | 17 moving | 0 in-flight

Moves made (35 total): 12 route-Creditor-Management | 5 route-Inbound-Comms-Response | 5 route-Analyst | 6 roy-handover | 4 escalate-Kevin | 2 priority-Urgent | 1 leave (false-positive duplicate)

CEO review: 1 submitted, 0 pass, 1 REDO (close rejected — different properties same thread)

Priority corrections: 2 items set to Urgent

Score: 192 stuck (target 0) | 274 open | 122 with Kevin

Remaining backlog: 167 stuck tasks not actioned this slot
