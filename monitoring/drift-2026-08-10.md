# Drift Report — 2026-08-10

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes, 0 renames (119 tables, 4,915 fields)
- Field references: 0 dead, 0 unresolved live IDs, 65 nominally orphaned constants (all explained — see §2)
- SOP drift: 5 stale sections, 4 undocumented behaviours, 24 declared version gaps
- Health checks: 3 pass, 1 VACUOUS, 0 fail
- Findings filed: 9 (`20260810-drift-052` … `060`)
- Airtable tasks created: 0 (deliberate — see §5)
- Commits / pushes / branches / PRs: none

---

## 1. Schema Changes

None. `monitoring/schema-2026-08-10.json` is byte-comparable in shape to
`schema-2026-08-09.json`: same 119 tables, same 4,915 fields, no field added, removed,
renamed, or retyped.

Snapshot taken via the meta API with the PAT at `~/.config/od/airtable_pat`, not the MCP
connector. Base `appnqjDpqDniH3IRl`.

---

## 2. Field Reference Issues

### DEAD: 0

Every one of the 422 field IDs and 35 table IDs in `monitoring/reference-map.json` resolves
against today's snapshot.

### Rogue / unresolved: 0 live

Full-repo scan (`.js`, `.mjs`, `.html`, `.py`; excluding `node_modules`, `.git`,
`monitoring/`, `test-results/`, `playwright-report/`, `.claude/`) with an identifier
boundary on both sides of the match:

- **833 distinct Airtable-shaped IDs** found across the repo
- **829 resolve** against today's schema, its 1,492 select choices, or its 395 view IDs
- **4 do not**, and all four are accounted for:
  - `selectedProjectId` (`js/shared.js:979`, `:1004`) — an ordinary JS variable, the known
    false positive the SKILL warns about
  - `selwuotKAoizHJl6z`, `selZazCz6gUJJ8Pl8`, `selypOeFtsBePQG1E` (`js/config.js:884-886`) —
    already commented out with a `// DRIFT: removed from Airtable — detected 2026-08-02`
    note. Dead, known, and deliberately left visible

`rec*` IDs are not resolvable from a schema snapshot and were not counted as unresolved.

### ORPHAN: 65 constants, 0 actionable

65 of the 361 `fld*` constants declared inside objects in `js/config.js` are never read
outside that file. Broken down, none needs action:

| Group | Count | Verdict |
|---|---|---|
| `ARREARS` | 19 | Deliberate. `js/config.js:224-229` documents that the arrears pipeline was cancelled, 53 stale rows remain, and the maps are kept rather than deleted pending Kevin's call |
| `ARREARSLOG` | 12 | Same decision, same comment block |
| `PROSPECT`, `PKEY`, `PBUDGET`, `BUCKET`, `OBJSTRAT`, `TEAM`, `F` | 33 | Parent objects are heavily used elsewhere (`TEAM` 305 references, `OBJSTRAT` 130); these are individual unused keys, not dead groups |
| `WF_STEP.tasks` | 1 | Inverse-link field, never read by design |

### Coverage gap — NOT re-filed

`js/config.js` now carries **514** Airtable IDs; `reference-map.json` maps **486**. The 28
unmapped IDs (24 `AI Recon Rules` / `AI Recon Audit` fields, `Tasks.Completion Date`,
`Tasks.Estimated Minutes`, `Prospects.Email Subject`, `tblQ9sFD7Fs5CaVwG`) all resolve
cleanly, so nothing is broken — but CHECK 2 reports 100% coverage while covering 95%.

This is already open as **`20260809-drift-031`** (filed yesterday at 22 IDs, now 28). Not
re-filed. The count in that finding is stale but the finding and its fix are unchanged.

---

## 3. SOP Drift

Yesterday's run covered `sop.html`, `sop-cfvs.html`, `sop-invoices.html`,
`inbound-comms-sop.html` and `os/tasks/sop.html`. Today covered the four pairs it did not:
`sop-pnl.html`, `sop-compliance.html`, `sop-sitemap.html`, plus a re-check of the CFV
days-overdue calculation.

### sop-pnl.html vs js/pnl.js

| Type | Detail | Filed |
|---|---|---|
| STALE_SOP | `:183` says the P&L is "built directly from **reconciled** transactions". `buildPnL()` (`js/pnl.js:115-132`) never reads `F.txReconciled`; the code's own health check at `:831` says "P&L counts all categorised transactions" | `052` |
| STALE_SOP + code risk | `:724` says the business picker is hard-coded to two options. `js/pnl.js:647-651` derives it from the Active flag, and `:652` silently repoints the whole P&L at another company if the current one is deactivated | `053` |
| Code defect | `js/pnl.js:295` builds the drill-down Business type-ahead from active businesses only, and `:300`/`:318` resolve the **displayed** value through the same list — a transaction linked to a deactivated business renders a blank Business cell | `054` |
| UNDOCUMENTED | Sync bar, its eight health checks and its Re-run button (which calls `loadDashboard()`) absent from the SOP | `060` |

Checked and **accurate**, no action: the six target constants, the 27-item allow-list and
its 3/9/15 split, all field and table IDs in section 10, the PATCH spec, the four
drill-down scopes, the 24-colour palette, and the AI model (`AI_MODEL_DEFAULT` →
`claude-sonnet-4-6`).

### sop-compliance.html vs compliance.html

| Type | Detail | Filed |
|---|---|---|
| Code defect (latent) | A certificate with a blank Renewal Date falls through `countCert` (`:531-538`) into `active++` while its cell shows grey "No date" (`:389-391`). **Live count 0** — control below | `056` |
| Code defect | Block apartment rows are coloured by a building insurance cert (`:566`) whose cell is hard-coded to "See building" (`:570`), so the red has no visible cause | `057` |
| UNDOCUMENTED | Sync bar, seven checks, Re-run button, and the `+ Task` FAB — while `:186` states "There is no write functionality" | `060` |
| STALE_SOP (not filed) | `:320` documents an Expired rule via `status === 'Expired'`; `compliance.html:493` drops those records before any cert object exists, so that branch is unreachable. Cosmetic — same outcome either way |

**Control for the blank-date defect.** 83 certificate records read with pagination.
Types: EICR 23, EPC 25, GSC 14, Landlord Insurance 9, Fire Alarm 4, Other 4, Emergency
Lighting 3, Lock Code 1. In-scope and not status-Expired: **36**. Of those 36, **0** have a
blank Renewal Date. Twelve records base-wide do, but every one is a type the page filters
out at `:448-451`. Filed anyway because the failure mode is a gas safety certificate
reading green.

### sop-sitemap.html vs js/sitemap.js

| Type | Detail | Filed |
|---|---|---|
| UNDOCUMENTED | The bulk "Update All Out-of-Sync SOPs" button POSTs records into `tbltuZz5Omrpo7t1x` (`js/sitemap.js:711-728`, `:748-784`). Section 7 describes updating an SOP as a manual code edit and never mentions it | `058` |
| STALE_SOP | Hand-typed 9-row registry claiming "12 pages" vs 27 entries in `PAGE_REGISTRY`; version described as the HTML header value when the page renders a GitHub commit count; an "Integrity Check" documenting a title check that has never existed and a Pass/Warning/Fail vocabulary the page never renders | `059` |

### sop-cfvs.html vs js/cfv.js — days overdue

`sop-cfvs.html:364-372` says days overdue comes from a persistent `cfv_{id}_startDate` in
localStorage and warns the counter "continues to climb across months — it does not reset".
`js/cfv.js:264-269` carries a comment explaining that approach was abandoned; `:272-281`
now derives the figure from the last reconciled payment with a 5-day early-payment
allowance. `cfv_{id}_startDate` is only ever removed (`:193`, `:800`, `:914`) and written by
nothing in the repo.

Verified live in Kevin's browser: **46 orphaned `cfv_*_startDate` keys** still sit in
localStorage from the old build, and the six current CFVs read 61, 52, 26, 24, 23 and 13
days from the new calculation. Filed as `055`.

### VERSION_GAP: 24 of 27

`pageVer` auto-bumps on push; `sopVer` is manual, so a gap is the normal state rather than
an event. Not filed individually — it would be 24 findings a day. The widest, which are
where the SOP is most likely to mislead:

| Page | pageVer | sopVer |
|---|---|---|
| tasks | 1.120 | 1.3 |
| comms | 2.56 | 2.4 |
| operations | 1.44 | 1.0 |
| os-strategy | 1.33 | 1.0 |
| pnl | 2.25 | 2.12 |
| invoices | 2.18 | 2.2 |
| sitemap | 1.19 | 1.2 |

Three pages are level: `ceo-brief`, `content-machine`, `ai-brain`.

---

## 4. Health Check Results

Run against the live site in Kevin's authenticated Chrome (`chaichoong.github.io`), per
open finding `20260809-drift-030`, not the preview browser. No localStorage value was read
or printed.

| Check | Result | Evidence |
|---|---|---|
| 4a Badge consistency | **PASS** | CFV sidebar red 3 / orange 2 vs a recount from `detectCFVs()`: 3 `cfv`, 2 `cfv actioned` — match. `potential` (1) is correctly not badged (`js/cfv.js:327-328`). `sitemapBadge` 24 vs 24 declared-version mismatches recomputed from `PAGE_REGISTRY` (27 entries) — match, and `gitSyncData` was null so the fallback path is the one that ran |
| 4b Token health | **PASS** | A PAT is present and non-empty. Value not read, not logged, not returned |
| 4c Data freshness | **PASS** | Page reported "showing data from 3 min ago"; `allTransactions` 8,917, `allTenancies` 94, `allCosts` 180, all read lexically |
| 4d Dismissal-key integrity | **VACUOUS on one shape** | 110 `cfv_*` keys. Date-shaped: **110 checked, 0 invalid**. Sentinel-shaped (`*_returned` / `*_returnDismissed`): **0 checked** — none exist in this browser, so that half of the check proved nothing today |

Two notes on the SKILL's own CHECK 4 text, neither filed (scheduled-task SKILL files are
outside version control — already `20260807-queue-fixer-008`):

- 4c names `_lastDashboardLoad`. No such variable exists anywhere in `js/`. The freshness
  label is built inline at `js/dashboard.js:90`
- The 4d key-shape list is incomplete. A fourth shape, `cfv_{id}_startDate`, exists in
  localStorage (46 keys) and is not in the SKILL's list — it is the dead key behind
  finding `055`

---

## 5. Actions Taken

### Auto-fixed: 0

Not permitted. No file edited, no `git add`, no commit, no push, no branch, no PR.

### Filed to the findings queue

| ID | Severity | Title |
|---|---|---|
| `20260810-drift-052` | high | sop-pnl.html tells finance readers the P&L is built from RECONCILED transactions; the code counts every categorised transaction |
| `20260810-drift-053` | medium | P&L silently repoints at a different company if the selected business is deactivated |
| `20260810-drift-054` | medium | P&L drill-down shows a blank Business cell for a transaction linked to a deactivated business |
| `20260810-drift-055` | medium | sop-cfvs.html documents a localStorage start date for Days Overdue that the code deleted |
| `20260810-drift-056` | medium | A certificate with no Renewal Date is counted as ACTIVE on the compliance KPI bar |
| `20260810-drift-057` | medium | Block apartment rows go red from a building insurance cert the row never displays |
| `20260810-drift-058` | medium | sop-sitemap.html never mentions the button that writes SOP-update records into Airtable |
| `20260810-drift-059` | medium | sop-sitemap.html keeps a hand-typed page registry 15 pages behind PAGE_REGISTRY |
| `20260810-drift-060` | low | The sync bar is the first thing on the P&L and Compliance pages and appears in neither SOP |

### Airtable tasks created: 0 — deliberate, same reasoning as 9 Aug

CHECK 5 says human-review items become Airtable tasks. Everything qualifying today is
already filed as a finding routed to `queue-fixer`. Raising a task as well puts one piece of
work on two routes at once, which Kevin's own rule forbids. The findings queue post-dates
that section of the SKILL.

### The one worth reading first

**`20260810-drift-052`.** `sop-pnl.html:183` tells Kevin the P&L is built from reconciled
transactions. It is not — `buildPnL()` counts every categorised transaction with a date, a
business link and an allow-listed sub-category, reconciled or not. The code's own sync-bar
check says so in plain text at `js/pnl.js:831`. It changes how much the profit figure should
be trusted before a reconciliation pass, and it is the number Kevin reads to judge whether a
business is making money.

### Not done, and why

- No commit, no push, no branch, no PR — superseded instruction from the caller, and Rule 2
  of the SKILL
- Queue lock not taken — Rule 1
- `monitoring/reference-map.json` not regenerated despite the SKILL's closing instruction,
  because that is an edit to a tracked repo file. The gap is carried by `20260809-drift-031`
- Both output files are gitignored anyway (`monitoring/.gitignore:3-4` exclude
  `schema-2*.json` and `drift-2*.md`), so there was nothing to commit. That exclusion is
  deliberate: this repo is public and these files carry tenant and financial data
