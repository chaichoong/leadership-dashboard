# Drift Report — 2026-07-01

## Summary
- Schema changes: 0 new tables, 0 removed tables, 2 new fields, 0 removed fields, 0 type changes
- Field references: 0 dead, 0 type mismatch, 0 true orphan, 0 new rogue (55 chronic rogue IDs unchanged)
- SOP drift: 7 of 10 pairs have material drift, 3 clean, 15 pageVer/sopVer version gaps
- Health checks: 2 pass, 2 limited (unauthenticated headless), 0 fail
- Auto-fixed: 0 items (nothing met the safe unattended bar)
- Human review: 0 new tasks — chronic SOP drift re-confirmed and refreshed on existing task `rec9ihFqDDSY8DMLZ`
- Maintenance: reference-map.json refreshed to 369 fields (added `spendSubs`)

## 1. Schema Changes

Base `appnqjDpqDniH3IRl`: 110 tables, 4,791 fields. Compared against `schema-2026-06-30.json`.

- **New fields (2, both additive link fields for the Income Buckets feature):**
  - `Chart of Accounts - Sub Categories` → `Income Buckets` (multipleRecordLinks) `fldqjAEH5H3e9cYwx`
  - `Income Buckets` → `Spend Sub-Categories` (multipleRecordLinks) `fld6yClkQoMlOkiU4`
- **Removed fields:** none
- **Type changes:** none
- **New/removed tables:** none

These are the two ends of a new Income-Buckets ↔ Sub-Categories link. `Spend Sub-Categories` (`fld6yClkQoMlOkiU4`) is already wired into `js/config.js` as `BUCKET.spendSubs` (js/config.js:304). The reverse link `fldqjAEH5H3e9cYwx` on the Sub-Categories table is auto-created and not referenced in code (harmless). No action.

## 2. Field Reference Issues

Source of truth: `monitoring/reference-map.json`. This run: config.js holds 369 field IDs / 31 tables / 20 records / 11 select choices. The map was 1 field behind (missing the new `spendSubs`); it has been refreshed to 369 and now matches config.js exactly.

- **DEAD (referenced in code, missing in Airtable): none.** All 369 mapped field IDs and 31 table IDs resolve to live Airtable fields.
- **TYPE_MISMATCH: none.** No type changes anywhere in the base vs yesterday, so no referenced field changed type.
- **ORPHAN: none reported.** Feature files read fields via the `F.*` constants, not raw `fldXXX` literals, so a raw-ID grep produces false orphans. Suppressed, consistent with prior runs.

### ROGUE (hardcoded IDs in feature js, bypassing config.js)
A raw `fld` grep across `js/*.js` (excluding `config.js`) surfaced 55 field-ID literals across `dashboard.js`, `skills-data.js`, `arrears.js`, `sitemap.js`, `fintable.js`, `costs.js`, `transactions.js`, `reconciliation.js`, `pnl.js`, `wealth.js`, `cashflow.js`. All resolve to live Airtable fields. This is the chronic local-field-map / doc-string-template-literal pattern logged INFO on every prior run — unchanged this run (no new files or IDs). Not auto-fixed: consolidating into `config.js` would touch ~11 feature files plus shared `config.js` in one pass, which breaks the concurrent-edit rule in `CLAUDE.md` and is beyond safe unattended scope.

## 3. SOP Drift

10 SOP/code pairs audited for material behaviour drift. Result: 3 clean, 7 with material drift, plus 15 pageVer/sopVer version gaps.

### Clean
- **sop-pnl.html vs js/pnl.js** — categories, sign convention, targets, drill-down, PATCH spec all match. Two minor non-material items (business list now Active-flag-derived with hardcoded fallback; one cosmetic bar colour).
- **sop-compliance.html vs compliance.html** — v1.1 rewrite still accurate: cert types, expired-skip, latest-renewal-wins, tag thresholds, block propagation, agent-managed logic, KPI counts all match.
- **os/business-plan-builder/sop.html vs os/business-plan-builder/index.html** — v1.3 faithful: 10 sections/3 groups, 3 providers, XML-fallback capture, context trim, generateLaunchPlan, milestones, export=print, localStorage keys, reset all match.

### Material drift

**sop.html vs js/dashboard.js** — mostly aligned (KPI calcs, balances, occupancy, recon, budget cards all match).
- STALE_SOP: cache spec wrong. SOP says `localStorage` key `_dlr_dashcache_v1`; code uses IndexedDB key `_dlr_dashcache_v2` (js/dashboard.js:9). SOP also contradicts the CLAUDE.md IndexedDB-for-large-caches rule the code correctly follows.
- Minor: Smart Refresh idle logic attributed to dashboard.js but lives in js/shared.js:6-34 (behaviour accurate).

**sop-cfvs.html vs js/cfv.js**
- STALE_SOP: detection replaced. SOP describes a simple current-month reconciled-transaction check; code uses the arrears engine `isCurrentlyInArrears` with tolerance windows (5d Working/Agent, 14d UC) — js/cfv.js:46-84,149-155.
- STALE_SOP: dismissals do NOT auto-expire at a fixed 25 days (stated in SOP §5,8,14). Code expires at next-due-day + tolerance — js/cfv.js:386-397,783-790.
- UNDOCUMENTED: Arrears Pipeline section renders below the CFV table (`renderArrearsSection`) — js/cfv.js:462-464.
- UNDOCUMENTED: "Last Payment" column (date + amount) — js/cfv.js:275-290,553-555.

**sop-invoices.html vs js/invoices.js**
- STALE_SOP: fetch filter documented as `NOT({Status}='Paid')`; code filters `{Status}='Unpaid'` — js/invoices.js:57. Differs for blank/other statuses.
- STALE_SOP: Matched Transaction written as `[txRecordId]` (bare ID array), not `[{id:...}]` as documented — js/invoices.js:992.
- UNDOCUMENTED: bulk actions (checkboxes, select-all, bulk set-business, bulk mark-paid with 10-record chunking) — js/invoices.js:645-804.
- UNDOCUMENTED: inline-edit model changed to always-visible spreadsheet cells (`saveCellInput`); old `editInvField` now effectively dead — js/invoices.js:305-382,560-643.
- UNDOCUMENTED: "Reconcile with Gmail" (`triggerGmailInvoiceReconcile`) — js/invoices.js:158-182,210.

**sop-sitemap.html vs js/sitemap.js** — materially stale.
- STALE_SOP: integrity check is a different mechanism. SOP says fetch each URL, check HTTP 200 + title + header version; code compares GitHub last-commit dates of source vs SOP files to detect git drift — js/sitemap.js:69,99,301.
- STALE_SOP: version display source wrong — comes from git commit count + PAGE_REGISTRY `pageVer`/`sopVer`, not an HTML header — js/sitemap.js:433-445,370-371.
- STALE_SOP: page table hardcoded (10 rows / "12 pages"); code renders dynamically from PAGE_REGISTRY (~24 pages). Page renamed "Site Map & Guides"; SOP still "Site Map & Links".
- UNDOCUMENTED: SOP-update-request queue writing to Airtable + per-page git-status column + attention badge — js/sitemap.js:224-256,346.

**inbound-comms-sop.html vs follow-up.html** — SOP at v2.4 describes v2.3-era behaviour.
- UNDOCUMENTED: expanded label set 11-14 (tenancy docs / Task created / Maintenance task / Maintenance completed) — follow-up.html:3014-3040,2603-2605.
- UNDOCUMENTED: bidirectional task-sync engine `handleLabelTransitionSync` (reopen/reassign, auto-complete-on-move) — follow-up.html:3044.
- UNDOCUMENTED: background completion sweep (5-min timer + on-visible), not on-load — follow-up.html:1137-1155,4514-4591.
- UNDOCUMENTED: accuracy tracking now team-wide via Airtable (log/refresh/purge), localStorage is a cache — follow-up.html:5343,5365,5400.
- UNDOCUMENTED: task creation side-effects (Slack DM, Gmail attachment upload, collaborators) — follow-up.html:2834-2864,3108.
- UNDOCUMENTED: "Polish (Sonnet)" draft button — follow-up.html:6865.
- UNDOCUMENTED: label 11 tenancy-docs auto-link via Haiku — follow-up.html:3182.
- STALE_SOP: dropdown live counts now labels 1,2,3,8,10,12,13 (SOP says 1,2,3,8) — follow-up.html:1157.
- STALE_SOP: maintenance (label 13) tasks default Upcoming/Not Urgent/no due date, not Today/Urgent — follow-up.html:2788-2812.

**os/strategy/sop.html vs os/strategy/index.html** — SOP at v1.0 describes an older state; the module is fully built.
- STALE_SOP: model wrong. SOP says "Claude Haiku 4.5"; code uses `claude-sonnet-4-6` everywhere — os/strategy/strategy.js:1488,2363,2478,2518,2573,2754.
- STALE_SOP: wizard scope understated. Wizard covers the full plan (Objective, Target, Customer, USPs, Method, Enticement) before the nine/three/one-year + QPs — strategy.js:1776-1849.
- STALE_SOP: changelog claims Objective Plan + Projects/Tasks auto-create "to follow in v1.1"; both are built — strategy.js:1779-1802,1278,1413,1686.
- STALE_SOP: projects hook is a manual "Push Projects →" button with approval preview, not "wizard offers after saving"; Owner/Dept/Deliverable/Collaborators are inline form fields, not wizard questions — index.html:70, strategy.js:626-683.
- STALE_SOP: button label "Skip" is actually "Move on →"; undocumented "← Back" control — index.html:95-96.
- STALE_SOP: PAT stored in localStorage (persists), SOP says "session only" — strategy.js:31-56.
- UNDOCUMENTED: "Export PDF" (html2pdf) — index.html:14,71.
- UNDOCUMENTED: `MAX_PUSHBACKS_PER_STEP = 1` (auto-accepts after one pushback), differs from SOP's "interrogate and reject" framing — strategy.js:1856,2116.

**os/tasks/sop.html vs os/tasks/index.html** — heavy drift; SOP v1.1 describes an old read-mostly version.
- STALE_SOP: "To do" status no longer exists anywhere. `STATUS_OPTIONS=['Today','Upcoming','Approval','Completed','Overdue']` — index.html:750.
- STALE_SOP: status now auto-derived from due date (`deriveTaskStatus`), not a stored manual value — index.html:1646.
- STALE_SOP: status sort order wrong. Code `{Overdue:0,Today:1,Approval:2,Upcoming:3,Completed:4}` — index.html:757.
- STALE_SOP: priority sort order wrong. Code `{Project:0,Urgent:1,'Not Urgent':2,High:1}` — index.html:758.
- STALE_SOP: "no create-task form, read/update only" (§5) is false — full create flow (`openCreateTaskModal`/`createSimpleTask`/inline/double-click adds) — index.html:7226,6505.
- STALE_SOP: Kanban is a rolling 4-week view with 4wk/12wk/12mo modes, not a fixed calendar-month Week1-4 board — index.html:811,5493.
- STALE_SOP: "Smart Daily Planner / Plan My Day" removed, replaced by Auto-Schedule (`buildDayTimeline`/`deferTask` gone) — index.html:4782,4539-4723.
- STALE_SOP: `os/tasks/jobs.html` referenced but does not exist; contractor filtering now inline.
- UNDOCUMENTED: recurring auto-roll on completion (`rollRecurringTask`/`catchUpRecurringTasks`) — index.html:6692,6759.
- UNDOCUMENTED: full delete + undo (`deleteTask`/`bulkDelete`) — index.html:3352,5144.
- UNDOCUMENTED: comments, collaborators (with project cascade), attachment upload, editable per-person capacity — index.html:3047,2937,776.
- UNDOCUMENTED: auto-reschedule also skips hard-deadline tasks (`!t.hardDeadline`) — index.html:4771.
- UNDOCUMENTED: extra views "Contractor Tasks" + "Meetings"; `?filter=maintenance` URL filter — index.html:469-470,687.

### Version gaps (pageVer ahead of sopVer)
15 pages: Leadership Dashboard (2.36/2.9), Objective & Strategy (1.14/1.0), Tasks & Projects (1.106/1.1), CFVs (1.27/1.6), Accounts Receivable Fixed (1.1/1.0), Accounts Payable Fixed (1.7/1.0), Accounts Payable Variable (2.17/2.2), Profit & Loss (2.22/2.12), Inbound Comms (2.51/2.4), Property Compliance (1.11/1.1), Operations (1.40/1.0), Business Launch Plan Builder (1.12/1.3), Accounts (1.7/1.0), Systemisation (1.3/1.0), Site Map & Guides (1.16/1.1), Skills Library (1.3/1.0). Chronic auto-bump-vs-manual-sopVer drift; the behaviour audit above is the meaningful signal.

## 4. Health Check Results

Dev server started (python http.server :8951), index.html loaded in a headless browser.

- **4a Badge/data consistency — LIMITED.** Cannot verify: `window.allTransactions` undefined because the app is behind an auth gate and no PAT is entered in the unattended run.
- **4b Token/auth health — LIMITED (expected).** `window.PAT` undefined. The PAT is entered at runtime by Kevin; its absence in a headless run is expected, not a fault. Auth gate renders correctly.
- **4c Stale data — N/A.** No dashboard load occurred (no auth), so no refresh timestamp to check.
- **4d localStorage dismissal integrity — PASS.** No `dismissed_*` / `cfv_*` keys present and none malformed. No corruption.
- **Shell load — PASS.** index.html loads with zero console errors; no JS syntax/load failures across the shell.

## 5. Actions Taken

### Auto-fixed
None. No dead references, no new rogue IDs, no simple renames in scope. The two new Income-Buckets fields require no code change (`spendSubs` already wired; reverse link unused). SOP behaviour drift is human-review by classification (STALE_SOP / UNDOCUMENTED) and needs judgment + multi-file SOP rewrites, so it is out of safe unattended auto-fix scope. Nothing met the auto-fix bar.

### Maintenance (committed, non-code)
- `monitoring/reference-map.json` refreshed to 369 fields (added `fld6yClkQoMlOkiU4` = `spendSubs`), summary date 2026-07-01.

### Escalated to Human Review
No new Airtable task created (avoids daily duplicates). The one review-worthy theme — SOP drift — is chronic and already tracked by open task `rec9ihFqDDSY8DMLZ` ("Drift Monitor: [WARNING] SOP docs behind code"), still "To do". This run refreshed that task's name to the current 2026-07-01 finding (7/10 pairs drift; tasks-OS and inbound-comms most severe). A detailed comment could not be posted — the Airtable connector lacks comment permission (needs reconnect with comment scope); full per-pair detail lives in this report instead. No other escalation needed.
