# Daily Ops — Sunday 23 August 2026

Started 07:06 BST, finished ~11:30 BST. All 9 phases ran. Sunday, so the CEO huddle,
the CEO brief and the phase 6b calendar jobs were correctly out of window.

Yesterday's run (22 Aug) died during phase 8: its progress file stopped at phase-7 and
7 findings it had claimed were still leased. Phase 1 reopened them.

---

## Phase 1 — Readiness

- `rantoday daily-ops` → exit 0, no end mark today, safe to start.
- `ready daily-ops` → ready first try, no wake lag.
- `mark daily-ops` → start stamped 2026-08-23T06:06:24.983Z.
- `findings.py reopen --stale` → **7 reopened**, all claimed by queue-fixer 21.3 hours ago:
  20260814-drift-monitor-127, 20260814-daily-ops-146, 20260816-ceo-huddle-154,
  20260816-task-hygiene-sweep-183, 20260816-inbound-messages-sweep-185,
  20260819-daily-ops-253, 20260820-uc-check-slack-notifier-260.
  This is the signature of yesterday's dead run, not a new fault.
- `check-routines.py` → exit 0. Only daily-ops ran in the last 26h (28 queue events,
  20 routines known, 5 other jobs used the queue). No second routine has appeared.

## Phase 2 — CEO huddle — SKIPPED (correct)

Weekend hard stop. `TZ=Europe/London date` = Sun 23 Aug 07:06 BST. The 09:00 worker never
sends at weekends, so a weekend row in CEO Briefs would sit as "NOT FINISHED" for ever
(happened 9 and 16 Aug). No departments convened, no Airtable write, no Slack. Next huddle
Monday 24 Aug ahead of the 09:00 brief.

## Phase 3 — Drift

Report: `monitoring/drift-2026-08-23.md`. Snapshot: `monitoring/schema-2026-08-23.json`.

- Schema: 119 tables / 4,917 fields, zero changes vs 22 Aug. Base static since 16 Aug.
- Field refs: 0 dead, 0 unresolvable fld/tbl IDs across 281 files. 341 rogue IDs, all resolving.
- **New signal:** `reference-map.json` now misses **44** of the 530 Airtable IDs in
  `js/config.js` (was 26 on 14 Aug). All 44 resolve, so nothing is broken, but the DEAD
  check only covers 92% of the surface and the gap is widening while
  `20260814-drift-monitor-128` sits open.
- SOP drift: 25 of 28 pages show pageVer ahead of sopVer, but 12 have no SOP file at all.
  The metric is red every day and carries no signal.
- `cfv_{id}_startDate` still has no writer anywhere. Filed three times, all open.
- **Health checks 4a–4d SKIPPED, not passed.** `preview_start` refuses in an unattended
  session and nothing was already running. A static substitute on the cfv key shapes ran
  instead; both documented shapes hold.
- Findings filed: 0. Every defect found is already queued; a duplicate would add noise.

## Phase 4 — Production sweep

Report: `monitoring/e2e-sweep-2026-08-23.md`. No platform defect.

- Production UP (200). Full authenticated mode via Kevin's signed-in Chrome, so no token
  was handled. 9,066 transactions, 94 tenancies, 180 costs loaded.
- 28 pages walked (25 in-shell panels + crm/how-it-works/compliance by URL):
  **28 PASS, 0 FAIL**, zero console errors, all 8 iframes loaded.
- STEP 4.5 data invariants exit 0 — 15 checks, 14 PASS with real control populations,
  1 SKIP on an empty population (not a control failure).
- The completion-date reopen leak (open CRITICAL) was clean for the third day running,
  control population 100.
- Income and Costs health bars red, but both are business data not bugs: 3 tenancies in
  cash-flow void (**£2,293.68 at risk**), 11 overdue costs, 15 costs with >10% variance.
- AI Brain feed stale — Today tab says "Synced 18 August", **5 days behind**, up from 3
  yesterday. The nightly publisher has landed nothing since the 18th.
- PAGE_REGISTRY has changed shape again: 28 ids, `coa` and `kpi-library` added, `os-bplan`
  gone. The skill's hardcoded list is two versions behind.
- Appended dated recurrence notes and refreshed due dates on rec3PJCQt34r7ICrh,
  recR0KMvBmvM1ZVK2, recrGG00unq8Pup2V. Dedupe control returned 3 rows, proving the search
  fired rather than silently returning zero.

### CORRECTED LATER IN THE RUN — the PECR alarm was false

This sweep reported "23 non-Ltd prospects emailed, up from 19 yesterday, breach risk
getting worse daily". Phase 6.4 verified it against live data and the send path and it is
**wrong**. See phase 6.4. No action needed from Kevin. Nothing was sent to him.

## Phase 5 — Task hygiene

Report: `monitoring/task-sweep-2026-08-23.md`.

- 214 live tasks, 202 compliant (**94.4%**, up from 94.3%). AI owns 102 (47.7%),
  people 100, nobody 12.
- 1 auto write applied (Business = Real Estate on a completed Roy Lavin task). Undo log written.
- **12 decisions held** in `monitoring/task-sweep-pending.json` — 6 AI agents, 3 people
  (3 Slack DMs on approval), 3 project links.
- Yesterday's 12 pending were never approved, so this is a corrected re-issue, not a new backlog.
- CEO review rejected the set as first written; four owner calls changed: dropped both SSE
  smart-meter decisions, dropped the SMS owner (Mica already owns that conversation), moved
  the window hinge and carpets off an AI agent to Roy Lavin.
- Needs Kevin: the **£1,073 SSE debt approved 18 Aug is still unsent**; two overdue council
  letters sitting at Not Urgent; approve SPF/DKIM before the 6 test emails.
- Filed `20260823-task-hygiene-sweep-320` — the inbound router stamps "Maintenance Ticket"
  on council letters and marketing emails (10 live records wrong).
- All 6 stale tasks reviewed, none skipped. Left blank honestly: 123 Reg (zero ledger rows
  against a working control) and NeighborsCU (Kevin already closed it as spam).

## Phase 6 — Business actions

### 6.1 UC check → Mica — SENT

- STEP 0 `uc-task-sync.py`: control passed (94 tenancies read, 22 UC candidates, 22 already
  correct, 0 created/updated). Exit 1, not a control failure.
- STEP 1 `uc-check-notify.py due`: control_total 113, open 46, due_count 1,
  duplicate_tenants empty, 1 skipped for rent date passed.
- **One Slack DM sent to Mica** (U08HW0TAWAE) verbatim: Marilyn Martin, £1296.45, rent due
  30 Aug 2026. Confirmed sent, ts 1787467087.933549.
- recalgQ1r1opAcW3C marked notified (22 total in notified.json).
- Duplicates still present: 4 tenancy/month pairs for 2026-09 hold two open UC tasks each
  (rec03VoJV2K6q4oSt/recaNgraIKzhHr9aU, rec2qe53EujjLO3lu/recfxeDLwKJhFbt9X,
  rec8doDlidAA6E82N/recUY6OBhJrdi6paW, recHVECDs6Mj33Qnb/recLpX2Zyw4hWA4L3).
  No new finding filed: `20260819-uc-notifier-231` and `20260820-uc-check-slack-notifier-260`
  already cover it. Both now 4-5 days old and unfixed.

### 6.2 Inbound messages sweep

Both halves ran headless; WhatsApp app was not needed, nothing skipped.

- iMessage: 1 incoming scanned, 0 group-skipped, 1 candidate, 1 task created.
- WhatsApp: 68 incoming scanned, 2 broadcast skipped, 34 group-no-mention, 18 non-text,
  14 candidates → 5 created, 2 dedupe-skipped, 7 triaged out.
- **6 tasks created**, under the 10 cap, nothing deferred. Dedupe control passed.
- 1 task closed as self-handled (an outgoing reply postdated its task by 8 days).
- Tier-1 flags: 0. One created task carries a conditional tier-1 instruction because its
  attachment was unreadable by a text-only sweep.
- Both watermarks advanced; the iMessage mark correctly kept its higher value.
- **11 inbound tasks remain open awaiting Kevin's approval, oldest 10 days.** That is the
  bottleneck, not the sweep.
- Filed `20260823-inbound-messages-sweep-321` (high) and `-322` (medium).

### 6.3 Agent dispatch

- **`agent-dispatch.py` has no `reconcile` subcommand at all**, despite SKILL.md making it
  mandatory step 1 since the 19 Aug tier-1 incident. Did it by hand: found **2 real orphans**
  from 22 Aug (recT2ZRet9FsuqQ6C, recsVjmdPPJlRRqEl) — finished deliverables on disk,
  Airtable Agent Output empty, invisible to Kevin. Both submitted. Filed as finding 323.
  **Fixed in today's PR.**
- Routed 6 inbound tasks off the AI CEO via od-ceo, all applied and verified.
- Submitted 5 deliverables (the cap): 2 orphans plus Roy Lavin (tier 1), Swinton insurance,
  Maia's phone.
- Parked 2 approved carry-outs this phase structurally cannot execute: rec1Z1mwI5OORCQyv
  (needs a repo change), rec79mCohnwb2PmpE (needs Kevin's access grant). Verify alarmed once
  on each, as designed.
- Also filed 324 (handover never clears Sent For Approval By, so a handed-over task returns
  every run for ever), 325 (queue returned 8 against a documented cap of 5), and
  **326 (critical)** below.

#### CRITICAL — Companies House penalty, Agile Estates

The Roy Lavin letter is a Companies House **final late-filing penalty of £2,390** against
Agile Estates. **Deadline 20 Aug has already passed.** Threatens debt recovery and court.

Behind it:
- The Oct 2025 instalment plan was **never set up** — £0.00 paid on every line. The task sat
  at "Not Urgent" for ten months.
- The **confirmation statement is 84 days overdue**, which is the usual route to strike-off,
  and it is tracked nowhere.

Filed as critical finding `20260823-agent-dispatch-326`. Reaches Kevin as a tier-1 approval
card; banner confirmed live.

#### Insurance correction

The Swinton policy covers **one building with 9 flats, not 27 properties**. Expires
**10 Sep**, no renewal offer received yet, and **Loss of Rent appears to have dropped off
the schedule**.

### 6.4 Prospecting — 0 found, and the PECR alarm disproved

**The 23 non-Ltd "breach" claim from phase 4 is wrong.** Verified live against Prospects
`tbljHVGJoKJf8acy3` (219 records, paginated): the 23 = 19 records with **blank** Entity Type
on Contact Route "Warm lane (email)", all dated 08 Aug, plus 4 solicited "Email reply (they
asked)" records from 13-14 Jul. **Cold non-Ltd emails: zero.**

- The entity gate is not leaking. `js/prospecting.js:527` blocks any non-Ltd cold send, and
  "Warm lane (email)" is not in `PROS_EMAIL_ROUTES` (line 95), so the page never emails
  those 19 at all.
- "Up from 19 yesterday" is a false trend. The 21 and 22 Aug sweeps both recorded 23; the 19
  is the blank-entity sub-count *inside* the 23, misread as a prior total. No record in the
  set is newer than 8 Aug.
- Filed `20260823-prospect-daily-run-327` (medium): the health check applies neither
  exemption its own send path applies, and folds "confirmed non-Ltd" with "entity never
  recorded" into one unattributable number. **Fixed in today's PR.**
- Run: **0 prospects found**. Worked the §2a ladder — 2 FB groups (3 terms), global FB post
  search, 3 LinkedIn content searches, group chronological feed. Everything returned
  supply-side VAs, job-seekers, non-UK, filled roles, or an existing duplicate (neuro speech
  therapy, rec08ROWIL4qoHlMP). No captcha or restriction warning. Sunday supply is genuinely
  thin and the VA groups are over-mined.
- GHL sync skipped: the single Approved record (Gemma Claire Cooper) has no email and route
  "No route yet", so there is nothing to sync and it must never be sequenced.

## Phase 6b — Calendar work — ALL SKIPPED (out of window)

Today is Sunday 23 Aug, month 08.

- Monthly rent due date (1st of month): not due, 23rd.
- Post manager weekly (Mondays): not due, Sunday.
- Update master prompt quarterly (1 Jan/Apr/Jul/Oct): not due.

## Phase 7 — Health checks

### 7.1 Drive auth — HEALTHY

HTTP 200. The worker read back the real Drive folder "Operations Director SOPs"
(id 1215f_LfF...), which is the only signal the check trusts. `alert_kevin` false,
`consecutive_gate` 0, so no Slack to Kevin — correct under the contract.
Control passed: `drive-auth-check.py selftest` 9/9, including the two cases that used to
slip through (worker reporting ok/valid while Drive failed, and a Cloudflare edge block).

### 7.2 CEO brief morning check — PASS, nothing was due

Ran at 10:37 London, past the 09:20 gate.

- The weekend hard stop is real, not an assumption. `isLondonSendTime()` at
  `scripts/slack-automation/money-daily-worker.js:879-890` gates on
  `londonDay >= 1 && londonDay <= 5`, so Sunday (0) never sends. Cron is `0 8-11 * * *`
  (every day, day decided in code) in `wrangler.money-daily.toml:32`.
- Live data agrees: CEO Briefs `tblIxbzDSOCI5hqJn` has zero Saturday rows; Sundays 9 and
  16 Aug carry One Thing but an empty Full Brief. Every weekday 6–21 Aug has a populated
  Full Brief.
- No row for today. Control passed: the same date filter returned the 16 Aug row, so the
  empty result for 23 Aug is genuine.
- Verdict: no brief was due, none missing, no manual send, no DM. Weekdays 17–21 Aug all
  have full briefs.
- Filed `20260823-ceo-brief-morning-check-328` (medium): **neither this check's SKILL.md nor
  daily-ops phase 7.2 has a weekend gate.** Run literally on a Saturday or Sunday it would
  find no record, manually fire `mode=send`, and DM Kevin that his brief was late — a false
  alarm plus a weekend brief that breaks the two-messages-a-day contract. Fix is a London
  day-of-week stop at the top of the check, mirrored in phase 7.2.

### 7.3 CEO memory sweep — distilled Saturday 22 Aug

Ran for **yesterday**, per the timing change, because that is a complete day.

- Slack CEO channel was quiet: two system assignment lines at 07:19, zero messages from
  Kevin. No deep session file existed, so 22 Aug had no record at all in CEO Conversations.
- The day actually ran in Claude Code, 08:53 to past 22:48, on one thread: clearing his
  Audible library into the brain.
- Two rulings extracted, neither previously recorded: (1) Kevin picks what enters the brain
  (unread books count, Amazon FBA Blueprint deleted not parked, Chen's AI for Beginners
  pushed to the front); (2) a stalled pipeline must name its cause.
- Wrote `Decisions/2026-08-22 Kevin picks which books enter the brain, and a stalled pipeline
  must name its cause.md` and `CEO Conversations/2026-08-22 Slack.md`.
- Fixed a memory-home error: the whole book-pipeline state sat inside
  `project_ceo_brief_reverse_engineered.md` while `project_audiobook_pipeline_auth_break.md`
  still ended on 1 Aug. Moved it, left a pointer, rewrote both MEMORY.md lines.
- Verified: inbox and `_failed` both empty, Chen's book filed 23 Aug, Amazon FBA absent,
  14 docs dated 22 Aug and 20 dated 23 Aug. The 121-book count and the 6 lapsed Plus titles
  are the session's own account and are marked unverified.
- **Concern carried forward:** task rec3Cm0ivFMwptDWk (approve the 6 four-line test emails
  plus seed) was due 21 Aug, still open, two days overdue. Same as yesterday's log, unmoved.
  The fix for the 0% reply rate on 148 emails is still untested.

## Phase 8 — Fix

**PR: https://github.com/chaichoong/leadership-dashboard/pull/107 — NOT merged, Kevin reviews.**

10 findings fixed:

- **323** built the missing `reconcile` subcommand in `agent-dispatch.py` (exits 1 naming
  orphaned deliverables, 2 if it cannot see the run dirs). Ran live: 23 deliverables checked,
  0 orphans.
- **324** `handover` now clears Sent For Approval By, so a handed-over task actually leaves
  the agent queue instead of returning every run.
- **325** not a code bug: the cap is 50 and already truncates; the stale "5" was in SKILL.md,
  now fixed.
- **327 + 182** PECR gate now mirrors the send path. Verified against all 219 live prospects
  it reads pass ("123 cold-emailed, all Ltd") instead of failing daily on 4 solicited replies
  and 19 warm-lane contacts.
- **321 + 261 + 305** `whatsapp-sweep.py sent` resolves a chat name to a JID or exits 2
  loudly. "Roy Lavin" now returns the same 31 matches as its JID (was 0), unblocking two
  tasks stuck open since 15 Aug.
- **315** Unknown project chip styled.
- **319** auto-reschedule result now read, so a failed move fails the health check instead of
  a hardcoded pass.

Every fix has a regression test, each back-tested by reverting the fix. `npm test` green:
**942 tests**.

Committed 14 monitoring reports (13–23 Aug).

**Refused to commit 4 JSON files, correctly:**
`task-sweep-worklist-2026-08-23.json`, `task-sweep-decisions-2026-08-23.json` and
`task-sweep-applied-2026-08-23.json` are inside the `never-commit` privacy markers (tenant
names, rent figures, email bodies — **this repo is public**, guarded by a test).
`schema-2026-08-23.json` is ignored by the `schema-2*.json` rule. Whether any of those
should ever be committed is Kevin's decision, not a routine's.

Left: 320 findings open, 15 critical. Filed 2 new — **329** (SKILL.md claims `submit` reads
the record back; it does not) and **330** (the Supabase twin still has the old
auto-reschedule bug).

## Phase 9 — Report

`loop-health.py` exit 0. 367 tasks read, 174 agent-linked. Needs Kevin 67, done in last
7 days 60, **NOT MOVING 23** (full list in the tool output; top three carried into the DM
in the order printed).

Longest-stalled: two certificate renewals at 19 days (Emergency Lighting, GSC), then a
Utilita overdue-payment letter at 15 days.

---

## What Kevin was actually sent today

1. The Daily Ops DM (this run's summary).
2. Mica's Universal Credit DM — Mica only, not Kevin.

No CEO brief: Sunday, none due. No other Slack messages sent by any phase, per the
21 Aug contract.
