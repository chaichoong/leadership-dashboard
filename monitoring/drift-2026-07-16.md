# Drift Report — 2026-07-16

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes
- Field references: 0 dead, 0 orphaned, 60 rogue (chronic, unchanged, all live)
- SOP drift: 0 stale, 0 new undocumented, version gaps unchanged (1 widened by a bug-fix bump)
- Health checks: 1 pass (page serve), 4 N/A (auth-gated in headless run)
- Auto-fixed: 0 items (nothing auto-fixable)
- Human review: 0 new tasks (existing tracked items remain open)

Clean cycle. No new drift detected against the 2026-07-15 snapshot. Base now holds 117 tables / 4,865 fields.

## 1. Schema Changes
Compared `schema-2026-07-16.json` against the most recent prior snapshot `schema-2026-07-15.json`.

- **New tables:** 0
- **Removed tables:** 0
- **New fields:** 0
- **Removed fields:** 0
- **Type changes:** 0
- **Renames:** 0

No structural change to the Airtable base in the last 24h.

## 2. Field Reference Issues
Verified all mapped IDs in `reference-map.json` (399 fields, 33 tables, 20 records, 11 select choices) against today's schema snapshot.

- **DEAD (in code, not in Airtable): 0.** Every mapped field/table ID resolves to a live field/table.
- **TYPE_MISMATCH: 0.** No referenced field changed type (corroborated by zero type changes in §1). `reference-map.json` stores JS-constant names only, not expected types, so type verification is bounded by the day-over-day schema diff, which is clean.
- **ORPHAN: 0.** Feature files reference config fields by named `F.*` constant, not raw `fld…` string — the intended architecture.
- **ROGUE (quoted Airtable IDs in `js/*.js` not present in `js/config.js`): 60 — chronic, unchanged, all resolve live.**
  - **36 `fld…` IDs**, all live. Clusters: `js/dashboard.js` (Projects/KPI/Tasks cluster, 23 fld + 1 tbl), `js/sitemap.js` (SOP Update Requests, 6 fld + 1 tbl), `js/arrears.js` (6 fld), and the shared Chart-of-Accounts trio (`fldO4BTJhFv5EsN6i`, `fldii4oUzSfmplihO`, `fldqr09KqLGGYCYkC`) spread across the finance files (`costs`, `pnl`, `reconciliation`, `transactions`, `wealth`, `wealth-ratios`, `cashflow`, `fintable`).
  - **5 `tbl…` IDs**, all live: `tbl5MY9FbImFQfvez` + `tblZ75JgE1wzDP0ps` (`js/ai-assistant.js`), `tblHGNzDmOs59r9QD` (`js/skills.js`), `tblJ3GFnAAoXf99e9` (`js/dashboard.js`), `tbltuZz5Omrpo7t1x` (`js/sitemap.js`).
  - **19 `rec…` IDs**, all live. 15 sit in `js/admin-repair-splits.js` — a one-off data-repair script with hardcoded record targets (expected for that file type). The remaining 4 are single-record anchors in `arrears`, `cashflow`, `skills`, `skills-data`.

No new rogue reference appeared this cycle; count matches the 07-15 baseline exactly. Centralising these into `config.js` is a multi-file refactor touching shared feature code — **not a safe unattended auto-fix**. Note: a naive `(fld|tbl|rec|sel)[A-Za-z0-9]{14}` scan also matches JS identifiers (`reconForecastKeys`, `reconciledInflows`, `recomputeCostFrom`, etc.); this report counts quoted string literals only.

**`reference-map.json`:** fully in sync with `js/config.js` (399 fld / 33 tbl / 20 rec / 11 sel — exact match, zero config IDs missing from the map). No update needed.

## 3. SOP Drift
Compared `PAGE_REGISTRY` `pageVer` vs `sopVer` for all 10 pages that carry a SOP file. All 10 SOP files present on disk.

- **STALE_SOP: 0.** No SOP describes behaviour removed from code.
- **UNDOCUMENTED: 0 new.** (The Tasks Activity trail flagged 07-14 remains the standing item — see below.)
- **VERSION_GAP: 11 pages** (unchanged count). One gap widened this cycle:

| Page | pageVer | sopVer | SOP file | Change since 07-15 |
|------|---------|--------|----------|--------------------|
| overview | 2.41 | 2.9 | sop.html | — |
| os-strategy | 1.15 | 1.0 | os/strategy/sop.html | — |
| tasks | 1.111 | 1.1 | os/tasks/sop.html | — |
| cfv | 1.30 | 1.6 | sop-cfvs.html | pageVer 1.29 → 1.30 |
| invoices | 2.18 | 2.2 | sop-invoices.html | — |
| pnl | 2.25 | 2.12 | sop-pnl.html | — |
| comms | 2.52 | 2.4 | inbound-comms-sop.html | — |
| compliance | 1.11 | 1.1 | sop-compliance.html | — |
| os-bplan | 1.14 | 1.3 | os/business-plan-builder/sop.html | — |
| prospecting | 1.4 | 1.3 | sop-prospecting.html | — |
| sitemap | 1.17 | 1.2 | sop-sitemap.html | — |

The `cfv` bump (1.29 → 1.30) came from commit `ee75100` ("CFV tab froze on spinner when document hidden — rAF never fires"), a rendering bug fix. It changes **no** CFV detection rule, threshold, or action button described in `sop-cfvs.html`, so it is **not** new behavioural drift — it only widens the CFV version gap already tracked under Airtable task `rec9ihFqDDSY8DMLZ`. `pageVer` auto-bumps on every code push; `sopVer` only bumps on a manual SOP rewrite, so a persistent gap is the expected steady state, not by itself a defect. No new task raised.

**Already-tracked behavioural drift (not re-raised):**
- CFV SOP drift — Airtable task `rec9ihFqDDSY8DMLZ`.
- Tasks Activity trail undocumented in SOP — Airtable task `recesmr9BNH2curGY` (created 07-14).

## 4. Health Check Results
Loaded the live GitHub Pages site (`https://chaichoong.github.io/leadership-dashboard/`) in a headless browser.

- **Page load:** PASS — serves cleanly, `document.title` = "Operations Director", **zero console errors**, auth gate renders (body text present, ~707 chars).
- **4a Badge count consistency:** N/A — no data loaded (unauthenticated; `window.allTransactions` undefined).
- **4b Token/auth health:** N/A — `window.PAT` absent by design (no interactive login in a background run; not real drift).
- **4c Stale data (>30 min):** N/A — no data session to time.
- **4d localStorage dismissal persistence:** N/A — no authenticated session.

The data/auth checks require interactive PAT login, unavailable in a scheduled headless run — consistent N/A across all prior cycles. The one runnable signal (clean page serve, no load-time JS errors) passed.

## 5. Actions Taken
### Auto-fixed
None. No dead references, no rogue-ref regressions, no simple renames — nothing met the auto-fix bar.

### Escalated to Human Review
None. No new schema change, type mismatch, behavioural SOP drift, or health failure this cycle. Existing tracked items remain open:
- `rec9ihFqDDSY8DMLZ` — CFV SOP behavioural drift.
- `recesmr9BNH2curGY` — Tasks Activity trail undocumented in SOP.

### Housekeeping
- Wrote `monitoring/schema-2026-07-16.json` (117 tables, 4,865 fields).
- `reference-map.json` verified in sync; no update needed.
