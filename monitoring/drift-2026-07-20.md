# Drift Report — 2026-07-20

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (identical to 2026-07-19 snapshot)
- Field references: 0 dead, 0 orphaned, 54 rogue (chronic, unchanged)
- SOP drift: 1 stale, 1 undocumented, 11 version gaps (all chronic, all tracked)
- Health checks: 5 pass, 0 warn, 0 fail (4 browser checks N/A — auth-gated)
- Auto-fixed: 0 items
- Human review: 0 new tasks — the one live finding is already open as **recbnIfmyoN3Idzgw**

**Clean run. No new drift.** Every finding below is a standing known item carried from prior
cycles. Nothing moved today — no source file changed since the 2026-07-19 run.

## 1. Schema Changes

No change. Today's snapshot (`monitoring/schema-2026-07-20.json`) is field-for-field identical
to `monitoring/schema-2026-07-19.json` (verified by deep-equality of the parsed JSON).

- Tables: 117 (0 added, 0 removed)
- Fields: 4,865 (0 added, 0 removed, 0 renamed, 0 type changes)

## 2. Field Reference Issues

Verified all 399 field IDs, 33 table IDs and 20 record IDs in `monitoring/reference-map.json`
against the live schema.

- **DEAD:** 0 — every code-referenced field and table still exists in Airtable.
- **ORPHAN:** 0 — reference-map is in exact sync with `js/config.js`. No config constant points
  at a departed field.
- **ROGUE:** 54 — field IDs hardcoded in feature `js/*.js` files rather than routed through
  `config.js`. **All 54 resolve to a live field in today's schema (0 dead).** Count unchanged
  from 2026-07-19 (453 distinct `fld` IDs across `js/`, 399 in `config.js`, 54 outside it).
  This is the chronic multi-file pattern reported every cycle.

  Distribution of the 54 (an ID can span files): `dashboard.js` 23, `skills-data.js` 18,
  `sitemap.js` 6, `arrears.js` 6, `fintable.js` 3, `costs.js` 3, `transactions.js` 2,
  `reconciliation.js` 2, `pnl.js` 2, `wealth.js` 1, `wealth-ratios.js` 1, `cashflow.js` 1.

  **Not auto-fixed — deliberate, matching prior policy.** Moving 54 live IDs into `config.js`
  is a large refactor of shared money-path files (dashboard, reconciliation, pnl, costs). It
  carries real regression risk and belongs in a reviewed change, not an unattended drift PR.
  Left as a standing known item.

## 3. SOP Drift

No SOP, `js/config.js`, or page source changed since the 2026-07-19 run (`git log` shows zero
commits touching `js/*.js`, `*.html`, or `os/**` since then). All findings are re-verified as
identical to yesterday. Version gaps compared as version tuples, not floats.

### STALE_SOP: 1 — `sop.html` §12 "Reconciliation Matching Logic" (chronic, tracked)

Re-verified today. The SOP still documents a 33% amount tolerance and a 5-day look-back/
look-ahead date window; the code enforces a 10% tolerance
(`COST_MATCH_TOLERANCE = 0.10` at [js/reconciliation.js:498](js/reconciliation.js:498)) and has
no date window. On a genuine supplier price rise the SOP implies the match still lands; the code
returns blank and leaves it for a human — a real behavioural difference on money attribution.

- Already escalated: open Airtable task **recbnIfmyoN3Idzgw** — confirmed still status
  **"To do"** today. No duplicate created.

### UNDOCUMENTED: 1 — the engine that replaced §12 (chronic, tracked)

`sop.html` §12 still omits the mechanism now in force: learned historical patterns,
reference-token matching (`pickCostByReference`), closest-amount picking (`pickCostForAmount`),
ACTIVE-costs-only gating, the Knowledge Base rule layer, and the auto-approve near-certain tier.
Same open task **recbnIfmyoN3Idzgw**.

### VERSION_GAP: 11 of 11 (chronic, count unchanged)

| Page | pageVer | sopVer | SOP file | Moved today? |
|------|---------|--------|----------|--------------|
| overview | 2.42 | 2.9 | sop.html | no |
| os-strategy | 1.16 | 1.0 | os/strategy/sop.html | no |
| tasks | 1.112 | 1.1 | os/tasks/sop.html | no |
| cfv | 1.30 | 1.6 | sop-cfvs.html | no |
| invoices | 2.18 | 2.2 | sop-invoices.html | no |
| pnl | 2.25 | 2.12 | sop-pnl.html | no |
| comms | 2.53 | 2.4 | inbound-comms-sop.html | no |
| compliance | 1.11 | 1.1 | sop-compliance.html | no |
| os-bplan | 1.15 | 1.3 | os/business-plan-builder/sop.html | no |
| prospecting | 1.4 | 1.3 | sop-prospecting.html | no |
| sitemap | 1.17 | 1.2 | sop-sitemap.html | no |

Gaps are the chronic independent-versioning pattern (auto-bumped `pageVer` runs ahead of
hand-bumped `sopVer`). Documentation lag, not a runtime risk. Not escalated as new tasks,
consistent with prior policy.

## 4. Health Check Results

| # | Check | Result |
|---|-------|--------|
| 4a | JS syntax — all 27 `js/*.js` parse clean (`node --check`) | **PASS** |
| 4b | Live deploy `index.html` | **PASS** (HTTP 200) |
| 4c | Live deploy `js/config.js` | **PASS** (HTTP 200) |
| 4d | Airtable API auth (PAT valid, base reachable) | **PASS** (HTTP 200) |
| 4e | Model-ID guard (`scripts/check-model-ids.sh`) | **PASS** (exit 0) |
| — | Badge count consistency | N/A — needs an authenticated browser session |
| — | `window.PAT` health | N/A — same |
| — | Stale-data timestamp | N/A — same |
| — | localStorage dismissal integrity | N/A — same |

The four browser checks are auth-gated (the dashboard requires an interactive Airtable-token
login) and cannot run in a headless scheduled context. Standing limitation, unchanged — not a
new failure. (Note: an initial Airtable meta call returned 422 due to a malformed query param;
a clean request returned 200 — auth is healthy.)

## 5. Actions Taken

### Auto-fixed
None. No dead references to comment out, no schema changes, and the 54 rogue IDs are
policy-excluded from unattended fixes (see §2).

### Escalated to Human Review
No new tasks. The single live actionable finding — `sop.html` §12 stale/undocumented — is
already tracked under open Airtable task **recbnIfmyoN3Idzgw** ("To do"), verified today. The
11 version gaps and 54 rogue IDs remain standing known items, not re-escalated.

---
*Working-tree note: an active session has uncommitted edits to `index.html`, `js/dashboard.js`,
`js/shared.js`, `tests/sync-invariants/helpers.js` plus two new untracked test files. This run
touched only `monitoring/` and committed nothing else. Rogue and health checks were run against
the working tree; results match `HEAD`, so those edits do not affect the drift figures.*
