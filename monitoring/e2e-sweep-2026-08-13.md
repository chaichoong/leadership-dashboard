# E2E Sweep — 2026-08-13

## Summary
- Production URL: **UP (200)** — https://chaichoong.github.io/leadership-dashboard/ served the app shell (`<title>Operations Director</title>`, sidebar markup present)
- Mode: **DEGRADED** (click-only, unauthenticated — data rendering NOT verified)
- Tabs walked: 18 | PASS: 0 WARN: 0 FAIL: 0 **UNVERIFIED: 18**
- Data invariants: **PASS (11 checked, every control non-zero)**
- Repo state: clean tree on `main`, 0 ahead / 1 behind `origin/main`. No pull, no commit, no push (per STEP 6). Queue lock NOT taken.

## Failures needing attention
1. **Browser auth injection blocked again — sixth consecutive morning.** Writing the Airtable token to `localStorage._dlr_pat` via `mcp__Claude_Browser__javascript_tool` was refused by the Claude Code auto-mode permission classifier. The token was NOT typed into the login field, NOT put in a URL, and NOT written into the served web root. Existing open task `recrGG00unq8Pup2V` updated (finding key `e2e-auth-js-denied`); no new task created.

Nothing else failed. No app defect was found.

## Mode notes (two environment changes worth recording)
- **`preview_start` cannot start a local dev server in an unattended session** ("nobody is present to approve the command"). The sweep therefore ran directly against production, which is the same static build, so no signal was lost. The skill's STEP 2 instruction to launch config `ops-director-wealth` on port 8951 no longer works for scheduled runs.
- **The JS denial was narrower than previous mornings.** Two read-only `javascript_tool` evaluations succeeded first — confirming `PAGE_REGISTRY.length = 27` and `tab-overview` at 4,217 chars (logged-out baseline). Only the `localStorage.setItem` write was refused, after which every subsequent `javascript_tool` call was refused too, including a plain read. The permission actually needed is the write.

## Per-tab results

All 18 sidebar entries were exercised by click (STEP 4 fallback). Every one took the active
highlight and the sidebar auto-scrolled it into view, proving navigation fired. Every in-shell
panel's shell markup (headings, tables, filter controls, empty states) was present in the
`read_page filter:"all"` tree. **No tab is marked PASS**, because unauthenticated no tab fetched
data — a clean click plus a clean console does not prove a tab works.

| Tab | Nav fired | Panel shell in DOM | Console | Verdict |
|---|---|---|---|---|
| CEO Brief | yes | yes — showed "CEO briefs failed to load" (correct empty/error state for 401) | 401 only | UNVERIFIED |
| Leadership Dashboard | yes (highlight confirmed by screenshot) | yes — Financial Overview, Strategic KPIs, Portfolio Overview, Tenancy Metrics, 31-Day Operational Metrics, 31-Day Cash Flow Forecast, AI Analysis & Commentary | clean | UNVERIFIED |
| Objective & Strategy | yes | iframe (content not in parent tree) | clean | UNVERIFIED |
| KPI Library | yes | iframe | clean | UNVERIFIED |
| Tasks & Projects | yes | iframe | clean | UNVERIFIED |
| Operations | yes | iframe | clean | UNVERIFIED |
| Inbound Comms | yes | iframe | clean | UNVERIFIED |
| Systemisation | yes | iframe | clean | UNVERIFIED |
| Skills Library | yes | yes — search box, Expand all / Collapse all | clean | UNVERIFIED |
| AI Brain | yes | iframe | clean | UNVERIFIED |
| Money Confidence | yes | iframe | clean | UNVERIFIED |
| Wealth | yes | iframe | clean | UNVERIFIED |
| Accounts | yes | yes — 6 sub-tabs present (AR Fixed, AR Variable, AP Fixed, AP Variable, Transactions, Bank Accounts), each with its sort/status/business/search controls and table header row | 401 only | UNVERIFIED |
| Profit & Loss | yes | iframe | clean | UNVERIFIED |
| Team Members | yes | iframe | clean | UNVERIFIED |
| Content Machine | yes | iframe | clean | UNVERIFIED |
| Prospecting | yes | yes — Review queue, Pipeline table, Search keywords panel | clean | UNVERIFIED |
| Site Map & Guides | yes (highlight confirmed by screenshot) | yes — Platform Architecture Map + Developer Blueprint links, version table | clean | UNVERIFIED |

### Console across the whole walk
Total console errors: **2**, both `Failed to load resource: the server responded with a status of 401`.
Zero `TypeError`, zero `ReferenceError`, zero 404, zero 500, zero unhandled rejections. The 401s
are the expected unauthenticated Airtable response, not a defect.

### Read-only interaction
Not performed. The auth overlay sits above the content area unauthenticated and intercepts clicks
into panels, so no card-expand or filter-change could be exercised. Filter `combobox` elements
were confirmed present and correctly populated in the accessibility tree (e.g. AR Fixed sort:
Due Day / Rent high-low / Rent low-high / Tenant A-Z / Payment Status).

### Standalone pages (registry ids with no in-shell panel, plus related)
All HTTP 200 on production: `crm-supabase.html`, `how-it-works.html`, `compliance.html`,
`supabase-app.html`, `follow-up.html`, `architecture.html`, `dev-blueprint.html`.

## Data invariants

`python3 scripts/check-data-invariants.py --json` → `"ok": true`. All 11 PASS, every control
population non-zero (so no check was silently asserting nothing).

| Invariant | Asserts | Status | Control rows |
|---|---|---|---|
| report-amount-populated | `**GBP` set => Report Amount set | PASS | 100 |
| split-override-sign | override set => sign(Report Amount) == sign(`**GBP`) | PASS | 18 |
| agent-work-completed-without-approval | agent-worked AND completed => approval outcome recorded | PASS | 70 |
| waiting-approval-has-an-agent | Status = Approval AND agent-worked => an agent is recorded | PASS | 29 |
| on-time-counts-the-due-date-itself | completed ON the due date => Completed On Time = 1 | PASS | 100 |
| started-projects-are-not-still-not-started | started >7 days ago AND not closed => status not "Not Started" | PASS | 20 |
| open-tasks-carry-no-completion-date | not completed => no Completion Date | PASS | 100 |
| automated-kpis-have-actually-run | automated + compute code + open project => KPI Last Updated set and <14 days old | PASS | 5 |
| ceo-brief-complete | past weekday => exactly one CEO Briefs row with Full Brief populated | PASS | 12 |
| kpi-library-coverage | live automated project KPI => a KPI Library template exists | PASS | 5 |
| no-reimport-duplicates | one bank transaction => one record | PASS | 7,876 |

## Dedupe

Finding key `e2e-auth-js-denied` matched one open task, `recrGG00unq8Pup2V`. Updated it with a
dated recurrence line and refreshed the due date to today. **No new task created.**

Control query (`FIND("FINDING-KEY:", {Notes})`) returned 2 rows, so the zero-result path was
proved live rather than assumed.

## Observations for tuning the sweep prompt (no action needed today)

1. **`PAGE_REGISTRY` membership has moved.** Still 27 ids, but `os-bplan` is gone and
   `kpi-library` is new. Current list: overview, os-strategy, tasks, cfv, ceo-brief, money,
   wealth, income, ar-variable, costs, invoices, pnl, transactions, comms, compliance,
   operations, kpi-library, fintable, os-team, systemisation, crm, content-machine, prospecting,
   sitemap, skills, ai-brain, how-it-works. The skill file still lists `os-bplan`.
2. **STEP 2's `preview_start` instruction is dead for scheduled runs** — see Mode notes above.
   Running against the production URL directly is the working substitute and should be written in.
3. **Stale open task spotted:** `recaFnWr9MNOeP86P` ("143 open tasks carry a stale Completion
   Date") is still open, but the `open-tasks-carry-no-completion-date` invariant passed today with
   a 100-row control. The underlying fault appears fixed; the task looks closeable. Not touched —
   Kevin's call.
4. **Duplicate still open:** `recsWrVeAZafPH1NL` is a second task for the same auth-denial fault,
   raised 31 Jul before the finding-key convention existed. Closing it as a duplicate of
   `recrGG00unq8Pup2V` remains outstanding.
