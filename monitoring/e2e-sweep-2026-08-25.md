# E2E Sweep — 2026-08-25

## Summary
- Production URL: UP (200), serving the app shell (title "Operations Director")
- Mode: FULL (authenticated via Kevin's already-signed-in Chrome against production; no token handled by the sweep)
- Data loaded: 9,086 transactions, 94 tenancies, 180 costs; tab-overview 289,130 chars
- Tabs walked: 29 registry ids | PASS: 26 in-shell panels + 3 standalone pages | WARN: 0 | FAIL: 0 | UNVERIFIED: 0
- Console: ZERO JavaScript errors across the entire walk (in-page error/rejection/console.error hook installed before the walk, plus the extension console reader)
- Iframes: all 9 readyState "complete"
- Data invariants: PASS (14 PASS, 1 SKIP with an empty population, 0 FAIL; exit 0)
- Assets: 48/48 deployed paths return 200 (32 js, 4 css, 6 OS iframe pages, 6 standalone pages)

## Per-tab results

All 26 in-shell panels rendered non-empty with no "undefined", "NaN" or "[object Object]" leaking into the UI.

| Tab | Result | Evidence (panel innerHTML chars / note) |
|---|---|---|
| overview | PASS | 297,160; 11/11 health checks; ~8.4s render (slowest tab) |
| os-strategy | PASS | iframe complete; business selector populated (5 businesses) + quarter selector, awaiting a business choice (intentional state) |
| tasks | PASS | iframe complete, 563,201 chars, 14/15 health checks |
| cfv | PASS | 2,193; Cash Flow Voids table rendered |
| ceo-brief | PASS | 98,755; 4/4 health checks |
| money | PASS | 8,347; 4/4 health checks |
| wealth | PASS | 681,722 once loaded (2s snapshot caught it mid-load) |
| income | PASS (data flag) | 75,726; 5/6 checks — see Business-data flags |
| ar-variable | PASS | 5,218 |
| costs | PASS (data flag) | 2,020,130; 3/5 checks — see Business-data flags; ~7.2s render |
| invoices | PASS | 8,048; 6/7 checks |
| pnl | PASS | 83,041; 8/8 checks |
| transactions | PASS | 887,669; 6/6 checks |
| coa | PASS | 93,747; 5/6 checks |
| comms | PASS | iframe follow-up.html complete, 356,480 chars |
| compliance | PASS | no in-shell panel by design; compliance.html returns 200 |
| operations | PASS | iframe complete, 171,086 chars, 11/11 checks |
| kpi-library | PASS | 854; lazy "Not yet loaded" state, loads on demand |
| agents | PASS | iframe complete, 1,014,852 chars, 7/7 checks |
| fintable | PASS | 2,537; accounts monitor table rendered |
| systemisation | PASS | iframe complete, 212,825 chars |
| os-team | PASS | iframe complete, 53,821 chars |
| crm | PASS | no in-shell panel by design; crm-supabase.html returns 200 |
| content-machine | PASS | iframe complete, 847,131 chars; took 26s to load (slowest iframe) |
| prospecting | PASS (data flag) | 4,513; 6/7 checks — see Business-data flags |
| sitemap | PASS | 38,672; 3/7 checks (version drift + GitHub API not yet fetched, both informational) |
| skills | PASS | 1,659 |
| ai-brain | PASS | iframe complete; brain feed dated 23 Aug (see Business-data flags) |
| how-it-works | PASS | no in-shell panel by design; how-it-works.html returns 200 |

Read-only interaction exercised: the health drawer was opened and closed on income, costs, prospecting and sitemap (toggleHealthDrawer). All four responded and revealed their check detail. No write action of any kind was taken.

## Data invariants

`python3 scripts/check-data-invariants.py --json` → exit 0.

PASS (14): report-amount-populated (control 100 rows), split-override-sign, agent-work-completed-without-approval, waiting-approval-has-an-agent, on-time-counts-the-due-date-itself, started-projects-are-not-still-not-started, open-tasks-carry-no-completion-date, task-category-ap-branch-is-reachable, automated-kpis-have-actually-run, first-touch-carries-no-booking-link, ceo-brief-complete, kpi-library-coverage, one-monthly-summary-per-business-month, no-reimport-duplicates.

SKIP (1): sop-queue-not-abandoned — "no records yet in the population this guards (queued-but-unprocessed SOP requests)"; field names verified live, so it starts asserting as soon as there are rows. This is a legitimate empty population, not a control failure.

FAIL: none. No CONTROL_FAILED status returned.

Note: the open-tasks-carry-no-completion-date invariant PASSES today. Task recR0KMvBmvM1ZVK2 (E2E Sweep [CRITICAL]: completion-stamp reopen leak) is still open for that same fault — worth Kevin confirming whether it can be closed.

## Failures needing attention

None. No app defect found: production up, every panel rendered real data, zero console errors, every invariant holding.

## Business-data flags (not app defects — the app is correctly reporting a real-world problem)

- **Prospecting PECR gate FAILING**: "23 non-Ltd prospect(s) have been emailed — PECR breach risk, stop and check". Matches existing open finding `e2e-prospecting-pecr-gate-fail` (task rec3PJCQt34r7ICrh). Recurrence logged, no new task.
- **AI Brain feed stale**: brain shows "Synced · 23 August 2026" — 2 days behind. Matches existing open finding `e2e-ai-brain-feed-stale` (same task rec3PJCQt34r7ICrh). Recurrence logged, no new task.
- **Costs**: 18 costs overdue; 15 costs with hard amount variance (>10%) awaiting reconciliation review.
- **Income**: 3 tenancies in Cash Flow Void — £2,293.68 at risk.

## Notes for tuning the sweep prompt

- PAGE_REGISTRY is now **29** ids; the sweep prompt still records 27. New since: `coa`, `kpi-library`, `agents`. Gone: `os-bplan`. 26 have an in-shell `tab-<id>` panel; the same 3 (compliance, crm, how-it-works) are standalone pages by design.
- Walking all 29 panels in a single browser eval **cannot** finish inside the 45s CDP limit — overview alone blocks ~8.4s, costs ~7.2s, transactions ~4.5s and the content-machine iframe ~26s. Walk in chunks of about six, or the eval times out and the page looks frozen when it is merely busy. A timed-out eval keeps running in the page, so a second walk launched on top of it stacks and makes the tab genuinely unresponsive for minutes.
- `mcp__Claude_Browser__preview_start` still cannot launch a local dev server unattended, and the harness browser's `javascript_tool` was refused by the auto-mode classifier the moment it was asked to write the token into localStorage (and on every call after). Kevin's signed-in Chrome remains the only route to FULL mode. Existing open finding `e2e-auth-js-denied` (task recrGG00unq8Pup2V).
- First dedupe query of the run returned a false zero because it requested a field named `Name`; the Tasks table field is `Task Name`, and an invalid `fields[]` entry returns an error object, not records. Controls caught it. Any future dedupe check here must request `Task Name`.
