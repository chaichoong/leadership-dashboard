# E2E Sweep — 2026-07-16

> Re-run at Kevin's request after a day of reconciliation fixes. Supersedes the 08:xx run
> (which was also clean). Adds STEP 4.5 live data invariants, which did not exist that morning.

## Summary
- Production URL: **UP** (HTTP 200, app shell + sidebar present)
- App boot: authenticated OK — 178 costs, 8,706 transactions
- Tabs enumerated from live PAGE_REGISTRY: **25**
- Tabs walked: 25 | **PASS: 25** WARN: 0 FAIL: 0
- Console errors across the entire walk: **0**
- Live data invariants (STEP 4.5): **all hold**
- No broken values in any UI (no `undefined` / `NaN` / `[object Object]` / error strings)

## Live data invariants — STEP 4.5
| Invariant | Result |
|---|---|
| report-amount-populated | PASS |
| split-override-sign | PASS — checked against 18+ live records |

The `split-override-sign` control matches a real population for the first time: before today
the base contained ZERO negative overrides, so that control matched nothing and the check was
vacuous by its own design. The 18 MHH expense portions created today give it teeth.

## Per-tab results
All 25 rendered with real content, an intentional auth prompt, or a valid empty state.

**In-shell (14):** overview (11/11 ✓) · cfv · money (4/4 ✓) · wealth · income (5/6 ✗) ·
ar-variable (6/6 ✓) · costs (3/5 ✗) · invoices · pnl · transactions · fintable · prospecting ·
sitemap · skills (4/5 ⚠)

**Iframe (9):** os-strategy · tasks · comms · operations · os-bplan · systemisation · os-team ·
content-machine (external, x-origin, HTTP 200) · ai-brain — all loaded with content.

**Standalone (2):** compliance (HTTP 200) · how-it-works (HTTP 200) — open in a new tab by
design (`window.open`), no in-shell panel.

Note: `cfv` and `wealth` read as still-loading on first touch and were re-checked — both
render fully (25,083 and 41,796 chars). Not a fault; they fetch on switch.

## Costs tab — the day's movement
| Metric | Start of day | Now |
|---|---:|---:|
| Overdue costs | 30 | **4** |
| Hard variance (>10%) | 25 | **13** |
| Active costs | 106 | 88 |

The remaining 3/5 on costs is the two zero-tolerance checks (overdue > 0, variance > 0). The
4 overdue are benign: Google Storage (July payment pending), Motivation (annual, no payment
history to anchor), Oura (unsynced Monese CB account), Hostinger (1d, lands about now).

## Reconciliation rules — verified live in this build
| Rule | Loaded |
|---|---|
| Reference matching (`pickCostByReference`) | YES |
| Amount matching (`pickCostForAmount`) | YES |
| Token extractor (`extractRefTokens`) | YES |
| Active-costs-only lookup (`isCostActive` filter) | YES |

Proven against real base data using the real functions:
- £45.30 → `recjoC2dmPuzZcUp7` (Swinton BE26ACTP, expects £45.30) ✓
- £42.01 → `recZeo9ZNlAwzdXCG` (Swinton RSAP, expects £42.01) ✓

**Reconciliation queue is EMPTY (0 unreconciled).** The rules are live but have nothing to act
on right now; they apply to the next transactions that arrive unreconciled.

## Failures needing attention
none

## Open items (not sweep failures)
- Anglian Water DD→property mapping unknown; the two £10 links are ARBITRARY. See the
  `project-anglian-water-dd-mapping` memory. Fix is to put the ref in the cost name.
- A parallel Claude session is active in this repo — today it stashed uncommitted edits,
  pushed main, and switched the working tree onto a feature branch mid-session.
