# Drift Report — 2026-08-22

Run as phase 3 of `daily-ops`. Queue lock NOT taken (Rule 1). Read-only with respect to code
(Rule 2, reinforced by the caller's override): no repo file edited, no `git add`, commit, pull,
branch, push or PR. This report is written to `monitoring/` and left uncommitted.

## Summary

- **Schema changes:** 0 new, 0 removed, 0 type changes, 0 renames — 119 tables, identical to the 21 Aug snapshot
- **Field references:** 0 dead of 422 mapped fields, 0 dead of 35 mapped tables, **0 unresolvable `fld`/`tbl` IDs across the whole repo** (649 distinct ID candidates, 341 rogue, 81 non-resolving and all of them `rec`/`sel` by construction)
- **SOP drift:** audited the newest shipped feature (client CEO Brief, PR #106, 21 Aug) against `docs/ceo-brief-client-onboarding.md` — **no documentation drift found**; 82 guard tests pass
- **Health checks:** 4 of 4 PASS, 0 warn, 0 fail. All non-vacuous except the sentinel branch of 4d, reported as 0-checked rather than as a pass
- **Auto-fixed:** 0 — prohibited
- **Filed to the findings queue:** 0 — no code defect found today
- **Airtable tasks created:** 1 (`recWpdm2wUmMHakDj`) — the client CEO Brief is merged but not live

---

## 1. Schema Changes

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat`. The token was never printed. Snapshot saved to
`monitoring/schema-2026-08-22.json`.

| Compared against | Tables | Fields added | Removed | Type changes | Renames |
|---|---|---|---|---|---|
| `schema-2026-08-21.json` | 119 = 119 | 0 | 0 | 0 | 0 |

The base has not moved since 16 Aug. Nothing to reconcile.

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (in `reference-map.json`, absent from schema) | **0 of 422** |
| DEAD tables | **0 of 35** |
| DEAD select choices | **0 of 11** — all 11 mapped `sel` IDs found in the raw schema |
| TYPE_MISMATCH | still cannot fire — the map stores no expected types (already filed, `20260814-drift-monitor-127`; not refiled). Proxy: 0 type changes against yesterday's snapshot |
| Rogue IDs (used outside `js/config.js`, absent from it) | 341 unique |
| **Unresolvable `fld`/`tbl` IDs** | **0** |
| `rec`/`sel` candidates that cannot resolve by construction | 81 |

Scan covered `.js`, `.mjs`, `.html` and `.py` across the whole repo, excluding `node_modules`,
`.git`, `monitoring/`, `test-results/`, `playwright-report/` and `.claude/`. Every `fld` and
`tbl` ID in the repo resolves against today's snapshot. Nothing is broken.

The 81 non-resolving candidates are the same three populations as 20 and 21 Aug: real record IDs
used as fixtures or pinned rows, deliberate placeholders (`recXXXXXXXXXXXXXX`,
`recTESTTESTTEST01`, `recHumanTeamRow00`, `recKARLO000000001`), and ordinary identifiers whose
first three letters collide with the prefix (`recentCorrections`, `reconciledInflows`,
`reconForecastKeys`, `recordSaveFailure`, `selectedProjectId`, `selectRecordAsync`). The meta API
carries no record IDs, so none of these can ever resolve. Expected, not investigated again.

The orphan-constant count was **not** re-derived today. `20260821-drift-279` established the
metric moves 17% run to run with no code change, so it cannot detect anything until it has a
script. Producing a fresh unreproducible number would be theatre.

## 3. SOP / documentation drift — the client CEO Brief (PR #106, merged 21 Aug)

Chosen because it is the only feature to ship since the last run, and because every other pair in
the SKILL's mapping has been audited in the past twelve days. Files: `ceo-brief-supabase.html`,
`js/ceo-brief-defaults.mjs`, `workers/ceo-brief-tenants/`, `docs/ceo-brief-client-onboarding.md`,
`ceo-brief-workflow.html`.

**No drift found.** What was checked, and what proved it:

- The onboarding doc prints its questions from `js/ceo-brief-defaults.mjs` and is guarded by
  `tests/ceo-brief-onboarding-doc.test.js`, which fails if a question exists in code and not in
  the doc. Ran it: **42 tests pass**. `tests/ceo-brief-tenants.test.js`: **40 pass**.
- `workers/ceo-brief-tenants/wrangler.toml` sets `crons = ["0 5-12 * * *"]` with no day-of-week,
  and decides the day and hour in `lib.mjs isSendWindow`. This is the shape the Cloudflare
  Sunday-is-1 incident demanded, and it is correct.
- `AI_MODEL_DEFAULT` / `AI_MODEL_LIGHT` in the worker's `[vars]` match `js/ai-models.js` exactly
  (`claude-sonnet-4-6`, `claude-haiku-4-5-20251001`). No stale model ID.
- `ceo-brief-workflow.html` describes Kevin's own brief only. It makes no claim that the client
  worker is live, so it is not misleading while the deploy is outstanding.
- `STRUCTURE.md` covers both new root pages.

One pre-existing gap, deliberately not filed: the `ceo-brief` entry in `PAGE_REGISTRY` has
`sopFile: ''`. So do ten other live pages (money, wealth, income, ar-variable, costs,
transactions, operations, kpi-library, fintable). That is a platform-wide decision about which
pages get an SOP, not drift introduced by this feature.

## 4. Health Check Results

Run against the deployed site in Kevin's own authenticated Chrome. **No token was entered or
read** — only presence was asserted. No `localStorage` value was printed.

| Check | Result | Evidence |
|---|---|---|
| **4a Badge consistency** | **PASS** | `detectCFVs()` returned 3 entries; all 3 survived `cfvIsVisible`; recount 2 `cfv` / 1 `cfv actioned`; the rendered `#cfvSidebarBadges` spans read **2** and **1**. Non-vacuous — 3 entries, split across two statuses |
| **4b Token/auth health** | **PASS** | `_dlr_pat` present and non-empty. Value never read |
| **4c Stale data** | **PASS** | `_dlr_cache` → `_dlr_dashcache_v2` `savedAt` **5 minutes** old, inside the 30-minute threshold. `allTransactions` 9,058 / `allTenancies` 94 / `allCosts` 180 / `allBusinesses` 5, all read lexically, not off `window` |
| **4d Dismissal-key integrity** | **PASS (date shape) / 0-checked (sentinel)** | Date shape: **108 keys checked, 0 invalid**. Sentinel shape: **0 keys checked — stated as zero, not as a pass** |

Console: no errors or exceptions on the tab.

**4c note, investigated and cleared.** The same IndexedDB store holds a second entry,
`ops_transactions`, whose `savedAt` is **23 h 40 m** old. That is not a stale-data defect:
`os/operations/index.html:3009` sets a 4-hour TTL and `:3112` enforces it on read, so the entry is
expired-and-ignored rather than expired-and-served. It is simply never evicted. Recorded so a
future run does not read the age as a new fault.

**4d note.** 108 `cfv_*` keys in four shapes: `_chaseStart`, `_startDate`, `cfv_dismissed_*`,
`cfv_reflag_dismissed_*`. No `_returned` or `_returnDismissed` key exists in this browser, so the
sentinel assertion had nothing to grade — the expected state already recorded in
`20260821-drift-278`, not a new defect.

## 5. Actions Taken

### Auto-fixed
None. Prohibited by Rule 2 and by the caller's override for this run.

### Filed to the findings queue
None. No code defect was found today, and the queue already holds 304 open items.

### Escalated to Airtable — 1 task

`recWpdm2wUmMHakDj` — **Drift Monitor [WARNING]: Client CEO Brief is merged but not live —
migration 0043 not applied and worker not deployed.** Status `To do`, Priority `High`, Time
Estimate `45 min`, due 23 Aug. Written without `typecast`, so no new single-select option could be
created; the write succeeded, which proves every value matched an existing option exactly.

Two verified facts behind it:

1. **Migration `0043_ceo_brief.sql` has not been run.** A REST probe of `ceo_briefs` on project
   `ptkyhzlsvijcwyovgrgv` returns HTTP 404 `PGRST205` (table not found). **Control:**
   `app_settings` and `org_modules` on the same project and the same key return HTTP 400 `42703`
   (column missing), which proves the probe reaches real tables and the 404 is genuine absence,
   not a broken query.
2. **Worker `ceo-brief-tenants` is not deployed.** The Cloudflare account lists 17 workers and it
   is not among them. The most recent deploy is `money-confidence-daily`, 21 Aug 16:55 — Kevin's
   own brief, which did ship.

Effect: PR #106 shipped the client CEO Brief yesterday and no client can be onboarded onto it.
Nothing was tracking either step. **Control on that claim:** the Tasks table returns 0 open tasks
matching "CEO Brief", "ceo-brief" or "worker", while the same query without the name filter
returns rows — so the zero is real, not a silent-zero from a wrong field name. The findings queue
carries no entry for it either.

An Airtable task rather than a finding because the fix is a credentialed deploy and a SQL run, not
a code edit. `queue-fixer` writes code only; a finding would have sat in a 304-item queue behind
six unmerged PRs.

### Not done
- `reference-map.json` not regenerated — `js/config.js` has not changed since the last run, so the
  map is current.
- No `git pull` — the working tree holds another routine's uncommitted reports and the caller
  barred repo writes.
- CHECK 3 was one pair, not ten. Every other pair in the mapping has been audited within the last
  twelve days and its findings are still open and unmerged; re-reporting them would inflate the
  queue without changing anything.
