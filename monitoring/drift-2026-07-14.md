# Drift Report — 2026-07-14

## Summary
- Schema changes: 3 new tables, 1 new field, 0 removed, 0 type changes (all benign, match known features)
- Field references: 0 dead, 0 type-mismatch, 0 orphan, 59 rogue (54 `fld` + 5 `tbl`, all live, chronic)
- SOP drift: 0 stale, 1 undocumented (Tasks Activity trail), 10 version gaps
- Health checks: 0 pass, 0 warn, 0 fail (all N/A — headless run, no interactive PAT auth)
- Auto-fixed: 1 item — `reference-map.json` extended with the new Prospects field/table IDs (committed to main, additive, no PR needed)
- Human review: 1 Airtable task created (recesmr9BNH2curGY, [INFO])

## 1. Schema Changes
Compared today's snapshot (`schema-2026-07-14.json`, 117 tables / 4865 fields) against the most recent prior snapshot (`schema-2026-07-13.json`, 114 tables).

**New tables (3) — all match known, recently-built features:**
- `tbl2ZTHBDBPo681UL` — **Task Activity** (audit trail for the Tasks page; see §3)
- `tbljHVGJoKJf8acy3` — **Prospects** (prospecting engine)
- `tblB5tZrXNaKFe02j` — **Prospect Keywords** (prospecting engine)

**New fields (1):**
- `Tasks.fldYcL44lWsSOvBcL` — "Task Activity" (multipleRecordLinks) — the link from Tasks to the new Task Activity table.

**Removed tables/fields:** none.
**Field type changes:** none.
**Field renames:** none.

None of these are concerning. All three tables map to features already recorded in project memory (Task Activity audit trail, Prospecting engine v1). They appear as "new" only because yesterday's snapshot predated them being written to the base.

## 2. Field Reference Issues
Verified all mapped field/table IDs in `reference-map.json` against today's schema snapshot.

- **DEAD (in code, not in Airtable): 0.** Every mapped field ID and table ID resolves to a live field/table.
- **TYPE_MISMATCH: 0.** No referenced field changed type (corroborated by zero type changes in §1). `reference-map.json` stores JS-constant names only, not expected types, so type verification is bounded by the day-over-day schema diff, which is clean.
- **ORPHAN: 0.** Feature files reference config field IDs by named `F.*` constant, not by raw `fld…` string, which is the intended architecture.
- **ROGUE (hardcoded IDs in js/ not in the central config map): 59 — chronic, unchanged.**
  - **54 `fld…` IDs** (identical count to 07-10 → 07-13). All resolve to live Airtable fields; none dead. Distribution: `js/dashboard.js` (Projects/KPI/Tasks/Systemisation cluster), `js/skills-data.js` (Tenancies/Tenants/Transactions/Costs/Tasks reference set), `js/sitemap.js` (SOP Update Requests, 6 IDs), `js/arrears.js` / `js/fintable.js`, and shared Chart-of-Accounts fields (`fldO4BTJhFv5EsN6i`, `fldii4oUzSfmplihO`, `fldqr09KqLGGYCYkC`) spread across the finance files.
  - **5 `tbl…` IDs**, all live: `tbl5MY9FbImFQfvez` + `tblZ75JgE1wzDP0ps` (`js/ai-assistant.js`), `tblHGNzDmOs59r9QD` (`js/skills.js`), `tblJ3GFnAAoXf99e9` (`js/dashboard.js`), `tbltuZz5Omrpo7t1x` (`js/sitemap.js`).

Centralising these into `config.js` is a multi-file refactor touching shared code — not a safe unattended auto-fix. No rogue reference is dead; no new rogue reference appeared this cycle.

**`reference-map.json` updated:** `js/config.js` grew from 372 → 399 mapped field IDs and 31 → 33 table IDs since the map was last generated. The 27 new fields and 2 new tables all belong to the new **Prospects** and **Prospect Keywords** tables (prospecting engine) and all resolve to live schema. Added them to the map with their JS-constant names (auto-fix, additive).

## 3. SOP Drift
Feature files changed since the last drift run (HEAD `09f2489`, diff from the 07-13 report commit `2ebc79e`): `index.html`, `js/ai-assistant.js`, `js/config.js`, `js/prospecting.js`, `js/shared.js`, `js/skills-data.js`, `os/tasks/index.html`. Of these, only **`os/tasks/index.html`** is an SOP-mapped feature file.

**UNDOCUMENTED (1):** `os/tasks/index.html` shipped a new user-facing **Task Activity audit trail** — an Activity section in the task drawer plus automatic logging of field changes and auto-status changes to the new Task Activity table (`tbl2ZTHBDBPo681UL`). `os/tasks/sop.html` has zero mentions of "activity" or "audit". Raised as human-review task **recesmr9BNH2curGY** ([INFO]).

*(New file `sop-prospecting.html` pairs with new `js/prospecting.js`; both are new and matched, and prospecting is outside the 10-pair SOP mapping — no drift.)*

**VERSION_GAP (pageVer ahead of sopVer)** — 10 standing gaps across the SOP-mapped pages, all pre-existing:

| SOP file | Page key | sopVer | pageVer |
|----------|----------|--------|---------|
| sop.html | overview | 2.9 | 2.41 |
| sop-cfvs.html | cfv | 1.6 | 1.29 |
| sop-invoices.html | invoices | 2.2 | 2.18 |
| sop-pnl.html | pnl | 2.12 | 2.25 |
| sop-compliance.html | compliance | 1.1 | 1.11 |
| sop-sitemap.html | sitemap | 1.2 | 1.17 |
| inbound-comms-sop.html | comms | 2.4 | 2.52 |
| os/strategy/sop.html | os-strategy | 1.0 | 1.15 |
| os/tasks/sop.html | tasks | 1.1 | 1.110 |
| os/business-plan-builder/sop.html | os-bplan | 1.3 | 1.14 |

`pageVer` auto-bumps on every source push; `sopVer` is bumped manually only when the SOP is rewritten. The gaps are documentation lag, not a code fault. The Tasks gap now has confirmed behavioural drift (the Activity trail above); the CFV gap remains tracked under existing task `rec9ihFqDDSY8DMLZ` and is not re-raised.

## 4. Health Check Results
- 4a. Badge/data consistency — **N/A** (no authenticated browser session)
- 4b. Token/auth health (`window.PAT`) — **N/A** (interactive Airtable auth not available in headless run)
- 4c. Stale-data timestamp — **N/A**
- 4d. localStorage dismissal integrity — **N/A**

Scheduled headless run. The dashboard requires interactive PAT auth to populate `window.PAT`, `window.allTransactions`, etc., which cannot happen without a logged-in browser. All four skipped, consistent with every prior scheduled cycle. Not counted as failures.

## 5. Actions Taken

### Auto-fixed
- **`monitoring/reference-map.json`** — added 27 new field IDs + 2 new table IDs (the Prospects / Prospect Keywords tables) with JS-constant names; updated summary counts to 399 fields / 33 tables and generated date to 2026-07-14. Additive, safe. Committed to main with this report (no code touched, so no PR).

No code auto-fixes: 0 dead references, no simple renames. The 59 rogue hardcoded IDs are a chronic multi-file condition that cannot be safely centralised unattended. No PR created.

### Escalated to Human Review
- **recesmr9BNH2curGY** — "Drift Monitor: [INFO] Document new Task Activity audit trail in os/tasks/sop.html". Due 2026-07-15. Documents the undocumented Activity trail feature and the pageVer/sopVer gap.

### Not re-raised (avoid duplicate noise)
- **rec9ihFqDDSY8DMLZ** — open Drift Monitor task covering CFV detection SOP drift / general SOP lag. Still valid; left as-is.
