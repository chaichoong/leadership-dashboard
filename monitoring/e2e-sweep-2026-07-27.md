# E2E Sweep — 2026-07-27

## Summary
- Production URL: UP (200) — title "Operations Director" present in body
- Mode: FULL (authenticated, JS injection worked; PAT loaded, 8,728 transactions / 180 costs / 94 tenancies in memory)
- Tabs walked: 25 | PASS: 22  WARN: 3  FAIL: 0  UNVERIFIED: 0
- Data invariants: PASS (3 checked, control populations 100 / 18 / 7,682 — none starved)
- Console errors across the whole walk: 0

Notes on this run:
- Port 8951 was already held by another chat's `ops-director-wealth` dev server. Per the sweep instructions, reused it by opening a browser tab at `http://localhost:8951/` rather than starting a second server.
- The app globals (`PAT`, `allTransactions`, etc.) are `let`-declared, so they resolve as bare identifiers but read as `undefined` on `window.*`. Auth confirmed via bare-identifier eval, not `window.PAT`.
- `compliance` and `how-it-works` navigate the top document (full-page views, not in-panel iframes), so they were verified individually with a navigate-back reset between them. The other 9 iframe tabs embed in-panel and were safe to walk in a batch.

## Per-tab results

### JS-rendered tabs (14) — all rendered, no console errors, no leaked undefined/NaN/[object Object]
- overview — PASS. Leadership Dashboard, 11/11 checks ✓, real financials (opening £3,017.05, income £33,180–£34,077, costs £22,740.04). 286k chars.
- cfv — PASS. Cash Flow Voids, 6/6 checks ✓.
- money — PASS. Money Confidence "safe to spend today", 4/4 checks ✓.
- wealth — PASS. Net Worth £2.9m, 7/7 checks ✓ (a transient 5/7 during first pass resolved to 7/7 on re-read). 538k chars.
- income — WARN. Renders fully (65 income sources: 35 Agent-Managed, 23 Universal Credit, 7 Working; sortable/filterable table with real tenant rows). Internal health bar shows 5/6 checks ✗. No console error, no broken value — the ✗ is one of the page's own self-diagnostic checks failing, not a render fault.
- ar-variable — PASS. Accounts Receivable Variable, 6/6 checks ✓.
- costs — WARN. Renders fully (Accounts Payable Fixed, real cost rows with variance badges). Internal health bar shows 3/5 checks ✗. Same as income: render + data fine, an internal self-check is red.
- invoices — PASS. Accounts Payable Variable, 7/7 checks ✓.
- pnl — PASS. Profit & Loss live from categories, 8/8 checks ✓.
- transactions — PASS. 8,728 transactions listed, 6/6 checks ✓.
- fintable — PASS. Accounts / bank monitors, 5/5 checks ✓.
- prospecting — PASS. Prospecting tab, 7/7 checks ✓.
- sitemap — WARN. Renders (Site Map & Guides, 25 pages). Internal health bar shows 3/7 checks ⚠.
- skills — PASS. Skills Library, 5/5 checks ✓.

### In-panel iframe tabs (9) — iframe present with valid src and real content
- os-strategy — PASS. `os/strategy/index.html`. "Objective & Strategy · Live · linked to Airtable", business selector prompt.
- tasks — PASS. `os/tasks/index.html`. "Who are you?" name selector (KB / MA / …).
- comms — PASS. `follow-up.html`. Inbound Comms Tracker Google sign-in gate (expected — needs Google auth to show emails).
- operations — PASS. `os/operations/index.html`. 8.6k chars of content.
- os-bplan — PASS. `os/business-plan-builder/index.html`. Business Launch Plan Builder intro.
- systemisation — PASS. `os/systemisation/index.html`.
- os-team — PASS. `os/team/index.html`. 6 active team members, synced.
- content-machine — PASS (load-only). Iframe src `https://chaichoong.github.io/content-machine/` — external origin, so contents are not inspectable cross-origin. Iframe element present with valid src; render confirmed to the extent same-origin policy allows.
- ai-brain — PASS. `ai-brain.html`, 3k chars of content.

### Full-page tabs (2)
- compliance — PASS. Property Compliance page, 7/7 checks ✓. Real data: 1 expired, 15 active, 23 no-cert, property × GSC/EICR/insurance table.
- how-it-works — PASS. "How Operations Director Works" guide page, full 18-journey module map rendered.

## Data invariants
Ran `python3 scripts/check-data-invariants.py --json` (read-only). Exit 0.
- report-amount-populated — PASS. Asserts `**GBP set => Report Amount set`. Control population 100 (guards the Jul 2026 8,667-txn Report Amount blanking).
- split-override-sign — PASS. Asserts `override set => sign(Report Amount) == sign(**GBP)`. Control population 18 (guards the split sign-flip incident).
- no-reimport-duplicates — PASS. Asserts one bank transaction => one record. Control population 7,682.
All three controls matched a live population, so none of the checks was silently asserting nothing.

## Failures needing attention
None. Zero FAILs, production up, all data invariants hold.

Watch-items (not failures, no escalation): income (5/6 ✗) and costs (3/5 ✗) show failing entries in the app's own health bar, and sitemap shows 3/7 ⚠. These are the platform's built-in self-diagnostics — the tabs themselves render correctly with real data and no console errors, so they are not E2E failures. Flagging for Kevin's awareness in case an automation behind those checks has gone stale.
