# E2E Sweep — 2026-08-23

## Summary
- Production URL: **UP (200)** — https://chaichoong.github.io/leadership-dashboard/ served the app shell (`<title>Operations Director</title>`).
- Mode: **FULL (authenticated, JS)** — driven through Kevin's already-signed-in Chrome (claude-in-chrome MCP) against production, so no Airtable token was handled and none entered a transcript. Confirmed authenticated with bare identifiers: `PAT` is a string >40 chars, `allTransactions` 9,066, `allTenancies` 94, `allCosts` 180, `tab-overview` 287,173 chars.
- Tabs walked: **25 in-shell panels + 3 standalone pages = 28** | PASS: 28 WARN: 0 FAIL: 0 UNVERIFIED: 0
- Data invariants: **PASS** — 15 checks, 14 PASS, 1 SKIP (empty population, not a control failure). Script exit 0.
- Console errors: **zero** across the whole walk.

## Registry shape (changed since this sweep's notes were last updated)
`PAGE_REGISTRY` now returns **28** ids, up from the 27 recorded in the skill. Added since: `coa`, `kpi-library`. Removed: `os-bplan` (Plan Builder retired from the shell 1 Aug 2026). 25 have an in-shell `tab-<id>` panel; the three that do not (`crm`, `how-it-works`, `compliance`) are standalone pages by design and were checked by URL instead. Noted so Kevin can retune the prompt; not a bug.

## Per-tab results

All 25 in-shell panels rendered non-empty with real data, no `undefined` / `NaN` / `[object Object]` leaking into the UI, and no console errors.

| Tab | Result | Evidence |
|---|---|---|
| overview | PASS | 293,669 chars, health bar 11/11 ✓, "Sunday, 23 August 2026" |
| os-strategy | PASS | iframe `os/strategy/index.html` loaded (body >200 chars) |
| tasks | PASS | iframe `os/tasks/index.html` loaded |
| cfv | PASS | 108,500 chars, 6/6 ✓ |
| ceo-brief | PASS | 91,440 chars, 4/4 ✓ |
| money | PASS | 8,348 chars, 4/4 ✓ |
| wealth | PASS | slow loader — 246 chars at first read, 681,843 chars once settled |
| income | PASS (render) | 75,726 chars. Health bar red 5/6, but the failing check is business data, not a defect: "Cash flow voids — 3 tenancy/ies in CFV — £2,293.68 at risk" |
| ar-variable | PASS | 7,217 chars, 6/6 ✓ |
| costs | PASS (render) | 2,020,205 chars. Health bar red 3/5, both failures are business data: "11 cost(s) overdue" and "15 cost(s) with hard variance (>10%)" |
| invoices | PASS | 8,048 chars, drawer all green once Gmail data settled |
| pnl | PASS | 83,040 chars, 8/8 ✓ |
| transactions | PASS | 888,765 chars, 6/6 ✓, 9,0xx transactions rendered |
| coa | PASS | 93,744 chars, drawer all green once settled |
| comms | PASS | iframe `follow-up.html` loaded |
| operations | PASS | iframe `os/operations/index.html` loaded |
| kpi-library | PASS | 50,917 chars, 5/5 ✓ |
| fintable | PASS | 25,953 chars, drawer all green once settled |
| systemisation | PASS | iframe `os/systemisation/index.html` loaded |
| os-team | PASS | iframe `os/team/index.html` loaded |
| content-machine | PASS | iframe `https://chaichoong.github.io/content-machine/` loaded |
| prospecting | PASS (render) | 681,603 chars. Health bar red 6/7 — see Findings |
| sitemap | PASS | 37,454 chars. Two ambers — see Findings |
| skills | PASS | 342,454 chars, 5/5 ✓ |
| ai-brain | PASS (render) | iframe `ai-brain.html` loaded, 26,830 chars. Feed date stale — see Findings |
| crm-supabase.html | PASS | HTTP 200 |
| how-it-works.html | PASS | HTTP 200 |
| compliance.html | PASS | HTTP 200 |

**Read-only interactions exercised:** `switchTab()` navigation across all 25 panels, plus the health drawer (`toggleHealthDrawer`) expanded on income, costs, prospecting, invoices, coa, fintable, sitemap and skills. Every drawer responded and re-ran its checks. Nothing was created, edited, approved, paid, deleted or sent.

## Data invariants (`scripts/check-data-invariants.py --json`, exit 0)

15 invariants: **14 PASS, 1 SKIP, 0 FAIL, 0 CONTROL_FAILED.**

Every PASS fired against a real control population, so none passed on an empty set. Notable populations: `no-reimport-duplicates` 8,003; `report-amount-populated` 100; `split-override-sign` 18; `open-tasks-carry-no-completion-date` 100; `one-monthly-summary-per-business-month` 41; `ceo-brief-complete` 15; `automated-kpis-have-actually-run` 5; `kpi-library-coverage` 5.

The one SKIP is `sop-queue-not-abandoned` — there are no queued-but-unprocessed SOP requests to assert against. Field names were verified live, so it starts asserting the moment there are any. Not a control failure.

Specifically clean today: `open-tasks-carry-no-completion-date` PASS with a control population of 100. That is the **third consecutive clean run** of the check behind the open [CRITICAL] task `recR0KMvBmvM1ZVK2`.

## Failures needing attention

**No platform defect found today.** Production is up, every tab renders with real data, no console errors, no broken invariant. The three items below are the app's own health checks correctly reporting problems in the *data*, and all three are recurrences of findings already open with Kevin.

1. **PECR gate: 23 non-Ltd prospects emailed** (`FINDING-KEY: e2e-prospecting-pecr-gate-fail`, task `rec3PJCQt34r7ICrh`). The Prospecting health bar reads: "23 non-Ltd prospect(s) have been emailed — PECR breach risk, stop and check them". This was **19 yesterday**, so it is getting worse, not holding. Compliance exposure, Kevin's call.
2. **AI Brain feed 5 days stale** (`FINDING-KEY: e2e-ai-brain-feed-stale`, same task `rec3PJCQt34r7ICrh`). The AI Brain Today tab reads "Synced · 18 August 2026" against today's 23 August. That is 5 days, up from 3 yesterday. The nightly publisher has not landed a fresh feed since the 18th.
3. **13 pages have an SOP version behind `pageVer`** (sitemap tab amber). Housekeeping, not a defect — `pageVer` auto-bumps on push, `sopVer` is bumped by hand. The second sitemap amber ("GitHub status not yet fetched") is a lazy-load state, not an error.

No new task was created — all three findings are already open and were updated in place per the dedupe rule. The duplicate-search control (`FIND("FINDING-KEY:", {Notes})` against open tasks) returned 3 rows, proving the query fires rather than returning a silent zero.

## Notes for tuning this sweep
- The Chrome route (drive Kevin's signed-in profile at production) is now the reliable way to reach FULL mode without ever handling the token. It worked cleanly today for the third day running. The harness browser (`mcp__Claude_Browser__preview_start`) still cannot launch a local dev server in an unattended session, and injecting the token into its localStorage is not something this sweep will do.
- `PAGE_REGISTRY` should be read live every run — it changed again today (28 ids). The skill's hardcoded list is now two versions behind.
- Chrome's `javascript_tool` returns `[BLOCKED: Cookie/query string data]` when a result string contains something that looks like a query string. Build results as arrays/objects of short fields rather than long joined strings.
