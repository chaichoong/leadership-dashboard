# Drift Report — 2026-08-24

Run as phase 3 of `daily-ops`. Queue lock NOT taken (Rule 1). Read-only with respect to code
(Rule 2, reinforced by the caller's override): no repo file edited, no `git add`, commit, pull,
branch, push or PR. Report written to `monitoring/` and left uncommitted.

## Summary

- **Schema changes:** 0 new, 0 removed, 0 type changes, 0 renames — 119 tables, identical to the 23 Aug snapshot
- **Field references:** 0 dead of 422 mapped fields, 0 dead of 35 mapped tables, 0 dead of 10 mapped select choices, **0 unresolvable `fld`/`tbl` IDs across the whole repo**
- **Rogue IDs:** 342 unique Airtable IDs used outside `js/config.js` across 282 scanned files — every `fld`/`tbl` one resolves
- **Coverage gap:** 44 IDs in `config.js` absent from `reference-map.json` (486/530 = 92%). Unchanged from yesterday. Already open as `20260814-drift-monitor-128` — not refiled
- **SOP drift:** 13 of 15 registry entries with an SOP file show a version gap; 13 entries have no SOP file at all
- **Health checks:** SKIPPED — no browser in an unattended session. Static substitute run instead (§4)
- **Auto-fixed:** 0 — prohibited
- **Filed to the findings queue:** 2 — `20260824-drift-monitor-332`, `20260824-drift-monitor-333`
- **Airtable tasks created:** 0 — nothing today met the human-review bar

---

## 1. Schema Changes

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat`. The token was never printed. Snapshot saved to
`monitoring/schema-2026-08-24.json`.

| Compared against | Tables | Fields added | Removed | Type changes | Renames |
|---|---|---|---|---|---|
| `schema-2026-08-23.json` | 119 = 119 | 0 | 0 | 0 | 0 |

The base has not moved since 16 Aug. Nothing to reconcile.

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (`reference-map.json` vs schema) | **0 of 422** |
| DEAD tables | **0 of 35** |
| DEAD select choices | **0 of 10** (choices re-derived from the raw schema, not the compact snapshot) |
| TYPE_MISMATCH | still cannot fire — the map stores no expected types (`20260814-drift-monitor-127`, open). Proxy: 0 type changes vs yesterday |
| Rogue IDs outside `js/config.js` | 342 unique across 282 scanned files |
| **Unresolvable `fld`/`tbl` IDs, whole repo** | **0** |
| IDs in `config.js` NOT covered by the map | **44** (38 `fld`, 3 `sel`, 2 `rec`, 1 `tbl`) — all resolve |

Scan covered `.js`, `.mjs`, `.html` and `.py` across the whole repo, excluding `node_modules`,
`.git`, `monitoring/`, `test-results/`, `playwright-report/` and `.claude/`. Boundary-anchored
regex per the 2026-08-04 correction.

**81 candidates did not resolve, and all 81 are expected.** Every one is a `rec` or `sel` ID.
The compact snapshot stores table and field IDs only, so a record ID can never resolve against
it, and select-choice IDs live under `options.choices` in the raw response rather than in the
snapshot. The two `sel` IDs found in code (`selI6Otekm3JyaBSJ`, `selx5WZMIWgrHrPOj`) were checked
directly against the raw schema and both exist. The residue also includes the known JavaScript
false positives the SKILL warns about — `selectedProjectId`, `reconciledInflows`,
`reconForecastKeys`, `recordSaveFailure`, `recentCorrections`, `recentlyCompleted`,
`selectRecordAsync` — plus deliberate test fixtures (`recTESTTESTTEST01`, `recKARLO000000001`,
`recWaitingOnKevin`). **No `fld` or `tbl` ID anywhere in the repo failed to resolve.**

Heaviest rogue-ID files: `os/tasks/index.html` (90), `os/team/index-supabase.html` (84),
`os/tasks/index-supabase.html` (74), `os/tasks/supabase-shim.js` (59), `follow-up.html` (50),
`follow-up-supabase.html` (50), `scripts/sync-airtable-mirror.py` (37), `os/strategy/strategy.js` (27).

The orphan-constant count was **not** re-derived. `20260821-drift-279` established it moves ~17%
run to run with no code change, so it cannot detect anything until it has a script.

## 3. SOP Drift

**Pair audited this run: `guides/skills.html` ↔ `js/skills.js` / `js/skills-data.js`.**
Chosen because every pair in the SKILL's mapping table has been deep-audited in the past twelve
days, and the skills guide carries the widest version gap of any entry (pageVer 1.10, sopVer 1.0,
file untouched since 29 Jul while `js/skills.js` grew to 732 lines).

**Most of this pair's drift is already on the queue and was not refiled.** `20260811-drift-077`
covers "Run a skill with one click" versus what `runSkill` actually does (opens an AI chat, PATCHes
the active-preset record, can pop a Drive tab). `20260811-drift-078` covers the library opening
filtered to My Skills and the undocumented AI Agents strip. Both remain accurate today; I re-read
the code against them rather than filing twins.

**What is still correct.** The four source pills the guide names (My Skills, Active, Presets, All)
match `renderSourcePills` (:536-565) exactly, including their meanings. The category examples the
guide gives, Operations and Legal, are both real entries in `SKILLS_CATEGORIES`
(`js/skills-data.js:7834`). The search box and the per-card detail expansion work as described.

**What has drifted — 2 filed:**

- `20260824-drift-monitor-332` (medium, code): `sendSkillMessage` pushes the user turn into
  `_skillConversation` before the fetch (:387) and pushes the assistant turn only on success
  (:411). The catch block (:415-418) renders the error into the chat but appends nothing to the
  array, leaving it ending on a user turn. The next reply appends a second consecutive user turn
  and the Anthropic Messages API rejects non-alternating roles, so every later message in that
  modal fails too. One transient blip permanently kills a skill run, and it presents as a fresh
  400 rather than as fallout from the first failure.
- `20260824-drift-monitor-333` (low, docs): the guide documents no Refresh button and none of the
  five checks `registerSkillsSyncBar` runs (:607-648). Two of those checks have user-visible
  consequences — a failed SOP fetch silently removes every SOP-generated skill from the list, and
  an unreadable settings record makes the Active pill count wrong — and the guide gives the user
  nothing to read them by.

### Version gaps

| Entry | pageVer | sopVer | SOP file |
|---|---|---|---|
| overview | 2.75 | 2.9 | sop.html |
| os-strategy | 1.40 | 1.1 | os/strategy/sop.html |
| tasks | 1.140 | 1.4 | os/tasks/sop.html |
| invoices | 2.23 | 2.19 | sop-invoices.html |
| pnl | 2.32 | 2.26 | sop-pnl.html |
| coa | 1.3 | 1.1 | sop-coa.html |
| comms | 2.64 | 2.60 | inbound-comms-sop.html |
| compliance | 1.15 | 1.14 | sop-compliance.html |
| systemisation | 1.6 | 1.0 | guides/systemisation.html |
| crm | 1.1 | 1.0 | guides/crm.html |
| prospecting | 1.15 | 1.5 | sop-prospecting.html |
| sitemap | 1.24 | 1.19 | sop-sitemap.html |
| skills | 1.10 | 1.0 | guides/skills.html |

Only `cfv` (1.33/1.33) and `ai-brain` (1.0/1.0) are aligned. Thirteen further registry entries
carry no SOP file at all. As established on 20-23 Aug, **this metric carries no signal**: `pageVer`
is auto-bumped by a GitHub Action on every source push while `sopVer` moves by hand, so a gap
opens on any change however small. It cannot distinguish a stale SOP from a typo fix, and the
`overview` row shows the comparison is not even well ordered (2.75 versus 2.9 depends on whether
you read the parts as integers or decimals).

## 4. Health Check Results

**SKIPPED — no browser.** This ran unattended inside `daily-ops`, so `preview_start` was not used;
starting a preview server here would also leave a port held after the session ends, which the
session-hygiene rule forbids.

Static substitute, run against source instead:

| Check | Result | Evidence |
|---|---|---|
| 4a badge counts | not verifiable statically | needs a live render |
| 4b PAT present | not verifiable statically | `_dlr_pat` never read; correct behaviour per the 2026-08-03 correction |
| 4c stale data | not verifiable statically | needs a live timestamp |
| 4d dismissal-key shapes | **PASS on catalogue** | Both shapes still exist in `js/cfv.js` and match the SKILL's catalogue: sentinel `'1'` written at :240, :784, :941 (`_returned`, `_returnDismissed`); ISO strings written at :337, :818, :867, :897 (`_chaseStart`, `cfv_dismissed_*`, `cfv_reflag_dismissed_*`) |

**Re-confirmed today:** `cfv_{id}_startDate` still has three `removeItem` calls (:246, :821, :935)
and no writer anywhere in the repo, while `sop-cfvs.html:391` and `:630` document it as the store
behind the Days Overdue figure. Already open three times over as `20260821-drift-276`,
`20260810-drift-055` and `20260816-drift-165`. Not refiled.

## 5. Actions Taken

### Auto-fixed
None. Prohibited by Rule 2 and by the caller's override.

### Filed to the findings queue
| ID | Severity | Item |
|---|---|---|
| `20260824-drift-monitor-332` | medium | Skills chat left with consecutive user turns after any AI error, breaking the run permanently |
| `20260824-drift-monitor-333` | low | `guides/skills.html` documents neither the Refresh button nor the five Skills health checks |

Queue depth after this run: 333 findings.

### Escalated to Human Review
None. No schema change, no type mismatch, no dead reference and no health-check failure occurred,
so nothing met the bar for an Airtable task.
