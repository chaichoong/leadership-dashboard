# Drift Report — 2026-07-30

## Summary
- Schema changes: **0 new, 0 removed, 0 type changes, 0 renames** (vs 2026-07-29)
- Field references: 0 dead, 0 type mismatches, 106 unique rogue (chronic, all resolve — **prior reports undercounted, see §2**)
- SOP drift: 0 new stale, 0 new undocumented, 23 version gaps (unchanged)
- Health checks: 5 static PASS, **runtime checks RAN this time** — page loads clean, 0 console errors
- Auto-fixed: 0 items (no PR — nothing eligible under standing policy)
- Human review: **2 new Airtable tasks**. Yesterday's 2 tasks both fixed and closed; the whole
  Drift Monitor backlog is now Completed — 0 open for the first time.

**One line:** Yesterday's two findings shipped as real fixes, and today's are both about the same
new thing — the client guides that landed yesterday are wired into slots built for developer SOPs,
so one Site Map click would overwrite a guide, and three Site Map rows now open nothing at all.

---

## 1. Schema Changes

**Zero.** Base is 118 tables / 4,877 fields, identical to yesterday, field for field.

No new tables, no removed tables, no new fields, no removed fields, no renames, no type changes.
Snapshot: `monitoring/schema-2026-07-30.json` (Airtable meta API, PAT at `~/.config/od/airtable_pat`).

---

## 2. Field Reference Issues

### DEAD (in code, not in Airtable)
**None.** All 413 field IDs, 35 table IDs, 19 record IDs and 11 select-choice IDs in
`monitoring/reference-map.json` resolve against today's snapshot.

### TYPE_MISMATCH
**None.** The day-over-day type diff is 0 changes across all 4,877 fields.

### reference-map.json sync
Exact sync with `js/config.js`: 413/413 fields, 35/35 tables, 19/19 records, 11/11 select choices —
0 missing, 0 stale. Config grew yesterday (403 → 413 fields, 34 → 35 tables) when the CEO Briefs
table and its 10 field IDs were added; the map was regenerated in the same commit (`f19dfc6`).
Stamp refreshed to 2026-07-30. No ID content changed.

### ROGUE (hardcoded field IDs in `js/` bypassing config.js) — 106 unique / 203 raw

| File | Unique IDs | What they are |
|---|---|---|
| [js/skills-data.js](js/skills-data.js) | 57 | Tenancies / Costs / Tasks fields inside skill definitions |
| [js/dashboard.js](js/dashboard.js) | 43 | Projects table field set |
| [js/arrears.js](js/arrears.js) | 17 | Tasks link fields |
| [js/fintable.js](js/fintable.js) | 6 | Accounts `**Institution`, `**Fintable User` |
| [js/sitemap.js](js/sitemap.js) | 6 | SOP Update Requests table |
| [js/pnl.js](js/pnl.js) | 3 | |
| [js/reconciliation.js](js/reconciliation.js) | 3 | |
| [js/transactions.js](js/transactions.js) | 3 | |
| [js/costs.js](js/costs.js) | 2 | Accounts `*Name` |
| [js/cashflow.js](js/cashflow.js) | 1 | |
| [js/wealth.js](js/wealth.js) | 1 | |
| [js/wealth-ratios.js](js/wealth-ratios.js) | 1 | |

**Every one resolves to a live field. Zero unresolved.**

**Correction to prior reports.** Yesterday's report said "52 unique, 58 raw" across 6 files. That
figure was wrong, and nothing in the code changed to cause the jump. Checked directly: for each of
the 12 files above, `git show 5e296ef:<file> | grep -oE 'fld[A-Za-z0-9]{14}' | sort -u | wc -l`
returns exactly the same count as the file does today — dashboard.js 43 then and now, skills-data.js
57 then and now, arrears.js 17 then and now. So the prior run's grep missed six files entirely and
undercounted the six it did read. Today's number is the true one and the correct baseline going
forward. Method used, so it can be repeated: every `js/**/*.js` except `js/config.js`, regex
`fld[A-Za-z0-9]{14}`, unique per file then unioned.

A monitor that silently undercounts is the same failure mode as a `filterByFormula` typo returning
zero rows, which is why this is written down rather than quietly fixed.

**Not auto-fixed, per standing policy** — moving these into `config.js` edits a shared file every
feature loads. Reviewed refactor, not an unattended one.

### Airtable IDs outside `js/` — all clean
Scanned `scripts/**`, root HTML, `os/**` and `guides/**`. Heaviest users:
`scripts/sync-airtable-mirror.py` (140), `os/tasks/index.html` (94),
`scripts/slack-automation/money-daily-worker.js` (23). **0 unresolved field IDs, 0 unresolved table
IDs.** The worker still reads the CLAUDE.md-mandated LEGACY `Payment Status` (`fldXZNI96v8HgjuSh`)
on Costs, not `Cost Status`.

---

## 3. SOP Drift

### STALE_SOP / UNDOCUMENTED
**None new.** 20 commits landed since yesterday's report, but only six `js/` files changed
(`ceo-brief.js`, `config.js`, `costs.js`, `prompts/boardroom-mentor.js`, `shared.js`, `wealth.js`)
and none of them is the primary source for a page that has an SOP file. `costs` and `wealth` both
have no SOP to fall behind. Everything else was Supabase twins, guides, workers and docs.

`guides/` is properly recorded in [STRUCTURE.md:18](STRUCTURE.md:18), as CLAUDE.md requires.

### NEW — the SOP regenerator would eat the client guides — WARNING

Yesterday four pages had their `sopFile` repointed at the new plain-English guides:

| Page | sopFile now | pageVer vs sopVer |
|---|---|---|
| systemisation | `guides/systemisation.html` | 1.6 vs 1.0 — **button live** |
| skills | `guides/skills.html` | 1.4 vs 1.0 — **button live** |
| ai-brain | `guides/ai-brain.html` | 1.0 vs 1.0 |
| crm | `guides/crm.html` | 1.0 vs 1.0 |

The Site Map's "request SOP update" button fires whenever `pageVer !== sopVer`
([js/sitemap.js:748](js/sitemap.js:748)) and writes `p.sopFile` into the SOP Update Requests table.
The nightly `sop-update-processor` then regenerates that exact path from scratch, copying the
developer SOP CSS verbatim out of the first 106 lines of `sop-pnl.html` (step 2e of its SKILL.md).
One click on Systemisation or Skills replaces a client guide with a technical SOP.

Two of the four already qualify today, so this is live, not theoretical. The processor's
file-to-page table also has no row for any of the four, and still lists `os-hub → os/index.html`
— a file that no longer exists (confirmed: `ls os/index.html` → No such file).

Escalated as `recmGGMiMCLpGpZRg`. Not auto-fixed: the fix needs a decision first (separate
`guideFile` field, or teach the regenerator to skip `guides/`), and it lands in shared `config.js`.

### VERSION_GAP (pageVer ahead of sopVer) — 23, unchanged

Same 23 as yesterday. Registry is now 27 entries (was 24) after `ceo-brief`, `crm` and the guides
work; the three new entries are all 1.0/1.0 so the gap count did not move. Versions compared as
tuples, not decimals.

Worst five: tasks 1.112 vs 1.1, comms 2.53 vs 2.4, operations 1.43 vs 1.0, overview 2.43 vs 2.9,
cfv 1.30 vs 1.6. **12 of the 23 have no `sopFile` at all**, so their `sopVer` sits at the 1.0
default and will never move — for those the gap means "there is no SOP", not "the SOP is stale".
Chronic backlog; not escalated per-page.

### INFO: `crm` will never bump its version
`crm` is in `PAGE_REGISTRY` but has no row in `scripts/pre-commit-action.py` `FILE_TO_PAGE`, so
`crm-supabase.html` can change forever while CRM's `pageVer` stays 1.0. It changed 30 lines
yesterday (`165c969`) and did not move. `js/ceo-brief.js` **was** added to that mapping yesterday,
so this is the one new page that got missed. Carried inside task `rec6WhHV3gsZ6dDj0`.

---

## 4. Health Check Results

| Check | Result |
|---|---|
| Static: 413 config + 106 rogue field IDs all resolve to live fields | **PASS** |
| Static: schema snapshot written and diffed | **PASS** (zero drift) |
| Static: hardcoded AI model IDs (`scripts/check-model-ids.sh`) | **PASS** (exit 0) |
| Static: forbidden-pattern scan | **PASS\*** |
| Static: sync-invariant gate (`npx playwright test tests/sync-invariants/`) | **70 passed, 0 failed** (was 61) |
| Static: unit suite (`npx vitest run`) | **57 passed, 0 failed** across 4 files |
| Runtime: `index.html` loads, console clean | **PASS** — 0 errors, 0 warnings |
| Runtime: globals present | **PASS** — PAGE_REGISTRY 27, TABLES 29, F 92, `escHtml`/`switchTab` live |
| Runtime: localStorage dismissal JSON valid | **PASS** (0 malformed) |
| Runtime: CEO Brief tab wiring | **PASS** — registry + panel + sidebar + `renderCeoBriefTab` + sync bar |
| Runtime: `journey.html` loads | **PASS** — 0 errors, `noindex` set, no broken images |
| Runtime: `guides/*.html` assets | **PASS** — 7 files, all link `tokens.css`, 0 missing images |
| Runtime: Site Map "Open" link for 3 pages | **FAIL** — silent dead click, see below |
| Live: 09:00 CEO brief written today | **PASS** — `recDX4qwkkHtKlzIP`, 2026-07-30 |
| Live: `/ceo-transcript` endpoint deployed | **PASS** — 403 without key |
| Runtime 4a badge counts / 4b PAT auth | NOT RUNNABLE headless (needs the in-app auth token) |

**Runtime checks ran this time.** Prior reports skipped §4 entirely as "no browser". A local server
plus the browser tools gets everything that does not need the Airtable PAT the in-app auth flow
injects. Only 4a and 4b still need a signed-in session.

### Both worker deployments confirmed live — not assumed
Yesterday's report could only say the worker code had changed. Today it is verified two ways:

1. **The 09:00 brief fired.** `CEO Briefs` holds a record dated **2026-07-30**
   (`recDX4qwkkHtKlzIP`, Money Light `green`, Safe To Act £244.64, One Thing "Do one focused
   session on Operation OS: advance the Supabase migration"). The 09:00 cron only exists in the
   new `wrangler.money-daily.toml`, so the deploy went out.
2. **The talk-back endpoint answers.** `GET https://contractor-bot.kevinbrittain.workers.dev/ceo-transcript`
   with no key returns **403**, which is the new handler's own guard. A worker without the deploy
   would return 404. The key file `~/.config/od/ceo_transcript_key` exists, and the consumer is the
   `ceo-memory-sweep` scheduled task — outside the repo, which is why a repo-only grep found no
   caller.

### The Site Map dead click — new FAIL
`js/sitemap.js:487` renders `switchTab(p.id)` for every one of the 27 registry rows. Three pages
have no tab panel in the main app: **compliance** (removed from `index.html` on 2 Jul, `9054e81`),
**crm** (added yesterday, lives only on the Supabase app) and **how-it-works** (standalone only).

Tested live rather than reasoned about: `switchTab('compliance')` threw nothing, moved no panel,
logged nothing. A silent no-op. The Standalone column in the next cell does open the real page, so
the page is reachable — the shortcut is not. Escalated as `rec6WhHV3gsZ6dDj0`; matters more now
that the Site Map is client-facing.

\* Forbidden-pattern detail — all pre-existing, none new:
- `console.log` in [js/admin-repair-splits.js](js/admin-repair-splits.js) (one-off admin repair
  utility) and [js/arrears.js](js/arrears.js) (`explainCFV()` console helper). INFO, both known.
- `document.write` in [js/arrears.js:1284](js/arrears.js:1284) and
  [js/costs.js:1792](js/costs.js:1792) — both into a fresh `printWin` popup with `escHtml()` on
  interpolated values. Known, accepted.
- `new Function` in [js/dashboard.js:395](js/dashboard.js:395) — the sandboxed custom-KPI evaluator,
  which shadows `PAT`, `fetch`, `localStorage`, `document`, `window` and friends as parameters.
  Known, deliberate.
- The `eval`/`document.write` strings in [js/skills-data.js](js/skills-data.js) are quoted rule text
  inside skill definitions, not executable code.
- No PAT or secret literal anywhere in `js/`, `scripts/` or root HTML.

**Model IDs, INFO:** `js/ai-models.js`, `wrangler.toml` and `wrangler.money-daily.toml` all agree —
`claude-sonnet-4-6` / `claude-haiku-4-5-20251001`. Three sources, zero drift. Both worker configs
gained `[observability] enabled = true` yesterday, so silent cron failures are now diagnosable in
Workers Logs.

---

## 5. Actions Taken

### Auto-fixed
**None, and nothing was eligible.** No dead references, no renames, no type mismatches, no schema
change at all. Both of today's findings need a design decision or a UI logic change, neither of
which is in the auto-fix list.

Only non-report change: `monitoring/reference-map.json` verification stamp refreshed to 2026-07-30
and its note corrected to record the true rogue count. No ID content changed.

### Escalated to Human Review — 2 new tasks

- **`recmGGMiMCLpGpZRg`** — *[WARNING] The SOP regenerator will overwrite the new plain-English
  client guides with developer SOPs.* Priority Not Urgent, due 2026-07-31. Detail in §3. Live for
  systemisation and skills today; ai-brain and crm join the moment either page is edited.

- **`rec6WhHV3gsZ6dDj0`** — *[WARNING] Site Map "Open" link is a dead click for three pages.*
  Priority Not Urgent, due 2026-08-01. Detail in §4. Also carries the smaller `crm` auto-bump
  mapping gap from §3, since both fixes live in the same corner of the code.

### Closed since last run — both of yesterday's, fixed properly
- **`recMQH9LlolBtenyY`** — merge the CEO Brief tab. Done at `ed85542`. Verified in the browser
  this morning: registry entry, `tab-ceo-brief` panel, sidebar item with health dot,
  `renderCeoBriefTab` and its sync bar all present. The 09:00 Slack footer no longer points at a
  tab that does not exist.
- **`recFRKKSiR5IwsRJa`** — CEO Briefs used by field name. Fixed at `f19443b`. `js/config.js` now
  holds `TABLES.ceoBriefs` plus ten `F.ceo*` IDs; `money-daily-worker.js` writes by ID with
  `typecast` deliberately OFF, and `js/ceo-brief.js` reads by ID. A rename in Airtable can no
  longer break the brief silently, and this monitor now watches those IDs.

**The Drift Monitor backlog is empty.** All seven tasks ever raised are Completed, including the
three SOP-documentation ones that were 11 and 14 days overdue at yesterday's run.

### Noted, not escalated
- 23 SOP version gaps (chronic; 12 are pages with no SOP file at all).
- 106 rogue field IDs (chronic; all resolve; count corrected today).
- INFO `console.log` in two admin/diagnostic files.
- `docs/ai-org-chart-spec.md:35` still says "Slack DM ~09:30" for a brief that sends at 09:00.
  Carried over from yesterday, still unfixed, still one stale line in a doc.

### Method notes for future runs
- The Tasks table primary field is **`Task Name`** (`fldgFjGBw6bTKJFCD`), not `{Name}`. Filtering on
  `{Name}` matches nothing and returns a silent zero that reads as "no open tasks" forever. Use
  `SEARCH('Drift Monitor', {Task Name})`.
- Rogue-reference counting: glob `js/**/*.js`, exclude `js/config.js`, regex `fld[A-Za-z0-9]{14}`,
  unique per file then unioned. Anything narrower undercounts — see §2.
- §4 runtime checks: `preview_start` on the `ops-director-brain` config (port 8957), then load
  `index.html`. Everything except 4a/4b works without a signed-in session.
