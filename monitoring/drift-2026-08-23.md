# Drift Report — 2026-08-23

Run as phase 3 of `daily-ops`. Queue lock NOT taken (Rule 1). Read-only with respect to code
(Rule 2, reinforced by the caller's override): no repo file edited, no `git add`, commit, pull,
branch, push or PR. Report written to `monitoring/` and left uncommitted.

## Summary

- **Schema changes:** 0 new, 0 removed, 0 type changes, 0 renames — 119 tables / 4,917 fields, identical to the 22 Aug snapshot
- **Field references:** 0 dead of 422 mapped fields, 0 dead of 35 mapped tables, **0 unresolvable `fld`/`tbl` IDs across the whole repo**
- **Rogue IDs:** 341 unique Airtable IDs used outside `js/config.js`, across 281 scanned files — all `fld`/`tbl` ones resolve
- **Coverage gap widened:** 44 Airtable IDs in `config.js` are absent from `reference-map.json` (was 26 on 14 Aug). All 44 resolve against today's schema, so nothing is broken; the defect is the coverage claim. Already filed as `20260814-drift-monitor-128` — not refiled
- **SOP drift:** 25 of 28 pages show `pageVer` ahead of `sopVer`. Metric carries no signal (see §3)
- **Health checks:** SKIPPED — no browser available in an unattended session. Static substitute run instead (§4)
- **Auto-fixed:** 0 — prohibited
- **Filed to the findings queue:** 0 — every code defect found today is already open in the queue
- **Airtable tasks created:** 0

---

## 1. Schema Changes

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat`. The token was never printed. Snapshot saved to
`monitoring/schema-2026-08-23.json`.

| Compared against | Tables | Fields added | Removed | Type changes | Renames |
|---|---|---|---|---|---|
| `schema-2026-08-22.json` | 119 = 119 | 0 | 0 | 0 | 0 |

The base has not moved since 16 Aug. Nothing to reconcile.

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (`reference-map.json` vs schema) | **0 of 422** |
| DEAD tables | **0 of 35** |
| TYPE_MISMATCH | still cannot fire — the map stores no expected types (`20260814-drift-monitor-127`, open). Proxy: 0 type changes vs yesterday |
| Rogue IDs (outside `js/config.js`) | 341 unique across 281 files |
| **Unresolvable `fld`/`tbl` IDs, whole repo** | **0** |
| IDs in `config.js` NOT covered by the map | **44** — all resolve; coverage is 486/530 = 92%, not 100% |

Scan covered `.js`, `.mjs`, `.html` and `.py` across the whole repo, excluding `node_modules`,
`.git`, `monitoring/`, `test-results/`, `playwright-report/` and `.claude/`. Boundary-anchored
regex per the 2026-08-04 correction.

Heaviest rogue-ID files, unchanged in rank from prior runs:
`os/team/index-supabase.html` (82), `os/tasks/index.html` (79), `os/tasks/index-supabase.html` (63),
`os/tasks/supabase-shim.js` (59), `follow-up.html` (45), `follow-up-supabase.html` (45),
`scripts/sync-airtable-mirror.py` (37).

**The map gap is growing.** 26 unmapped IDs on 14 Aug, 44 today — a 70% rise in nine days while
`20260814-drift-monitor-128` sat open. Every DEAD/ORPHAN number this check prints is computed over
a shrinking fraction of the real surface. Not refiled: a duplicate would add noise to a 319-deep
queue. The existing finding needs its numbers refreshed, not a twin.

The orphan-constant count was **not** re-derived. `20260821-drift-279` established it moves ~17%
run to run with no code change, so it cannot detect anything until it has a script.

## 3. SOP Drift

`pageVer` is auto-bumped by a GitHub Action on every push; `sopVer` is bumped by hand. The result:

| Pages checked | With a version gap | In sync |
|---|---|---|
| 28 | **25** | 3 (`cfv`, `content-machine`, `ai-brain`) |

A metric that is red on 89% of rows every single day cannot flag anything. It is not evidence of
25 stale SOPs; 12 of the 25 have no SOP file at all (`sopFile: ''`), so their `sopVer: '1.0'` is a
placeholder that will never move. Reported as-is rather than as 25 defects.

Targeted spot-check today: **`sop-cfvs.html` vs `js/cfv.js`**, on the `cfv_{id}_startDate` key.
Confirmed again — the key has **no writer anywhere in the repo**. `js/cfv.js` only deletes it
(:246, :821, :935), while `sop-cfvs.html:391` and `:630` document it as the store behind the
days-overdue figure. The real derivation is at `js/cfv.js:312-329`, from tenancy due dates.
Already filed three times (`20260816-drift-173`, `20260821-drift-276`, `20260821-drift-278`) and
all three are open. Not refiled.

No new feature has shipped since the last run — `HEAD` is still `90a480b` (PR #106, 21 Aug), the
pair audited yesterday. There was nothing new to audit.

## 4. Health Check Results

**SKIPPED — no browser.** `preview_start` refused: dev servers cannot be started from an
unattended session, and no preview server was already running. Per the skill, the check is skipped
and noted rather than faked. 4a (badge recount), 4b (PAT presence), 4c (staleness) and the runtime
half of 4d all require a live page and were not run. **0 checked, not 0 failed.**

Static substitute for 4d, which does not need a browser: re-derived the localStorage key shapes
from `js/cfv.js` source. Both documented shapes still hold —

| Shape | Keys | Written at |
|---|---|---|
| ISO timestamp | `cfv_dismissed_*`, `cfv_reflag_dismissed_*`, `*_chaseStart` | :337, :818, :867, :897 |
| sentinel `'1'` | `*_returned`, `*_returnDismissed` | :240, :784, :941 |
| **no writer** | `*_startDate` | — deleted at :246, :821, :935 only |

The CHECK 4d catalogue in SKILL.md still omits `*_startDate`, as `20260821-drift-278` records.

## 5. Actions Taken

### Auto-fixed
None. Prohibited by Rule 2 and by the caller's explicit override.

### Filed to the findings queue
None. Everything found today is already open:
`20260814-drift-monitor-127` (no stored types), `20260814-drift-monitor-128` (map stale — count
has grown 26 → 44), `20260816-drift-173` / `20260821-drift-276` / `20260821-drift-278`
(`cfv_{id}_startDate`), `20260821-drift-279` (orphan count unreproducible).

### Escalated to human review
None created. The queue stands at **319 findings, 15 of them critical**, several unfixed for over
a week. Adding a task today would compete with that backlog rather than reduce it.
