# Drift Report — 2026-07-10

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (identical to 2026-07-09 snapshot)
- Field references: 0 dead, 0 orphaned, 54 rogue (chronic, −1 vs 07-09; all valid/live)
- SOP drift: 0 stale, 0 undocumented, 10 standing version gaps (2 SOP files updated in lockstep this cycle)
- Health checks: N/A — headless run, no authenticated browser
- Auto-fixed: 0 items (nothing safe/new to auto-correct; reference-map already in sync)
- Human review: 0 new items (no new drift; existing CFV-SOP task not re-raised)

## 1. Schema Changes
Fetched full base schema (`appnqjDpqDniH3IRl`, 114 tables) via curl PAT — the Airtable MCP connector is still unauthenticated in headless runs, so the documented curl path was used. Compact snapshot written to `monitoring/schema-2026-07-10.json`.

Diffed against `monitoring/schema-2026-07-09.json`:
- **New tables:** 0
- **Removed tables:** 0
- **New fields:** 0
- **Removed fields:** 0
- **Type changes:** 0
- **Renamed fields:** 0

Schema is byte-stable vs yesterday. No upstream Airtable changes this cycle.

## 2. Field Reference Issues
Verified all 372 field IDs + 31 table IDs + 20 record IDs + 11 select-choice IDs from `reference-map.json` against today's schema.

- **DEAD (in code, not in Airtable): 0.** Every mapped field ID resolves to a live field.
- **DEAD tables: 0.**
- **TYPE_MISMATCH: 0.** No referenced field changed type (corroborated by the zero type changes in §1).
- **ORPHAN (mapped but unreferenced across js/): 0.** Every mapped field is still referenced somewhere in js/.
- **ROGUE (hardcoded `fld…` in js/ not in the config field map): 54.** Down 1 from the 55 standing at 07-09 (a `js/wealth.js` refinement dropped one hardcoded reference). All 54 resolve to real, live Airtable fields; none dead. This is a chronic, stable condition, not new drift. Centralising 54 hardcoded IDs across 9 feature files is a multi-file refactor touching shared `config.js`, not a safe unattended auto-fix, so they are left standing (same call as every prior cycle). Distribution by file:
  - `js/dashboard.js` — Projects/KPI cluster, Tasks, Systemisation Workflows (largest group)
  - `js/skills-data.js` — Tenancies, Tenants, Transactions, Costs, Tasks reference set
  - `js/sitemap.js` — SOP Update Requests table
  - `js/fintable.js`, `js/costs.js` — Accounts (`*Name`, `**Institution`, `**Fintable User`)
  - `js/arrears.js` — Tasks link fields (Tenancies/Tenants/Rental Units/Properties)
  - Sub Category / Category name fields shared across arrears, cashflow, costs, dashboard, pnl, reconciliation, transactions, wealth, wealth-ratios

- **`reference-map.json` config-synced.** Set-equal to `config.js` (372 IDs, no adds, no stale entries). No map maintenance needed this cycle.

## 3. SOP Drift
Two SOP-mapped feature files changed since the 07-09 report (`c84c781` → `8e12e95`): `js/dashboard.js` and `js/pnl.js`. Both changes are the app-wide maintenance-budget reduction (£3,000 → £1,000; derived operating cushion £16,000 → £14,000; variable-cost reserve £6,000 → £4,000).

- **STALE_SOP: 0.** Both affected SOPs were updated in lockstep in the same window:
  - `sop.html` — `MAINT_TARGET_GBP` row → £1,000; `variableCostReserve` → £4,000; `requiredOperatingCushion` → £14,000; all narrative references corrected.
  - `sop-pnl.html` — `PNL_MAINT_TARGET` fallback and budget lines → £1,000/mo.
  - Remaining `£3,000` / `16,000` strings in `sop.html` sit inside the historical changelog block (line ~737), not the live behaviour doc — correct to leave.
- **UNDOCUMENTED: 0.** No new behaviour introduced beyond the budget constant change, which is now documented.
- **VERSION_GAP: 10 standing** (pages carrying a SOP where `pageVer` > `sopVer`), essentially unchanged from 07-09:

| Page | pageVer | sopVer |
|------|---------|--------|
| Leadership Dashboard | 2.41 | 2.9 |
| Objective & Strategy | 1.15 | 1.0 |
| Tasks & Projects | 1.108 | 1.1 |
| CFVs | 1.29 | 1.6 |
| Accounts Payable Variable (Invoices) | 2.18 | 2.2 |
| Profit & Loss | 2.25 | 2.12 |
| Inbound Comms | 2.52 | 2.4 |
| Property Compliance | 1.11 | 1.1 |
| Business Launch Plan Builder | 1.14 | 1.3 |
| Site Map & Guides | 1.17 | 1.2 |

`pageVer` auto-bumps on every push; `sopVer` moves only on a manual bump. Note: the `sop.html` and `sop-pnl.html` *content* is current this cycle (budget change applied), but their `sopVer` counters were not bumped to match — a cosmetic counter lag, not stale documentation. Not auto-bumped (manual SOP-owner step per the versioning convention). The one gap with a known behavioural cause — the CFV detection change (`CFV_EARLY_PAY_DAYS = 5`, 2026-07-03) landing without a `sop-cfvs.html` rewrite — remains tracked in open Airtable task `rec9ihFqDDSY8DMLZ` (now overdue). Not re-raised or duplicated.

(12 further pages show a pageVer > sopVer arithmetic gap but carry no SOP file — nothing to update; excluded.)

## 4. Health Check Results
**N/A — headless scheduled run, no authenticated browser.** No preview server was running and the dashboard gates all data behind interactive PAT login, so `window.PAT`, `window.allTransactions`, badge counts and localStorage dismissal state cannot be exercised without a logged-in session. Consistent with every prior cycle.

- 4a. Badge/count consistency — N/A
- 4b. Token/auth health — N/A
- 4c. Stale-data timestamp — N/A
- 4d. localStorage dismissal JSON validity — N/A

## 5. Actions Taken

### Auto-fixed
None. No dead references, no rogue IDs safe to centralise unattended, no simple rename mismatches. `reference-map.json` already set-equal to `config.js` (no adds needed). No PR created.

### Escalated to Human Review
None new. No schema changes, no type mismatches, no new SOP drift, no health-check failures. The chronic 54 rogue references and 10 SOP version gaps are pre-existing and consistent with prior cycles; the one behaviourally-caused gap (CFV SOP) is already tracked in `rec9ihFqDDSY8DMLZ`. No new Airtable task raised to avoid duplicate noise.

### Committed
- `monitoring/schema-2026-07-10.json` (compact snapshot)
- `monitoring/drift-2026-07-10.md` (this report)
