# Drift Report — 2026-07-22

## Summary
- Schema changes: **1 new field**, 0 removed, 0 type changes, **1 table renamed**
- Field references: 0 dead, 0 orphaned, 52 rogue in `js/` (chronic, unchanged) + 78 in `os/`
- SOP drift: 1 stale, 1 undocumented, 11 version gaps (all chronic, all already tracked)
- Health checks: **7 pass, 0 warn, 0 fail** (4 browser checks N/A — auth-gated)
- Auto-fixed: 0 code items (reference-map.json updated, no PR needed)
- Human review: **0 new tasks** — 1 carried over (`recbnIfmyoN3Idzgw`)

**Clean run.** Yesterday's CRITICAL is closed: the duplicate `CAT_NAME_FIELD` declaration was
resolved in the working tree that owned it, the constant now lives once in `js/config.js:414`
with an explanatory comment in `js/costs.js:98`, and the full sync-invariant suite is back to
**61/61 green** (it was 14 failing yesterday). Airtable task `rec4xhaqUYN2VgENW` shows
**Completed**.

## 1. Schema Changes

Snapshot `monitoring/schema-2026-07-22.json` vs `monitoring/schema-2026-07-21.json`.

- Tables: 117 (0 added, 0 removed, **1 renamed**)
- Fields: 4,867 (**1 added**, 0 removed, 0 renamed, 0 type changes)

| Change | Table | Field | Type | ID |
|---|---|---|---|---|
| NEW | Debt Terms | Payment Match | singleLineText | `fldgcwyxNgr8n4128` |
| RENAMED TABLE | `Net Worth Statement` → `ZZ LEGACY — Net Worth Statement (NOT USED)` | — | — | — |

Neither is drift.

- **Payment Match** is already wired in code as `DEBT.payMatch`
  ([js/config.js:384](js/config.js:384)) and read at [js/wealth.js:189](js/wealth.js:189) — the
  Airtable half of the "one source of truth for credit-card payments" work (commit `69c81f2`).
  Code and schema landed together.
- The **table rename** is intentional housekeeping and matches commit `036200a` ("Warn against
  the orphaned net-worth table in config"). The `ZZ LEGACY … (NOT USED)` prefix makes the dead
  table obvious in the Airtable UI. No code reads it — verified: the table ID appears in no
  `js/` file.

## 2. Field Reference Issues

Every field, table, record and select-choice ID in `monitoring/reference-map.json` checked
against today's live schema.

- **DEAD:** 0 — every code-referenced field and table still exists in Airtable.
- **TYPE_MISMATCH:** 0.
- **ORPHAN:** 0 — no config constant points at a departed field.
- **ROGUE:** **52** distinct field IDs hardcoded in feature `js/*.js` files rather than routed
  through `config.js`. **All 52 resolve to a live field in today's schema (0 dead).** Count
  unchanged from 2026-07-21.

  Distinct-per-file: `dashboard.js` 21, `skills-data.js` 18, `sitemap.js` 6, `arrears.js` 4,
  `fintable.js` 3, `costs.js` 1.

  **New this run — the `os/` pages were included in the scan for the first time:** a further
  **78** distinct field IDs are hardcoded in OS pages, concentrated in
  `os/tasks/index.html` (73) and its Supabase twin `os/tasks/index-supabase.html` (61, heavily
  overlapping), plus `os/systemisation/` (4 each) and `os/operations/` (2 each). All 78 also
  resolve to live fields — **0 dead.** Reported for visibility only; the count is not a
  regression, it is newly-measured surface that prior runs never looked at.

  **Not auto-fixed — deliberate, matching prior policy.** Routing 130 live IDs through
  `config.js` touches shared money-path files and both halves of the Supabase shadow build. That
  is a reviewed refactor, not an unattended drift PR.

### reference-map.json refresh

`js/config.js` had gained 2 field IDs since the last run. Both are now committed, both verified
present in Airtable, and both added to the map (403 fields tracked, up from 401):

| ID | Constant | Table / Field |
|---|---|---|
| `fldgcwyxNgr8n4128` | `DEBT.payMatch` | Debt Terms / Payment Match |
| `fldii4oUzSfmplihO` | `CAT_NAME_FIELD` | Chart of Accounts - Categories / Category Name |

The second is the constant behind yesterday's CRITICAL. It was held out of the map last run
because it sat uncommitted; it has since landed cleanly, so it is now tracked.

## 3. SOP Drift

No file in the SOP mapping table changed since the last run. The 27 commits merged in the last
24 hours are all Supabase/CRM/onboarding work (`crm-supabase.html`, `onboarding.html`,
`supabase-app.html`, edge functions, migrations `0032`–`0036`, `vercel.json`, `MASTER-PLAN.md`,
`STRUCTURE.md`). None is SOP-covered, so no SOP finding moved today. Both chronic findings were
re-verified against the live code rather than carried over on trust.

### STALE_SOP: 1 — `sop.html` §12 "Reconciliation Matching Logic" (chronic, tracked)

Re-verified today. The SOP still documents a **33%** amount tolerance
([sop.html:631](sop.html:631)) and a **±5-day** date window ([sop.html:707](sop.html:707)); the
code enforces a **10%** tolerance (`COST_MATCH_TOLERANCE = 0.10`,
[js/reconciliation.js:498](js/reconciliation.js:498)) and has no date window at all. On a genuine
supplier price rise the SOP implies the match still lands; the code returns blank and leaves it
for a human. A real behavioural difference on money attribution.

- Already escalated: Airtable task **recbnIfmyoN3Idzgw**, confirmed still **"To do"** today
  (due 2026-07-18, now 4 days overdue). No duplicate created.

### UNDOCUMENTED: 1 — the engine that replaced §12 (chronic, tracked)

`sop.html` §12 still omits the mechanism actually in force: learned historical patterns,
reference-token matching (`pickCostByReference`), closest-amount picking (`pickCostForAmount`),
ACTIVE-costs-only gating, the Knowledge Base rule layer, and the auto-approve near-certain tier.
Same open task.

### VERSION_GAP: 11 of 11 SOP-backed pages (chronic, count unchanged)

| Page | pageVer | sopVer | SOP file |
|------|---------|--------|----------|
| overview | 2.43 | 2.9 | sop.html |
| os-strategy | 1.16 | 1.0 | os/strategy/sop.html |
| tasks | 1.112 | 1.1 | os/tasks/sop.html |
| cfv | 1.30 | 1.6 | sop-cfvs.html |
| invoices | 2.18 | 2.2 | sop-invoices.html |
| pnl | 2.25 | 2.12 | sop-pnl.html |
| comms | 2.53 | 2.4 | inbound-comms-sop.html |
| compliance | 1.11 | 1.1 | sop-compliance.html |
| os-bplan | 1.15 | 1.3 | os/business-plan-builder/sop.html |
| prospecting | 1.4 | 1.3 | sop-prospecting.html |
| sitemap | 1.17 | 1.2 | sop-sitemap.html |

Chronic independent-versioning pattern (auto-bumped `pageVer` runs ahead of hand-bumped
`sopVer`). Documentation lag, not a runtime risk. Not escalated, consistent with prior policy.

**INFO — 12 pages still carry no SOP at all** (`sopFile: ''`): money, wealth, income,
ar-variable, costs, transactions, operations, fintable, systemisation, os-team, skills,
how-it-works. Unchanged from yesterday. Worth restating: Wealth is now at pageVer 1.2 after
three days of behaviour changes (money groups, credit-card single-source, manual-debt roll
forward) with no SOP to fall out of date. A page guide was added for Wealth (commits `1a60a56`,
`2e4c327`), which narrows the gap but is not a registered SOP.

## 4. Health Check Results

| # | Check | Result |
|---|-------|--------|
| 4a | JS syntax — all 27 `js/*.js` parse clean (`node --check`) | **PASS** |
| 4b | Live deploy `index.html` | **PASS** (HTTP 200) |
| 4c | Live deploy `js/config.js` | **PASS** (HTTP 200) |
| 4d | Live deploy `js/costs.js` (yesterday's fix) | **PASS** (HTTP 200) |
| 4e | Airtable API auth (PAT valid, base reachable) | **PASS** (HTTP 200) |
| 4f | Model-ID guard (`scripts/check-model-ids.sh`) | **PASS** (exit 0) |
| 4g | Sync-invariant suite (`npm run test:sync`) | **PASS — 61/61** |
| — | Badge count consistency | N/A — needs an authenticated browser session |
| — | `window.PAT` health | N/A — same |
| — | Stale-data timestamp | N/A — same |
| — | localStorage dismissal integrity | N/A — same |

The four browser checks are auth-gated (the dashboard requires an interactive Airtable-token
login) and cannot run in a headless scheduled context. Standing limitation, unchanged — not a
new failure.

### 4g recovered — yesterday's CRITICAL is closed

`Identifier 'CAT_NAME_FIELD' has already been declared` no longer occurs. The constant is
declared once, at [js/config.js:414](js/config.js:414); `js/costs.js:98` now carries a comment
recording that it moved and why (shared global scope, plain `<script>` tags). Grep confirms one
declaration across all `js/*.js`. All 14 previously-failing tests pass. Airtable task
`rec4xhaqUYN2VgENW` reads **Completed**.

## 5. Actions Taken

### Auto-fixed
No code changes, and no PR. Nothing dead to comment out; the 130 rogue IDs are policy-excluded
from unattended fixes (§2). `monitoring/reference-map.json` was refreshed with the 2 new
committed field IDs — a monitoring artefact, not app code.

### Escalated to Human Review

**No new tasks this run.** Nothing crossed the escalation bar: 0 dead references, 0 type
mismatches, 0 health failures, and both schema changes are code-matched and intentional.

Carried over: the `sop.html` §12 stale/undocumented finding remains tracked under open Airtable
task **recbnIfmyoN3Idzgw** ("To do", due 2026-07-18, now 4 days overdue). Re-escalating would
create a duplicate. It is the oldest unaddressed drift item and the only one with a money-path
consequence.

---
*Working tree clean at run time; run executed against `HEAD` = `0c3fee9` after `git pull`. This
run committed only `monitoring/`.*
