# E2E Sweep — 2026-08-12

## Summary
- **Production URL: UP** (HTTP 200, app shell present — `<title>Operations Director</title>`, sidebar markup, tokens.css)
- **Deployed build matches source:** `index.html` and `js/config.js` fetched from GitHub Pages are byte-identical to local `main` at `c873635`. No stale deploy.
- **Mode: DEGRADED** (click-only, unauthenticated — data rendering NOT verified). `mcp__Claude_Browser__javascript_tool` was refused by the Claude Code auto-mode permission classifier on **every** call, including a trivial read of `PAGE_REGISTRY`. The PAT could not be written to `localStorage._dlr_pat`. The token was NOT typed into the login field (prohibited), NOT put in a URL, and NOT written into the served web root. **Fifth consecutive morning in this state** (8, 9, 10, 11, 12 Aug 2026).
- **Tabs walked: 18** (all sidebar entries) | PASS: 0 | WARN: 0 | FAIL: 0 | **UNVERIFIED: 18**
- **Standalone pages checked: 3** — all 200.
- **Data invariants: PASS (11 of 11 checked)** — this check queries Airtable directly and is unaffected by the browser denial, so number-correctness IS covered today.
- **Failures needing attention: 1** — the JS denial itself.

## Navigation walk (degraded mode)

Every one of the 18 sidebar entries was clicked as a real user would. Navigation is sound: each
click moved the active highlight to the clicked item and the sidebar auto-scrolled it into view
(verified by screenshot at three checkpoints — CEO Brief, Operations, Leadership Dashboard).

| Tab | Nav fired | Console | Verdict |
|---|---|---|---|
| CEO Brief | yes | 401 only | UNVERIFIED |
| Leadership Dashboard | yes | clean | UNVERIFIED |
| Objective & Strategy | yes | clean | UNVERIFIED |
| KPI Library | yes | clean | UNVERIFIED |
| Tasks & Projects | yes | clean | UNVERIFIED |
| Operations | yes | clean | UNVERIFIED |
| Inbound Comms | yes | clean | UNVERIFIED |
| Systemisation | yes | clean | UNVERIFIED |
| Skills Library | yes | clean | UNVERIFIED |
| AI Brain | yes | clean | UNVERIFIED |
| Money Confidence | yes | clean | UNVERIFIED |
| Wealth | yes | clean | UNVERIFIED |
| Accounts | yes | clean | UNVERIFIED |
| Profit & Loss | yes | clean | UNVERIFIED |
| Team Members | yes | clean | UNVERIFIED |
| Content Machine | yes | clean | UNVERIFIED |
| Prospecting | yes | clean | UNVERIFIED |
| Site Map & Guides | yes | clean | UNVERIFIED |

**Honesty note.** Logged out, panels render their shell but fetch no Airtable data, so a clean
click plus a clean console does **not** prove a tab works. Nothing is marked PASS. The one panel
that showed visible content behind the auth overlay was CEO Brief, and it showed a correct,
user-friendly empty state ("CEO briefs failed to load") rather than a raw error — good error
handling, but it is the logged-out path, not the working path.

### Console across the whole 18-tab walk

Only **three** errors in total, all traceable to being logged out:

- `Failed to load resource: the server responded with a status of 401` (x2)
- `Uncaught (in promise) {error: Object}` (x1) — an unhandled promise rejection carrying the 401 response object

No `TypeError`, no `ReferenceError`, no 404 or 500, no missing asset, on any tab. For shell
integrity that is a genuinely clean result.

The unhandled rejection is a small robustness gap rather than a live bug: the Airtable 401 is not
caught anywhere, so it escapes as an unhandled promise rejection instead of a toast. Filed as a
low-severity finding, not fixed here (this sweep is read-only with respect to code).

### Standalone pages (registry ids with no in-shell panel)

`PAGE_REGISTRY` is a page catalogue, not a tab list. Three ids have no `tab-<id>` panel in
`index.html` by design; checked directly on production instead:

| Page | Status |
|---|---|
| `compliance.html` | 200 (33,535 bytes) |
| `crm-supabase.html` | 200 (45,313 bytes) |
| `how-it-works.html` | 200 (87,876 bytes) |

### Registry shape has changed since this sweep's notes were written

Enumerated from source (`js/config.js`, `javascript_tool` being unavailable): still **27** ids, but
the membership has moved. `os-bplan` is **gone** (Plan Builder retired from the shell, 1 Aug) and
**`kpi-library` is new**. The list recorded in the sweep's own SKILL.md is therefore stale. Current
27: overview, os-strategy, tasks, cfv, ceo-brief, money, wealth, income, ar-variable, costs,
invoices, pnl, transactions, comms, compliance, operations, **kpi-library**, fintable,
systemisation, os-team, crm, content-machine, prospecting, sitemap, skills, ai-brain, how-it-works.
24 have in-shell panels; the 3 above do not.

## Data invariants

`python3 scripts/check-data-invariants.py --json` → exit 0, `{"ok": true}`. **11 of 11 PASS**, no
`CONTROL_FAILED`:

| Invariant | Result |
|---|---|
| report-amount-populated | PASS |
| split-override-sign | PASS |
| agent-work-completed-without-approval | PASS |
| waiting-approval-has-an-agent | PASS |
| on-time-counts-the-due-date-itself | PASS |
| started-projects-are-not-still-not-started | PASS |
| open-tasks-carry-no-completion-date | PASS |
| automated-kpis-have-actually-run | PASS |
| ceo-brief-complete | PASS |
| kpi-library-coverage | PASS |
| no-reimport-duplicates | PASS |

Worth calling out: **`open-tasks-carry-no-completion-date` is now green.** That is the invariant
that fired [CRITICAL] on 8 and 9 Aug over 143 open tasks carrying a stale Completion Date. The
11 Aug fix (the stamp comes from app code, `AUTOMATION PORT #1`, not the Airtable automation) has
held for a second day.

## Failures needing attention

**1. [FAIL] The sweep cannot log itself in — fifth morning running.**
`mcp__Claude_Browser__javascript_tool` is denied by the permission classifier, so the saved-token
key cannot be set and the app is walked logged out. Every tab is UNVERIFIED. Until this is fixed
the morning sweep is a shell-and-console check only, and a data-rendering regression would go
unnoticed. Number-correctness is still covered by the invariant check, which does not use the
browser.
**Fix (one line): pre-approve `mcp__Claude_Browser__javascript_tool` for the prod-e2e-sweep /
daily-ops task.**

## Actions taken

- **No new Airtable task created.** Dedupe search on the finding key
  `AND({Is Completed}=0, FIND("FINDING-KEY: e2e-auth-js-denied", {Notes}))` → **0 rows**, with the
  prefix control `FIND("FINDING-KEY:", {Notes})` → **1 row** (`recaFnWr9MNOeP86P`), proving the
  query fires rather than silently matching nothing.
  A broader title search then found **two already-open tasks for this exact fault**, both raised
  before the FINDING-KEY convention existed and therefore invisible to the key search:
  `recrGG00unq8Pup2V` (30 Jul) and `recsWrVeAZafPH1NL` (31 Jul). Creating a third would have been
  the duplication the rule exists to prevent.
  **Updated the oldest match `recrGG00unq8Pup2V`**: appended a dated recurrence line with today's
  evidence, stamped `FINDING-KEY: e2e-auth-js-denied` so future runs dedupe on the key, refreshed
  the due date to today, and noted that `recsWrVeAZafPH1NL` should be closed as a duplicate.
- **Slack DM sent to Kevin** (one message, on failure only).
- Low-severity finding filed via `scripts/findings.py` for the unhandled 401 promise rejection.
- Report left in `monitoring/`, **not committed** — `queue-fixer` commits it.

## Note for whoever tunes this sweep

Two things in `~/.claude/scheduled-tasks/prod-e2e-sweep/SKILL.md` are now stale:
1. The recorded PAGE_REGISTRY list still names `os-bplan` and omits `kpi-library`.
2. The degraded-mode instructions assume panel text is readable via `get_page_text`. It largely is
   not: logged out, the auth overlay sits over the content area and dominates `innerText`, and OS
   tabs are iframes whose text never appears in the parent document at all. In degraded mode the
   real evidence is the active-highlight screenshot plus the per-tab console, which is what was
   used here.
