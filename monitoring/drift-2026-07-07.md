# Drift Report — 2026-07-07

## Summary
- Schema changes: **1 new table** (AI Brain Index), 0 removed, 0 type changes, 0 field renames
- Field references: **0 dead**, 0 real orphans, 54 rogue (chronic, unchanged)
- SOP drift: **0 new stale**, 0 new undocumented, 22 version gaps (standing)
- Health checks: 0 pass, 0 warn, 0 fail — **4 N/A** (headless run, no authenticated browser)
- Auto-fixed: **0 items** (nothing met the safe auto-fix bar)
- Human review: **0 new tasks** (the one open item, CFV SOP drift, is already tracked in Airtable task `rec9ihFqDDSY8DMLZ` — not duplicated)

Net: clean cycle. No js/ feature file changed since the last run, so no field-reference or SOP-behaviour drift was introduced on any documented page. One new Airtable table appeared and is already correctly wired in code.

---

## 1. Schema Changes

Compared today's snapshot (`schema-2026-07-07.json`, 113 tables / 4,815 fields) against the prior snapshot (`schema-2026-07-06.json`, 112 tables).

**New tables (1):**
- **AI Brain Index** (`tbl5MY9FbImFQfvez`) — 6 fields: Name (singleLineText), Folder (singleLineText), Summary (multilineText), DocLink (url), Sensitive (checkbox), Updated (singleLineText).
  - Already referenced in code: `js/ai-assistant.js:96` as `const BRAIN_INDEX_TABLE = 'tbl5MY9FbImFQfvez'`. This is the index companion to the existing `BRAIN_TODAY_TABLE` (`tblZ75JgE1wzDP0ps`) for the AI Brain module. New table + new code reference are consistent — informational, no action.

**Removed tables:** none.
**Field additions / removals in existing tables:** none.
**Field type changes:** none.
**Field renames:** none.

Zero-delta on every existing table means no referenced field could have silently changed type this cycle.

---

## 2. Field Reference Issues

Verified all **369 field IDs + 31 table IDs + 20 record IDs** from `monitoring/reference-map.json` against today's schema.

- **DEAD:** 0. Every config.js field, table, and record ID resolves in Airtable.
- **TYPE_MISMATCH:** 0. Zero-delta schema diff means no referenced field changed type.
- **ORPHAN:** 0 real. (A raw-ID scan flags 317 config-mapped fields as "unused elsewhere," but this is a false-positive method: feature files reference fields via the `F.<const>` indirection, e.g. `F.category`, not the literal `fld…` ID. Confirmed by inspection. Consistent with every prior run reporting 0.)
- **ROGUE (hardcoded IDs in feature js, bypassing config.js):** 54 unique `fld` IDs across 11 files, all valid Airtable fields. **No js/ feature file changed since the last run** (`git diff 535da86..HEAD` touches only Supabase-migration HTML/boot files, `os/systemisation/index.html`, and docs), so this set is byte-identical to the 2026-07-06 report. The 53→54 count difference is a counting-method artifact, not new drift. Distribution (by file, non-unique):
  - js/dashboard.js (23), js/skills-data.js (18), js/sitemap.js (6), js/arrears.js (6), js/costs.js (3), js/fintable.js (3), js/pnl.js (2), js/reconciliation.js (2), js/transactions.js (2), js/cashflow.js (1), js/wealth.js (1)
  - Plus local table/record consts outside config.js (chronic, by design): `tbl5MY9FbImFQfvez`, `tblZ75JgE1wzDP0ps` (ai-assistant.js), `tblHGNzDmOs59r9QD` + `recqbcIz2R2griDn3` (skills.js), `tblJ3GFnAAoXf99e9` (dashboard.js), `tbltuZz5Omrpo7t1x` (sitemap.js), `recY5XDZspRDNjZOO` (cashflow.js), `recoGcXRXCniyJsTz` (arrears.js), plus the one-off `admin-repair-splits.js` record list.

**reference-map.json:** current. config.js has exactly 369 fld / 31 tbl / 20 rec IDs, all present in the map, none missing. No update needed this run.

No field-reference item met the auto-fix bar. Centralising 54 chronic rogue IDs across 11 feature files is a multi-file refactor, out of safe unattended scope.

---

## 3. SOP Drift

**No new STALE_SOP or UNDOCUMENTED drift this cycle.** The only commits since the last run (`535da86..e5f2ab0`) are:
- `e5f2ab0` Restructure: "Automation" → "AI Agents" tab
- `df59a9f` One-click "Regenerate from video" on the SOP document
- `6253c30` AGENTIC redefined (C = Conclusion)
- `7c8c253` / `6db546a` Money Confidence on Supabase
- `990993d` Tasks/Operations Supabase clones shared-token fix

Files changed: `docs/agentic-extraction-spec.md`, `money-supabase.html`, `os/operations/index-supabase.html`, `os/operations/supabase-boot.js`, `os/systemisation/index.html`, `os/tasks/index-supabase.html`, `os/tasks/supabase-boot.js`, `supabase-app.html`.

None map to a documented production SOP pair in the audit table. The `-supabase.html` files are the parallel migration build (the SOP map points at the production `os/tasks/index.html`, not `index-supabase.html`); `os/systemisation` and the money page have no SOP entry. So no documented page's behaviour changed.

**VERSION_GAP (pageVer ahead of sopVer) — standing:** 22 of 24 registry entries; 10 of those have a real SOP file. Only `content-machine` (1.0/1.0) and `ai-brain` (1.0/1.0) are level. Worst offenders unchanged:

| Page | pageVer | sopVer | SOP file |
|------|---------|--------|----------|
| Tasks & Projects | 1.108 | 1.1 | os/tasks/sop.html |
| Inbound Comms | 2.52 | 2.4 | inbound-comms-sop.html |
| Leadership Dashboard | 2.40 | 2.9 | sop.html |
| Objective & Strategy | 1.15 | 1.0 | os/strategy/sop.html |
| CFVs | 1.29 | 1.6 | sop-cfvs.html |
| Profit & Loss | 2.24 | 2.12 | sop-pnl.html |
| Accounts Payable Variable | 2.18 | 2.2 | sop-invoices.html |
| Business Launch Plan Builder | 1.14 | 1.3 | os/business-plan-builder/sop.html |
| Site Map & Guides | 1.17 | 1.2 | sop-sitemap.html |
| Property Compliance | 1.11 | 1.1 | sop-compliance.html |

pageVer auto-bumps on every push; sopVer only moves on a manual SOP rewrite, so a standing gap is expected. The substantive behavioural half of this — the CFV detection change (`CFV_EARLY_PAY_DAYS = 5`; live days-overdue derivation) landed 2026-07-03 without a matching `sop-cfvs.html` update — is already captured in open Airtable task **`rec9ihFqDDSY8DMLZ`** (status "To do", due 2026-07-04, now overdue). Its notes remain accurate. Not re-raised or duplicated.

---

## 4. Health Check Results

All 4 sub-checks **N/A** this run (same posture as 2026-07-04 → 2026-07-06):
- 4a badge-count consistency — N/A
- 4b token/auth health — N/A
- 4c stale-data timestamp — N/A
- 4d localStorage dismissal integrity — N/A

This is a headless scheduled run with no authenticated browser session. The dashboard requires a PAT entered in-browser before `window.PAT` and the data arrays populate, so a static preview would only render an unauthenticated shell yielding no signal. No preview server started.

---

## 5. Actions Taken

### Auto-fixed
None. No finding met the safe auto-fix bar (no dead references to comment out; the rogue-ID centralisation is a multi-file refactor out of unattended scope).

### Escalated to Human Review
No new task created. The single open drift item — CFV SOP behaviour drift + standing version-gap — is already tracked in Airtable task `rec9ihFqDDSY8DMLZ` (open, overdue since 2026-07-04). Creating a duplicate would add noise, not signal.

### Committed
- `monitoring/schema-2026-07-07.json` (today's snapshot)
- `monitoring/drift-2026-07-07.md` (this report)
- `monitoring/reference-map.json` unchanged (already current)

Note: `.claude/launch.json` had a pre-existing uncommitted local edit (two static-server configs for the AI Agents / Brain pages) at session start. Left untouched — not a drift artifact and not part of this commit.
