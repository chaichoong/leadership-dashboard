# Drift Report — 2026-06-11

## Summary
- Schema changes: 2 new fields, 0 removed, 0 type changes, 0 renames
- Field references: 0 dead, 0 type mismatches, 35 rogue (code-level, all valid), 18 doc-string IDs
- SOP drift: 10 version gaps; 6 SOPs deep-audited → ~30 concrete stale/undocumented findings; 4 version-gap-only
- Health checks: 2 pass, 0 fail, 3 not-verifiable (auth-gated headless)
- Auto-fixed: 0 items (no safe auto-fix this run — see note)
- Human review: 1 Airtable task created ([WARNING] SOP drift)

---

## 1. Schema Changes
Compared `schema-2026-06-11.json` against prior snapshot `schema-2026-06-05.json`.

**New fields (2):**
- `Team Members` → `Next Review Date` (date) — `fldnxFD3fRg7fVDQp`
- `Team Members` → `Handbook Acknowledged` (date) — `fldgV6cTKV82pkGMQ`

Both match the recent HR / Team Members phase-one commit (a16e2ea). Already referenced in code. No action.

**Removed / type changes / renames:** none.
**Tables added / removed:** none. (107 tables, 4771 fields total in base.)

---

## 2. Field Reference Issues

**DEAD (referenced in code, missing in Airtable):** none. Every one of the 392 distinct field IDs referenced across config.js + feature files resolves to a live Airtable field.

**TYPE_MISMATCH:** none detected. (Note: reference-map stores constant names, not types, so type drift is only inferable via the DEAD check; no DEAD = structurally sound.)

**ROGUE (hardcoded field IDs in feature js, bypassing config.js):** 35 code-level + 18 doc-string. **All resolve to valid live fields** — no broken references.

Code-level rogue breakdown (established local field-maps, pre-existing pattern, not new drift):
| File | Count |
|------|-------|
| js/dashboard.js | 22 |
| js/sitemap.js | 6 |
| js/arrears.js | 6 |
| js/costs.js | 3 |
| js/fintable.js | 3 |
| js/pnl.js | 2 |
| js/transactions.js | 2 |
| js/reconciliation.js | 2 |
| js/cashflow.js | 1 |
| js/skills-data.js (doc-string IDs in template literals, not live refs) | 18 |

**Why not auto-fixed:** migrating 35 IDs into config.js would touch 9 feature files at once — exactly the concurrent-edit / shared-file risk CLAUDE.md warns against, and beyond the "comments / config refs / simple naming" scope allowed for auto-fixes. Logged as INFO. Recommend a single dedicated session to consolidate dashboard.js + sitemap.js local maps into config.js if desired.

**ORPHAN:** the raw-ID grep orphan check is unreliable here — feature files read fields via `F.constName` (and the other constant objects), not raw `fldXXX` literals, so a raw-ID scan reports false orphans. Not reported.

---

## 3. SOP Drift

All 10 SOP-backed pages have `pageVer` ahead of `sopVer` (dotted-version compare). pageVer auto-bumps on every source push while sopVer is manual, so a large gap = many code changes since the SOP was last updated.

| Page | pageVer | sopVer | Gap |
|------|---------|--------|-----|
| tasks (os/tasks) | 1.93 | 1.1 | ~92 |
| comms (follow-up) | 2.50 | 2.4 | ~46 |
| overview (dashboard) | 2.34 | 2.9 | ~25 |
| cfv | 1.23 | 1.6 | ~17 |
| sitemap | 1.16 | 1.1 | ~15 |
| invoices | 2.16 | 2.2 | ~14 |
| os-strategy | 1.13 | 1.0 | ~13 |
| compliance | 1.11 | 1.1 | ~10 |
| pnl | 2.21 | 2.12 | ~9 |
| os-bplan | 1.11 | 1.3 | ~8 |

### Deep-audited (6 pairs)

**overview — sop.html vs js/dashboard.js, js/kpi-sources.js**
- STALE_SOP: dashboard cache moved from localStorage `_dlr_dashcache_v1` to IndexedDB `_dlr_dashcache_v2` (SOP Section 3 still says localStorage).
- STALE_SOP: AI Recon accuracy log moved from localStorage `_recon_accuracy_log` to Airtable `reconAudit` table with auto-prune (SOP Section 6).
- UNDOCUMENTED: automated Strategic KPI computation — `runAutomatedKpis()` loads Projects table, computes and persists KPIs to Airtable on every load.
- UNDOCUMENTED: arrears engine auto-sweep on load (`runArrearsEngine`) opens/progresses/pauses Arrears Records by tenant type.
- UNDOCUMENTED: non-blocking Gmail-invoice / invoice-extraction / Fintable-sync fetches fired during data load.

**cfv — sop-cfvs.html vs js/cfv.js**
- STALE_SOP: SOP Section 8 says dismissals "auto-expire after 25 days"; code expires at next rent-due day + 2-day tolerance, not a fixed 25 days.
- STALE_SOP: Section 4 describes simple calendar-month payment detection; code delegates to `isCurrentlyInArrears()` with tolerance windows.
- STALE_SOP: Section 6 implies synchronous auto-return; code async-queues returns after the detection loop.
- UNDOCUMENTED: last reconciled payment date & amount tracked per CFV (`buildCFVEntry`); not in SOP Section 7 columns.
- UNDOCUMENTED: `syncDismissalsFromAirtable()` reconstructs localStorage dismissals from Airtable comments.
- UNDOCUMENTED: `cfv_{id}_chaseStart` only initialised for 'potential'/'cfv' statuses, not 'cfv actioned'.

**invoices — sop-invoices.html vs js/invoices.js**
- STALE_SOP: Section 8 fetch query documented as `NOT({Status}='Paid')`; code uses `{Status}='Unpaid'` (line 57).
- STALE_SOP: Section 3 says clicking anywhere on a row opens Gmail; code only renders a mailto icon link in the date cell, no row-level handler.
- UNDOCUMENTED: spreadsheet-style always-visible cell inputs (Amount/Payee/Description/Ref) — SOP Section 5 describes click-to-edit modal.
- UNDOCUMENTED: bulk invoice selection (checkboxes, `applyBulkBusiness()`) entirely absent from SOP.
- UNDOCUMENTED: `triggerGmailInvoiceReconcile()` full status realignment not in SOP.

**pnl — sop-pnl.html vs js/pnl.js**
- STALE_SOP: Section 8 says business list is hardcoded to ['Real Estate','Operations Director']; code derives from Airtable Active flag with that list as fallback.
- STALE_SOP: chart colours in SOP Section 4 (revenue `#22c55e`, GP `#3b82f6`, NP `#10b981`, target `#ef4444`) differ from code's sage-executive tokens (`#2C6E49`, `#3560A8`, `#2C6E49`, `#A33B3B`) — low impact.
- UNDOCUMENTED: 8 sync-bar health checks incl. grand-vs-monthly total drift detection (`registerSyncBar`).

**comms — inbound-comms-sop.html vs follow-up.html** (largest behaviour gap)
- STALE_SOP: SOP lists labels 1–10; code implements labels 11–14 (Tenancy Docs, Kevin To Respond, Maintenance, Maintenance Completed).
- STALE_SOP: task reassignment logic swaps emails between labels 8 and 12 by assignee; SOP documents only label 8 creation.
- UNDOCUMENTED: "Polish (Sonnet)" higher-quality rewrite button (uses claude-sonnet); SOP mentions only "Revise".
- UNDOCUMENTED: labels 12 & 13 are task-linked with auto-completion to 9/14; SOP documents only label 8 → 9.
- UNDOCUMENTED: label 11 auto-links emails to Airtable Tenancies and stars threads.
- UNDOCUMENTED: compliance processing auto-runs on move to label 10.
- UNDOCUMENTED: knowledge-base build modal on first Inbox load; live label counts on 1/2/3/8.

**tasks — os/tasks/sop.html vs os/tasks/index.html** (largest version gap)
- STALE_SOP: "Plan My Day" planner overlay removed, replaced by "Auto-Schedule".
- STALE_SOP: Kanban described as 4-column month view only; code supports 4-week, 12-week, and 12-month modes.
- STALE_SOP: 6 views listed; code adds a 7th, "Contractor Tasks".
- STALE_SOP: priority model lists "High"; code migrated to ["Project","Urgent","Not Urgent"] with "High" as legacy fallback. (Note: Airtable Status still has a "To do" choice the SOP-vs-code agent reported as removed from the UI list — verify.)
- UNDOCUMENTED: QuickTask inline task creation (quick-task.js).
- UNDOCUMENTED: status reconciliation — detects drift between Airtable-stored and locally-derived status, offers batch reconcile (`reconcileStaleStatuses()`).
- UNDOCUMENTED: comments modal with Slack integration (`openCommentsModal`, `notifyCollabsOnComment`).

### Version-gap-only (not deep-audited this run)
- os-strategy (gap ~13), sitemap (gap ~15), compliance (gap ~10), os-bplan (gap ~8). Flagged for SOP refresh; behaviour comparison deferred to next run.

---

## 4. Health Check Results
Ran via headless preview server (python http.server :8811). The app shell is served statically; live data and PAT are entered interactively by Kevin, so they are absent in a scheduled headless run.

| Check | Result |
|-------|--------|
| Shell boot (DOM + scripts) | PASS — title "Operations Director", zero console errors/warnings on load |
| 4d localStorage dismissal JSON validity | PASS — 0 `dismissed_`/`cfv_` keys, none malformed |
| 4a Badge count consistency | NOT VERIFIABLE — `allTransactions` undefined (no auth/data headless) |
| 4b Token/auth health | NOT VERIFIABLE — PAT absent by design in headless run |
| 4c Stale-data timestamp | NOT VERIFIABLE — no data load occurred headless |

No failures. The 3 not-verifiable checks are auth-gated; they require an interactive session with Kevin's PAT.

---

## 5. Actions Taken

### Auto-fixed
None. No DEAD references, type mismatches, or simple renames this run. The 35 rogue field IDs are valid and a pre-existing local-map pattern; consolidating them is a multi-file refactor outside the safe auto-fix scope, so it was not auto-corrected.

### Escalated to Human Review
- **[WARNING] Airtable task created** in Tasks table — "Drift Monitor: SOP docs behind code on 10 pages (6 audited)". Due 2026-06-12. Notes link to this report and list the highest-impact stale/undocumented items (cfv 25-day dismissal claim, invoices fetch-query mismatch, comms labels 11–14, tasks Plan-My-Day removal).

### Maintenance
- Regenerated `monitoring/reference-map.json` from current config.js (274 named field constants, up from 235; 21 tables, 9 records, 11 select choices). All entries validated against live schema — 0 dead.
- Saved `monitoring/schema-2026-06-11.json` (compact snapshot).
