# E2E Sweep — 2026-07-23

## Summary
- Production URL: **UP** (HTTP 200, 63,354 byte body, `<title>Operations Director</title>` + `id="sidebar"` + `switchTab(` present)
- Mode: **DEGRADED** — JS available for navigation and DOM reads, but the auth injection was blocked by the Claude Code permission classifier, so the app ran **unauthenticated**. Shell, routing, iframes and consoles verified; **Airtable data rendering NOT verified**.
- Tabs walked: **25** (enumerated live from `PAGE_REGISTRY`) | PASS: 3 | WARN: 0 | FAIL: 0 | UNVERIFIED: 22
- Data invariants: **PASS** (3 checked, all controls populated)
- Console: **zero errors** across the whole walk. Only warnings, all the expected `[loadStrategicKpis] failed Error: Auth failed` from running signed out.

## Auth
`javascript_tool` was denied for `localStorage.setItem('_dlr_pat', …)`:

> Permission for this action was denied by the Claude Code auto mode classifier.

Generic JS (`typeof PAGE_REGISTRY`, `switchTab`, DOM reads) ran fine, so this is a targeted block on writing the credential, not a blanket JS denial. Per the sweep rules the token was **not** typed into the app's login field and was not written anywhere in the web root. This is recorded as a FAIL so it stays visible rather than silently degrading the sweep every morning.

**Fix:** pre-approve `mcp__Claude_Browser__javascript_tool` for this scheduled task in settings, or give the sweep an allowlisted auth helper.

## Per-tab results

All 25 `tab-<id>` containers exist and are non-empty. No `undefined`, `NaN` or `[object Object]` leaked into any panel's visible text. No `switchTab` threw.

| Tab | Container | Evidence | Verdict |
|---|---|---|---|
| overview | 4,217 ch | "Leadership Dashboard" + Financial Overview / Strategic KPIs / Portfolio / Tenancy / 31-Day Forecast headings | UNVERIFIED (data) |
| os-strategy | iframe | `/os/strategy/index.html` → "Objective & Strategy", 4,243 ch | UNVERIFIED (data) |
| tasks | iframe | `/os/tasks/index.html` → "Tasks & Projects", 489,082 ch | UNVERIFIED (data) |
| cfv | 2,075 ch | "Cash Flow Voids — Auto-detected from Airtable" | UNVERIFIED (data) |
| money | 841 ch | "Not yet loaded ↻ Refresh — No checks" (correct signed-out state) | UNVERIFIED (data) |
| wealth | 246 ch | "Loading wealth data…" (blocked on auth) | UNVERIFIED (data) |
| income | 5,168 ch | "Accounts Receivable Fixed" | UNVERIFIED (data) |
| ar-variable | 6,852 ch | sync bar rendered, 5/6 checks | UNVERIFIED (data) |
| costs | 6,833 ch | "Accounts Payable Fixed" | UNVERIFIED (data) |
| invoices | 7,726 ch | sync bar rendered, 6/7 checks | UNVERIFIED (data) |
| pnl | 211 ch | "Loading transactions…" (blocked on auth) | UNVERIFIED (data) |
| transactions | 10,791 ch | sync bar rendered, 3/6 checks | UNVERIFIED (data) |
| comms | iframe | `/follow-up.html` → "Inbound Comms", 358,483 ch | UNVERIFIED (data) |
| compliance | full-page nav | navigates to Property Compliance; its own PAT card rendered correctly | UNVERIFIED (data) |
| operations | iframe | `/os/operations/index.html` → "Operations — Tenants, Tenancies…", 166,124 ch | UNVERIFIED (data) |
| os-bplan | iframe | `/os/business-plan-builder/index.html` → "Business Launch Plan Builder", 92,998 ch | UNVERIFIED (data) |
| fintable | 2,537 ch | "Accounts — Monitors active bank account syncs" | UNVERIFIED (data) |
| systemisation | iframe | `/os/systemisation/index.html` → "Systemisation — Business Proce…", 237,828 ch | UNVERIFIED (data) |
| os-team | iframe | `/os/team/index.html` → "Team Members", 46,819 ch | UNVERIFIED (data) |
| content-machine | iframe | remote `chaichoong.github.io/content-machine/` — origin confirmed live by curl (200, 961,811 b). Cross-origin body not readable from the parent, so not asserted in-browser | UNVERIFIED |
| prospecting | 4,354 ch | "Prospecting" | UNVERIFIED (data) |
| sitemap | 32,913 ch | full page list rendered; sync bar 3/7 checks | **PASS** |
| skills | 330,819 ch | skills library rendered; sync bar 3/5 checks | **PASS** |
| ai-brain | iframe | `/ai-brain.html` loaded, body populated | UNVERIFIED (data) |
| how-it-works | full-page nav | "How Operations Director Works" guide rendered in full (screenshot verified) | **PASS** |

**Read-only interaction:** clicked the Wealth sidebar item. `tab-wealth` switched to `display:block`, `tab-overview` to `display:none`, active nav moved to "📈 Wealth". Click navigation works. No writes performed anywhere.

**Note on the two full-page tabs:** `compliance` and `how-it-works` are whole-window navigations, not in-shell panels. That is by design, but it kills the JS execution context mid-walk, so the sweep must visit them last or separately. Worth encoding in the task file.

## Data invariants

`python3 scripts/check-data-invariants.py --json` → exit 0.

| Invariant | Asserts | Control pop. | Status |
|---|---|---|---|
| report-amount-populated | `**GBP` set ⇒ `Report Amount` set | 100 | PASS |
| split-override-sign | override set ⇒ `sign(Report Amount) == sign(**GBP)` | 18 | PASS |
| no-reimport-duplicates | one bank transaction ⇒ one record | 7,654 | PASS |

Every control matched a real population, so none of these checks was silently asserting nothing. This is the part of the sweep that was fully verified today, and it is the part that caught the two worst incidents this platform has had.

## Failures needing attention

1. **Auth injection blocked by the permission classifier** — the sweep could not sign in, so no tab's Airtable data was checked. 22 of 25 tabs are UNVERIFIED, not PASS. Until this is pre-approved, the daily sweep only proves the app renders, not that the numbers are right.

Nothing else failed. Production is up, every tab renders, every iframe loads, the console is error-free, and all three live financial invariants hold.
