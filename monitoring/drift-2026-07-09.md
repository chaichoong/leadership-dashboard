# Drift Report — 2026-07-09

## Summary
- Schema changes: 3 new fields, 0 removed, 0 type changes, 0 renames
- Field references: 0 dead, 0 orphaned, 55 rogue (chronic, unchanged +1 vs 07-08)
- SOP drift: 0 stale, 0 undocumented, 10 standing version gaps
- Health checks: N/A (headless scheduled run, no authenticated browser)
- Auto-fixed: 0 items (reference-map.json config-synced — 3 new fields added)
- Human review: 0 new items (the one live SOP gap is already tracked in Airtable `rec9ihFqDDSY8DMLZ`)

Clean cycle. No new drift that meets the auto-fix or escalation bar.

## 1. Schema Changes
Compared today's snapshot (`schema-2026-07-09.json`, 114 tables / 4,825 fields) against `schema-2026-07-08.json` (114 tables).

**3 new fields — all expected, matching the net-worth Wealth work in commits `68f3f2b`, `87f6d81`:**

| Table | Field | Type | Field ID |
|-------|-------|------|----------|
| Properties | Include in Net Worth | checkbox | `flddayDb0932Mk8uA` |
| Business | Include in Net Worth | checkbox | `fldrAYgb6aYS1zTSr` |
| Accounts | Net Worth Class | singleSelect | `fld8MHoybEal88D0Z` |

- New tables: 0
- Removed tables: 0
- Removed fields: 0
- Type changes: 0
- Renames: 0

All 3 new fields are already wired into `js/config.js` (constants `F.propInclude`, `F.bizInclude`, `F.accNetWorthClass`, lines 327-329) and resolve live in Airtable.

## 2. Field Reference Issues
Verified all 372 field IDs + 31 table IDs + 20 record IDs from `reference-map.json` against today's schema (after adding the 3 new net-worth fields — see §5).

- **DEAD (code → not in Airtable): 0.** Every config.js field, table and record ID resolves to a live Airtable field/table.
- **ORPHAN (in Airtable + config but unreferenced across js/): 0.** Every mapped field is still referenced (raw ID or `F.` constant) somewhere in js/.
- **TYPE_MISMATCH: 0.** No referenced field changed type.
- **ROGUE (hardcoded `fld…` in js/ not in the config field map): 55.** Up 1 from the 54 standing at 07-08. The new one is `fldO4BTJhFv5EsN6i` ("Sub Category Name", Chart of Accounts - Sub Categories) now also referenced in `js/wealth.js` after the net-worth net-cash-flow edits. All 55 resolve to real, live Airtable fields; none are dead.

This is a chronic, stable condition, not new drift. Centralising 55 hardcoded IDs across 11 feature files is a multi-file refactor, not a safe unattended auto-fix, so they are left standing (same call as every prior cycle). Distribution by file (non-unique):
- `js/dashboard.js` (Projects/KPI cluster, Tasks, Systemisation Workflows)
- `js/skills-data.js` (Tasks, Tenancies, Costs, Tenants, Transactions)
- `js/sitemap.js` (SOP Update Requests cluster)
- `js/arrears.js` (Tasks link fields)
- `js/fintable.js` (Accounts: **Fintable User, **Institution, *Name)
- `js/costs.js`, `js/pnl.js`, `js/reconciliation.js`, `js/transactions.js`, `js/cashflow.js`, `js/wealth.js` (Chart-of-Accounts Category / Sub Category Name)

## 3. SOP Drift
No SOP-mapped feature file changed since the last run. Commits since the 07-08 report (`7dd60d5` → `b8d3349`) touched only: `js/wealth.js`, `js/money.js`, `js/cashflow.js` (money-view signposts), and a Sitemap Supabase clone (static HTML + Supabase shim, not `js/sitemap.js`). None of the ten SOP-to-code pairs had its documented code path modified.

- **STALE_SOP:** 0 new.
- **UNDOCUMENTED:** 0 new.
- **VERSION_GAP:** 10 standing gaps (pages carrying a SOP where `pageVer` > `sopVer`), unchanged from 07-08:

| Page | pageVer | sopVer |
|------|---------|--------|
| Leadership Dashboard | 2.40 | 2.9 |
| Objective & Strategy | 1.15 | 1.0 |
| Tasks & Projects | 1.108 | 1.1 |
| CFVs | 1.29 | 1.6 |
| Accounts Payable Variable (Invoices) | 2.18 | 2.2 |
| Profit & Loss | 2.24 | 2.12 |
| Inbound Comms | 2.52 | 2.4 |
| Property Compliance | 1.11 | 1.1 |
| Business Launch Plan Builder | 1.14 | 1.3 |
| Site Map & Guides | 1.17 | 1.2 |

`pageVer` auto-bumps on every push; `sopVer` moves only on a manual SOP rewrite, so a standing gap is expected. The one gap with a known behavioural cause — the CFV detection change (`CFV_EARLY_PAY_DAYS = 5`, live days-overdue derivation, 2026-07-03) landed without a matching `sop-cfvs.html` update — is already captured in open Airtable task `rec9ihFqDDSY8DMLZ` (status "To do", due 2026-07-04, now overdue). Not re-raised or duplicated.

## 4. Health Check Results
**N/A — headless scheduled run, no authenticated browser.** No preview server was running (`preview_list` empty) and the dashboard gates all data behind interactive PAT login, so `window.PAT`, `window.allTransactions`, badge counts and localStorage dismissal state cannot be exercised without a logged-in session. Consistent with prior cycles.

- 4a. Badge/count consistency — N/A
- 4b. Token/auth health — N/A
- 4c. Stale-data timestamp — N/A
- 4d. localStorage dismissal JSON validity — N/A

## 5. Actions Taken

### Auto-fixed
- **`monitoring/reference-map.json` config-synced.** Added the 3 new net-worth field IDs that entered `config.js` this cycle (`fld8MHoybEal88D0Z` → `F.accNetWorthClass`, `flddayDb0932Mk8uA` → `F.propInclude`, `fldrAYgb6aYS1zTSr` → `F.bizInclude`). Map now holds 372 field IDs, matching config.js exactly. No PR — this is the standard drift-map maintenance step committed with the report.

### Escalated to Human Review
- None. No finding met the escalation bar. The only live SOP behavioural gap (CFV) is already tracked in Airtable `rec9ihFqDDSY8DMLZ`; not duplicated.
