# Drift Report — 2026-06-12

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes, 0 renames
- Field references: 0 dead, 0 type mismatches, 53 rogue (code-level, all valid live fields)
- SOP drift: 10 version gaps (unchanged); deep-audit findings from 2026-06-11 still open
- Health checks: 2 pass, 0 fail, 3 not-verifiable (auth-gated headless)
- Auto-fixed: 0 items (no safe auto-fix this run — see note)
- Human review: 0 new tasks — codebase unchanged since last run; existing task `rec9ihFqDDSY8DMLZ` already covers the SOP drift

**Headline:** The codebase is byte-identical to the last run. `HEAD` is still `fc08053` (the 2026-06-11 drift report commit) — zero source commits since 2026-06-11. The Airtable schema is also unchanged vs `schema-2026-06-11.json`. No new drift has entered the system. This run re-verified the prior findings, confirmed nothing regressed, and avoided creating duplicate tasks/PRs.

---

## 1. Schema Changes
Compared `schema-2026-06-12.json` against prior snapshot `schema-2026-06-11.json`.

- Tables: 107 (unchanged). Fields: 4,771 (unchanged).
- New fields: none. Removed fields: none. Type changes: none. Renames: none. Tables added/removed: none.

The base is identical to yesterday's snapshot at the field level.

---

## 2. Field Reference Issues

Source of truth: `monitoring/reference-map.json` (274 named field constants, 21 tables, 9 records, 11 select choices). `js/config.js` last changed on 2026-06-09 (auto-bump only) — the reference-map is still current, so no regeneration was needed this run.

**DEAD (referenced in code, missing in Airtable):** none. All 274 mapped field IDs plus the 53 hardcoded feature-file IDs resolve to live Airtable fields.

**TYPE_MISMATCH:** none. No schema changes occurred, so no type drift is possible.

**ROGUE (hardcoded field IDs in feature js, bypassing config.js):** 53 distinct IDs across 9 feature files. **All 53 resolve to valid live fields — zero broken references.** This is the pre-existing local field-map pattern, not new drift.

| File | Rogue IDs |
|------|-----------|
| js/skills-data.js | 57 refs (mostly doc-string IDs in template literals) |
| js/dashboard.js | 41 |
| js/arrears.js | 17 |
| js/fintable.js | 6 |
| js/sitemap.js | 6 |
| js/costs.js | 3 |
| js/pnl.js | 3 |
| js/reconciliation.js | 3 |
| js/transactions.js | 3 |
| js/cashflow.js | 1 |

(Counts above are per-file occurrences; the deduplicated genuine-rogue set across all files is 53 IDs not present in config.js.)

**Why not auto-fixed:** consolidating 53 IDs into `config.js` would touch 9 feature files plus the shared `config.js` in one pass — exactly the concurrent-edit / shared-file risk `CLAUDE.md` warns against, and beyond the "comments / config refs / simple naming" scope allowed for auto-fixes. Logged as INFO, consistent with the 2026-06-11 decision. A single dedicated session should consolidate the `dashboard.js` and `skills-data.js` local maps into `config.js` if Kevin wants to close this.

**ORPHAN:** raw-ID grep is unreliable here — feature files read fields via `F.constName` (and the other constant objects), not raw `fldXXX` literals, so a raw-ID scan reports false orphans. Not reported (same as prior run).

---

## 3. SOP Drift

No source files changed since the 2026-06-11 deep audit, so every SOP-vs-code finding from that run still stands and no new drift was introduced. The 10 version gaps are unchanged (pageVer auto-bumps on source push; no push has happened, so the gaps did not move).

The full deep-audit findings (≈30 stale/undocumented items across overview, cfv, invoices, pnl, comms, tasks) are documented in `monitoring/drift-2026-06-11.md` §3 and are tracked by the open Airtable task below. They are **not re-listed here** to avoid noise — the codebase has not changed, so re-auditing would reproduce the identical list.

**Highest-impact items still open** (from prior audit, unchanged):
- cfv: SOP claims dismissals "auto-expire after 25 days"; code expires at next rent-due + 2-day tolerance.
- invoices: SOP documents fetch as `NOT({Status}='Paid')`; code uses `{Status}='Unpaid'`.
- comms: SOP lists labels 1–10; code implements labels 11–14.
- tasks: "Plan My Day" planner removed (now "Auto-Schedule"); Kanban now multi-mode.

---

## 4. Health Check Results
Ran via headless preview server (`python -m http.server :8811`, config `ops-director`). The shell is served statically; live data and PAT are entered interactively by Kevin, so they are absent in a scheduled headless run.

| Check | Result |
|-------|--------|
| Shell boot (DOM + scripts) | PASS — title "Operations Director", 23 scripts, shell present, 0 console errors/warnings on load |
| 4d localStorage dismissal JSON validity | PASS — 0 `dismissed_`/`cfv_` keys, none malformed |
| 4a Badge count consistency | NOT VERIFIABLE — `window.allTransactions` undefined (no auth/data headless) |
| 4b Token/auth health | NOT VERIFIABLE — `window.PAT` absent by design in headless run |
| 4c Stale-data timestamp | NOT VERIFIABLE — no data load occurred headless |

No failures. The 3 not-verifiable checks are auth-gated; they require an interactive session with Kevin's PAT.

---

## 5. Actions Taken

### Auto-fixed
None. No DEAD references, type mismatches, or simple renames this run. The 53 rogue field IDs are valid and a pre-existing local-map pattern; consolidating them is a multi-file refactor outside the safe auto-fix scope.

### Escalated to Human Review
None new. The codebase is unchanged since the last run, and the SOP drift is already tracked by the open Airtable task:
- **`rec9ihFqDDSY8DMLZ`** — "Drift Monitor: [WARNING] SOP docs behind code on 10 pages (6 audited 2026-06-11)" — Status "To do", due 2026-06-12. Creating a duplicate would add noise, so no new task was created.

### Maintenance
- Saved `monitoring/schema-2026-06-12.json` (compact snapshot — 107 tables, 4,771 fields).
- `monitoring/reference-map.json` left as-is — `config.js` has not changed since it was last generated (2026-06-11), so it remains accurate.
- Committed this report and the schema snapshot to `main`.
