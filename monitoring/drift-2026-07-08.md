# Drift Report — 2026-07-08

## Summary
- Schema changes: **1 new table** (AI Brain Inbox) + **2 new fields** (AI Brain Today: Answer, Options), 0 removed, 0 type changes, 0 renames
- Field references: **0 dead**, 0 orphaned, 54 rogue (chronic, unchanged from 07-07)
- SOP drift: **0 new stale**, 0 new undocumented, 10 standing version gaps
- Health checks: 0 pass, 0 warn, 0 fail — **4 N/A** (headless run, no authenticated browser)
- Auto-fixed: **0 items** (nothing met the safe auto-fix bar)
- Human review: **0 new tasks** (the one open item, CFV SOP drift, is already tracked in Airtable task `rec9ihFqDDSY8DMLZ` — not duplicated)

Net: clean cycle. No SOP-mapped feature file changed since the last run, so no field-reference or SOP-behaviour drift was introduced on any documented page. One new table and two new fields appeared, all part of the AI Brain module and already correctly wired in code.

---

## 1. Schema Changes

Compared today's snapshot (`schema-2026-07-08.json`, 114 tables / 4,822 fields) against the prior snapshot (`schema-2026-07-07.json`, 113 tables / 4,815 fields).

**New tables (1):**
- **AI Brain Inbox** (`tbliR8KkOV4SKNiIZ`) — 5 fields: Text (multilineText), Kind (singleSelect), Submitted (singleLineText), Status (singleSelect), Result (multilineText).
  - Already referenced in code: `ai-brain.html:352` as `var INBOX_TABLE = 'tbliR8KkOV4SKNiIZ'`. This is the capture inbox for the AI Brain "add note / add video / ask" flow. New table + new code reference are consistent — informational, no action.

**New fields in existing tables (2):**
- **AI Brain Today** → **Answer** (singleLineText) — used at `ai-brain.html:249, 250, 274` to store and display the tapped answer to a daily question.
- **AI Brain Today** → **Options** (singleLineText) — used at `ai-brain.html:252` to render pipe-delimited answer options.
  - Both are part of the "AI Brain: Setup tab goes real" / daily-question loop shipped in commit `f76a006`. Schema and code match — informational, no action.

**Removed tables / fields:** none.
**Field type changes:** none.
**Field renames:** none.

Zero-delta on every pre-existing field means no referenced field could have silently changed type this cycle.

---

## 2. Field Reference Issues

Verified all 369 field IDs and 31 table IDs in `reference-map.json` against today's schema.

- **DEAD (code → not in Airtable): 0.** Every config.js field ID and table ID still resolves to a live Airtable field/table.
- **ORPHAN (in Airtable + config but unreferenced across js/): 0.** Every mapped field is still referenced (raw ID or `F.` constant) somewhere in js/.
- **ROGUE (hardcoded `fld…` in js/ not in the config field map): 54** — unchanged from 07-07. All 54 resolve to real, live Airtable fields; none are dead. They are concentrated in the Projects (KPI compute), Tasks, Tenancies, Costs, Accounts, SOP Update Requests and Chart-of-Accounts clusters. This is a chronic, stable condition, not new drift. Moving them into config.js is a refactor, not a safe auto-fix, so they are left as standing (same call as prior cycles).

`config.js` was not touched this cycle, so `reference-map.json` needs no update.

Full rogue list (file-of-record: their owning js feature files):
- Projects/KPI: KPI Compute Code, KPI Name, KPI Current, KPI Last Updated By, KPI Last Updated, KPI Automated, KPI Target, KPI Detail JSON, KPI Source, KPI Unit, Project Name, Project Status, Project Completed?, Completed Tasks, Total Tasks, Start Date, End Date, Owner, Business
- Tasks: Tenants, Rental Units, Completion Date, Contact Name, Source, Inbound Approval, Properties, Contact Email, Tenancies, Inbound Communication Task
- Tenancies: Payment Status (Derived/Debug), Payment Status (AR Fixed), Payment Status (Auto) Legacy, Previous Payment Status, (Deprecated) Payment Status (Auto), Initial Rent Due Date, Tenancy Agreement
- Costs: Due Date (This Period), Days Until Due (This Period), Paid This Period?, Accounts
- Accounts: **Fintable User, **Institution, *Name
- SOP Update Requests: Request, SOP File, Page Version, Requested At, Page ID, Status
- Chart of Accounts: Sub Category Name, Category Name
- Transactions: Tenants; Tenants → Current Unit; Systemisation Workflows → SOP Document

---

## 3. SOP Drift

No SOP-mapped feature file changed since the last run. The 07-07→07-08 commits touched only: Supabase shadow-build twins (`*-supabase.html`, `*-shim.js`, `*-boot.js`), `ai-brain.html` (no mapped SOP), `js/skills.js` (no mapped SOP), `index.html`, `os/systemisation/index.html` (no mapped SOP), MASTER-PLAN/PRODUCTISATION/STRUCTURE docs, and sync scripts. None of the ten SOP-to-code pairs had its documented code path modified.

- **STALE_SOP:** 0 new.
- **UNDOCUMENTED:** 0 new.
- **VERSION_GAP:** 10 standing gaps (pages that carry a SOP where `pageVer` > `sopVer`):

| Page | pageVer | sopVer |
|------|---------|--------|
| Leadership Dashboard | 2.40 | 2.9 |
| Objective & Strategy | 1.15 | 1.0 |
| Tasks & Projects | 1.108 | 1.1 |
| CFVs | 1.29 | 1.6 |
| Accounts Payable Variable (Invoices) | 2.18 | 2.2 |
| Profit & Loss | 2.24 | 2.12 |
| Inbound Comms | 2.52 | 2.4 |
| Property Compliance | 1.11 | 1.1 |
| Business Launch Plan Builder | 1.14 | 1.3 |
| Site Map & Guides | 1.17 | 1.2 |

`pageVer` auto-bumps on every push; `sopVer` moves only on a manual SOP rewrite, so a standing gap is expected. The one gap with a known behavioural cause — the CFV detection change (`CFV_EARLY_PAY_DAYS = 5`, live days-overdue derivation) landed 2026-07-03 without a matching `sop-cfvs.html` update — is already captured in open Airtable task **`rec9ihFqDDSY8DMLZ`** (status "To do", due 2026-07-04, now overdue). Not re-raised or duplicated.

---

## 4. Health Check Results

**N/A — headless scheduled run, no authenticated browser.** No preview server was running (`preview_list` empty) and the dashboard gates all data behind interactive PAT login, so `window.PAT`, `window.allTransactions`, badge counts and localStorage dismissal state cannot be exercised without a logged-in session. Consistent with the prior four cycles.

- 4a. Badge/count consistency — N/A
- 4b. Token/auth health — N/A
- 4c. Stale-data timestamp — N/A
- 4d. localStorage dismissal JSON validity — N/A

---

## 5. Actions Taken

### Auto-fixed
None. No dead references, no simple rename mismatches, and the 54 rogue IDs all resolve to live fields (a refactor, not a safe auto-fix). Nothing met the auto-fix bar, so no `fix/drift-2026-07-08` branch or PR was created.

### Escalated to Human Review
No new task created. The single open drift item — CFV SOP behaviour drift + its standing version gap — is already tracked in Airtable task `rec9ihFqDDSY8DMLZ` (open, overdue since 2026-07-04). Its notes remain accurate. Creating a duplicate would add noise, not signal.

---

*Snapshot: `schema-2026-07-08.json` (114 tables / 4,822 fields). Prior: `schema-2026-07-07.json` (113 / 4,815).*
