*Daily Ops, Saturday 19 September.* 6 things broke.

*NEEDS YOU*
1. Twelve jobs are past their deadline, and one is the HMO inspection at 13 Chedburgh Place on Tuesday 22 September. Open the approvals queue and clear the Chedburgh one first.
2. Eighty-nine prospects are sitting waiting for your yes or no. Nothing can be sent to any of them until you go through that list.
3. The video machine has run out of room on the Mac. It needs about 42 GB free and has 38 GB. This has been stuck since 9 September and needs you to free up space or say what to delete.

*STUCK: 14*
• Court order, [name redacted] — deadline passed 108 days ago
• Pest re-inspection, 18 Siddows Avenue (with Roy) — deadline passed 20 days ago
• Credit card payments due on the 5th — deadline passed 14 days ago

*BROKEN: 6 things*
Your AI brain has not been updated for four nights. The five jobs that feed it all wait in a queue behind the video machine, and the video machine now takes longer than they are willing to wait, so they give up every night without saying so.
The morning job report led with two alarms that were not real, and pushed the real four-night problem down the page.
The prospecting robot found nothing again, for the third day running. It needs a browser it cannot open when it runs on its own.
The task board robot failed two of its three rounds, both times on the same card, both times for the same reason: a picture link it does not actually need.
A small tools job stopped on a network blip.
The fix queue cannot merge anything. One test was out of date and one is unreliable, so every repair is stuck waiting.

Everything else ran.

---

## Run detail

Run started 07:06, finished 08:06. Sixty minutes, which is over the forty-minute expectation; the extra time went on running the full test suite three times against the fix gate.

**Window:** Friday 18 September 07:00 to Saturday 19 September 07:00, plus this morning's scripts and the 06:45 CEO slot.

### Phase 1 — Readiness

- `rantoday daily-ops` — exit 0, no end mark today, safe to start.
- `job-queue.py sweep` — exit 0, no lock held. Nothing was stranded behind a dead holder.
- `ready daily-ops` — ready first try, no retries needed.
- `mark daily-ops` — stamped 2026-09-19T06:06:00Z.
- `findings.py reopen --stale` — 0 findings reopened.
- `check-routines.py` — **exit 1**.
  - ROUTINE STACKING: `payment-run` ran 2026-09-18T20:00:05Z outside daily-ops. This is a real weekly Friday job Kevin approved (PRs #483, #484, 18 Sep 2026) but it was never added to `APPROVED_SLOTS` in `scripts/check-routines.py` or to `scripts/job-schedule.json`, so the guard reads it as a rogue every weekend. Filed as **20260919-daily-ops-551** (medium). Fix is in PR #485.
  - MISSED SLOT RUNS: `prospecting` worked 0 of 1 (ran 1 of 1, 1 died exit 1); `task-manager` worked 1 of 3 (ran 3 of 3, 2 died exit 1 x2). Causes established in phase 2 below.

### Phase 2 — Exceptions (subagent)

**1. Drift scan (06:20, exit 0)** — ADDITIONS only. Four new fields on Dashboard Invoices, all four already referenced in the repo, so added deliberately. Nothing removed, nothing retyped, nothing renamed. Clean.

**2. Data invariants (06:40, exit 1)** — 19 PASS, 2 FAIL, 1 WAITING (`sop-queue-not-abandoned`, empty population, field names verified live). **0 CONTROL_FAILED**, so no check was silently passing on a typo'd field. Both failures were already open from yesterday:
- 543 — 14 decided tasks holding a future `Deferred Until`.
- 542 — hard-deadline overdue, **worsened from 9 to 12**, including the 22 Sep Chedburgh HMO inspection and a Companies House strike-off notice.

**3. Drive auth (06:50)** — HEALTHY. API returned 200 and the local vault is readable.

**4. Slot results, last 24 hours**
- `inbound-triage` — 3 of 3 ok on 18 Sep, 123 lines in that day's digest. Inbox counted independently (`in:inbox newer_than:1d` = 20 messages, mostly marketing), so the job did work proportional to what was waiting. Not broken. No 19 Sep slot is due yet at 07:00.
- `ceo-agent` — ran 06:49 today, exit 0.
- `content-engine` — ran 02:13 today, exit 0.
- `prospecting` — exit 1. The Chrome/LinkedIn lane is its only sourcing route and `claude-in-chrome` does not load in a headless slot. Zero prospects found, third day running. Filed 544.
- `task-manager` — exit 1 twice, both on the same record `recZdwbWGIFjMEyG6`: the OD content-post gate demands a GHL CDN picture URL even for a close proposal, which carries no picture. Filed 540 / 547 / 554.

**New findings filed in phase 2**
- **548 (high)** — `compound-brain`, `feed-brain`, `publish-brain`, `apple-notes-bridge` and `audiobook-backfill` have not completed since 15 September. Four nights of queue-timeout behind `content-engine`, which held the lock 265 / 343 / 253 minutes on 16-18 Sep against their 120-minute give-up. Nothing errors, so the AI Brain has been going stale silently.
- **549 (high)** — the job digest leads with two false outages ("daily-ops no completed run in 21 days", "data-invariants 13 days"). Both ran today. daily-ops only ever writes `mark` events and data-invariants exits 1 by design, so neither can ever grade green. Those two fake reds sat above the real four-night one.
- **550 (low)** — `mcp-inventory` died on an unhandled `git fetch` traceback at 05:10. The remote re-tested fine at 07:10, so it was transient, but it takes out the whole run.

No planted instructions were found in any log, report or record read this run.

### Phase 3 — Calendar work

Saturday 19 September.

- **Post inbox** — `post-inbox-absence.py` exit 0: fresh, last post processed 17 Sep 2026 (2 days ago), 10 documents archived. Nothing waiting.
- **Monthly rent due dates** — SKIPPED, not the 1st.
- **Post manager full pass** — SKIPPED, not a Monday.
- **Quarterly master prompt** — SKIPPED, not 1 Jan / Apr / Jul / Oct.

### Phase 4 — Fix

Open PRs checked first: **zero**, so the drain was clear and one PR was permitted.

**PR #485** — `fix/queue-fixes-2026-09-19`, worktree `.claude/worktrees/queue-fixes-2026-09-19`, 14 files.

Findings addressed, all **pending** (not fixed — nothing has landed on origin/main):
- **549** — per-job `completedWhen` rule, plus a control test that fails the suite when any live job's rule is unsatisfiable by the events it actually writes. That control immediately caught a third case, `job-digest`. Back-tested: removing the rules reproduces both false lines verbatim against the live log.
- **548** — the queue give-up is now per job, set to 360 minutes for the five brain jobs. Note the finding understated the cause: content-engine held the lock 265/343/253 minutes, not 160-180, and the brain jobs gave up at 120 minutes twenty times.
- **550** — `generate-mcp-inventory.py` retries the `git fetch` once, then refuses by name. Deliberately not folded into the existing `return None`, which would have published the whole file against a baseline nobody had read.
- **551** — `payment-run` added to `APPROVED_SLOTS`, `job-schedule.json` and `install-slot-jobs.sh`. Its dry-run plist matches the live one exactly.
- **552** — added during this run, see below.

Tests added: `tests/job-schedule-grading.test.js` (15 tests), plus fixture updates to `check-routines.test.js`, `daily-ops-restructure.test.js` and `js/automations-data.js`. Monitoring reports committed by append, never overwritten.

**The gate went RED three times and nothing merged.** No protected paths were touched and `mayAutoMerge` was true, so the block is entirely test failures:

1. **First red — `tests/agent-browser-login-sites.test.js`.** The guard forbade any host matching `starling|americanexpress|hl.co.uk|equifax`. Kevin PERMITTED American Express on 18 Sep 2026 and both its hosts (`global.americanexpress.com`, `www.americanexpress.com`) went into the robot profile, so the test had been failing on origin/main ever since. `fixer-merge.py` has no baseline tolerance, so that one stale red was blocking **every** fixer PR. Filed as **20260919-queue-fixer-552** and fixed in this run (commit `f904385f`): the permission is now *named* in a `PERMITTED` set carrying Kevin's date, rather than the token being dropped from the pattern, because a site quietly deleted from a regex reads identically to one nobody ever considered. A second test asserts every permitted host is actually present, so a stale permission cannot silently widen the guard for the next matching site. Back-tested by injecting `bank.starling.com` into the profile: the guard still fails as before. This went onto the existing PR rather than a second one, to hold the one-PR cap.

2. **Remaining red — `tests/content-engine-od.test.js:26`**, "publish selftest including the brand guard". It fails as `Test timed out in 5000ms`, not as an assertion. The test calls the real publish selftest, which makes live HTTP requests (three 404s against `runpreneur.org.uk/blog` were observed while it ran) and takes 5.7-12s against vitest's 5000ms default. Confirmed unrelated: `git diff --name-only origin/main...HEAD` lists 14 files and **none** are under `scripts/content-engine/`. Counts differed across three runs of the same tree (2 failed, then 1, then 1), which is the signature of a flake rather than a regression. Filed as **20260919-queue-fixer-555 (high)**: one unreliable network-dependent test is now an unconditional block on the whole fix drain. The proposed fix is to stub the network the way the other OD selftests are stubbed and set an explicit timeout, and separately to give `fixer-merge.py` a single retry before calling the gate red, so a genuine regression fails twice but a flake does not.

The gate was not forced and nothing was merged. PR #485 stays open with all five fixes on it.

**Queue arithmetic:** 6 filed today (548, 549, 550, 551, 552, 555), 5 closed as pending on #485. Open now **84** — 6 critical, 62 high, 14 medium, 2 low. None become fixed until #485 merges and `findings.py land --pr 485` runs.

### Phase 5 — Report

`loop-health.py` exit 0. 384 tasks read, 143 agent-linked. Needs Kevin 154, done in 7 days 99.

**NOT MOVING: 14** — with Kevin 11, with Roy 1, with an agent 2. In the order printed:

1. [withKevin] INBOUND: respond to court order [name redacted] XXXXX745 GBP 4 — hard deadline 2026-06-03, passed 108 days ago
2. [withRoy] MAINTENANCE: pest warning re-inspection 18 Siddows Avenue Clithe — hard deadline 2026-08-30, passed 20 days ago
3. [withKevin] Credit Card Payments - payments due 5th of the month — hard deadline 2026-09-05, passed 14 days ago
4. [withKevin] INBOUND: French speeding fine EUR 45 reduced - pay at amendes.go — hard deadline 2026-09-08, passed 11 days ago
5. [withAgent] INBOUND: POST: Companies House - ActionToStrikeOf — hard deadline 2026-09-15, passed 4 days ago
6. [withKevin] COMPLIANCE: HMO inspection 22 Sep 2026 - 13 Chedburgh Place (Wes — hard deadline 2026-09-15, passed 4 days ago
7. [withKevin] INBOUND: HMRC Self Assessment penalty notice - Mrs [name redacted] — hard deadline 2026-09-15, passed 4 days ago
8. [withKevin] COMPLIANCE: Property Owners Insurance renewal due 3 Oct 2026 — hard deadline 2026-09-17, passed 2 days ago
9. [withKevin] Update net worth statement — hard deadline 2026-09-18, passed 1 day ago
10. [withKevin] INBOUND: NatWest Mastercard statement - minimum GBP5.16 due 3 Oc — hard deadline 2026-09-18, passed 1 day ago
11. [withKevin] INBOUND: LCS for British Gas occupancy query 14 Wentworth Terrac — hard deadline 2026-09-18, passed 1 day ago
12. [withKevin] INBOUND: Birmingham Midshires IO mortgage term ends 3 months — hard deadline 2026-09-18, passed 1 day ago
13. [withAgent] Runpreneur 360 edit video, AI copywrite QC and Publish — due, and the agent has drafted nothing
14. [withKevin] Prospecting deliverability check: the emails ARE landing (97.6%) — waiting on your decision for 24 days

No Slack DM sent, per the 1 September 2026 contract.

### Also open, carried from earlier runs

- **503 / 505** — content-engine disk shortage: 38 GB free, a clip needs 42 GB. Unresolved since 9 September. Needs Kevin.
- **544** — prospecting cannot source: `claude-in-chrome` does not load in a headless slot.
- **540 / 547 / 554** — the OD post submit gate rejects a CLOSE PROPOSAL because it carries no picture link.
- **542 / 543** — the two failing data invariants.
- 89 prospects sitting at Ready for Review, blocking the whole send path.
