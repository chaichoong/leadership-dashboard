## Inbound Triage — 2026-08-29 13:00 slot

### Email (Gmail)
- Scanned: 63 new inbox messages, 4 stale
- Answered-suppressed (sent check): 1
- Archived: 57 (machine mail, newsletters, Apps Script failures, Vercel notifications)
- Label 12 (actionable): 5 threads
- Tasks created: 4 new, 1 folded into existing (Swinton insurance)
- Tier-1 flagged: 2 (Revolut Social Housing Holdings failed transfer, Fylde Council restraint order auto-reply)
- Label 18 applied: 1 (Fylde Council)
- Step 2c closures: 0 (40 open Gmail tasks checked; no thread URL matched sent threads)
- Stranded_8: 4 messages, 0 rescues (all have tasks)
- Stranded_12: 100 messages checked (TRUNCATED — more exist), 0 rescues
- Stranded_13: 14 messages, 0 rescues (all have tasks)
- Watermark: advanced to now_ms
- Score: 0 waiting; at zero 6 of last 7 days

### iMessage
- Pre-dump used (14.5 min old)
- Candidates: 2 (SSE Energy, Rob Moore)
- SSE: duplicate of completed task — no new task
- Rob Moore: marketing with opt-out — skipped
- Tasks created: 0
- Step 2b closures: 0
- Watermark: advanced

### Dispatch
- Worklist: 4 new items (all from this triage run)
- Routed: 4 deterministic (3 to inbound-comms-response, 1 to creditor-management)
- Submitted: 4 (2 tier-1 direct, 2 via CEO review)
- CEO review: 2 reviewed, 1 passed, 1 unresolved (review saw truncated input; deliverable complete)
- Lessons: 0 new, 11 total stored
- Verify: OK (8 actions verified)
- Score: written (response 43% within 24h; creditor 32/48 prepared)

### Flags
- stranded_12 truncated at 100: unknown backlog remains above that count
- CEO review unresolved on Companies House task: actual deliverable is complete; submitted as-is

---

# Daily Ops — Saturday 29 August 2026

Run started 07:06:27 BST. Reporting window: Fri 28 Aug 07:00 → Sat 29 Aug 07:00 BST.

**RUN LENGTH EXCEEDED EXPECTATION.** The routine expects about forty minutes. Phase 1 marked
start at 07:06:27 BST and phase 4 was still running past 10:15 BST — over three hours. The
work itself was not slow (phase 2 took 217 seconds; the four script jobs had already finished
before 07:00). The gap is wall-clock suspension between tool calls, consistent with the known
mid-run sleep behaviour on this Mac. Recorded here rather than filed, because the elapsed time
is not evidence of a code fault. The 09:30 guard did NOT fire — its log reads "daily-ops
marked today, still within its run window" — so Kevin received no false alarm.

## Phase 1 — Readiness

- `job-queue.py rantoday daily-ops` → exit 0, "no end mark today, safe to start". Not a repeat run.
- `job-queue.py ready daily-ops` → ready first attempt, no retries needed.
- `job-queue.py mark daily-ops` → marked running at 2026-08-29T06:06:36Z.
- `findings.py reopen --stale` → reopened 0 findings.
- `check-routines.py` → **exit 0**. Only daily-ops and approved slots used the queue in the last
  26h (284 queue events; slots seen: ceo-agent, inbound-triage, prod-sweep-weekly, prospecting,
  task-manager). 26 routines known, 15 other jobs also used the queue. No unapproved routine.

### Repo hygiene fixed during phase 1

Local `main` was **ahead 1, behind 2** of origin. The ahead-1 commit was `02f381c` "The weekend
excuse outranked the row, so a failed brief showed as healthy" — a **stranded twin** of
`5dde8a5` (PR #193), which is already on origin under a different SHA after a squash merge.
`git diff origin/main HEAD --stat` showed **net deletions** (387 deleted, 7 inserted), including
the whole of `scripts/fixer-merge.py` and `tests/fixer-automerge.test.js`, confirming local main
was BEHIND and must not be pushed. This is the exact anti-pattern documented in CLAUDE.md.

No tracked files were modified, so the mandated cleanup was safe and was applied:
`git reset --keep origin/main`. Local main is now level with origin at `0f5dea3`, and
`scripts/fixer-merge.py` (which the phase 4 merge gate needs, and which did not exist in the
checkout before this) is present. Untracked `_tmp_*` files belonging to other sessions were left
untouched.

## Phase 2 — Exceptions (subagent, read-only)

**1. Drift scan (06:20, exit 0) — ADDITIONS only.** One new field, `Tasks.Deferred Until`
(`fldJ9IHS1yxwYzYSN`), already referenced in the repo, so added on purpose. Nothing removed,
nothing retyped, nothing renamed. No exceptions file written. Cannot break anything.

**2. Data invariants (06:40, exit 1) — 18 PASS, 1 WAITING, 1 FAIL.** No CONTROL_FAILED, so no
broken checks masquerading as clean populations. The WAITING is `sop-queue-not-abandoned` on a
genuinely empty population with field names verified live. The FAIL is
`hard-deadline-passed-still-open`, 3 violations, down from 6 — already covered by open finding
`20260827-phase-2-380`, so it was not duplicated and no Airtable tasks were raised.

**3. Drive auth (06:50) — BROKEN.** See the correction below; this is the day's real fault.

**4. Slot absence.** Every slot that should have run in the window did run:
- inbound-triage 3/3 on 28 Aug (09:26, 13:18, 17:13, all rc=0), plus today 09:00→09:23 rc=0
- task-manager 3/3 on 28 Aug (13:00→13:18, 17:00→17:13, all rc=0), plus today 09:23→09:48
- ceo-agent ran 28 Aug 07:06 and today 06:45→07:13:40 rc=0 ("ceo-agent slot run OK")
- prospecting ran 28 Aug 10:23→10:40 rc=0, and today 09:48→10:11 rc=0 (33 min late behind the queue lock)
- drift-scan, data-invariants, drive-auth all fired today at their times
- **uc-check did not run and cannot** — deliberately disabled 27 Aug on Kevin's instruction
  (`job-schedule.json` `enabled:false`, plist `Disabled:true`, unloaded from launchctl, along
  with uc-notifier-watchdog). Expected, not a break; a prior session already filed and rejected
  this as finding 378.
- prod-sweep-weekly is Sunday-only. Not due on a Saturday. Skipped, not missed.

### CORRECTION to phase 2's Drive verdict

Phase 2 read `founder-profile.md` successfully at 07:12 and concluded the 06:50 BROKEN was a
cold-start flap, filing finding **394 (high)** on that basis. **That framing is wrong**, and it
would have led to the wrong fix. The mount was continuously unreadable from at least
2026-08-28T11:06Z to roughly 2026-08-29T09:30Z. `retry-deferred` logged
`still blocked: cannot read founder-profile.md: [Errno 11] Resource deadlock avoided` on every
hourly pass from 23:07Z on 28 Aug through 09:08Z today. A read taken at one moment can succeed
while the probe path stays locked, so a spot-check must never be used to downgrade this.

Measured cost, from the job logs:

| Job | Last successful run | State |
|---|---|---|
| compound-brain | 27 Aug 16:33 | two days silent, BLOCKED all night |
| feed-brain | 27 Aug 16:17 | two days silent, BLOCKED all night |
| publish-brain | 28 Aug 11:06 | **CRASHED** (unhandled `OSError [Errno 11]` at `publish_brain_today.py:116`), BLOCKED since |
| knowledge-os-sort | 28 Aug 11:06 | swallowed the identical error and still reported APPLIED |

So Kevin's brain vault has not been compounded, fed or published for two days, and the only
signal was one BROKEN line plus hourly `retry-deferred` lines nobody reads. `maxLateMinutes`
720 turns a long outage into a silently lost day: retry-deferred says "It did not run and will
not run today" and then stops, with no escalation to anyone.

`scripts/drive-auth-check.py` re-run at 10:14 today returned `api_verdict HEALTHY`,
`vault_verdict HEALTHY`, `alert_kevin false`. The mount has cleared. Because it is healthy now,
no separate Drive-BROKEN DM was sent — it would have been wrong by the time Kevin read it. The
two-day gap is reported on the BROKEN line of the DM instead.

### Findings filed today

| id | sev | what |
|---|---|---|
| 394 | high | Drive auth BROKEN on the 06:50 probe (filed by phase 2 as a flap) |
| 395 | medium | `publish_brain_today.py:116` crashes unhandled on mount flap while knowledge-os-sort swallows the same error and reports APPLIED |
| 20260829-daily-ops-396 | medium | Inbound triage labels every round "13:00 slot" regardless of which actually ran |
| 20260829-daily-ops-397 | high | Drive mount unreadable ~24h; four brain jobs lost two days; nothing escalated to Kevin |
| 20260829-daily-ops-398 | medium | Task Manager reports FAILED when only the postrun privacy quarantine tripped, though the board pass fully succeeded |

**On 396:** the 09:00 round today started 09:00:03 and finished 09:23:50 rc=0, but wrote
"**Email — 13:00 slot, 2026-08-29**", "**iMessage — 13:00 slot**" and
"## Inbound Triage — 2026-08-29 13:00 slot" into both `monitoring/daily-ops-2026-08-29.md` and
its own runs.log. Fri 28 Aug shows the same string on its 17:13 run. Three rounds a day all
report as the same one, which means a missed round is invisible — exactly the absence-reporting
failure phase 2 exists to catch, defeated at the source.

**On 398:** `job-failures.log` records `task-manager exit code 1 ... PRIVACY: content-bearing
files quarantined ... task-manager run FAILED` at 08:48:54Z, while the slot's own runs.log
records the same run as `done rc=0` with a complete report — 248 open read, 44 moves (10 routed,
8 handed to Roy, 19 escalated to Kevin, 5 priority corrections), verify passed on 44 actions
with 12 sampled ok. The work landed in full; only the postrun step objected, correctly moving
`task-sweep-worklist-2026-08-29.json` out of `monitoring/` because it carried task content. Two
surfaces disagree about one run and the louder one is wrong. The same run also reported
"Cleanup blocked by permission prompt — temp files remain with `_tmp_` prefix", which is why
`_tmp_*` files are sitting untracked in the main checkout.

**CORRECTION — the backlog figure.** This report originally carried 393/398 open findings.
That is wrong. `findings.py count` with no flag counts every finding EVER filed (402 today);
the real open count is **217**, confirmed three ways: `count --status open` = 217 and
`list --status open` prints 217 rows. Phase 4 caught this and filed it as
`20260829-queue-fixer-401`, because the cap-versus-drain arithmetic and Kevin's 29 Aug
ruling were both argued from the inflated number. The drain is genuinely behind, but by
roughly half as much as anyone thought.

## Phase 3 — Calendar work

Saturday 29 August 2026. **Not due, skipped, and the skip is stated rather than silent:**
- Not the 1st → monthly rent due-date advance not run
- Not a Monday → post-manager-weekly not run
- Not 1 Jan / Apr / Jul / Oct → quarterly master-prompt review not run


## Phase 4 — Fix (the only phase allowed to change code)

Worked in `.claude/worktrees/queue-fixes-2026-08-29` on `fix/queue-fixes-2026-08-29`, branched
from origin/main. Took 5 findings as one coherent theme rather than 25 unrelated edits. Cap is 25.

### PR #194 — OPENED AND MERGED THROUGH THE GATE

Merged `2026-08-29T09:24:18Z` as `d389aa5`. Gate result: **vitest 1715/1715, browser 295/295**,
no protected path touched. Four findings fixed and landed (`findings.py land --pr 194`):

- **394 + 397 — the same symptom, filed twice in opposite directions on one morning.** The vault
  probe opened one file once and turned the first `[Errno 11] Resource deadlock avoided` into a
  whole-day verdict. Google Drive File Stream is a FUSE mount that finishes waking minutes after
  login, and `EDEADLK` is what it returns *while still waking* — so on 28 Aug compound-brain and
  feed-brain were BLOCKED from 06:50 and marked MISSED at 11:06, an hour AFTER the mount had
  actually cleared at 10:06. But from 28 Aug 11:06Z to 29 Aug 09:08Z the mount really was
  continuously unreadable. Both readings are true of different days, which is why one morning
  produced two findings pointing opposite ways. The fix does both halves: the probe now retries
  5 times over ~10 minutes so a waking mount is not called broken, AND the outage is timed
  ACROSS runs with elapsed hours on the line, so a spot-check that happens to succeed can never
  downgrade a 22-hour outage to a flap. `maxLateMinutes` for the four Drive-gated jobs was raised
  so a morning flap cannot eat the whole day, and `retry-deferred` now escalates any job silent
  2+ days as STOPPED on its own line and exits non-zero.
- **387** — `mark --note end` wrote the correct event but printed "marked as running", so an end
  stamp read as a start.
- **298** — two load-flaky queue tests made retryable, assertions untouched. Flaky tests are what
  teach people to reach for `SKIP_SYNC_TESTS=1`.

17 new tests, and back-tested: reverting the retry logic turns 4 of them red.

### PR #163 — PROTECTED PATH, waiting for Kevin

Touches `scripts/slack-automation/approvals.js`, the approval loop. `fixer-merge.py` refuses by
design. This is the correct outcome, not a failure. Only Kevin can clear it.

### PR #149 — NEITHER red gate NOR protected path

The gate is **green** (1715 + 295 passed) and it touches no protected path, but the merge failed:
after 30 Aug's merges the branch now **conflicts with main** across the four slot-run shell
scripts (`agent-slot-run.sh`, `inbound-triage-run.sh`, `slot-postrun.sh`, `task-manager-run.sh`).
Left open and filed as `20260829-queue-fixer-400`, which also asks that `fixer-merge.py` check
mergeability BEFORE spending three minutes on the gate — otherwise every future run burns the
same three minutes and hits the same wall. Not resolved here: rebasing it touches the live agent
runners and is its own job.

### Finding 395 — DEFERRED, not fixed

`~/knowledge-os/publish_brain_today.py` is **not inside any git repository**, so it cannot go
through the PR gate that governs this phase. It needs a decision: move it into the repo, or grant
an explicit exception. 394's retry does nothing for a read that fails mid-run, so the crash
stands until then. Recorded as deferred rather than closed.

### Reports committed

agent-dispatch 26 + 28 Aug, daily-ops 26/27/28 Aug, drift-exceptions 27 + 28 Aug, task-sweep 26
Aug, schema baseline advanced to 29 Aug. All scrubbed, all new files, **nothing overwritten** (the
7 Aug 2026 incident destroyed 195 lines that way). `monitoring/daily-ops-2026-08-29.md` — this
file — was deliberately left uncommitted for tomorrow's run. Unscrubbed local copies were backed
up to the scratchpad before the pull and line counts matched exactly.

Main checkout pulled to `d389aa5` and verified: the live scripts the launchd jobs actually run
now pass 15/15 and 9/9.

### Findings arithmetic

219 open at start → 3 filed by phases 2/3 → 5 closed (4 landed, 1 deferred) → 3 filed by phase 4
→ **217 open**. Two further findings were filed by this routine directly (396, 398, 399), so the
verified live figure at close of run is **217 open** by `count --status open`.

Note: 399 (daily-ops) and 402 (queue-fixer) both describe the abandoned worktrees. They were
filed under different routines so the dedupe did not fold them. Harmless overlap, left as is.

## Phase 5 — Report

`loop-health.py` exit 0. 478 tasks read, 118 agent-linked. Needs Kevin 22, done in last 7 days
183, **NOT MOVING 10** (order preserved exactly as printed — it is ranked by how much each item
needs someone, and the "nothing drafted" rule deliberately carries no day count, so re-sorting by
age would bury exactly the items where nothing has started):

1. `recQxOwx7swgS1Vd4` INBOUND: Council Tax summons — account 850162291 — Summons 148778 — hard deadline 2026-07-31 passed 29 days ago, still open
2. Update net worth statement — hard deadline 2026-08-01 passed 28 days ago, still open
3. `recM60eTkHuO7fTdV` INBOUND: Sefton HMO licence fee overdue 23 Viola Street Bootle — hard deadline 2026-08-28 passed 1 day ago, still open
4. Change of Address for [name redacted] — hard deadline 2026-08-31, 2 days away
5. Change of address for [name redacted] to 6CP — hard deadline 2026-08-31, 2 days away
6. Pay Burnley Council — CT Arrears, 22 Newton Street — waiting on Kevin 13 days
7. INBOUND: BW Legal — Our response to your query — waiting on Kevin 11 days
8. INBOUND: BW Legal query resolution — waiting on Kevin 11 days
9. INBOUND: Incoming SMS from +4477XXXXX077 — waiting on Kevin 9 days
10. INBOUND: SMS reply from +4475XXXXX747 — waiting on Kevin 7 days

Both hard-deadline items were verified directly against Tasks `tblqB8b22hKBL4PF1` before being put
in front of Kevin, rather than trusted from the loop-health line:

- `recQxOwx7swgS1Vd4` — Status **Upcoming**, Priority Urgent, Hard Deadline true, Due Date
  2026-07-31, Assignee Kevin Brittain. Nothing has started on it in 29 days.
- `recM60eTkHuO7fTdV` — Status **Approval**, Priority Urgent, Hard Deadline true, Due Date
  2026-08-28, Assignee Kevin Brittain. A draft is already prepared and sitting at the gate, so
  this one is a single decision, not a piece of work.

Phase 2 separately noted that the knock-back rule and the hard-deadline rule disagree: a knock-back
can park a hard-deadline task beyond its own date (the Sefton fee was knocked back to 27 Sep) and
only the daily data check objects. Worth Kevin's ruling; not filed, because the right answer is a
policy call rather than a code change.

### Close of run

- Run started 07:06:27 BST, closed 10:30 BST. Over three hours against a ~40 minute expectation,
  almost entirely wall-clock suspension between tool calls rather than slow work. Flagged per the
  routine's own instruction.
- The 09:30 guard did not fire; Kevin received no false alarm.
- Two contracted Slack messages stand: the 09:00 CEO brief (no failure logged) and this routine's
  DM. No script, no agent and no other job sent Kevin a separate message today.
- No Drive-BROKEN DM was sent even though the morning probe said BROKEN, because a re-check at
  10:14 returned HEALTHY on both the API and the vault. A DM saying Drive was down would have been
  wrong by the time he read it. The two-day gap it caused is on the BROKEN line instead.
- Every phase ran. Nothing was skipped except phase 3, which was not due and says so.
- `~/knowledge-os/logs/daily-ops-progress.json` deleted at close so tomorrow starts fresh.
