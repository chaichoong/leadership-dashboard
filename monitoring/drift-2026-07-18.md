# Drift Report — 2026-07-18

## Summary
- **Schema changes:** 0 new, 0 removed, 0 type changes (vs 2026-07-17)
- **Field references:** 0 dead, 0 orphaned, 54 rogue (chronic, all resolve live)
- **SOP drift:** 1 stale, 1 undocumented, 11 version gaps — all chronic, all already tracked
- **Health checks:** 5 pass, 4 N/A (auth-gated browser checks)
- **Auto-fixed:** 0 items (no PR — nothing new and safe to auto-correct)
- **Human review:** 0 new tasks (3 existing open Drift Monitor tasks cover today's findings)

**Verdict: clean run. No new drift since 2026-07-17.** The base schema is byte-identical
across the day, no code reference is dead, and the only code change since the last cycle
(commit `28edb5d`, the Skills-tab 422 fix) touched a page with no SOP, so it opened no new gap.

---

## 1. Schema Changes

Compared today's snapshot (`schema-2026-07-18.json`) against yesterday's (`schema-2026-07-17.json`).

- 117 tables, 4,865 fields — unchanged.
- New tables: none. Removed tables: none.
- New fields: none. Removed fields: none.
- Type changes: none. Renames: none.

No baseline drift to report.

## 2. Field Reference Issues

Verified all 399 field IDs, 33 table IDs, and 20 record IDs in `monitoring/reference-map.json`
against the live schema.

- **DEAD:** 0 — every code-referenced field and table still exists in Airtable.
- **ORPHAN:** 0 — reference-map is byte-for-byte in sync with `js/config.js`
  (fields 399/399, tables 33/33, records 20/20, zero diff either direction). No config
  constant points at a field that has left the base, and no live-referenced field is missing
  from config.
- **ROGUE:** 54 — field IDs hardcoded in feature `js/*.js` files rather than routed through
  `config.js`. **All 54 resolve to a live field in today's schema (0 dead).** This is the
  chronic multi-file pattern reported every cycle. Count fell from 60 → 54 today: commit
  `28edb5d` (Skills tab 422 fix — "settings GET must not carry fields[]") removed six field
  IDs from `js/skills-data.js` when it stopped sending an explicit `fields[]` list.

  Distribution of the 54: `dashboard.js` 23, `skills-data.js` 18, `sitemap.js` 6,
  `arrears.js` 6, `fintable.js` 3, `costs.js` 3, `transactions.js` 2, `reconciliation.js` 2,
  `pnl.js` 2, `wealth.js` 1, `wealth-ratios.js` 1, `cashflow.js` 1 (an ID can span files).

  **Not auto-fixed — deliberate, matching prior policy.** Moving 54 live IDs into `config.js`
  is a large refactor of shared feature code, not a mechanical correction. It carries real
  regression risk on money-path files (dashboard, reconciliation, pnl, costs) and belongs in a
  reviewed change, not an unattended drift PR. Left as a standing known item.

  Note: 203 `fld` IDs also appear in `*.html` files (standalone/iframe pages such as the OS
  pages and Skills). Those are outside CHECK 2's `js/*.js` scope and are the standalone-page
  architecture, not new drift.

## 3. SOP Drift

All 11 SOP files present on disk. Version gaps are compared as version tuples, not floats
(`2.42` is ahead of `2.9`).

### STALE_SOP: 1 — `sop.html` §12 "Reconciliation Matching Logic" (chronic, tracked)

Unchanged from prior cycles and still accurate as a finding. The SOP still documents a
33% amount tolerance and a 5-day look-back/look-ahead date window; the code enforces a 10%
tolerance (`COST_MATCH_TOLERANCE = 0.10` at [js/reconciliation.js:498](js/reconciliation.js:498))
and has no date window. On a genuine supplier price rise the SOP implies the match still lands;
the code returns blank and leaves it for a human — a real behavioural difference on money
attribution.

- `sop.html` last touched 2026-07-09 (`55f59b9`).
- `js/reconciliation.js` last touched 2026-07-16 (`5ed9137`), after a multi-commit rewrite.

Already escalated: open Airtable task **recbnIfmyoN3Idzgw** (status "To do").

### UNDOCUMENTED: 1 — the engine that replaced §12 (chronic, tracked)

`sop.html` §12 still does not mention the mechanism now in force: learned historical patterns,
reference-token matching, closest-amount picking, ACTIVE-costs-only gating, the Knowledge Base
rule layer, or the auto-approve near-certain tier. Same open task as above.

### VERSION_GAP: 11 of 11 (chronic, count unchanged)

| Page | pageVer | sopVer | SOP file | Moved today? |
|------|---------|--------|----------|--------------|
| overview | 2.42 | 2.9 | sop.html | no |
| os-strategy | 1.16 | 1.0 | os/strategy/sop.html | no |
| tasks | 1.112 | 1.1 | os/tasks/sop.html | no |
| cfv | 1.30 | 1.6 | sop-cfvs.html | no |
| invoices | 2.18 | 2.2 | sop-invoices.html | no |
| pnl | 2.25 | 2.12 | sop-pnl.html | no |
| comms | 2.53 | 2.4 | inbound-comms-sop.html | no |
| compliance | 1.11 | 1.1 | sop-compliance.html | no |
| os-bplan | 1.15 | 1.3 | os/business-plan-builder/sop.html | no |
| prospecting | 1.4 | 1.3 | sop-prospecting.html | no |
| sitemap | 1.17 | 1.2 | sop-sitemap.html | no |

No pageVer moved today (the only code commit, `28edb5d`, was on the Skills page, which has no
SOP). Gaps are the chronic independent-versioning pattern.

## 4. Health Check Results

| # | Check | Result |
|---|-------|--------|
| 4a | JS syntax — all 27 `js/*.js` parse clean (`node --check`) | **PASS** |
| 4b | Live deploy `index.html` | **PASS** (HTTP 200) |
| 4c | Live deploy `js/config.js` | **PASS** (HTTP 200) |
| 4d | Airtable API auth (PAT valid, base reachable) | **PASS** (HTTP 200) |
| 4e | Model-ID guard (`scripts/check-model-ids.sh`) | **PASS** (exit 0) |
| — | Badge count consistency | N/A — needs an authenticated browser session |
| — | `window.PAT` health | N/A — same |
| — | Stale-data timestamp | N/A — same |
| — | localStorage dismissal integrity | N/A — same |

The four browser checks are auth-gated (the dashboard requires an interactive Airtable-token
login) and cannot run in a headless scheduled context. Standing limitation, unchanged — not a
new failure.

## 5. Actions Taken

### Auto-fixed
None. No dead references to comment out, no new rogue IDs, no simple renames. The 54 chronic
rogue IDs are intentionally left for a reviewed refactor (see §2).

### Escalated to Human Review
No new tasks created. Today's findings are all chronic and already tracked by three open
Drift Monitor tasks in Airtable — creating duplicates would be noise:

- **recbnIfmyoN3Idzgw** — [WARNING] sop.html §12 reconciliation matching rules stale (covers
  today's STALE_SOP + UNDOCUMENTED). Status: To do.
- **rec9ihFqDDSY8DMLZ** — [WARNING] CFV detection SOP behind code (2026-07-03). Status: To do.
- **recesmr9BNH2curGY** — [INFO] Document Task Activity audit trail in os/tasks/sop.html.
  Status: To do.

### Note on repo state
The main worktree had uncommitted edits from another session at run start
(`index.html`, `js/dashboard.js`, `js/shared.js`, `tests/sync-invariants/*`). This run touched
only `monitoring/` and left those edits untouched. Rogue and health checks were run against both
the working tree and committed `HEAD` — both give 54 rogue, confirming the uncommitted edits do
not affect the drift figures.
