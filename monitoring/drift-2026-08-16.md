# Drift Report — 2026-08-16

Run as phase 3 of `daily-ops`. Queue lock NOT taken (Rule 1). Read-only with respect to
code: no repo file edited, no `git add`, no commit, no branch, no push, no PR. Every code
change is filed to the findings queue instead. Report left uncommitted for phase 8.

## Summary

- **Schema changes:** 1 new field, 0 removed, 0 type changes, 1 rename (119 tables) — both already matched in code
- **Field references:** 0 dead, 0 unresolvable `fld`/`tbl` IDs anywhere in the repo, 327 rogue IDs across the repo
- **SOP drift:** see section 3
- **Health checks:** 4a PASS (with one new defect found), 4b PASS, 4c PASS, 4d PASS on the date half / VACUOUS on the sentinel half
- **Auto-fixed:** 0 — prohibited by Rule 2 and by the caller override
- **Filed to the findings queue:** 25 (`20260816-drift-157` … `-181`) — 11 high, 10 medium, 4 low
- **Airtable tasks created:** 0 — the findings queue is the route, per Rule 2

---

## 1. Schema Changes

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat` (the MCP connector is still the broken one).
The token was never printed. Snapshot saved to `monitoring/schema-2026-08-16.json`.

Compared against `schema-2026-08-14.json` (the last run; no run on 15 Aug). 119 tables both
sides, none added or removed. Two changes, both in **Income Buckets** (`tbldMPjXTu7ho5f0T`):

| Change | Field | Detail |
|---|---|---|
| NEW FIELD | `Start Date` (`fldo5P3NSnyKtrx7t`), type `date` | |
| RENAME | `fld50s2fcXr4vEiVy` | `Balance` → `Opening Balance` |

**Neither is drift.** Both are already carried in `js/config.js:529-536`, which references the
new field as `start` and documents the rename in a comment dated 2026-08-14. They arrived with
the income-buckets balance fix (#86, commit `46ec4de`). No code reads either field by name, so
the rename cannot bite the Chart-of-Accounts name-coupling trap.

0 field types changed anywhere in the base.

---

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (in `reference-map.json`, absent from schema) | **0 of 422** |
| DEAD tables | **0 of 35** |
| TYPE_MISMATCH | **still cannot fire** — see below |
| Files scanned | 249 (`.js`, `.mjs`, `.html`, `.py`; excluding `node_modules`, `.git`, `monitoring/`, `test-results/`, `playwright-report/`, `.claude`) |
| Distinct Airtable-shaped IDs in the repo | 856 |
| IDs inside `js/config.js` | 529 |
| Rogue (used outside `config.js`, absent from it) | 327 |
| Unresolvable `fld`/`tbl` IDs | **0** |

Every `fld` and `tbl` ID in the repo resolves against today's snapshot. Nothing is broken.

### The 69 unresolvable candidates, all accounted for

- **67 `rec`-prefixed** — record IDs are not carried by the schema meta API, so they cannot
  resolve by construction. Six of those are ordinary JavaScript identifiers that match the
  pattern, not Airtable IDs: `recentCorrections`, `reconciledInflows`, `reconForecastKeys`,
  `recordSaveFailure`, `recentlyCompleted`, plus test placeholders (`recTESTTESTTEST01`,
  `recWaitingOnKevin`, `recHumanTeamRow00`, `recXXXXXXXXXXXXXX`).
- **1 `selectedProjectId`** (`js/shared.js:983`) — a variable name.
- **2 genuine select-choice IDs** — `selI6Otekm3JyaBSJ` and `selx5WZMIWgrHrPOj`, both verified
  PRESENT in the raw meta response, which carries choice IDs the compact snapshot drops.

The three dead select-choice IDs in `js/config.js` (`selwuotKAoizHJl6z`, `selZazCz6gUJJ8Pl8`,
`selypOeFtsBePQG1E`) are still absent from the schema and still commented out. No live
reference, so still nothing to file.

### Two known defects in the check itself — both worse today, neither re-filed

- **`20260814-drift-monitor-127` (open, high)** — TYPE_MISMATCH has never been able to fire:
  `reference-map.json` stores `{fieldId: constantName}` with no expected type. Today's "0
  mismatches" again comes from code that has nothing to compare against. The only type check
  that did run is snapshot-vs-snapshot (section 1), which caught 0.
- **`20260814-drift-monitor-128` (open, medium)** — the map is still the 2 Aug generation.
  `js/config.js` now holds **496** `fld`/`tbl` IDs, of which **39 are unmapped** (was 26 on 14
  Aug). The daily "0 dead of 422" line is measured against a denominator now 7.9% short of the
  code, and drifting further each week.

---

## 3. SOP Drift

Focused this run on three pairs not walked in the last five days: **CFVs**
(`sop-cfvs.html` ↔ `js/cfv.js`), **Site Map** (`sop-sitemap.html` ↔ `js/sitemap.js`) and the
brand-new **Chart of Accounts** pair (`sop-coa.html` ↔ `js/coa.js`, shipped yesterday in #89).
Dashboard and Invoices were done 14 Aug; Strategy, Systemisation, Skills, Prospecting, Tasks,
Team and Inbound Comms on 11–13 Aug.

### sop-cfvs.html ↔ js/cfv.js — the worst pair found so far. 11 filed

The SOP header still reads v1.6, 16 Apr 2026. `js/cfv.js` was substantially reworked on
2026-08-11 (`867f337`). Filed individually:

- **`-163` (high)** — auto-return **PATCHes Airtable on every dashboard load**, after every
  reconciliation, and when the AI assistant gathers context. The SOP says it only runs on the
  CFV tab.
- **`-164` (high)** — dismissals do **not** expire after 25 days (stated four times). They
  expire at the next due day + 2. No 25-day constant exists in the file.
- **`-165` (high)** — Days Overdue comes from the last reconciled payment with a 5-day
  early-pay credit, not the documented `cfv_{id}_startDate` counter. Nothing writes that key
  any more; three places delete it.
- **`-166` (high)** — "Not a CFV" **does** write to Airtable (a tagged `[CFV-DISMISSED:date]`
  comment), and those comments are read back to restore dismissals across devices. The SOP's
  lifecycle table says "No — localStorage only".
- **`-167` (high)** — an undocumented Rent Statements block renders on the same tab and offers
  transaction edits and tenancy reassignment that PATCH Airtable, which changes what CFV
  detection treats as paid.
- **`-168` (high)** — the SOP sends the operator to a "Cash Flow Voids" sidebar item that does
  not exist; the tab opens from the top tab bar and the badges hang off Leadership Dashboard.
  Same class of error as the invoices one filed on 14 Aug.
- **`-169` (high)** — the maintenance warning names the wrong failure mode: the PATCH sends
  status **names**, so a renamed option breaks it, not a changed option ID.
- **`-170` (medium)** — Path B has no "due day + 2" gate at all; an unpaid previous cycle
  flags a Potential CFV **before** this month's due day. The in-code comment about 5-day and
  14-day tolerances is itself stale.
- **`-171` (medium)** — "a transaction in the current calendar month" is a fallback used only
  when `arrears.js` fails to load. Plus an undocumented guard: no reconciled transaction at
  all = unpaid, whatever the dates.
- **`-172` (medium)** — the required-scope list omits `data.recordComments:write`, and the
  comment POST fails **silently** (`console.warn` only), so the button says Done and no audit
  trail is written.
- **`-173` (low)** — a further twelve documentation gaps rolled into one revision pass: the
  Chase column and all of §9 are computed but never rendered, the Last Payment column and the
  sync bar with its six health checks are undocumented, all five status actions (not one) bust
  the cache and reload, the per-row comment fetch fans out one GET per visible row behind a
  4-concurrent limiter, and two summary-card scopes are stated wrongly.

Verified still accurate and **not** raised: every Airtable base, table, field and select-option
ID in the SOP; `CFV_TOLERANCE_DAYS = 2`; payment matching on the `txTenancy` link with
`txReconciled` only; Potential CFVs never written to Airtable until confirmed; all four re-flag
conditions; all seven automatic comment texts verbatim; the auto-return sequence including the
retry-on-failure flag clear; the five summary cards; table sort order; the 0-3/4-7/8+ colour
thresholds; the contact-column fallbacks; the action buttons per status; confirmation gating on
every status change; the comments panel behaviour; the sidebar badge maths; and the `#cfv`
direct link.

### sop-sitemap.html ↔ js/sitemap.js — 5 filed, one of them a live trust failure

The SOP is dated 12 April 2026 and has not been revised since.

- **`-158` (high)** — **"Update All Out-of-Sync SOPs" queues work nothing processes.** The
  button writes to the SOP Update Requests table and toasts "will be processed automatically".
  The consumer has been PAUSED in its own frontmatter since 30 Jul, is `enabled: false` in
  `scripts/job-schedule.json:165`, and is not one of the nine `daily-ops` phases. Queried the
  table live today: 19 rows, all Completed, newest request **16 Apr 2026**. The page then shows
  a green "Update Requested — Processing (13 SOPs)" for 24 hours, so the failure looks like a
  success and then clears itself.
- **`-159` (high)** — §5 documents an integrity check that does not exist (HTTP 200 fetch per
  page, title verification, pass/warn/fail grid). The real page compares declared versions and
  calls the GitHub commits API.
- **`-160` (high)** — the registry table in the SOP lists 12 pages and nine rows with April
  versions. `PAGE_REGISTRY` holds 28, and the page renders the list itself.
- **`-161` (medium)** — the page stores a **GitHub personal access token** in `localStorage`
  via a raw `prompt()` that pre-fills the existing token in clear text. The SOP never mentions
  a credential is held in the browser.
- **`-162` (low)** — §§6-7 tell the operator to bump versions by hand; `pageVer` is auto-bumped
  by a GitHub Action and only `sopVer` is manual.

### sop-pnl.html ↔ js/pnl.js — 7 filed, and three of them are code bugs not doc drift

The SOP header says v2.12, 17 Apr 2026. `PAGE_REGISTRY` claims `sopVer` 2.26.

- **`-174` (high, CODE)** — the two inverted cost cards print the **wrong word**. `pnlKpiCard`
  takes the indicator wording from `good`, not from the sign of `diff` (`js/pnl.js:617`,
  `:622`), so maintenance at £1,500 against a £1,000 budget renders a red **"▼ £500 below
  budget"** when it is £500 **over**, and being under budget renders "above budget". I read
  both lines to confirm. The colour is correct; only the sentence lies.
- **`-175` (high, CODE)** — the P&L **never re-renders when fresh data lands**. `renderPnL` is
  called from exactly one place outside `pnl.js` (the tab switch). The 15-minute refresh
  replaces `allTransactions` and redraws only the overview. Sitting on the P&L while a refresh
  completes leaves every figure stale with no cue. A cold open can also build the whole report
  from a 24-hour-old IndexedDB snapshot and then never update it.
- **`-176` (high, CODE)** — the drill-down modal sums the **raw** `Report Amount` while the
  grid stores expenses sign-flipped, so clicking a cost cell showing `£500` opens a modal
  headed `Sum: -£500.00`.
- **`-177` (medium, CODE)** — `_pnlAiLoading = true` is set **before** the panel lookup, and
  that early `return` sits outside the `try/finally` that clears it, so a missing panel jams
  Generate Analysis silently for the life of the page. Read and confirmed.
- **`-178` (medium)** — the newest month in every window is a **part-month**, divided by a full
  month and compared against a full monthly target. Undocumented.
- **`-179` (medium)** — the SOP is fourteen versions behind what the registry advertises, and
  the registry figure is fed to the AI assistant with an `inSync` flag.
- **`-180` (low)** — fourteen further gaps in one revision pass, including the missing warning
  that renaming any of the 27 allow-listed sub-category strings in Airtable drops that P&L row
  to zero with no error.

Verified still accurate and **not** raised: all 27 allow-listed sub-category names character
for character and in grid order; all six targets and both budget fallbacks; that reconciliation
is not a filter; the inclusion test; period options and default; deactivated-business handling;
the four drill scopes; the inline-edit PATCH shape including `returnFieldsByFieldId`; every
Airtable ID quoted in §10; the nine KPI cards and both budget formulas; grid formatting and
sticky behaviour; all four charts down to `borderDash` and `cutout`; the AI context payload,
`max_tokens`, prompt structure and proxy URL; and that `airtableFetch` paginates fully, so the
P&L genuinely sees every transaction.

### sop-coa.html ↔ js/coa.js — clean, nothing filed

Shipped together yesterday and they match. Spot-checked and correct: all six health-check names
verbatim (`js/coa.js:643-711`); the 8-second Undo toast (`:632`); Needs/Wants as the only Money
Group values (`:45`, `:361`); alphabetical `localeCompare` sort (`:170`); the delete gate
requiring both zero links and no name/ID dependency (`:210`, `:380-410`); and the rename lock
rationale. This is what a same-commit SOP update buys.

### VERSION_GAP

24 of 28 registry entries have `pageVer` ahead of `sopVer`; only **13** are real pairs, the
other 11 have an empty `sopFile` and no SOP that could be behind. Unchanged in character from
14 Aug except that the sidebar badge presents the inflated 24 as actionable — filed as `-157`.

---

## 4. Health Check Results

Run against the live site (`https://chaichoong.github.io/leadership-dashboard/`) in Kevin's
authenticated Chrome. No localStorage value was read, logged or returned — only key names,
counts and booleans.

| Check | Result | Evidence |
|---|---|---|
| **4a** Badge consistency | **PASS for CFVs, FAIL for Site Map** | CFVs: recounted from `detectCFVs()` through the same `cfvIsVisible` filter the render path uses — 5 raw, 5 visible, split 2 `cfv` / 3 `cfv actioned`; rendered badge is red `2`, orange `3`, both visible. Exact match, third day running. Site Map: badge reads red `24`; recounted from `PAGE_REGISTRY` in the page, only **13** of those 24 have an SOP file at all. Filed. |
| **4b** Token/auth health | **PASS** | A PAT is present and non-empty. Value not read. |
| **4c** Stale data | **PASS** | 8,983 transactions, 94 tenancies, 180 costs loaded. `#lastUpdated` 07:14 against a local wall clock of 07:14:22. |
| **4d** Dismissal-key integrity | **PASS on the date half, VACUOUS on the sentinel half** | 110 `cfv_*` keys checked by shape. Date-shaped: 110 checked, 110 parse as valid Dates, 0 bad. Sentinel-shaped (`*_returned`, `*_returnDismissed`): **0 keys exist**, so that half asserted nothing. |

The vacuous sentinel half is legitimate — no tenancy is in the returned state, same as the
last five runs. `20260809-drift-030` already covers rewriting CHECK 4.

The machine is still on `Europe/Paris` (UTC+2), so `#lastUpdated` 07:14 local is 06:14 London.

---

## 5. Actions Taken

### Auto-fixed

**None, and none attempted.** Rule 2 of the SKILL and the caller's override both prohibit this
phase from touching code. No file was edited, nothing staged, committed, branched or pushed.
This report is left uncommitted in `monitoring/` for phase 8.

### Filed to the findings queue — 25

| ID | Sev | Item |
|---|---|---|
| `20260816-drift-157` | medium | Site Map badge shows red 24 when only 13 are actionable |
| `-158` | high | "Update All Out-of-Sync SOPs" queues work nothing processes, and says it is processing |
| `-159` | high | sop-sitemap.html documents an integrity check that does not exist |
| `-160` | high | sop-sitemap.html registry four months stale: 12 pages listed, 28 in code |
| `-161` | medium | Site Map stores a GitHub PAT in localStorage; SOP never mentions it |
| `-162` | low | sop-sitemap.html tells the operator to bump versions a GitHub Action owns |
| `-163` | high | CFV auto-return PATCHes Airtable on every dashboard load, not just the CFV tab |
| `-164` | high | CFV dismissals do not expire after 25 days (stated four times) |
| `-165` | high | Days Overdue no longer uses the documented localStorage counter |
| `-166` | high | "Not a CFV" does write to Airtable, and dismissals restore from comments |
| `-167` | high | Undocumented Rent Statements block on the CFV tab writes to Transactions |
| `-168` | high | CFV SOP sends the operator to a sidebar item that does not exist |
| `-169` | high | CFV maintenance warning names the wrong failure mode (IDs vs names) |
| `-170` | medium | CFV detection has no "due day + 2" gate; Path B documentation is wrong |
| `-171` | medium | "Current calendar month" is a fallback path, not the payment rule |
| `-172` | medium | CFV scope list omits recordComments:write, and the failure is silent |
| `-173` | low | sop-cfvs.html revision pass: 12 further gaps |
| `-174` | high | **CODE** — over-budget cost cards print "below budget" |
| `-175` | high | **CODE** — P&L never re-renders when a background refresh lands |
| `-176` | high | **CODE** — drill-down modal sign is the opposite of the grid cell |
| `-177` | medium | **CODE** — failed AI panel lookup jams Generate Analysis permanently |
| `-178` | medium | Newest month in every P&L window is a part-month; undocumented |
| `-179` | medium | P&L SOP fourteen versions behind what the registry advertises |
| `-180` | low | sop-pnl.html revision pass: 14 further gaps |
| `-181` | medium | No drift report has reached git since 6 Aug; `.gitignore` excludes them |

11 high, 10 medium, 4 low. Four of them (`-174` … `-177`) are defects in shipped JavaScript
rather than documentation drift, which is the first time this phase has produced that in a
week — a consequence of walking two pairs whose SOPs are detailed enough to test the code
against.

### Escalated to Human Review

**None.** Per Rule 2 the findings queue is the route; no Airtable task was created, so no
single-select value was written and the "15 mins" class of accident could not recur.

### Not done

- **`monitoring/reference-map.json` was not regenerated.** It is now 39 IDs short of
  `js/config.js` (was 26 on 14 Aug). Regenerating it is a repo write, so it stays with the
  open finding `20260814-drift-monitor-128` rather than being done here.
- **This report is not in git and will not be.** `monitoring/.gitignore:3-4` ignores both
  `drift-2*.md` and `schema-2*.json`. The last drift report actually committed is **6 Aug**
  (`627c5cb`), ten days ago, even though the SKILL says phase 8 commits every one and the
  audit trail lands daily. Filed as `-181`. Until that is resolved, these reports exist only
  on this Mac.
- **Pairs not walked this run:** Dashboard and Invoices (done 14 Aug), Strategy, Systemisation,
  Skills, Prospecting, Tasks, Team, Inbound Comms (11–13 Aug), Compliance (not walked since
  before 11 Aug — the oldest uncovered pair, and the next one due).
