# Drift Report — 2026-08-21

Run as phase 3 of `daily-ops`. Queue lock NOT taken (Rule 1). Read-only with respect to code
(Rule 2, reinforced by the caller's override): no repo file edited, no `git add`, commit,
branch, push or PR. Seven code findings filed to the queue. This report is written to
`monitoring/` and left uncommitted for phase 8.

## Summary

- **Schema changes:** 0 new, 0 removed, 0 type changes, 0 renames — 119 tables, 4,917 fields, identical to the 20 Aug snapshot
- **Field references:** 0 dead of 422 mapped fields, 0 dead of 35 mapped tables, **0 unresolvable `fld`/`tbl` IDs across the whole repo** (255 files scanned, 864 distinct IDs, 334 rogue), 35 orphaned config constants
- **SOP drift:** `os/strategy/sop.html` ↔ `os/strategy/strategy.js` audited — the largest version gap in `PAGE_REGISTRY` and the oldest SOP file on disk. 3 medium + 1 low filed. A fifth item found via CHECK 4 against `sop-cfvs.html`
- **Health checks:** **CHECK 4 RAN FOR REAL today** — 3 PASS, 1 PARTIAL, 0 FAIL. Yesterday's run skipped it; Kevin's own Chrome has an authenticated session, so no token was entered and nothing was vacuous except one documented branch
- **Auto-fixed:** 0 — prohibited
- **Filed to the findings queue:** 8 (`20260821-drift-273` … `-280`) — 4 medium, 4 low
- **Airtable tasks created:** 0 — the findings queue is the route

---

## 1. Schema Changes

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat`. The token was never printed. Snapshot saved to
`monitoring/schema-2026-08-21.json`.

| Compared against | Tables | Fields added | Removed | Type changes | Renames |
|---|---|---|---|---|---|
| `schema-2026-08-20.json` | 119 = 119 | 0 | 0 | 0 | 0 |

The base has not moved since 16 Aug. Nothing to reconcile.

---

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (in `reference-map.json`, absent from schema) | **0 of 422** |
| DEAD tables | **0 of 35** |
| TYPE_MISMATCH | **still cannot fire** — the map stores no expected types (already filed, `20260814-drift-monitor-127`; not refiled). Proxy used instead: 0 type changes against yesterday's snapshot |
| Rogue IDs (used outside `js/config.js`, absent from it) | 334 unique — 244 `fld`, 15 `tbl`, 71 `rec`, 4 `sel` |
| **Unresolvable `fld`/`tbl` IDs** | **0 of 259** |
| `rec`/`sel` candidates that cannot resolve by construction | 75 |
| ORPHAN constants in `config.js` | 35 of 398 pairs |

Scan covered 255 files across `.js`, `.mjs`, `.html`, `.py`, excluding `node_modules`, `.git`,
`monitoring/`, `test-results/`, `playwright-report/` and `.claude/`. Every `fld` and `tbl` ID in
the repo resolves against today's snapshot. Nothing is broken.

The 75 non-resolving `rec`/`sel` candidates are the same three populations recorded on 20 Aug —
real record IDs used as fixtures or pinned rows, deliberate placeholders (`recXXXXXXXXXXXXXX`,
`recTESTTESTTEST01`, `recHumanTeamRow00`), and ordinary identifiers whose first three letters
collide with the prefix (`recentCorrections`, `reconciledInflows`, `recordSaveFailure`,
`selectedProjectId`, `selectRecordAsync`). The meta API carries no record IDs, so none can ever
resolve. Expected, not investigated again.

**The orphan count moved from 42 to 35 with no code change** — filed as `20260821-drift-279`.
Since the 20 Aug run the only commits are `scripts/`, `workers/` and `.claude/` files
(`f928a04`, 20 Aug 19:18); nothing under `js/` moved. Today's method: both `name: 'fld…'` and
`const NAME = 'fld…'` forms, 398 pairs, whole repo minus `js/config.js`, tests included. A
metric that swings 17% on an unchanged codebase cannot detect a real change, which is the only
reason to keep it.

---

## 3. SOP Drift — `os/strategy/sop.html` ↔ `os/strategy/strategy.js`

Chosen on evidence, not rotation. Version gaps across `PAGE_REGISTRY`, worst first:

| Page | pageVer | sopVer | Gap |
|---|---|---|---|
| tasks | 1.131 | 1.3 | 128 |
| **overview** | 2.65 | 2.9 | 56 |
| **os-strategy** | **1.37** | **1.0** | **37** |
| cfv | 1.33 | 1.6 | 27 |
| sitemap | 1.19 | 1.2 | 17 |
| prospecting | 1.14 | 1.4 | 10 |
| systemisation / skills | 1.6 | 1.0 | 6 |
| invoices | 2.22 | 2.18 | 4 |
| pnl / comms / compliance / crm | — | — | 1 each |

`os-strategy` wins on a second measure the others do not: its SOP has never been revised at all.
The file is dated April 2026 and its mtime is 30 Apr 2026, against a 4,087-line implementation.

**What is still correct.** The Quick Reference table ID `tblEBvFw8DonwxzGh`, the
Business × Quarter × Year grain, the `js/prompts/boardroom-mentor.js` prompt path, the field map
for Objective, Targets, Undertakings 1–20, USP 1–5, Method Steps 1–10, the 3×3 stepping-stone
grid, and the quarterly review loop in section 5.

**What has drifted** (4 findings filed):

1. `20260821-drift-273` (medium) — six whole features shipped since the SOP was written and none
   is mentioned: quarter close (`openQuarterClose` :2354 → `executeQuarterClose` :2566), PDF
   export (`exportPlanToPDF` :3838), the AI push proposal and approval modal
   (`buildPushProposal` :1668, `showPushApprovalModal` :1905, `executePush` :2004) including
   task extraction from stepping stones (:1858), wizard session persistence and reset (:2762–:2891),
   the Main Methods linked-table workflow (:838–:908), and the KPI subsection (:966). Keyword
   search of the SOP returns zero for "quarter close", "Export PDF", "PDF", "approval", "KPI",
   "Workflow" and "sync".
2. `20260821-drift-274` (medium) — the SOP names **Claude Haiku 4.5** as the wizard model in two
   places. All six AI call sites in `strategy.js` pass `AI_MODEL_DEFAULT` (:1883, :3312, :3427,
   :3467, :3522, :3703). Wrong figure for anyone reasoning about AI spend, and a hardcoded model
   ID in documentation.
3. `20260821-drift-275` (medium) — `os/strategy/index.html:31` tells the user "Token is stored in
   your browser session only". `authenticate()` (:26-38) writes it to `localStorage._dlr_pat`
   **and** `localStorage.airtable_pat` as well as `sessionStorage`. Verified live: the
   localStorage copy is present and non-empty while the sessionStorage copy is empty, which is
   exactly the persistent copy outliving a session. That sentence is the one a user relies on
   before typing a live token on a shared machine.
4. `20260821-drift-277` (low) — section 4 documents three wizard controls and describes "Skip" as
   leaving the field empty. There are four buttons, the middle one reads "Move on", and
   `wizSkip()` (:3543) clears nothing, so the existing value survives. "Back" (:3556) is
   undocumented.

**Fifth item, found via CHECK 4** — `20260821-drift-276` (medium): `sop-cfvs.html` :368 and :607
say the days-overdue figure comes from `cfv_{id}_startDate`. No `setItem` for that key exists
anywhere in the repo; `js/cfv.js` only ever deletes it (:246, :821, :935). The live source is
`cfv_{id}_chaseStart` (:333 read, :337 write). Rolls into the pending `sop-cfvs` revision,
`20260816-drift-173`.

---

## 4. Health Check Results

Run against the deployed site in Kevin's own authenticated Chrome. **No token was entered or
read** — only presence was asserted. No `localStorage` values were printed.

| Check | Result | Evidence |
|---|---|---|
| **4a Badge consistency** | **PASS** | `detectCFVs()` returned 5 entries; all 5 survived `cfvIsVisible`; recount 3 red / 2 orange; rendered `#cfvSidebarBadges` showed **3** and **2**. Non-vacuous — 5 entries, split 2 `cfv` / 1 `potential` / 2 `cfv actioned` |
| **4b Token/auth health** | **PASS** | `_dlr_pat` present and non-empty in localStorage. Value never read |
| **4c Stale data** | **PASS** | IndexedDB `_dlr_dashcache_v2` `savedAt` was **1 minute** old, well inside the 30-minute threshold. `allTransactions` 9,049 / `allTenancies` 94 / `allCosts` 180 / `allBusinesses` 5, read lexically, not off `window` |
| **4d Dismissal-key integrity** | **PARTIAL** | Date shape: **109 checked, 0 invalid — PASS**. Sentinel shape: **0 checked — VACUOUS, not a pass** |

Console: no errors or exceptions on the tab.

**4d detail.** 109 `cfv_*` keys in four shapes: `_chaseStart` 54, `_startDate` 46,
`cfv_dismissed_*` 4, `cfv_reflag_dismissed_*` 5. Two problems, both filed as
`20260821-drift-278`:

- `cfv_*_startDate` is not in the SKILL's key catalogue, so 46 keys (42% of the total) were
  graded by an else-branch never written for them. They happen to hold ISO dates so they passed,
  which is passing for the wrong reason.
- No `_returned` or `_returnDismissed` key exists at all, so the sentinel assertion checked
  nothing. Those keys are written at `js/cfv.js` :240, :784 and :941, so the returned-payment
  path has apparently never run in this browser. That is the expected state, recorded here so a
  future run does not read a zero as a new defect.

Yesterday's run skipped CHECK 4 entirely on the grounds that a clean browser profile has no PAT.
Kevin's own Chrome does, which makes every assertion above real without entering a token.

---

## 5. Actions Taken

### Auto-fixed
None. Prohibited by Rule 2 and by the caller's override for this run.

### Filed to the findings queue

| ID | Sev | Item |
|---|---|---|
| `20260821-drift-273` | medium | Strategy SOP v1.0 vs pageVer 1.37 — six features undocumented |
| `20260821-drift-274` | medium | Strategy SOP hardcodes "Claude Haiku 4.5"; code uses `AI_MODEL_DEFAULT` |
| `20260821-drift-275` | medium | Strategy page claims the PAT is session-only; it persists in localStorage |
| `20260821-drift-276` | medium | CFV SOP points days-overdue at a key nothing writes |
| `20260821-drift-277` | low | Wizard controls: "Skip" mis-described, "Back" undocumented |
| `20260821-drift-278` | low | CHECK 4d key catalogue stale; sentinel branch vacuous |
| `20260821-drift-279` | low | Orphan-constant metric not reproducible run to run |
| `20260821-drift-280` | low | Drift reports are gitignored — phase 8 cannot commit them, audit trail stopped 6 Aug |

### Escalated to Airtable
None. The findings queue is the route.

### Not done
- `reference-map.json` not regenerated — `js/config.js` has not changed since the last run, so
  the map is current and rewriting it would be a no-op commit for phase 8 to carry.
- No `git pull` — the working tree holds another routine's uncommitted reports and the caller
  barred repo writes.

### Note on where this report lands

`monitoring/.gitignore` ignores `drift-2*.md` and `schema-2*.json`, so this file and today's
snapshot are local to this Mac. 81 older drift and schema files are tracked, and the last one
committed is `monitoring/drift-2026-08-06.md`. Phase 8 cannot add an ignored path, so the daily
audit trail the SKILL describes has not existed since 8 Aug. Filed as `20260821-drift-280`.
