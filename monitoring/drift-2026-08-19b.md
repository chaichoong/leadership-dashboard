# Drift Report — 2026-08-19 (second run, 15:26 BST)

Second drift pass of the day. The first ran at 12:17 and is at `monitoring/drift-2026-08-19.md`;
that file is left untouched and this one is written alongside it. Queue lock NOT taken (Rule 1).
Read-only with respect to code: no repo file edited, no `git add`, `pull`, commit, branch or push.
Both code findings filed to the queue. Report left uncommitted for phase 8.

## Summary

- **Schema changes:** 0 new, 0 removed, 0 type changes, 0 renames (119 tables, 4,917 fields) — identical to both the 12:17 snapshot and 18 Aug
- **Field references:** 0 dead of 422 mapped fields, 0 dead of 35 mapped tables, **0 unresolvable `fld`/`tbl` IDs across the whole repo**, 334 rogue IDs, 35 orphaned config constants
- **SOP drift:** `inbound-comms-sop.html` ↔ `follow-up.html` audited (least-covered pair in the mapping table) — 2 medium filed
- **Health checks:** CHECK 4 SKIPPED — `preview_start` refuses in an unattended session; no browser
- **Auto-fixed:** 0 — prohibited by Rule 2 and by the caller override
- **Filed to the findings queue:** 3 (`20260819-drift-monitor-248`, `-249`, `-250`) — 1 high, 2 medium
- **Airtable tasks created:** 0 — the findings queue is the route

---

## 1. Schema Changes

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat`. The token was never printed. Snapshot saved to
`monitoring/schema-2026-08-19b.json`.

| Compared against | Tables | Fields added | Removed | Type changes | Renames |
|---|---|---|---|---|---|
| `schema-2026-08-19.json` (12:17 today) | 119 = 119 | 0 | 0 | 0 | 0 |
| `schema-2026-08-18.json` | 119 = 119 | 0 | 0 | 0 | 0 |

The base has not moved since 16 Aug. Nothing to reconcile.

---

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (in `reference-map.json`, absent from schema) | **0 of 422** |
| DEAD tables | **0 of 35** |
| TYPE_MISMATCH | **still cannot fire** — the map stores no expected types (already filed, `20260814-drift-monitor-127`) |
| Files scanned containing Airtable-shaped IDs | 112 (`.js`, `.mjs`, `.html`, `.py`; excluding `node_modules`, `.git`, `monitoring/`, `test-results/`, `playwright-report/`, `.claude`) |
| ID occurrences | 3,048 across 864 unique IDs |
| IDs inside `js/config.js` | 530 |
| Rogue (used outside `config.js`, absent from it) | 334 unique |
| **Unresolvable `fld`/`tbl` IDs** | **0** |
| `rec`/`sel` candidates (cannot resolve by construction — the meta API carries no record IDs) | 109 |
| ORPHAN constants in `config.js` (resolve in Airtable, referenced by no other file) | 35 |

Every `fld` and `tbl` ID in the repo resolves against today's snapshot. Nothing is broken.

The 35 orphans are unchanged from the 12:17 run and from 18 Aug. Two of them (`propInclude`,
`bizInclude`) are already filed as `20260818-drift-monitor-195`. The remaining 33 are dormant
constants, not defects, and are not filed individually because that would be queue noise.

One methodology note for whoever reads this next: counting orphans by raw ID rather than by
constant name gives **207**, not 35, because most constants are consumed as `F.name` and their
literal `fld…` string never appears outside `config.js`. The 35 figure is the correct one and
is produced by grepping for the constant *name* across the repo. A future run that reports a
sudden jump to ~200 has changed its method, not found 172 new orphans.

---

## 3. SOP Drift

### Pair selection

`os/tasks/sop.html` was deep-audited this morning (11 findings, `-220`…`-230`) and
`os/strategy/sop.html` was deep-audited on 11 Aug (`20260811-drift-069`…`-076`). Re-auditing
either would have produced duplicates — a spot check of the strategy pair confirmed that every
substantive gap I found there (the Haiku-vs-Sonnet claim, the undocumented Close-the-quarter
flow, the wizard step order, the Skip/Move-on button) was already on the queue from 11 Aug.

Findings-per-SOP across the mapping table: `sop.html` 46, `sop-cfvs.html` 23, `sop-prospecting.html`
17, `sop-sitemap.html` 15, `sop-compliance.html` 14, `os/strategy/sop.html` 14, `sop-invoices.html`
13, `sop-pnl.html` 13, `os/tasks/sop.html` 13, **`inbound-comms-sop.html` 3**. That pair was
audited.

### `inbound-comms-sop.html` ↔ `follow-up.html` — 2 filed

This pair is in better shape than the rest. The SOP and the page were last changed in the *same*
two commits (`588947b`, `30a7009`, 12 Aug — the label 8/12 agent-routing work), the registry gap
is one point (page 2.60, SOP 2.59), and the label routing, Approver mapping, completion-on-filing
warnings and the 5-minute reassignment sweep were all checked against the code and hold.

What has drifted is the AI layer:

- **`-248` (medium)** — the SOP says six times that Respond, Follow Up, Delay Payment and Revise
  are drafted by Claude Sonnet. All four paths pass `window.AI_MODELS.light`. `draftFollowUp()`
  is the single generator behind all three draft buttons (`follow-up.html:6849`), `reviseDraft()`
  is light too (`:6992`), and `draftUnsubscribeReply()` is light at 300 tokens (`:6675`). The
  token counts quoted in the SOP are correct; only the model is wrong. `:668` also hardcodes
  `claude-sonnet-4-6` and `claude-haiku-4-5-20251001` into the document, which is the same defect
  already filed against the strategy SOP as `20260811-drift-076` — a model rotation in
  `js/ai-models.js` would leave two SOPs quoting retired IDs.
- **`-249` (medium)** — the draft modal has a fifth button, **✨ Polish (Sonnet)**
  (`follow-up.html:777`, `polishDraftWithSonnet()` at `:7026`), which is the *only*
  `AI_MODELS.default` call on the page, runs at 800 tokens and warns in its own tooltip that it
  costs ~3× a normal draft. The word "Polish" appears nowhere in the SOP. Combined with `-248`
  the effect is inverted: the operator believes every reply is already premium, so never reaches
  for the one control that would make it so. Its `draft_polished` audit entry is also absent from
  the SOP's audit-log list.

### Checked and found correct

Label table for 4–7, 9, 14 including the completion-on-filing warning; the Approver field ID
`fldLLAG5HQPEFEfE5` and its 8→Mica / 12→Kevin mapping; `LABELS_WITH_COUNTS = ['1','2','3','8','12','13']`;
the 5-minute background reassignment sweep and its audit entries; the batches-of-5 inbox
classification; `generateAirtableTaskContent()` at 300 tokens.

### Version gaps

24 `VERSION_GAP`s in `PAGE_REGISTRY`, unchanged from the 12:17 run — see
`monitoring/drift-2026-08-19.md` §3 for the table. Not re-filed.

---

## 4. Health Check Results

**SKIPPED — no browser.** `preview_start` returned "Dev servers can't be started from unattended
sessions". No server was already listening on any of the four ports in `.claude/launch.json`
(8951, 8955, 8961, 8957) or on the usual 3000/5173/8000/8001/8080. So 4a (badge counts), 4b (PAT
presence), 4c (staleness) and the runtime half of 4d could not run. Session-capability limit, not
a page failure.

The static half of 4d was verified at 12:17 today against an unmodified `js/cfv.js` and is not
repeated here.

---

## 5. Actions Taken

### Auto-fixed

None. Rule 2 forbids it and the caller override repeated the prohibition. No repo file was edited,
staged, committed, branched or pushed. No `git pull` was run.

### Filed to the findings queue

| ID | Severity | Title |
|---|---|---|
| `20260819-drift-monitor-248` | medium | Inbound Comms SOP says Sonnet drafts and revises every reply; all four paths run on `AI_MODELS.light` |
| `20260819-drift-monitor-249` | medium | Undocumented "Polish (Sonnet)" button is the only premium-model control on the page, at ~3× cost |
| `20260819-drift-monitor-250` | **high** | Every drift report since 6 Aug is invisible to git — `monitoring/.gitignore` ignores `drift-2*.md` |

Queue depth after this run: 250 filed.

## 6. The audit trail this routine claims to leave does not exist

Filed as `-250`, and worth stating in the report itself because the report is the evidence.

`monitoring/.gitignore:3-4` ignores `schema-2*.json` and `drift-2*.md`. Its own comment says why
that was safe — *"the scheduled task commits them to main directly"* — because this routine used
to run an explicit `git add`, which overrides an ignore rule. The 6 Aug single-writer change
removed that, and `SKILL.md` now promises phase 8 commits every report and that *"the audit trail
still lands daily"*.

It does not. `git add` on an ignored path is a silent no-op that exits 0.

| | Last file to reach git | Files sitting untracked |
|---|---|---|
| `drift-*.md` | `drift-2026-08-06.md` | 08-08, 08-10 … 08-19 (9) |
| `schema-*.json` | `schema-2026-08-06.json` | same dates |

80 older drift/schema files **are** tracked — they were added before the ignore rule could bite —
which is exactly why the folder looks healthy in the repo and why this ran undetected for 13 days.

The cost is not cosmetic. The schema snapshot is the only baseline a later run can diff against.
A clean clone or a rebuilt Mac cannot detect any Airtable schema change made since 6 Aug, because
the intervening snapshots exist nowhere but this machine's disk. Both patterns sit **above** the
`never-commit:begin` marker, so they are housekeeping ignores, not privacy ones — committing them
breaks no rule.

### Escalated to human review

None. Per Rule 2 the findings queue is the route, so no Airtable task was created and no
single-select value was written.
