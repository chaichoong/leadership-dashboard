# Drift Report — 2026-08-11

Run as phase 3 of `daily-ops`. Queue lock NOT taken (Rule 1). No file in the repo edited,
no `git add`, no commit, no push, no branch, no PR (Rule 2 + caller override). Every code
change is filed to the findings queue instead.

## Summary

- **Schema changes:** 0 new, 0 removed, 0 type changes (119 tables, 4,915 fields)
- **Field references:** 0 dead, 0 unresolvable rogue IDs, 84 config constants unused outside `js/config.js`
- **SOP drift:** 9 filed — 4 high, 5 medium, across `os/strategy/sop.html`, `guides/systemisation.html`, `guides/skills.html`
- **Health checks:** 2 pass, 1 fail, 1 vacuous-in-part
- **Auto-fixed:** 0 (not permitted)
- **Filed to findings queue:** 12
- **Airtable tasks created:** 0 — deliberate, see §5

---

## 1. Schema Changes

None. `monitoring/schema-2026-08-11.json` is byte-equivalent in shape to
`schema-2026-08-10.json`: same 119 tables, same 4,915 fields, no renames, no type changes.

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat` (the MCP connector is still the broken one).

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (in `reference-map.json`, absent from schema) | **0 of 235** |
| DEAD tables | **0 of 18** |
| Rogue IDs that do not resolve against today's schema | **0** |
| Files scanned | 219 (`.js`, `.mjs`, `.html`, `.py`; excluding `node_modules`, `.git`, `monitoring/`, `test-results/`, `playwright-report/`) |
| Distinct Airtable-shaped IDs found | 833 |
| IDs used outside `js/config.js` and absent from it | 319, across 49 files |

Every `fld`/`tbl` ID in the repo resolves against today's schema, so nothing is broken. The
319 hardcoded IDs outside `config.js` are the known pattern first measured on 2026-08-04 —
top offenders unchanged: `os/tasks/index.html` (89), `os/team/index-supabase.html` (84),
`os/tasks/index-supabase.html` (74), `os/tasks/supabase-shim.js` (59),
`scripts/sync-airtable-mirror.py` (37).

Eight of the 93 `rec`/`sel` candidates are ordinary JavaScript identifiers, not Airtable IDs,
and were eyeballed rather than name-filtered as the SKILL requires: `recAlreadyDecided`,
`recWaitingOnKevin`, `recentCorrections`, `recentlyCompleted`, `reconForecastKeys`,
`reconciledInflows`, `recordSaveFailure`, `selectedProjectId`.

**Coverage caveat, unchanged:** `monitoring/reference-map.json` still carries
`summary.generated: 2026-08-02`, so CHECK 2 covers the map, not today's `config.js`. Carried
by `20260809-drift-031`. Not regenerated here because that is an edit to a tracked repo file.

**Low-signal note:** 84 field constants defined in `config.js` are referenced nowhere else in
the repo. Most sit inside field-group objects read dynamically, and 60 of them are unnamed in
the map (`UNNAMED_IN_CONFIG`), so this is not actionable as it stands and no finding was
filed. It would become useful if the reference map recorded the constant path.

## 3. SOP Drift

Audited today (chosen because they were last deep-audited on 4 Aug, while
`sop-pnl`/`sop-cfvs`/`sop-compliance`/`sop-sitemap` were done on 9–10 Aug):

| Pair | Result |
|---|---|
| `os/strategy/sop.html` vs `os/strategy/index.html` + `strategy.js` | 6 findings (2 high, 4 medium) |
| `guides/systemisation.html` vs `os/systemisation/index.html` | 2 findings (2 high) |
| `guides/skills.html` vs `js/skills.js` | 2 findings (2 medium) |

The strategy SOP is stamped "Version 1.0 · Last updated April 2026" (`sop.html:75`) with a
changelog that still calls Projects/Tasks auto-create "to follow in v1.1" (`:262`). Four
months of feature work sit on the other side of it, which is the root of most of the six.

### Are there two competing strategy SOPs?

No — a deliberate split, confirmed in code. `js/config.js:23` registers
`sopFile: 'os/strategy/sop.html'` and `js/sitemap.js:499` builds the Open SOP link straight
from it, so the Airtable shell opens the operator SOP. `guides/strategy.html` is reached only
from the Supabase client twin (`sitemap-supabase.html:897`, whose own comment at `:893-895`
says clients see guides while the owner keeps the SOPs). They disagree on exactly one fact,
and it is the same error in both: the stepping-stone green-flip claim (filed as `073`).

### VERSION_GAP — pageVer ahead of sopVer

Thirteen of the fourteen registry entries that declare a SOP file are out of step:

| Page | pageVer | sopVer |
|---|---|---|
| Tasks & Projects | 1.120 | 1.3 |
| Leadership Dashboard | 2.59 | 2.9 |
| Objective & Strategy | 1.33 | 1.0 |
| CFVs | 1.31 | 1.6 |
| Site Map & Guides | 1.19 | 1.2 |
| Prospecting | 1.10 | 1.3 |
| Systemisation | 1.6 | 1.0 |
| Skills Library | 1.4 | 1.0 |
| Profit & Loss | 2.27 | 2.26 |
| Property Compliance | 1.14 | 1.13 |
| CRM | 1.1 | 1.0 |
| Accounts Payable Variable | 2.18 | 2.18 (in step) |
| Inbound Comms | 2.56 | 2.56 (in step) |
| AI Brain | 1.0 | 1.0 (in step) |

The gap is a symptom, not a defect in itself — each SOP finding filed today carries its own
"bump sopVer" instruction so the number moves when the document is actually corrected.

## 4. Health Check Results

Run against the deployed site (`https://chaichoong.github.io/leadership-dashboard/`) in
Kevin's own Chrome, because the preview-server profile carries no PAT and stops at the auth
gate. No localStorage value was read or printed; only keys, counts and booleans.

| Check | Result | Evidence |
|---|---|---|
| 4a Badge count consistency | **FAIL** | `detectCFVs()` returned 5 (3 `cfv`, 2 `cfv actioned`), none dismissed, while `#cfvSidebarBadges` was empty. Cause found and filed as `079` — see below |
| 4b Token/auth health | **PASS** | PAT present and non-empty, both in localStorage and lexically. Value never read |
| 4c Data freshness | **PASS** | `allTransactions` 8,933, `allTenancies` 94, `allCosts` 180, read lexically. Label read "showing data from 1 min ago" |
| 4d Dismissal-key integrity | **VACUOUS on one shape** | 110 `cfv_*` keys. Date-shaped: **110 checked, 0 invalid**. Sentinel-shaped (`*_returned` / `*_returnDismissed`): **0 checked** — none exist in this browser, so that half proved nothing. Same as 10 Aug |

**4a in full, because it is a real defect and it took three loads to pin down.** After a hard
reload the sidebar showed no CFV badge at all while five CFVs existed and none were
dismissed. Running the badge block from `js/dashboard.js:1004-1014` by hand rendered `3` and
`2` immediately, so the data was present from the first paint. Polling showed the badge
appear only when the full Airtable refresh finished: still empty at 08:11:32, rendered at
08:13:30. The cache render path (`dashboard.js:916-942`) paints the whole dashboard, hides
the loading overlay and never touches the badges; they are updated at `:1002-1014`, which is
downstream of the network fetch. For roughly two minutes the dashboard looks complete and
reports zero overdue rent. Filed as `079`.

Investigating it surfaced a second, latent bug: `dashboard.js:1006-1008` applies the
`cfv_dismissed_` filter to confirmed CFVs as well as potential ones, while both other callers
(`cfv.js:410-431` feeding `:487`, and `cfv.js:216-223`) dismiss `potential` only, and only
`cfv.js` expires a dismissal. A tenancy dismissed as potential that later escalates to a
confirmed CFV would be hidden from the sidebar while still listed on the page. Filed as `080`.

The 4d key-shape list in the SKILL is still missing a fourth shape, `cfv_{id}_startDate`
(carried by finding `055`), and 4c still names a `_lastDashboardLoad` variable that does not
exist anywhere in `js/`. Both are SKILL-file defects, already covered by
`20260807-queue-fixer-008`.

Two `429` warnings were logged during load (`fetchTasksForKpi`, `fetchProspectsForKpi` at
`js/dashboard.js:481`/`:511`) — Airtable rate-limiting the strategic-KPI fetches. Both are
caught and warned, not thrown. Noted, not filed: a single rate-limit under a hard reload plus
my own polling is expected behaviour, not drift.

## 5. Actions Taken

### Auto-fixed: 0

Not permitted. No file edited, no `git add`, no commit, no push, no branch, no PR.

### Filed to the findings queue

| ID | Severity | Title |
|---|---|---|
| `20260811-drift-069` | high | `os/strategy/sop.html` documents none of the three Airtable write paths, including Close the quarter |
| `20260811-drift-070` | high | `guides/systemisation.html` never mentions Go live, which starts an agent that edits Airtable unattended daily |
| `20260811-drift-071` | high | Marking a task Done silently runs a four-stage AI pipeline: Loom import, SOP generation, Drive upload, skill creation |
| `20260811-drift-072` | high | Strategy SOP push table documents three wizard questions and one field that do not exist |
| `20260811-drift-073` | medium | Both strategy documents promise a completed task turns its stepping stone green; no code implements it |
| `20260811-drift-074` | medium | Strategy SOP names a Skip button that does not exist, and Move on carries text forward rather than clearing it |
| `20260811-drift-075` | medium | Strategy SOP says accepted wizard text is written straight into the form; every write is gated by a preview or merge picker |
| `20260811-drift-076` | medium | Strategy SOP says the wizard runs on Haiku; all six AI calls use `AI_MODEL_DEFAULT` (Sonnet) |
| `20260811-drift-077` | medium | `guides/skills.html` promises one-click Run; Run opens an AI chat, writes to Airtable and can open a Drive tab |
| `20260811-drift-078` | medium | Skills Library opens filtered to My Skills so every preset is hidden; its AI Agents strip is undocumented |
| `20260811-drift-079` | medium | Sidebar CFV badge blank for the whole cached-render window: 3 unactioned CFVs invisible for 2-3 minutes |
| `20260811-drift-080` | medium | Two different dismissal filters feed the same CFV badge; the sidebar can hide a confirmed CFV the page shows |

### The three worth reading first

**`070` and `071`, together.** The Systemisation guide is what a client reads before touching
the page. It describes a place to type process notes. The page can also put an AI agent in
charge of editing live tenancy records every day (`Go live`, confirm text at
`os/systemisation/index.html:4019-4026`, default test tables `['tenancies','tenants']` at
`:4008`), and ticking a task Done fires an unattended AI pipeline that writes a SOP, uploads
it to Google Drive and creates a skill (`:860-916`). Neither appears in the guide. This is
the module Kevin sells as the process-to-agent engine, and the client-facing document for it
does not mention agents.

**`079`.** Kevin's overdue-rent alarm reads zero for the first two minutes of every dashboard
load. Not a wrong number — an absent one, which looks identical to "nothing to chase".

### Airtable tasks created: 0 — deliberate, same reasoning as 9 and 10 Aug

CHECK 5 says human-review items become Airtable tasks. Everything qualifying today is already
filed as a finding routed to `queue-fixer`. Raising a task as well puts one piece of work on
two routes at once, which Kevin's own rule forbids. The findings queue post-dates that
section of the SKILL.

### Not done, and why

- No commit, no push, no branch, no PR — caller override and Rule 2
- Queue lock not taken — Rule 1
- `monitoring/reference-map.json` not regenerated despite the SKILL's closing instruction: that
  is an edit to a tracked repo file. Carried by `20260809-drift-031`
- CHECK 4 was run in Kevin's Chrome against the live site rather than through `preview_start`,
  because the preview browser profile holds no PAT and never gets past the auth gate. The
  preview server on port 8951 was started and is left running
- Both output files are gitignored anyway (`monitoring/.gitignore` excludes `schema-2*.json`
  and `drift-2*.md`) — deliberate, since the repo is public and these carry tenant data
