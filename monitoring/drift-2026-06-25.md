# Drift Report — 2026-06-25

## Summary
- Schema changes: 3 new tables, 0 removed, 0 type changes, 0 renames
- Field references: 0 dead, 0 type mismatches, ~60 rogue (code-level, all valid live fields)
- SOP drift: 10 version gaps (chronic, unchanged theme); SOP coverage gap widened by new tabs
- Health checks: 1 structural pass (clean load, no JS errors), 4 data checks not-verifiable (auth-gated headless)
- Auto-fixed: 0 code changes. reference-map.json regenerated (274 → 368 fields) to track config.js growth
- Human review: 0 new tasks — existing task `rec9ihFqDDSY8DMLZ` (still "To do") already covers SOP drift

**Headline:** First drift run after a large block of feature work merged. Since the last run (2026-06-12, `fc08053`), HEAD advanced to `b3ec0b3` with the Money Confidence + Wealth merge, Systemisation work, P&L/AR splits, and the Meetings module. Despite that volume, the system is healthy: every one of the 368 field IDs and 31 table IDs now in `config.js` resolves to a live Airtable field. Zero dead references, zero type mismatches, zero removed tables. The 3 new Airtable tables are all already wired into code. No breakage entered the system.

---

## 1. Schema Changes

Compared `schema-2026-06-25.json` against prior snapshot `schema-2026-06-24.json`.

- Tables: 110 (was 107). Fields: 4,788 (was 4,771).
- **New tables (3):**
  - `tblm5ZxyoiLfaBAS4` — Personal Budgets
  - `tbldMPjXTu7ho5f0T` — Income Buckets
  - `tblTz8ErAmQGu7rIZ` — Debt Terms
- Removed tables: none. Type changes: none. Renames: none.

All 3 new tables back the merged Wealth / Money Confidence work (per-property budgets, income-bucket allocation, loan amortisation from debt terms). Each is referenced in `js/config.js` and `js/wealth.js` / `js/money.js`, so they are live, not orphaned.

---

## 2. Field Reference Issues

Source of truth: `monitoring/reference-map.json`, regenerated this run from `js/config.js`.

- **DEAD (referenced in code, missing in Airtable): none.** All 368 mapped field IDs plus the hardcoded feature-file IDs resolve to live Airtable fields.
- **TYPE_MISMATCH: none.** No type changes occurred upstream, so no type drift is possible.
- **ORPHAN: none reported.** Feature files read fields via `F.constName` (and the other constant objects), not raw `fldXXX` literals, so a raw-ID grep produces false orphans. Suppressed, consistent with prior runs.

### reference-map.json regenerated (config.js grew)
`js/config.js` expanded from 274 → **368** field IDs (and 21 → 31 catalogued table IDs) since the last run, driven by the merged work: 94 new field IDs across Objective & Strategy, Property Valuations, Specific Net Worth Statement by Month, Personal Budgets, Income Buckets, Debt Terms, and Business. All 94 resolve to live fields. `reference-map.json` has been regenerated to match current config.js so the next run diffs against an accurate baseline. (65 of the field constants live inside config.js arrays rather than `key: 'fld'` lines and are catalogued as `UNNAMED_IN_CONFIG` — they are still tracked, just without a single constant name.)

### ROGUE (hardcoded field IDs in feature js, bypassing config.js)
~60 distinct IDs across feature files. **All resolve to valid live fields — zero broken references.** This is the pre-existing local field-map pattern, not new drift:

| File | Notes |
|------|-------|
| js/skills-data.js | mostly doc-string IDs inside markdown template literals (not code reads) |
| js/dashboard.js | local Projects/Business field-ID map used via `getField()` |
| js/arrears.js | link-field IDs in PATCH payloads (Tasks ↔ Units/Tenancies/Tenants/Properties) |
| js/sitemap.js | SOP Update Requests field-ID map |
| js/fintable.js | Accounts field-ID map |
| js/costs.js, js/transactions.js, js/reconciliation.js, js/pnl.js | shared Category / Sub-Category name IDs |

**Why not auto-fixed:** consolidating these into `config.js` would touch ~7 feature files plus the shared `config.js` in one pass — exactly the concurrent-edit / shared-file risk `CLAUDE.md` warns against, and beyond the "comments / config refs / simple naming" scope allowed for unattended auto-fixes. Logged as INFO, consistent with the 2026-06-11 and 2026-06-12 decisions. A dedicated session should consolidate the `dashboard.js` and `skills-data.js` maps if Kevin wants to close this.

---

## 3. SOP Drift

### Version gaps (10, chronic)
Every page that has a SOP file shows `pageVer` ahead of `sopVer`. This is structural, not new: the auto-bump GitHub Action increments `pageVer` on every source push, while `sopVer` is bumped manually only when the SOP itself is rewritten.

| Page | pageVer | sopVer |
|------|---------|--------|
| overview (Leadership Dashboard) | 2.36 | 2.9 |
| os-strategy | 1.14 | 1.0 |
| tasks | 1.102 | 1.1 |
| cfv | 1.27 | 1.6 |
| invoices | 2.16 | 2.2 |
| pnl | 2.22 | 2.12 |
| comms | 2.51 | 2.4 |
| compliance | 1.11 | 1.1 |
| os-bplan | 1.12 | 1.3 |
| sitemap | 1.16 | 1.1 |

The deep behavioural audit (≈30 stale/undocumented items across overview, cfv, invoices, pnl, comms, tasks) was performed on 2026-06-11 and is tracked by open Airtable task `rec9ihFqDDSY8DMLZ` ("To do"). Not re-listed here to avoid noise.

### SOP coverage gap widened (INFO, no new task)
The merged feature work added several full tabs that carry **no SOP at all** (`sopFile: ''` in PAGE_REGISTRY): Money Confidence, Wealth, Accounts Receivable Fixed, Accounts Receivable Variable, Transactions, Systemisation, Team Members, plus the existing Costs / Accounts / Skills tabs. These are new builds; whether each needs a published SOP is Kevin's call. Flagged as INFO and folded under the existing SOP-drift task rather than spawning a duplicate.

---

## 4. Health Check Results

Static server (`python3 -m http.server 8811`) launched via preview; `index.html` loaded headless.

| Check | Result | Detail |
|-------|--------|--------|
| App shell load | **PASS** | Title, sidebar, and `PAGE_REGISTRY` (23 entries) all present. `config.js` + `shared.js` parse and execute with **zero console errors** after the large merge — no syntax/load regression |
| 4a Badge / `allTransactions` consistency | NOT VERIFIABLE | `allTransactions` undefined headless — data load is PAT-gated, no token in headless context |
| 4b Token / auth health | NOT VERIFIABLE | `window.PAT` empty headless (expected); real token only exists in an authenticated browser session |
| 4c Stale-data timestamp | NOT VERIFIABLE | No data loaded, so no refresh timestamp to read |
| 4d localStorage dismissal integrity | NOT VERIFIABLE | Dismissal keys only populate in an authenticated session |

The clean, error-free shell load is the meaningful positive signal this run: it confirms the merged Wealth / Money Confidence / Meetings code did not break page parsing or the registry. The four data-level checks remain auth-gated headless, identical to prior runs.

---

## 5. Actions Taken

### Auto-fixed
- **No code changes.** No dead references, type mismatches, or simple rename mismatches existed to fix. No `fix/drift-2026-06-25` branch or PR created.
- **`monitoring/reference-map.json` regenerated** from current `js/config.js` (274 → 368 fields, 21 → 31 tables, records and select choices recatalogued). This keeps the next run's reference baseline accurate after the config.js growth. Non-destructive metadata update.
- **`monitoring/schema-2026-06-25.json`** written (compact snapshot of all 110 tables / 4,788 fields).

### Escalated to Human Review
- **No new Airtable tasks created.** The only open drift theme (SOP docs behind code) is already tracked by `rec9ihFqDDSY8DMLZ`, still "To do". Creating a second task would duplicate it. All other findings are either healthy (schema/field additions) or chronic-INFO (rogue IDs, version gaps).

### Open items carried forward
1. SOP behavioural drift + 10 version gaps — task `rec9ihFqDDSY8DMLZ` (open).
2. Rogue field-ID consolidation into config.js — INFO, needs a dedicated single-file-at-a-time session if Kevin wants it closed.
3. New tabs without SOPs (Money Confidence, Wealth, AR, Transactions, Systemisation, Team) — Kevin to decide which warrant published SOPs.
