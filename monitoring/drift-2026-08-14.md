# Drift Report — 2026-08-14

Run as phase 3 of `daily-ops`. Queue lock NOT taken (Rule 1). Read-only with respect to
code: no repo file edited, no `git add`, no commit, no branch, no push, no PR. Every code
change is filed to the findings queue instead. Report left uncommitted for phase 8.

## Summary

- **Schema changes:** 0 new, 0 removed, 0 type changes, 0 renames (119 tables, 4,916 fields)
- **Field references:** 0 dead, 0 unresolvable `fld`/`tbl` IDs anywhere in the repo, 328 rogue IDs across 76 files
- **SOP drift:** 10 filed — 6 high, 3 medium, 1 low, across `sop.html` and `sop-invoices.html`; 13 real version gaps
- **Health checks:** 4 pass (4d half-vacuous, as expected)
- **Auto-fixed:** 0 — prohibited by Rule 2 and by the caller override
- **Filed to the findings queue:** 12 (`20260814-drift-monitor-127` … `-138`)
- **Airtable tasks created:** 0 — the findings queue is the route, per Rule 2

---

## 1. Schema Changes

None. `monitoring/schema-2026-08-14.json` matches `schema-2026-08-13.json` exactly in shape:
119 tables, 4,916 fields, no field added, removed, renamed or retyped.

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat` (the MCP connector is still the broken one).
The token was never printed.

---

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (in `reference-map.json`, absent from schema) | **0 of 422** |
| DEAD tables | **0 of 35** |
| TYPE_MISMATCH | **0 — but the check cannot fire, see below** |
| Files scanned | 238 (`.js`, `.mjs`, `.html`, `.py`; excluding `node_modules`, `.git`, `monitoring/`, `test-results/`, `playwright-report/`, `.claude`) |
| Distinct Airtable-shaped IDs outside `config.js` | 629 |
| Rogue (used outside `config.js`, absent from it) | 328, across 76 files |
| Unresolvable `fld`/`tbl` IDs | **0** |

Nothing is broken. Every `fld` and `tbl` ID in the repo resolves against today's snapshot.

### The unresolvable candidates, all accounted for

70 candidates did not resolve against the compact snapshot. All are explained:

- **67 `rec`-prefixed** — record IDs are not carried by the schema meta API at all, so they
  cannot resolve by construction. Four of those are ordinary JavaScript variable names that
  match the pattern: `recentCorrections`, `reconciledInflows`, `reconForecastKeys`
  (`js/cashflow.js`, `follow-up.html`).
- **1 `selectedProjectId`** (`js/shared.js`) — a variable name, not an Airtable ID.
- **2 genuine select-choice IDs** — `selI6Otekm3JyaBSJ` (`follow-up.html:2625`) and
  `selx5WZMIWgrHrPOj` (`js/skills-data.js:1262`). Both verified PRESENT against the raw meta
  API response, which carries choice IDs the compact snapshot drops.

### Three dead select-choice IDs in config.js — already disabled

`selwuotKAoizHJl6z`, `selZazCz6gUJJ8Pl8`, `selypOeFtsBePQG1E` (`js/config.js:898-900`,
`costActive` / `costDueToday` / `costUpcoming`) are absent from today's schema. All three are
already commented out and referenced nowhere else in the repo. No finding filed — there is no
live reference to break.

### Two defects in the check itself (filed)

- **`20260814-drift-monitor-127` (high)** — the TYPE_MISMATCH check has never been able to
  fire. `monitoring/reference-map.json` stores each field as `{fieldId: constantName}` with
  no type, so there is nothing to compare today's type against. Every report since the check
  existed has printed `TYPE_MISMATCH: 0` from code that never looks. A field flipped from
  `singleLineText` to `number` would read as PASS forever.
- **`20260814-drift-monitor-128` (medium)** — `reference-map.json` was generated on
  2026-08-02 and covers 457 of the 483 `fld`/`tbl` IDs now in `js/config.js`. All 26 unmapped
  IDs resolve today, so nothing is broken, but the daily "0 dead of 422" line is measured
  against a denominator 5.4% short of the code.

### Rogue hardcoded IDs — 328, unchanged in character

Top files: `os/team/index-supabase.html` (82), `os/tasks/index.html` (78),
`os/tasks/index-supabase.html` (63), `os/tasks/supabase-shim.js` (59), `follow-up.html` (45),
`follow-up-supabase.html` (45), `scripts/sync-airtable-mirror.py` (37),
`os/strategy/strategy.js` (24). Same known pattern first measured on 2026-08-04. All resolve,
so this is a maintainability debt, not a live defect, and it is already on the queue.

---

## 3. SOP Drift

Focused this run on the two pairs behind the last two code commits: **Leadership Dashboard**
(`sop.html` ↔ `js/cashflow.js` + `js/dashboard.js`, after the Opening Balance split f3385ec)
and **Invoices** (`sop-invoices.html` ↔ `js/invoices.js`, after the clear-filters change #83).
Strategy, Systemisation, Skills, Prospecting, Tasks, Team and Inbound Comms were covered on
11–13 Aug and were not re-walked.

**Both pairs are in worse shape than the Inbound Comms pair was yesterday.** Ten items filed.

### sop.html ↔ dashboard / cashflow — 5 filed

- **`-129` (high, UNDOCUMENTED)** — opening the dashboard **executes JavaScript stored in an
  Airtable field** via `new Function()` (`js/dashboard.js:429`) and **PATCHes computed KPI
  values back to Airtable** (`:611-617`) on every load and every 15-minute refresh. The SOP
  tells the operator there is no back-end (`sop.html:194`) and documents exactly one write
  path, the Recon Audit log (`:375`). The code's own comment calls its denylist "a speed bump,
  not a wall". Failures are `console.warn` only.
- **`-130` (high, STALE_SOP)** — §10's credit-card plan is wrong in every number. Friday
  schedule, £750 buffer, 2% minimums and the £5,500 limit exist nowhere in `js/`. The live
  code is a single-payment avalanche allocator whose APR is **reverse-engineered by
  text-matching "INTEREST" in transaction descriptions** and reads 0% when none is found.
- **`-131` (high, UNDOCUMENTED)** — the forecast table has **seven** columns, not the six
  documented, and its Closing balance silently nets off recommended withdrawals plus £470/week
  of commitments held in **`localStorage`, not Airtable**.
- **`-132` (medium, STALE_SOP)** — §11 documents a Balance Calculator that no longer exists,
  and says what-if exclusions live in `sessionStorage` and reset on reload. They live in
  `localStorage._cf_exclusions` and survive browser restarts until their date passes, quietly
  suppressing real income or costs from the forecast.
- **`-133` (medium, STALE_SOP)** — §622 still documents "Tier 3: fallback match on amount
  alone", which was deliberately removed because identical rents were cross-clearing
  tenancies.

### sop-invoices.html ↔ js/invoices.js — 5 filed

- **`-134` (high, STALE_SOP)** — the page's most prominent red-stop warning tells Kevin to
  distrust a Reconcile run that is now safe. The 100-thread cap is gone; the script pages the
  whole label and **refuses to write at all** if it would truncate.
- **`-135` (high, STALE_SOP)** — §2 sends the operator to a sidebar item that does not exist.
  The page lives under **Accounts → AP Variable subtab**, which is also where the badge sits.
- **`-136` (high, UNDOCUMENTED)** — the sync bar's own ↻ Refresh calls
  `triggerGmailInvoiceSync()`, i.e. it reaches into Gmail and **creates Airtable records**. The
  SOP never mentions the bar exists.
- **`-137` (high, UNDOCUMENTED)** — "Refresh from Gmail" reads only the **newest 50 threads**,
  so an older missing invoice will never appear however many times it is clicked; and the
  sync-health Gmail count caps at 100, so a large label shows a **permanent "Mismatch"** — the
  exact state the SOP tells the operator to escalate over.
- **`-138` (low, UNDOCUMENTED)** — the new Clear filters button (the change behind the version
  bump) is undocumented, and it resets neither Sort nor row selections.

Checked and still accurate, so not raised: budget constants (`sop.html:351-356` ↔
`js/config.js:867-870`), the 15-minute refresh and idle behaviour (`:647-650` ↔
`js/shared.js:7`, `js/dashboard.js:921`), `CFV_TOLERANCE_DAYS = 2` (`:412` ↔ `js/cfv.js:8`),
the 33% / £1 outflow tolerance (`:631` ↔ `js/cashflow.js:208`).

### VERSION_GAP

24 registry entries have `pageVer` ahead of `sopVer`; only **13** are real pairs, the other 11
have an empty `sopFile` and no SOP to be behind. Comparison is tuple-wise, not numeric.
Real gaps: `overview` 2.63/2.9, `os-strategy` 1.33/1.0, `tasks` 1.129/1.3, `cfv` 1.33/1.6,
`invoices` 2.22/2.18, `pnl` 2.27/2.26, `comms` 2.60/2.59, `compliance` 1.14/1.13,
`systemisation` 1.6/1.0, `crm` 1.1/1.0, `prospecting` 1.14/1.4, `sitemap` 1.19/1.2,
`skills` 1.6/1.0. Unchanged in character from yesterday. `pageVer` auto-bumps on every push
and `sopVer` is manual, so a gap of 1 is normal noise; the wide ones where the SOP has never
been revised (`os-strategy`, `systemisation`, `crm`, `skills`) are the ones worth a human's
attention — and today's run shows the narrow gaps are not safe either: `invoices` sat at a
gap of 4 while hiding two write paths and a broken navigation instruction.

---

## 4. Health Check Results

Run against the live site (`https://chaichoong.github.io/leadership-dashboard/`) in Kevin's
authenticated Chrome. No localStorage value was read, logged or returned — only key names,
counts and booleans.

| Check | Result | Evidence |
|---|---|---|
| **4a** Badge consistency | **PASS** | Recounted from `detectCFVs()` through the same `cfvIsVisible` filter the render path uses: 5 raw, 5 visible, split 2 `cfv` / 3 `cfv actioned`. Rendered `#cfvSidebarBadges` is red `2`, orange `3`. Exact match. Identical to yesterday. |
| **4b** Token/auth health | **PASS** | A PAT is present and non-empty. Value not read. |
| **4c** Stale data | **PASS** | 8,950 transactions, 94 tenancies, 180 costs loaded. `#lastUpdated` 07:26 against a local wall clock of 07:26:55. Refresh completed during the run. |
| **4d** Dismissal-key integrity | **PASS on the date half, VACUOUS on the sentinel half** | 110 `cfv_*` keys checked by shape. Date-shaped (`cfv_dismissed_*`, `cfv_reflag_dismissed_*`, `*_chaseStart`): 110 checked, 110 parse as valid Dates, 0 bad. Sentinel-shaped (`*_returned`, `*_returnDismissed`): **0 keys exist**, so that half asserted nothing. |

The vacuous sentinel half is legitimate — no tenancy is in the returned state, same as the
last four runs. `20260809-drift-030` already covers rewriting CHECK 4.

**Re-confirmed the hidden-badge trap from yesterday.** At 12 seconds after load,
`#refreshingBadge` read "Refreshing… (showing data from 21 min ago)" and was genuinely
visible (a refresh was in flight). At 27 seconds the same element carried the same stale text
but `getComputedStyle().display` was `none` and `offsetParent` was `null`. Text alone is not
a signal; visibility must be asserted first. Already filed as `20260813-drift-monitor-119`.

**The machine is still on `Europe/Paris` (UTC+2)**, so `#lastUpdated` 07:26 local is 06:26
London. Recorded, not filed — correct behaviour for a local render, but the same date-boundary
risk noted yesterday still stands.

---

## 5. Actions Taken

### Auto-fixed

None. Prohibited by Rule 2 and by the caller's explicit override.

### Filed to the findings queue — 12 (`20260814-drift-monitor-127` … `-138`)

| ID | Sev | Title |
|---|---|---|
| `-127` | high | TYPE_MISMATCH check has never been able to fire — no expected types stored |
| `-128` | medium | `reference-map.json` stale since 2 Aug — 26 of 483 config IDs unmapped |
| `-129` | high | Dashboard executes Airtable-stored JS and writes KPIs back; SOP says no back-end |
| `-130` | high | `sop.html` §10 credit-card plan: every number wrong |
| `-131` | high | Forecast Closing nets off withdrawals + £470/wk of browser-local commitments |
| `-132` | medium | §11 Balance Calculator gone; exclusions persist in `localStorage`, not `sessionStorage` |
| `-133` | medium | "Tier 3 amount-only match" was deliberately removed |
| `-134` | high | Reconcile 100-thread danger box is obsolete and misleading |
| `-135` | high | SOP points at a sidebar item that does not exist |
| `-136` | high | Sync-bar Refresh writes Airtable records; SOP never mentions the bar |
| `-137` | high | Gmail sync reads only newest 50 threads; health count caps at 100 |
| `-138` | low | New Clear filters button undocumented; resets neither Sort nor selections |

Eight high, three medium, one low. Queue depth after this run: 138 findings total.

### Escalated to Airtable

None. The findings queue is the route (Rule 2). No Airtable task was created, so no
single-select value was written and none of the 6 Aug typo risk applies.

### Git

No `git add`, commit, branch, push or PR. `git pull origin main` only (fast-forward
`a5a21ac..517faab`). Working tree left exactly as found, plus this report and today's schema
snapshot under `monitoring/` (snapshots are gitignored by `monitoring/.gitignore`).
