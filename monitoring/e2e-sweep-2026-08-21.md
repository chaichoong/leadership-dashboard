# E2E Sweep — 2026-08-21

## Summary
- Production URL: UP (200, 67,186 bytes, app shell present)
- Mode: **FULL** (authenticated, JS) — via Kevin's own Chrome profile, no token handled by the sweep
- Tabs walked: 28 (25 in-shell panels + 3 standalone pages) | PASS: 24  WARN: 2  FAIL: 2  UNVERIFIED: 0
- Data invariants: **PASS** (12 checked, every control non-zero)
- Data loaded: 9,049 transactions, 94 tenancies, 180 costs; overview panel 294,277 chars
- JS exceptions across the whole walk: **0**

## Auth route changed (finding 023 is effectively resolved)
Previous sweeps ran DEGRADED because token injection was denied. This run took the route
finding `20260808-prod-e2e-sweep-023` recommended: drive the already-authenticated Chrome
profile against production via `mcp__claude-in-chrome`. The sweep never reads, prints or
handles the PAT at all — Chrome already holds it. `mcp__Claude_Browser__javascript_tool`
also worked normally today, so finding `20260809-prod-sweep-041` (JS denied) did not
reproduce. `preview_start` for a local dev server IS still blocked in unattended sessions,
which no longer matters: production is the deployed build and was tested directly.

## Registry shape changed
PAGE_REGISTRY now returns **28** ids (was 27). `coa` and `kpi-library` were added, `os-bplan`
is gone (Plan Builder retired from the shell, per CLAUDE.md 1 Aug). 25 have in-shell panels;
`compliance`, `crm`, `how-it-works` are standalone pages as documented — checked by URL.

## Per-tab results
| Tab | Result | Evidence |
|---|---|---|
| overview | PASS | 294,277 chars, 11/11 checks |
| os-strategy | PASS | iframe complete, "Live · linked to Airtable", intentional empty state pending business pick |
| tasks | PASS | iframe complete, 546,028 chars |
| cfv | PASS | 6/6 checks |
| ceo-brief | WARN | rendered 88,508 chars, 4/4 checks; one Airtable **HTTP 429** (rate limit, transient) |
| money | PASS | 4/4 checks |
| wealth | PASS | 7/7 checks, net worth £2,926,113.65 |
| income | PASS | renders; 5/6 — the fail is a business alert (4 tenancies in CFV, £3,191.20 at risk), not an app fault |
| ar-variable | PASS | 6/6 checks |
| costs | PASS | renders; 3/5 — business alerts (14 overdue, 14 hard variance) |
| invoices | PASS | 7/7 checks; health drawer opened and closed correctly |
| pnl | PASS | 8/8 checks |
| transactions | PASS | 6/6 checks, 9,049 transactions; card expand responded |
| coa | PASS | 5/6 — warn is housekeeping (6 unused lines) |
| comms | PASS | iframe complete, 355,728 chars |
| operations | PASS | iframe complete, 171,084 chars |
| kpi-library | WARN | 5/5 checks; one status-0 fetch to a Google Apps Script endpoint — consistent with an opaque no-cors response, probably benign |
| fintable | PASS | 5/5 checks |
| systemisation | PASS | iframe complete, 236,860 chars |
| os-team | PASS | iframe complete, 49,838 chars |
| content-machine | PASS | iframe complete, 847,131 chars |
| prospecting | **FAIL** | 6/7 — PECR gate fail, see below |
| sitemap | PASS | 3/7, but 3 of the 4 non-passes are *active* checks awaiting a manual Re-run (by design); the 4th is SOP-version drift on 13 pages |
| skills | PASS | 5/5 checks |
| ai-brain | **FAIL** | feed 3 days stale, presented as a successful sync — see below |
| compliance.html | PASS | HTTP 200, 33,535 bytes |
| crm-supabase.html | PASS | HTTP 200, 45,313 bytes |
| how-it-works.html | PASS | HTTP 200, 87,876 bytes |

## Data invariants
All 12 PASS, each with a non-zero control population:
report-amount-populated (100), split-override-sign (18), agent-work-completed-without-approval
(100), waiting-approval-has-an-agent (53), on-time-counts-the-due-date-itself (100),
started-projects-are-not-still-not-started (20), open-tasks-carry-no-completion-date (100),
automated-kpis-have-actually-run (5), ceo-brief-complete (14), kpi-library-coverage (5),
one-monthly-summary-per-business-month (41), no-reimport-duplicates (7,986).

## Failures needing attention

### 1. AI Brain nightly publisher dead 3 days, page says "Synced" — finding 20260821-prod-e2e-sweep-281
The tab reads "Synced · 18 August 2026". Scanned the whole feed table `tblZ75JgE1wzDP0ps`
with pagination: 31 rows, exactly ONE distinct Date, `2026-08-18`. So 19, 20 and 21 Aug never
published. Nothing raises it: `ai-brain` has no sync bar (17 tabs do, this is one of 8 without),
and `setSync` at ai-brain.html:232 prints "Synced · <date>" in the success style whenever any
row carries a date, so a three-day-old feed reads identically to a fresh one.

### 2. Prospecting PECR gate — finding 20260821-prod-e2e-sweep-282
Card reports "23 non-Ltd prospect(s) have been emailed". Verified independently against
Airtable with full pagination: 213 prospects, 137 at 'Contacted (1:1)', **0 replies of any
kind**. The 23 split 3 'Sole Trader / Partnership' + 1 'Unknown' + **19 with Entity Type
BLANK**. Only 4 are confirmed non-Ltd. The 19 blanks mean the Companies House entity gate
never recorded a result before those sends, so there is no evidence either way. The check
folds blank into non-Ltd and reports one number, which hides that distinction. Related to
open finding `20260812-drift-095` (send path has no entity gate) but distinct.

## Notes for tuning
- Memory `project_prospecting_engine` records "46 emailed, 0 replies" as at 16 Aug. It is now
  **137 emailed, still 0 replies**. Open finding `20260809-prospect-daily-047` says zero
  outreach had ever been approved; that is now clearly out of date and worth re-reading.
- Near-miss worth recording: the Objective & Strategy iframe contains the literal text
  "Invalid token. Please check and try again." A text-only check would have called it a FAIL.
  Both `#authScreen` and `#authError` compute to `display:none` — it is latent boilerplate and
  the page is authenticated. Any future sweep must test computed style, not text presence.
- `window.PAT` / `window.allTransactions` remain undefined on a healthy authenticated app
  (script scope). The bare-identifier check in the skill is correct and was used.
- CDP `Runtime.evaluate` times out at 45s, so tab walks must run in batches of ~5 with a
  2.5s settle, not one long loop.
