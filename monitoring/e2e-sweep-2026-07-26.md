# E2E Sweep — 2026-07-26

## Summary
- Production URL: UP (200) — https://chaichoong.github.io/leadership-dashboard/ served the app shell (`<title>Operations Director</title>`, sidebar markup present)
- Mode: FULL (authenticated, JS injection worked)
- Auth state: PAT set, 8,725 transactions, 94 tenancies, 180 costs, 5 businesses loaded
- Tabs walked: 25  |  PASS: 25  WARN: 0  FAIL: 0  UNVERIFIED: 0
- Data invariants: PASS (3 checked — all controls fired)
- Console errors across the whole walk: 0

## Per-tab results

Tabs split by panel type: 14 in-shell panels, 9 lazy iframe pages, 2 standalone pages.

### In-shell panels (14) — all PASS, no console errors, real data rendered, no value leaks (undefined/NaN/[object Object])
- overview — PASS. 11/11 health checks ✓. Opening balance £4,739.70, monthly income £33,180–£34,077, monthly costs £22,740.04. Read-only interaction verified: Monthly Income card expanded inline showing the "In Payment (60)" breakdown (Day 1, IMMO LTD, £350.00).
- cfv — PASS. 6/6 checks ✓. Cash Flow Voids auto-detected list rendered.
- money — PASS. 4/4 checks ✓. "Safe to act today" panel rendered.
- wealth — PASS render. Net worth £2,914k+ shown. Internal health bar 5/7 ⚠ (see note A).
- income (Accounts Receivable Fixed) — PASS render. Internal health bar 5/6 ✗ (see note A).
- ar-variable — PASS. 6/6 checks ✓.
- costs (Accounts Payable Fixed) — PASS render. Internal health bar 3/5 ✗ (see note A).
- invoices (Accounts Payable Variable) — PASS. 7/7 checks ✓.
- pnl — PASS. 8/8 checks ✓. Live P&L from categories.
- transactions — PASS. 6/6 checks ✓. 8,725 transactions listed.
- fintable (Accounts monitor) — PASS. 5/5 checks ✓.
- prospecting — PASS. 7/7 checks ✓. Funnel + queue rendered.
- sitemap (Site Map & Guides) — PASS render. 25 pages listed. Internal health bar 3/7 ⚠ (see note A).
- skills (Skills Library) — PASS. 5/5 checks ✓.

### Lazy iframe pages (9) — all PASS, iframe src set and body populated, no console errors
- os-strategy — iframe loaded (os/strategy/index.html). Renders its own Airtable-PAT auth gate (data inside iframe not verified — normal; sweep injects only the shell token).
- tasks — iframe loaded (os/tasks/index.html). Full task view rendered (~478k chars, team-member picker present).
- comms — iframe loaded (follow-up.html). Renders its own Google sign-in gate (normal).
- operations — iframe loaded (os/operations/index.html). Content rendered.
- os-bplan (Plan Builder) — iframe loaded (os/business-plan-builder/index.html). Content rendered.
- systemisation — iframe loaded (os/systemisation/index.html). Renders its own Airtable-key gate (normal).
- os-team (Team Members) — iframe loaded (os/team/index.html). "6 active team members across 6 departments" rendered.
- content-machine — iframe loaded (https://chaichoong.github.io/content-machine/). Cross-origin (expected) so inner content not readable; src correct, no load error.
- ai-brain — iframe loaded (ai-brain.html). Content rendered (~3k chars).

### Standalone pages (2) — all PASS, no console errors
- compliance (Property Compliance) — navigated directly to compliance.html. 7/7 checks ✓. Live data: 1 EXPIRED, 0 expiring (30d), 15 items.
- how-it-works — navigated directly to how-it-works.html. Full guide rendered (~25k chars).

## Data invariants
Ran `python3 scripts/check-data-invariants.py --json` (read-only, exit 0). All PASS, all controls fired (so none was a silent no-op):
- report-amount-populated — PASS. Asserts "**GBP set => Report Amount set". Control population 100. Regression guard for the Jul 2026 blanking of 8,667 transactions.
- split-override-sign — PASS. Asserts "override set => sign(Report Amount) == sign(**GBP)". Control population 18. Regression guard for the split sign-flip incident.
- no-reimport-duplicates — PASS. Asserts one bank transaction => one record. Control population 7,679.

## Failures needing attention
None. Zero FAILs, zero console errors, production up, all data invariants hold. No Slack DM sent and no Airtable task raised, per the quiet-on-green rule.

## Note A — internal health-bar sub-checks (informational, not defects)
Four tabs show amber/red counts in their own health bars. These are the app's business monitors working as designed — they surface genuine business conditions, not app breakage. The tabs themselves render correctly with no JS errors:
- income 5/6 ✗ — "Cash flow voids: 5 tenancy/ies in CFV — £3,847.08 at risk". A real rent-at-risk signal.
- costs 3/5 ✗ — "Overdue costs: 15 cost(s) overdue" and "Amount variance flags: 16 cost(s) with hard variance (>10%)". Real cost-review signals.
- wealth 5/7 ⚠ — two non-critical wealth checks amber.
- sitemap 3/7 ⚠ — "11 page(s) have an SOP version behind pageVer" (SOP-vs-page version drift) and "GitHub status not yet fetched (auto-loads when tab opens)".

These are for Kevin's operational awareness at the weekly review, not sweep failures.

## Sweep health note
App shape matches the sweep's expectations: PAGE_REGISTRY returned the same 25 tab ids recorded in the task file. No structural drift this run.
