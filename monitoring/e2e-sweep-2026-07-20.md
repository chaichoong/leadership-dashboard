# E2E Sweep — 2026-07-20

## Summary
- Production URL: **UP (200)** — https://chaichoong.github.io/leadership-dashboard/ served the app shell (`<title>Operations Director</title>`, `#sidebar`, `switchTab`).
- Tabs walked: **25**  |  PASS: **25**  WARN: 0  FAIL: 0
- Data invariants: **PASS (2 checked)** — both controls fired (populations 100 and 18), so neither check was silently asserting nothing.
- Console errors: **none** across the whole walk. All network requests returned 200.
- Auth: booted authenticated (PAT present, 82 chars), 8,731 transactions loaded, PAGE_REGISTRY = 25 tabs.

The registry now lists **25** tabs (was 24 at last tuning). New tab since last check: **`prospecting`**. Enumerated live from `PAGE_REGISTRY`, so it was covered automatically.

## Per-tab results

Rendering was confirmed by reading each `tab-<id>` container (or its iframe / standalone page). "len" = container innerHTML length; iframe tabs verified by reading the loaded frame's readyState + body.

### In-shell data tabs (rendered real content, no bad values)
| Tab | Evidence |
|---|---|
| overview | PASS — 281k chars, 62k visible text, 28 KPI cards |
| cfv | PASS — 113k chars, rent-balance content |
| money | PASS — "Safe to act today" panel rendered |
| wealth | PASS — 534k chars, per-property valuation cards |
| income | PASS — 73k chars |
| ar-variable | PASS — 5k chars |
| costs | PASS — 1.9M chars (full cost list) |
| invoices | PASS — invoice rows rendered, bulk controls present |
| pnl | PASS — 82k chars P&L |
| transactions | PASS — 885k chars; search filter accepted input |
| fintable | PASS — sync monitor rendered |
| prospecting | PASS — 292k chars, 104k visible text |
| sitemap | PASS — 32k chars |
| skills | PASS — 342k chars |

### Iframe tabs (frame loaded, readyState complete, real body content)
| Tab | Frame | Evidence |
|---|---|---|
| os-strategy | os/strategy/index.html | PASS — "Objective & Strategy", complete |
| tasks | os/tasks/index.html | PASS — "Tasks & Projects", 477k body |
| comms | follow-up.html | PASS — "Inbound Comms", 342k body |
| operations | os/operations/index.html | PASS — 169k body |
| os-bplan | os/business-plan-builder/index.html | PASS — "Business Launch Plan Builder", 90k body |
| systemisation | os/systemisation/index.html | PASS — 236k body |
| os-team | os/team/index.html | PASS — "Team Members", 43k body |
| ai-brain | ai-brain.html | PASS — "AI Brain", complete |
| content-machine | https://chaichoong.github.io/content-machine/ | PASS — cross-origin (can't introspect DOM); external URL returns 200 by direct fetch |

### Standalone tabs (open as their own page; verified they load + render)
| Tab | Page | Evidence |
|---|---|---|
| compliance | compliance.html | PASS — "Live — 20 Jul 2026", "7/7 checks ✓", "1 EXPIRED"; live data, no console errors |
| how-it-works | how-it-works.html | PASS — 25k chars, full guide rendered |

## Data invariants
Ran `python3 scripts/check-data-invariants.py --json` (read-only). Exit 0.

| Invariant | Asserts | Status | Control |
|---|---|---|---|
| report-amount-populated | `**GBP` set => `Report Amount` set | **PASS** | 100 records (valid, fired) |
| split-override-sign | override set => sign(`Report Amount`) == sign(`**GBP`) | **PASS** | 18 records (valid, fired) |

These are the two live checks that guard the platform's two worst incidents (the 8,667-txn `Report Amount` blanking and the split sign-flip). Both hold; both controls matched real records, so neither is a check that "passed" by matching nothing.

## Failures needing attention
None.

## Notes for tuning the sweep
- The sweep is self-maintaining: tabs are read from `PAGE_REGISTRY` live, so `prospecting` (new since the last hardcoded list of 24) was covered with no prompt change needed.
- `compliance` and `how-it-works` have no in-shell `tab-<id>` container. `switchTab` treats them as `standalone` pages and calls `window.open(...)`. In the automated browser `window.open` is blocked, so they were verified by loading the pages directly instead — both render with live data.
- **Interaction-picker caution (methodology, not an app bug):** while choosing a "read-only" control on the invoices tab, a loose label match clicked the bulk **"Mark as Paid"** button. No write occurred — `applyBulkMarkPaid()` returns at `if (targets.length === 0) return;` before any confirm or PATCH because no invoice was selected. Verified: no modal, no confirm dialog, no non-GET Airtable request. Worth tightening the interaction picker in this sweep to hard-exclude any button whose label contains pay/paid/approve/delete/send/create/save, so a future run can't rely on that guard.
