# Drift Report — 2026-08-20

Run as phase 3 of `daily-ops`, 07:14 BST. Queue lock NOT taken (Rule 1). Read-only with
respect to code (Rule 2, reinforced by the caller's override): no repo file edited, no
`git add`, commit, branch, push or PR. Four code findings filed to the queue. This report is
written to `monitoring/` and left uncommitted for phase 8.

## Summary

- **Schema changes:** 0 new, 0 removed, 0 type changes, 0 renames — 119 tables, 4,917 fields, byte-identical to the 19 Aug snapshot
- **Field references:** 0 dead of 422 mapped fields, 0 dead of 35 mapped tables, **0 unresolvable `fld`/`tbl` IDs across the whole repo**, 334 rogue IDs in 938 occurrences, 42 orphaned config constants (see the method note below — this is NOT a jump from 35)
- **SOP drift:** `sop-pnl.html` ↔ `js/pnl.js` audited — the only pair in the mapping table never previously covered. 2 medium + 2 low filed
- **Health checks:** CHECK 4 SKIPPED — the browser opens on a clean profile with no PAT, so every badge, staleness and dismissal-key assertion would be vacuous. Entering the token is prohibited
- **Auto-fixed:** 0 — prohibited
- **Filed to the findings queue:** 4 (`20260820-drift-monitor-256` … `-259`) — 2 medium, 2 low
- **Airtable tasks created:** 0 — the findings queue is the route

---

## 1. Schema Changes

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat`. The token was never printed. Snapshot saved to
`monitoring/schema-2026-08-20.json`.

| Compared against | Tables | Fields added | Removed | Type changes | Renames |
|---|---|---|---|---|---|
| `schema-2026-08-19b.json` | 119 = 119 | 0 | 0 | 0 | 0 |

The base has not moved since 16 Aug. Nothing to reconcile.

---

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (in `reference-map.json`, absent from schema) | **0 of 422** |
| DEAD tables | **0 of 35** |
| TYPE_MISMATCH | **still cannot fire** — the map stores no expected types (already filed, `20260814-drift-monitor-127`) |
| Rogue IDs (used outside `js/config.js`, absent from it) | 334 unique, 938 occurrences |
| **Unresolvable `fld`/`tbl` IDs** | **0** |
| `rec`/`sel` candidates that cannot resolve by construction | 75 |
| ORPHAN constants in `config.js` (resolve in Airtable, referenced by no other file) | 42 |

Every `fld` and `tbl` ID in the repo resolves against today's snapshot. Nothing is broken.

**Method note on the orphan count.** Yesterday's report said 35 and warned that a sudden jump
means the method changed. It did: today's constant-name extraction also matches constants
declared as `const NAME = 'fld…'`, not only `name: 'fld…'` inside an object literal, which is
7 constants the narrower regex never saw. So 42 is the same population measured slightly
wider, not 7 new orphans. The two already-filed ones (`propInclude`, `bizInclude`,
`20260818-drift-monitor-195`) are still in the list. The remaining 40 are dormant constants,
not defects, and are not filed individually because that would be queue noise.

The 75 non-resolving `rec`/`sel` candidates split three ways and none is a defect: real
record IDs used as fixtures or as pinned Airtable rows (`js/admin-repair-splits.js`,
`follow-up-supabase.html`, `journey.html`, the `tests/` tree), deliberate placeholders
(`recXXXXXXXXXXXXXX`, `recTESTTESTTEST01`, `recHumanTeamRow00`), and ordinary JavaScript
identifiers whose first three letters collide with the prefix (`recentCorrections`,
`reconciledInflows`, `recordSaveFailure`, `selectedProjectId`, `selectRecordAsync`). The
meta API carries no record IDs, so none of these can ever resolve. This is expected, and is
recorded here so the next run does not re-investigate it.

---

## 3. SOP Drift — `sop-pnl.html` ↔ `js/pnl.js`

Chosen because it is the one pair in the SKILL.md mapping table with no finding ever filed
against it. Both files read in full; every field ID and sub-category name in the SOP checked
against today's live schema and against the Chart of Accounts table.

**What is correct.** The 27-name allow-list in `PNL_SECTIONS` matches the SOP's three tables
exactly, and all 27 names exist verbatim in `tblOTdRcPf8AgRz25` (49 sub-categories live; the
22 excluded are capex, loan capital, transfers and personal lines, which is what the SOP
says). All 10 field IDs and 4 table IDs in the section 10 reference tables resolve. The nine
KPI cards, the four canvases, the four drill-down scopes, the 1/3/6/12 period options and the
deactivated-business notice all match the code.

**What has drifted** (4 findings filed):

| ID | Sev | Item |
|---|---|---|
| `20260820-drift-monitor-256` | medium | Document header and changelog say **Version 2.12, 17 Apr 2026**; `js/config.js:33` advertises `sopVer: '2.26'`. The Site Map shows 2.26. Fourteen bumps recorded against a file untouched since April |
| `20260820-drift-monitor-257` | medium | The sync bar — a Refresh that re-fetches every transaction via `loadDashboard()`, plus 8 named health checks including the monthly-vs-grand-total arithmetic drift catcher — is undocumented. Section 9's button table omits Refresh entirely |
| `20260820-drift-monitor-258` | low | Quick reference says `js/pnl.js` is 795 lines (it is 951); field table calls `fldsbuAJCTsXHug4C` "Description" (live name `*Name`) and `fldMRjSVzZVYeHb0A` "Chart of Accounts Sub-Category" (live name `Chart of Accounts - Sub Category`) |
| `20260820-drift-monitor-259` | low | SOP prints the literal `claude-sonnet-4-6` twice; the code sends `AI_MODEL_DEFAULT` from `js/ai-models.js`. Correct today, silently wrong the day the default is retired. Same class as `-248`/`-249` |

### Version gaps across PAGE_REGISTRY

24 entries have `pageVer` ahead of `sopVer`. Most are the ordinary one-or-two-bump lag that
follows a page edit, and several are string-comparison artefacts (`2.65` vs `2.9` is a
65-vs-9 minor, not a regression). The ones that are genuinely abandoned — `sopVer: '1.0'`
against a page in the 1.1x–1.4x range — are Objective & Strategy, Money Confidence, Wealth,
the four Accounts Receivable/Payable pages, Transactions, Operations, KPI Library, Accounts,
Systemisation, Team Members, CRM, Skills Library and How It Works. Most of those have no SOP
file at all in the registry, so the `1.0` is a placeholder rather than a stale document. Not
filed: filing 24 near-identical findings would bury the four real ones above.

---

## 4. Health Check Results — SKIPPED

`preview_start` opened `https://chaichoong.github.io/leadership-dashboard/` successfully and
the page rendered its authentication screen with **zero console errors**. That is the whole
of what could be observed.

The browser profile is clean: `localStorage` holds 0 keys, no `_dlr_pat`, no `cfv_*` keys.
So every CHECK 4 assertion would be vacuous rather than passing —

- **4a badge consistency** — the sidebar never renders; nothing to recount
- **4b token health** — "no PAT present" is a fact about this throwaway profile, not about
  Kevin's browser. Reporting it as a fail would be a false alarm; reporting it as a pass
  would be a lie. Entering the token to get past the gate is a prohibited credential action
- **4c staleness** — `_lastDashboardLoad` never gets set
- **4d dismissal-key integrity** — 0 keys of each shape, which SKILL.md itself says must be
  reported as VACUOUS, not PASS

This is the same outcome as 18 and 19 Aug. CHECK 4 cannot run unattended against a fresh
profile, and the honest report is that it did not run.

---

## 5. Actions Taken

### Auto-fixed
None. Rule 2 forbids it and the caller's instruction repeated the prohibition.

### Filed to the findings queue
`20260820-drift-monitor-256` (medium), `-257` (medium), `-258` (low), `-259` (low) — all four
against the P&L SOP. One finding per item; nothing batched.

### Escalated to human review
None. No type mismatch, no schema change, no dashboard failure. The four SOP items are
documentation fixes that `queue-fixer` can make, so the queue is the right route and an
Airtable task would only duplicate them.
