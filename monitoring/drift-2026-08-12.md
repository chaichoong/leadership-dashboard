# Drift Report — 2026-08-12

Run as phase 3 of `daily-ops`. Queue lock NOT taken (Rule 1). No file in the repo edited,
no `git add`, no commit, no push, no branch, no PR (Rule 2 + caller override). Every code
change is filed to the findings queue instead.

## Summary

- **Schema changes:** 0 new, 0 removed, 0 type changes (119 tables, 4,915 fields)
- **Field references:** 0 dead, 0 type mismatches, 0 unresolvable IDs anywhere in the repo
- **SOP drift:** 12 filed — 7 high, 5 medium, across `sop-prospecting.html`, `guides/tasks.html`, `guides/team.html`
- **Health checks:** 3 pass, 1 pass-with-a-vacuous-half
- **Auto-fixed:** 0 (not permitted)
- **Filed to findings queue:** 12 (`20260812-drift-095` … `-108`)
- **Airtable tasks created:** 0 — the findings queue is the route, per Rule 2

---

## 1. Schema Changes

None. `monitoring/schema-2026-08-12.json` matches `schema-2026-08-11.json` exactly in shape:
119 tables, 4,915 fields, no new fields, no removed fields, no renames, no type changes.

Fetched with curl against `https://api.airtable.com/v0/meta/bases/appnqjDpqDniH3IRl/tables`
using the PAT at `~/.config/od/airtable_pat`.

## 2. Field Reference Issues

| Check | Result |
|---|---|
| DEAD fields (in `reference-map.json`, absent from schema) | **0 of 422** |
| TYPE_MISMATCH | **0** |
| DEAD tables | **0 of 35** |
| Files scanned | 230 (`.js`, `.mjs`, `.html`, `.py`; excluding `node_modules`, `.git`, `monitoring/`, `test-results/`, `playwright-report/`, `.claude`) |
| Distinct Airtable-shaped IDs found | 840 |
| Unresolvable `fld`/`tbl` IDs | **0** |
| IDs used outside `js/config.js` and absent from it | 326, across 49 files |

Nothing is broken. Every `fld` and `tbl` ID in the repo resolves against today's schema.

The 326 hardcoded IDs outside `config.js` are the known pattern first measured on 2026-08-04.
Top offenders unchanged in rank, up 7 on yesterday: `os/team/index-supabase.html` (82),
`os/tasks/index.html` (78), `os/tasks/index-supabase.html` (63), `os/tasks/supabase-shim.js`
(59), `scripts/sync-airtable-mirror.py` (37), `follow-up.html` (28),
`follow-up-supabase.html` (28), `os/strategy/strategy.js` (24).

100 `rec`/`sel` candidates matched the boundary regex; the non-resolving ones were eyeballed
rather than name-filtered, as the SKILL requires, and are ordinary JavaScript identifiers
(`recAlreadyDecided`, `reconciledInflows`, `selectedProjectId` and similar).

**Coverage caveat, and it is widening.** `monitoring/reference-map.json` still carries
`summary.generated: 2026-08-02`. `js/config.js` now holds **514** Airtable-shaped IDs, of
which **28** are absent from the map — up from 22 when `20260809-drift-031` was filed three
days ago. CHECK 2 therefore covers the map, not today's `config.js`. Not regenerated here
because that is an edit to a tracked repo file. Carried by `20260809-drift-031`; the count in
that finding is now stale by six.

## 3. SOP Drift

Chosen because they had not been deep-audited: `sop-prospecting.html` (last touched in the
8 Aug summary table only) and the two guides never audited at all, `guides/tasks.html` and
`guides/team.html`. The 9–11 Aug runs covered `sop.html`, `sop-cfvs`, `sop-invoices`,
`sop-pnl`, `sop-compliance`, `sop-sitemap`, `inbound-comms-sop`, `os/tasks/sop.html`,
`os/strategy/sop.html`, `guides/systemisation.html` and `guides/skills.html`.

### sop-prospecting.html vs js/prospecting.js — 7 findings

Two of these are **code defects, not documentation drift**, and both sit on the PECR line:

| Type | Detail | Filed |
|---|---|---|
| Code defect | The send path has no entity check. `isLtd` (`js/prospecting.js:455`) only picks tags (`:473`); `sendProspectEmailViaGHL` gates on Contact Route and the `[BOOKING-LINK]` placeholder (`:510-513`) and never reads Entity Type. The SOP's "hard rule" at `:119` is not enforced anywhere in code | `095` |
| Code defect | The PECR health check inspects only `In Sequence` and `Replied` (`:780`), but the send path writes `Contacted (1:1)` (`:534`) and nothing in the file ever writes `In Sequence`. The hard-fail check can never fire on the breach it exists to catch | `096` |
| STALE_SOP | `sop-prospecting.html:141` and `index.html:818` both say "nothing sends". Approving POSTs a real email (`:520`) and writes the status back (`:534`). `:92` of the same SOP correctly says it sends on approve — the document contradicts itself | `097` |
| STALE_SOP | The 3-email nurture sequence and the `od-prospect-nurture` tag are documented as live (`:92`, `:140`); the code sends two personal follow-ups and never applies that tag (`:244`, `:473`), and the skill records the tag as retired | `098` |
| STALE_SOP | Status flow at `:142` names `In Sequence`, which no code sets, and omits `Contacted (1:1)` and the other eleven filter-bar statuses (`:604`) | `099` |
| STALE_SOP | Credential location at `:139` covers only the agent's config files; the browser path reads `localStorage` keys and forbids the shared location ID (`:457-462`) | `100` |
| STALE_SOP | `:114` puts the compliance footer in GHL templates; it is generated in code from `OD_SENDER` (`js/config.js:163-170`), so a fix applied to the templates would change nothing | `101` |

Checked and accurate: queue status `Ready for Review`, both table IDs, the 8-second Undo, the
card contents, edited text being what sends, the `[BOOKING-LINK]` refusal, both tags, the
Gmail label rule, the sidebar badge source, health checks 1–3 and 5 including the 3-day
staleness rule, the Companies House three-way classification, the 5/day target and 20-profile
cap, and the stop-on-friction kill switch.

Not filed (low): `:73` says the agent runs seven days a week while the empty-state string says
"each weekday morning" (`js/prospecting.js:262`); and section 7 lists six health checks where
the code registers seven (`:787`, sidebar badge).

### guides/tasks.html vs os/tasks/index.html — 4 findings

| Type | Detail | Filed |
|---|---|---|
| Code defect | Drawer **Delete** calls `performDeleteTask()` directly (`:3762-3774`) and fires an immediate Airtable `DELETE` (`:3807-3810`). The confirmation modal `openDeleteConfirm()` (`:3776-3797`) is dead code, called from nowhere. Only a 5.5-second Undo toast recovers it | `102` |
| UNDOCUMENTED | Assigning Slack-DMs the assignee (`:2211-2217`) and silently adds them to Collaborators (`:2252-2265`); completing DMs every collaborator (`:2196-2205`). The guide mentions none of it | `103` |
| UNDOCUMENTED | Complete does not complete a recurring task — `rollRecurringTask()` (`:7238-7263`) rolls the same record forward and sets it back to `Upcoming`. The guide never mentions recurring tasks or the Recurring tab | `104` |
| UNDOCUMENTED | `+ Add Task` writes a live "(Untitled)" record before you type (`:7689-7724`); only Cancel removes it | `107` |
| STALE_SOP | "Weeks ahead" is not a tab — it is the Kanban tab's mode toggle (`:5722-5724`). Seven of the nine tabs are undocumented | `108` |

Checked and accurate: the `+ Add Task` button position and colour, the Cancel/Delete/Complete
footer order, inline auto-save in the drawer, due date / assignee / project fields, the My Day
and Task List labels, and My Day's hours total.

### guides/team.html vs os/team/index.html — 1 finding

| Type | Detail | Filed |
|---|---|---|
| STALE_SOP | The guide tells Kevin to fill in and update details in the profile drawer and to set each person's manager there (`:80-81`, `:85`). `openDrawer()` (`:615-733`) renders all eight sections as plain text with no input and no PATCH; the only write on the page is `submitAchievement()` (`:1133-1159`) | `106` |

Checked and accurate: all seven tab names and their order, the Directory search and filters,
the Org Chart's manager source, the Training and Reviews filters, the "+ Add Achievement"
label, all eight profile section headings, and Onboarding covering leavers as well as joiners.

Not filed (borderline): `guides/team.html:57` calls Handbook "a simple team handbook everyone
can read", but `renderHandbook()` (`:1061-1105`) is a stub — an external link, four cards
reading "to be populated by HR", and an Acknowledged column of "Phase two" tags.

### VERSION_GAP

23 of 27 registry entries have `pageVer` ahead of `sopVer`. Only 12 are real SOP pairs; the
other 11 are pages with no `sopFile` sitting at the default `sopVer: 1.0`. Unchanged from
yesterday other than the routine `pageVer` bumps. Comparison must be tuple-wise, not numeric:
`1.125` is ahead of `1.3`.

Real pairs with a gap: `overview` 2.61/2.9, `os-strategy` 1.33/1.0, `tasks` 1.125/1.3,
`cfv` 1.33/1.6, `invoices` 2.20/2.18, `pnl` 2.27/2.26, `compliance` 1.14/1.13,
`systemisation` 1.6/1.0, `crm` 1.1/1.0, `prospecting` 1.10/1.3, `sitemap` 1.19/1.2,
`skills` 1.4/1.0.

## 4. Health Check Results

Run against the live site (`https://chaichoong.github.io/leadership-dashboard/`) in Kevin's
authenticated Chrome, not a preview server — the route `20260809-drift-030` recommends. It
worked; `javascript_tool` was not blocked on this path. No localStorage value was read,
logged or returned.

| Check | Result | Evidence |
|---|---|---|
| **4a** Badge consistency | **PASS** | Recounted from `detectCFVs()` through the same `cfvIsVisible` filter the render path uses (`js/cfv.js:454`, `:508`). 5 raw entries, 5 visible: 3 confirmed + 0 potential = red badge 3 (rendered 3); 2 actioned = orange badge 2 (rendered 2). Match on both. Sitemap badge 23 also matches its own formula (`js/sitemap.js:229-245`, `gitSyncData` not loaded so the declared-version fallback applies) |
| **4b** Token/auth health | **PASS** | A PAT is present and non-empty. Value not read |
| **4c** Stale data | **PASS** | `#lastUpdated` read 07:16 against a wall clock of 07:16. 8,933 transactions, 94 tenancies, 180 costs loaded |
| **4d** Dismissal-key integrity | **PASS on the date half, VACUOUS on the sentinel half** | 110 `cfv_*` keys checked by shape. Date-shaped (`cfv_dismissed_*`, `cfv_reflag_dismissed_*`, `*_chaseStart`): 110 checked, 110 valid, 0 bad. Sentinel-shaped (`*_returned`, `*_returnDismissed`): **0 keys exist**, so that half asserted nothing. All 55 `_chaseStart` keys carry the `cfv_` prefix, so none escaped the check |

The vacuous sentinel half is expected and legitimate — no tenancy is currently in the returned
state, so there is nothing to assert. Noted rather than filed; `20260809-drift-030` already
covers rewriting CHECK 4, and calls out exactly this.

## 5. Actions Taken

### Auto-fixed
None. Rule 2 forbids it, and the caller's override repeats the prohibition. No repo file was
edited, no branch created, no commit, no push, no PR.

### Filed to the findings queue

| ID | Sev | Title |
|---|---|---|
| `20260812-drift-095` | high | Prospecting send path has no entity check: a non-Ltd prospect can be cold-emailed despite the "hard rule" |
| `20260812-drift-096` | high | PECR health check is blind to `Contacted (1:1)`, so the gate stays green on a breach |
| `20260812-drift-097` | high | SOP and `index.html` both say "nothing sends"; approving emails the prospect immediately |
| `20260812-drift-098` | high | SOP still documents the retired 3-email nurture sequence and the retired tag |
| `20260812-drift-099` | high | SOP status flow omits `Contacted (1:1)` and invents `In Sequence` |
| `20260812-drift-100` | medium | SOP points at the wrong credential store for the browser sync |
| `20260812-drift-101` | medium | SOP puts the PECR footer in GHL templates; the code builds it from `OD_SENDER` |
| `20260812-drift-102` | high | Task drawer Delete destroys the record with no confirmation; the confirm modal is dead code |
| `20260812-drift-103` | high | Assigning or completing a task Slack-DMs people, and assigning adds a collaborator |
| `20260812-drift-104` | high | Complete does not complete a recurring task; the guide never mentions recurring |
| `20260812-drift-106` | high | `guides/team.html` tells Kevin to edit profiles that are read-only |
| `20260812-drift-107` | medium | `+ Add Task` writes an "(Untitled)" record before you type; only Cancel removes it |
| `20260812-drift-108` | medium | `guides/tasks.html` names a "Weeks ahead" view that does not exist and omits 7 of 9 tabs |

### Escalated to human review (Airtable tasks)
None created. Under Rule 2 the findings queue is the route for everything here, and none of
today's items is a Kevin-decision item that the queue cannot carry.

## 6. What this run could not do

- **Regenerate `monitoring/reference-map.json`.** It is a tracked repo file and this phase is
  read-only. The gap grew from 22 to 28 IDs in three days, so CHECK 2's coverage claim keeps
  degrading. `20260809-drift-031` carries the fix.
- **Fix anything.** By design.
