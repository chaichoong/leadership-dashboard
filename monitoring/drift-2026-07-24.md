# Drift Report — 2026-07-24

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (117 tables / 4,867 fields, identical to 2026-07-23)
- Field references: 0 dead, 0 type mismatches, 52 rogue in `js/`, 121 rogue in `os/` + root HTML (chronic, all resolve to real fields)
- SOP drift: 0 new stale findings, 11 version gaps, 14 pages with no SOP at all
- Health checks: 7 pass, 0 warn, 0 fail (4 browser checks not runnable headless)
- Auto-fixed: 0 code items. `monitoring/reference-map.json` already in exact sync with `js/config.js` — date stamp only
- Human review: 0 new tasks. 1 carried over (`recbnIfmyoN3Idzgw`, now 6 days overdue)

Quiet run. Airtable is unchanged for a second consecutive day. Every repo change since the last
run belongs to the Supabase shadow build or to docs/monitoring, and none of it is covered by an
SOP in the mapping table.

## 1. Schema Changes

**None.** Snapshot `monitoring/schema-2026-07-24.json` is field-for-field identical to
`schema-2026-07-23.json`: same 117 tables, same 4,867 fields, no renames, no type changes, no
additions, no removals.

Against `schema-baseline.json` the accumulated historical delta is unchanged from yesterday
(22 tables added, 21 fields added, 1 table renamed, 0 removals, 0 type changes). That is the
long-run growth of the base since the baseline was taken, not new drift.

## 2. Field Reference Issues

### DEAD: 0
All 403 field IDs and 34 table IDs in `js/config.js` exist in today's Airtable schema. Every one
of the 173 rogue IDs elsewhere in the codebase also resolves to a live field. Nothing has been
deleted upstream.

### TYPE_MISMATCH: 0
No field changed type between yesterday's snapshot and today's, and none changed against the
baseline either. No code path can have been silently re-typed under it.

### ROGUE: 52 unique in `js/`, 121 unique in `os/` + root HTML
Hardcoded `fld…` IDs that bypass `js/config.js`. Unchanged from yesterday. The concentration:

| File | Rogue IDs | What they are |
|---|---|---|
| [js/dashboard.js](js/dashboard.js) | 21 | The Projects table field set |
| [js/skills-data.js](js/skills-data.js) | 18 | Tenancies/Costs/Tasks fields used by skill definitions |
| [js/sitemap.js](js/sitemap.js) | 6 | SOP Update Requests table |
| [js/arrears.js](js/arrears.js) | 4 | Tasks link fields |
| [js/fintable.js](js/fintable.js) | 3 | Accounts `**Institution`, `**Fintable User` |
| [js/costs.js](js/costs.js) | 1 | Accounts `*Name` |

(Yesterday's table read 20/2 for `dashboard.js`/`fintable.js`; the unique total was and is 52,
because one ID appears in both files. Same IDs, different attribution, no new rogue references.)

The `os/` + root figure of 121 excludes `os/operations/ops-schema.json`, a schema data dump
rather than executable code.

Still not auto-fixed, consistent with standing policy: moving 173 IDs into `config.js` touches a
shared file across many features and is a reviewed refactor, not an unattended one.

### ORPHAN
Not separately escalated, per prior runs. 4,867 Airtable fields against 403 referenced ones makes
a raw "unreferenced field" count noise rather than signal — most of the base is driven by
Airtable interfaces and automations, not by this repo.

## 3. SOP Drift

Ten commits landed since the previous run (`29d94dc`..`fbc6dcb`), touching 15 files:
the Supabase multi-tenancy stress-test harness and its audit doc, four Supabase edge functions,
two SQL migrations, `skills-shim.js`, `skills-supabase.html`, `MASTER-PLAN.md`, `STRUCTURE.md`,
the prospect-daily skill file, and last run's own monitoring output.

No `js/` feature file changed. No SOP-mapped page changed. The Supabase twins and shims are the
parallel shadow build, which no SOP in the mapping table covers, and the `skills` page carries no
SOP at all. **No new STALE_SOP or UNDOCUMENTED findings.**

### VERSION_GAP — 11 pages

| Page | pageVer | sopVer | SOP file |
|---|---|---|---|
| overview | 2.43 | 2.9 | sop.html |
| os-strategy | 1.16 | 1.0 | os/strategy/sop.html |
| tasks | 1.112 | 1.1 | os/tasks/sop.html |
| cfv | 1.30 | 1.6 | sop-cfvs.html |
| invoices | 2.18 | 2.2 | sop-invoices.html |
| pnl | 2.25 | 2.12 | sop-pnl.html |
| comms | 2.53 | 2.4 | inbound-comms-sop.html |
| compliance | 1.11 | 1.1 | sop-compliance.html |
| os-bplan | 1.15 | 1.3 | os/business-plan-builder/sop.html |
| prospecting | 1.4 | 1.3 | sop-prospecting.html |
| sitemap | 1.17 | 1.2 | sop-sitemap.html |

Identical to yesterday. The pattern is structural: `pageVer` auto-bumps on every push, `sopVer`
is hand-bumped. Documentation lag, not a runtime risk. Not escalated, per prior policy.

**INFO — 14 pages carry no SOP at all** (`sopFile: ''`): money, wealth, income, ar-variable,
costs, transactions, operations, fintable, systemisation, os-team, content-machine, skills,
ai-brain, how-it-works. Unchanged.

## 4. Health Check Results

| # | Check | Result |
|---|---|---|
| 4a | JS syntax — all 27 `js/*.js` parse clean (`node --check`) | **PASS** |
| 4b | Live deploy `index.html` | **PASS** (HTTP 200) |
| 4c | Live deploy `js/config.js` | **PASS** (HTTP 200) |
| 4d | Live deploy `js/dashboard.js` | **PASS** (HTTP 200) |
| 4e | Airtable API auth (PAT valid, base reachable) | **PASS** (HTTP 200) |
| 4f | Model-ID guard (`scripts/check-model-ids.sh`) | **PASS** (exit 0) |
| 4g | Sync-invariant suite (`npm run test:sync`) | **PASS — 61/61 in 38.0s** |
| — | Badge count consistency | N/A — needs an authenticated browser session |
| — | `window.PAT` health | N/A — same |
| — | Stale-data timestamp | N/A — same |
| — | localStorage dismissal integrity | N/A — same |

The four browser checks are auth-gated (the dashboard needs an interactive Airtable-token login)
and cannot run in a headless scheduled context. Standing limitation, not a new failure.

## 5. Actions Taken

### Auto-fixed
No code changes, and no PR. Nothing dead to comment out; the rogue IDs are policy-excluded from
unattended fixes (§2).

`monitoring/reference-map.json` needed no content change this run. Its four ID sets were compared
element-by-element against the IDs currently in `js/config.js` and match exactly (403 fields,
34 tables, 19 records, 11 select choices, zero missing, zero stale). Only the `generated` date
and note were updated.

### Escalated to Human Review

**No new tasks this run.** Nothing crossed the bar: 0 dead references, 0 type mismatches, 0
health failures, 0 schema changes.

Carried over: Airtable task **recbnIfmyoN3Idzgw** — "Drift Monitor: [WARNING] sop.html section
12 documents reconciliation matching rules that no longer exist (says 33% tolerance, code uses
10%)". Status **To do**, due 2026-07-18, now **6 days overdue**. Re-checked today and still true:
[sop.html:631](sop.html:631) documents "Within **33%** of expected cost", while
[js/reconciliation.js:498](js/reconciliation.js:498) sets `COST_MATCH_TOLERANCE = 0.10`. A cost
between 10% and 33% of variance will not auto-match, but the SOP tells the reader it should.
Re-escalating would create a duplicate. It remains the oldest unaddressed drift item and the only
one on a money path.

---
*Working tree clean at run time apart from this run's own `monitoring/` output; run executed
against `HEAD` = `fbc6dcb` after `git pull origin main`.*
