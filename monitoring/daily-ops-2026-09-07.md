# Daily Ops — Monday 7 September 2026

Ran 07:06 to 07:30 (24 minutes). All five phases ran, in order, none skipped.

---

*Daily Ops, Monday 7 September.* 4 things broke.

*NEEDS YOU*

1. Say yes to one command and the whole fix queue starts moving again. Last night's
   fixes are written and waiting, including the one that restores your email sorting,
   but nothing can ship until the agent names on this Mac are brought in line with the
   names changed in the code on Saturday. The command is
   `python3 scripts/sync-scheduled-tasks.py --push`. It changes settings on your Mac,
   so it was not run without you.
2. The court order for Clifford Sinclair, £4,506 of costs, passed its deadline on
   3 June. That is 96 days ago and it is still open and unanswered.
3. Credit card payments were due on the 5th. Two days late.

*STUCK: 8*
- Respond to court order, Clifford Sinclair, £4,506 — deadline passed 96 days ago
- Update net worth statement — deadline passed 37 days ago
- Pest warning re-inspection, 18 Siddows Avenue — deadline passed 8 days ago

*BROKEN: 4 things*
Your email sorting has done nothing for four days, and the cause turned out not to be
Google. The video content job did not run for three nights. Three of the daily data
checks failed. And the fix queue cannot ship anything at all until item 1 above is done.

Everything else ran. The morning checks, the three daily task passes, the CEO huddle,
prospecting and the Sunday production sweep all ran and all finished cleanly.

---

## Phase 1 — Readiness

- `rantoday daily-ops` → exit 0, no end mark today, safe to start.
- `job-queue.py sweep` → exit 0, no lock held. Nothing was stranded behind a dead holder.
- `ready daily-ops` → ready first try, no retries needed.
- `mark daily-ops` → stamped 2026-09-07T06:06:43Z.
- `findings.py reopen --stale` → 0 findings reopened.
- `check-routines.py` → **exit 0**. 282 queue events, 26 routines known, 18 other jobs
  also used the queue. No unapproved job is stacking up.

Slot run counts in the window (no MISSED SLOT RUNS line):

| Slot | Ran | Expected |
|---|---|---|
| ceo-agent | 2 | 1 |
| inbound-triage | 3 | 3 |
| prod-sweep-weekly | 1 | 1 |
| prospecting | 1 | 1 |
| task-manager | 3 | 3 |

Every approved slot took its turn. Turning up is not the same as working — see phase 2.

## Phase 2 — Exceptions

**Drift scan (06:20)** — exit 0, CLEAN. 124 tables, 4,985 fields, 939 repo ids scanned.
No exceptions file written for 7 Sep. Nothing moved.

**Data invariants (06:40)** — exit 1. **16 PASS, 3 FAIL, 0 SKIP** across 19 checks.

1. `knock-back-only-parks-live-approvals` — 2 violations. Both "Sefton HMO licence fee"
   tasks are Completed but still carry Deferred Until 2026-09-27.
2. `rejections-record-why` — 5 violations. Rejections dated 1–3 Sep with no Verdict Reason.
3. `hard-deadline-passed-still-open` — 4 violations. Worst is `recZMDlT4l2lcwMhB`, the
   court order Clifford Sinclair M01CL745, £4,506 costs, due 3 Jun, still at Today.

No new Airtable tasks were raised for these. All three already have open tasks from
yesterday's sweep (`rec9tbFzPDia6Ww9o`, `rechXmRiqbCbDEOjz`, `recpcftSIXL8PjNhf`, all
Status Today). They are 24 hours old and undone, so they need Kevin, not another task.

**Drive auth (06:50)** — exit 0, HEALTHY. API 200, folder read back, local vault readable.

**Slot reports — what actually happened, not what arrived:**

- `task-manager` — 3 of 3, all exit 0.
- `ceo-agent`, `prospecting`, `prod-sweep-weekly` — all exit 0.
- `inbound-triage` — **FAILED all three runs** (08:22, 12:42, 16:21) on a Gmail 403
  quota error. Zero email triaged since 3 Sep, the fourth straight day. Already filed
  as `20260906-daily-ops-477`; it had not been picked up.
- `content-engine` — **did not run at all.** No row in `job-status.jsonl` for the whole
  24h window. `retry-deferred` exited 1 four times overnight declaring it STOPPED for
  2+ days.

Root cause of the content-engine outage found and filed as **`20260907-daily-ops-486`**
(high): `drive_ready()` in `scripts/job-queue.py` returns on the FIRST file it opens, so
one un-hydrated 1.6GB Insta360 `.exe` in the raw-video folder declared the entire Drive
mount dead. Verified read-only this morning — 14 files there, that file now reads fine,
so the gate cleared itself while the mechanism stayed broken.

Could not verify: whether the Gmail quota would reset today (first run is 09:00). Also
noted that the inbound-triage slot-results file recorded `ok:true` for a run whose email
lane was broken, so that health signal could not be trusted. Phase 4 fixed that half.

## Phase 3 — Calendar work

- **1st of month rent due dates** — not due (7th). Skipped, announced.
- **Post inbox daily check** — `post-inbox-absence.py` exit 0. Fresh: last post processed
  03 Sep 2026 (4 days ago), 7 documents archived.
- **Monday full post pass** — ran. STEP 0 returned exit 0 so carried on to STEP 1. Post
  Inbox root holds only `Processed/` and `Split/`; no unprocessed PDFs.
  **No new scanned post to process, and the pipe is being fed** (both halves reported
  together, per the 31 Aug rule).
- **Quarterly master prompt review** — not due (next 1 Oct). Skipped, announced.

## Phase 4 — Fix

Zero PRs were open at the start, so the drain was clear and one PR was opened.
**Main was never committed to.** Work was done in a worktree.

**PR #280** — "Queue fixer 2026-09-07: 3 findings — the Drive probe, and the Gmail lane
that lied". Three fixes, each back-tested by breaking the fix and watching the test go red.

1. **`20260907-daily-ops-486`** — `drive_ready()` returned on the first file `os.listdir`
   handed back, so a 1.6GB un-hydrated Insta360 `.exe` condemned the whole Drive mount
   and killed content-engine for three nights. Now tries every candidate, returns ready
   on the first that reads, probes smallest-first. `drive-auth-check.py` shares the
   function and is hardened too. 3 tests.

2. **`20260907-daily-ops-488` (NEW — the actual cause of the four-day email outage)** —
   Google uses one sentence, `Quota exceeded for quota metric`, for both its daily cap
   and its per-minute metric. The classifier treated both as fatal, so 3, 5 and 6 Sep
   each abandoned the email lane over a limit that refills in 60 seconds. Verified
   read-only at 07:20: Gmail answers normally (53 labels, 25 inbox matches).
   **Nothing was ever exhausted on Google's side. The classifier alone lost four days.**
   The metric name now decides; a per-minute hit waits 65s and retries, capped at 600s
   per run. 3 tests + 6 selftests.

3. **`20260907-daily-ops-487` (NEW — the `ok:true` half of 477)** — the pre-flight
   `labels` probe passes on a quota the full scan then dies on, so 6 Sep wrote `ok:true`
   twice while no digest has been written since 3 Sep. `cmd_scan` now stamps completion;
   a new `slot-verify` appends a superseding broken row after the agent. Both writer and
   grader collapse by (date, slot, lane). 4 tests + 8 selftests.

**PR #280 is OPEN. Gate RED. Nothing merged.** The three gate failures are unrelated to
this PR and pre-exist it — proven by stashing all the work and re-running clean, which
produced the same three failures. Browser suite passed 315/315.

Cause: PR #279 renamed the agents in the repo on 6 Sep, but the live
`~/.claude/scheduled-tasks/*/SKILL.md` files were never updated (56 diff lines, names
only). Filed as **`20260907-daily-ops-489`**: until `sync-scheduled-tasks.py --push`
runs, the fixer's gate is red on *every* PR and the drain is zero. That push writes to
Kevin's `~/.claude` config and changes which agent names the live routines route to, so
it was not run unattended.

**Findings closed honestly:**
- 486, 487, 488 → `pending --pr 280` (written, not shipped).
- 416 → `fixed` (Drive verdict HEALTHY today).
- 423 → `deferred` (part 1 shipped 25 Aug, part 2 is the approval gate = protected path).

Also noted: 486 asked for a DM when a job is STOPPED 2+ days. That is a system alert
retired in the 1 Sep Slack contract, so it was deliberately not built.

**Queue: 248 open at start, 3 filed, 5 closed → 246 open. The drain is the red gate,
not the cap.**

## Phase 5 — Report

`loop-health.py` exit 0. 403 tasks read, 119 agent-linked.
Needs Kevin: 45. Done in 7 days: 129. **NOT MOVING: 8.**

Full NOT MOVING list, in the order printed (not re-sorted by age):

1. INBOUND: respond to court order Clifford Sinclair M01CL745 £4,506 — hard deadline
   2026-06-03 passed 96 days ago, still open
2. Update net worth statement — hard deadline 2026-08-01 passed 37 days ago, still open
3. MAINTENANCE: pest warning re-inspection 18 Siddows Avenue Clitheroe — hard deadline
   2026-08-30 passed 8 days ago, still open
4. Credit Card Payments, payments due 5th of the month — hard deadline 2026-09-05
   passed 2 days ago, still open
5. Pay Burnley Council, CT Arrears, 22 Newton Street — waiting on decision 22 days
6. INBOUND: BW Legal, our response to your query — waiting on decision 19 days
7. INBOUND: BW Legal query resolution — waiting on decision 19 days
8. Prospecting deliverability check: the emails ARE landing (97.6%) — waiting on
   decision 13 days

No Slack DM sent, per the 1 Sep contract. This file is the record.
