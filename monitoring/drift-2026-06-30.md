# Drift Report — 2026-06-30

## Summary
- Schema changes: 1 new field (`Tasks.record_id`, formula), 0 removed, 0 type changes
- Field references: 0 dead, 0 orphaned (suppressed, see below), 0 new rogue
- SOP drift: 1 new undocumented behaviour (AP-Variable bulk Mark-as-Paid, on a no-SOP tab), 10 chronic version gaps, 13 SOP-coverage gaps
- Health checks: 2 pass (shell + dismissal-JSON), 3 not-verifiable (auth-gated headless)
- Auto-fixed: 0 items (nothing in the safe auto-fix scope)
- Human review: 0 new tasks (existing SOP-drift task `rec9ihFqDDSY8DMLZ` re-confirmed still "To do")

Near-clean run. One benign new Airtable field, one new feature behaviour folding under the existing SOP gap. Nothing broken, nothing to auto-fix.

## 1. Schema Changes

Live base `appnqjDpqDniH3IRl`: **110 tables, 4,789 fields.** Compared field-by-field against `monitoring/schema-2026-06-29.json` (4,788 fields).

- New tables: **none**
- Removed tables: **none**
- Added fields: **1** — `Tasks.record_id` (type `formula`)
- Removed fields: **none**
- Type changes: **none**
- Rename changes: **none**

`Tasks.record_id` is a new formula field added upstream in the Tasks table. It is **not referenced anywhere in code** (config.js field count unchanged at 368). No action: it is a brand-new live field, not a code dependency. Logged INFO only. If Kevin wants the JS to read it, it would need adding to `config.js` `F.*` and `reference-map.json`.

Snapshot saved to `monitoring/schema-2026-06-30.json`.

## 2. Field Reference Issues

Source of truth: `monitoring/reference-map.json` (368 fields, 31 tables, 20 records, 11 select choices). Live `config.js` ID counts match the map exactly (368 / 31 / 20 / 11), so the map is current — no regeneration needed.

- **DEAD (referenced in code, missing in Airtable): none.** All 368 mapped field IDs and 31 table IDs resolve to live Airtable fields.
- **TYPE_MISMATCH: none.** No type changes anywhere in the base vs yesterday, so no referenced field changed type.
- **ORPHAN: none reported.** Feature files read fields via the `F.*` constants, not raw `fldXXX` literals, so a raw-ID grep produces false orphans. Suppressed, consistent with prior runs.

### ROGUE (hardcoded IDs in feature js, bypassing config.js)
A raw `fld/tbl/rec/sel` grep across `js/*.js` (excluding `config.js`) surfaced **83 candidate IDs**. After classification against the live schema:

- **55 fld/tbl IDs resolve to live fields/tables** (53 fld + 2 tbl: `tblHGNzDmOs59r9QD` (Deprecated) Settings, `tbltuZz5Omrpo7t1x` SOP Update Requests). The pre-existing local field-map pattern (`getField()` in `dashboard.js`, doc-string template literals in `skills-data.js`, link-field PATCH payloads in `arrears.js`, etc.). All valid, zero broken references.
- **18 genuine record IDs** — admin/repair and link-field hardcodes (`admin-repair-splits.js` and a few in `cashflow.js` / `arrears.js` / `skills.js`). Expected, present since prior runs.
- **3 select-choice IDs** in `skills-data.js` (`sel5UTLLcZTdRVq6m`, `selx5WZMIWgrHrPOj`, `selzQhQoQQXe3DXMK`) — inside markdown doc-string template literals, not live reads.
- **7 false positives** — JS variable names matching the `rec…`/`sel…` regex: `recomputeCostFrom`, `reconForecastKeys`, `reconTenancyChang`, `reconciledInflows`, `reconciledOutflow`, `selectedBusinessI`, `selectedProjectId`. Not Airtable IDs.

No NEW rogue files or IDs since 2026-06-29 (55 fld/tbl + 18 rec + 3 sel — identical breakdown). This is the chronic local-field-map pattern logged INFO on every prior run. Not auto-fixed: consolidating into `config.js` would touch ~10 feature files plus shared `config.js` in one pass — beyond the safe unattended auto-fix scope and against the concurrent-edit rule in `CLAUDE.md`.

## 3. SOP Drift

**One real feature change on `main` since the 2026-06-29 run:** commit `6cfbac5` — "Fix: add bulk 'Mark as Paid' to Accounts Payable Variable tab" (+68 lines in `js/invoices.js`, +1 in `index.html`). Auto-bumped `invoices` `pageVer` 2.16 → 2.17.

### New undocumented behaviour (INFO)
The AP-Variable bulk "Mark as Paid" action is new and not documented in any SOP. The AP-Variable surface has **no SOP file** of its own (it falls under the 13 no-SOP tabs below), so this folds under the existing SOP-coverage gap rather than spawning a new task. The `invoices` tab's own `sop-invoices.html` (sopVer 2.2) is unaffected by this change.

### Version gaps (10, chronic)
Every page carrying a SOP file shows `pageVer` ahead of `sopVer`. Structural, not new: the auto-bump GitHub Action increments `pageVer` on every source push, while `sopVer` is bumped manually only when the SOP itself is rewritten.

| Page | pageVer | sopVer |
|------|---------|--------|
| overview (Leadership Dashboard) | 2.36 | 2.9 |
| os-strategy | 1.14 | 1.0 |
| tasks | 1.103 | 1.1 |
| cfv | 1.27 | 1.6 |
| invoices | 2.17 | 2.2 |
| pnl | 2.22 | 2.12 |
| comms | 2.51 | 2.4 |
| compliance | 1.11 | 1.1 |
| os-bplan | 1.12 | 1.3 |
| sitemap | 1.16 | 1.1 |

Only change vs 2026-06-29: `invoices` pageVer 2.16 → 2.17 (the AP-Variable fix). All other rows identical.

The deep behavioural audit (~30 stale/undocumented items across overview, cfv, invoices, pnl, comms, tasks) was performed 2026-06-11 and is tracked by open Airtable task `rec9ihFqDDSY8DMLZ` ("Drift Monitor: [WARNING] SOP docs behind code on 10 pages"), **re-confirmed still "To do"** this run via the official Airtable connector. The new AP-Variable behaviour is noted here and falls naturally within that task's scope. Not re-listed item-by-item to avoid noise.

### SOP coverage gap (INFO, no new task)
13 tabs carry no SOP at all (`sopFile: ''` in PAGE_REGISTRY): money, wealth, income (AR Fixed), ar-variable, costs (AP Fixed), transactions, operations, fintable, systemisation, os-team, content-machine, skills, how-it-works. Whether each needs a published SOP is Kevin's call. Folded under the existing SOP-drift task.

## 4. Health Check Results

Static server (`ops-director`, port 8811) launched via preview; `index.html` loaded headless.

| Check | Result | Detail |
|-------|--------|--------|
| App shell load | **PASS** | Title "Operations Director", sidebar, and `PAGE_REGISTRY` (23 entries) all present. `config.js` + `shared.js` parse and execute (`escHtml`, `switchTab` both `function`) with **zero console errors** |
| 4a Badge / `allTransactions` consistency | NOT VERIFIABLE | `allTransactions` is 0 headless — data load is PAT-gated, no token in headless context |
| 4b Token / auth health | NOT VERIFIABLE | `PAT` absent headless — auth runs only in the live browser session |
| 4c Stale-data timestamp | NOT VERIFIABLE | No dashboard load occurs without auth, so no refresh timestamp to read |
| 4d localStorage dismissal-JSON validity | **PASS** | 0 corrupt keys matching `dismissed_` / `cfv_` prefixes — all parse as valid JSON |

The three NOT-VERIFIABLE rows are the expected headless limitation (auth-gated), not failures, and are consistent with every prior run.

## 5. Actions Taken

### Auto-fixed
None. No dead references, no new rogue IDs, no simple renames in scope. The new `Tasks.record_id` field requires no code change (code does not reference it). Nothing met the safe unattended auto-fix bar.

### Escalated to Human Review
No new Airtable task created. The only review-worthy items are chronic and already tracked:
- **SOP drift** (10 version gaps + 13 coverage gaps + new AP-Variable undocumented behaviour) → existing open task `rec9ihFqDDSY8DMLZ`, re-confirmed "To do" this run.

No new escalation needed.
