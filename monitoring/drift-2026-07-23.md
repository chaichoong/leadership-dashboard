# Drift Report — 2026-07-23

## Summary
- Schema changes: 0 new, 0 removed, 0 type changes (117 tables / 4,867 fields, identical to 2026-07-22)
- Field references: 0 dead, 0 type mismatches, 52 rogue in `js/`, 121 rogue in `os/` + root HTML (chronic, all resolve to real fields)
- SOP drift: 0 new stale findings, 11 version gaps, 14 pages with no SOP at all
- Health checks: 7 pass, 0 warn, 0 fail (4 browser checks not runnable headless)
- Auto-fixed: 0 code items. `monitoring/reference-map.json` refreshed (monitoring artefact only)
- Human review: 0 new tasks. 1 carried over (`recbnIfmyoN3Idzgw`, now 5 days overdue)

Quiet run. Nothing changed in Airtable, and only two files changed in the repo since the last
run, neither of which is covered by an SOP.

## 1. Schema Changes

**None.** Snapshot `monitoring/schema-2026-07-23.json` is field-for-field identical to
`schema-2026-07-22.json`: same 117 tables, same 4,867 fields, no renames, no type changes, no
additions, no removals.

## 2. Field Reference Issues

### DEAD: 0
All 403 field IDs and 34 table IDs in `js/config.js` exist in today's Airtable schema. Every one
of the 52 rogue IDs in `js/` also resolves to a live field. Nothing has been deleted upstream.

### TYPE_MISMATCH: 0
No field changed type between yesterday's snapshot and today's, so no code path can have been
silently re-typed under it.

### ROGUE: 52 in `js/`, 121 in `os/` + root HTML
Hardcoded `fld…` IDs that bypass `js/config.js`. Unchanged from yesterday in `js/` (52, same
IDs). The concentration:

| File | Rogue IDs | What they are |
|---|---|---|
| [js/skills-data.js](js/skills-data.js) | 19 | Tenancies/Costs/Tasks fields used by skill definitions |
| [js/dashboard.js](js/dashboard.js) | 20 | The whole Projects table field set ([js/dashboard.js:112](js/dashboard.js:112)–[130](js/dashboard.js:130)) |
| [js/sitemap.js](js/sitemap.js) | 6 | SOP Update Requests table |
| [js/arrears.js](js/arrears.js) | 4 | Tasks link fields |
| [js/fintable.js](js/fintable.js) | 2 | Accounts `**Institution`, `**Fintable User` |
| [js/costs.js](js/costs.js) | 1 | Accounts `*Name` |

The `os/` figure of 121 excludes `os/operations/ops-schema.json`, which holds a further 641 IDs
but is a schema data dump, not executable code. (Yesterday's report quoted 78 for `os/`; that
count covered a narrower file set. Today's sweep includes `os/**` plus root-level HTML and the
Supabase twins/shims, which is the honest denominator. The underlying code did not gain 43 rogue
IDs overnight.)

Still not auto-fixed, consistent with standing policy: moving 173 IDs into `config.js` touches a
shared file across many features and is a reviewed refactor, not an unattended one.

### ORPHAN
Not separately escalated. The `js/config.js` reference set and the live schema agree completely,
and 4,867 Airtable fields against 403 referenced ones makes a raw "unreferenced field" count
noise rather than signal — most of the base is used by Airtable interfaces and automations, not
by this repo.

## 3. SOP Drift

Only two files changed since the previous run (`6a4958e`..`HEAD`):
`os/tasks/index-supabase.html` (+4/-1) and `supabase-app.html` (+15/-2). Both belong to the
parallel Supabase shadow build, which no SOP in the mapping table covers. No js/ feature file,
and no SOP-mapped page, changed. **No new STALE_SOP or UNDOCUMENTED findings.**

### VERSION_GAP — 11 pages

| Page | pageVer | sopVer | SOP file |
|---|---|---|---|
| overview | 2.43 | 2.9 | sop.html |
| os-strategy | 1.16 | 1.0 | os/strategy/sop.html |
| tasks | 1.112 | 1.1 | os/tasks/sop.html |
| cfv | 1.30 | 1.6 | sop-cfvs.html |
| invoices | 2.18 | 2.2 | sop-invoices.html |
| pnl | 2.25 | 2.12 | sop-pnl.html |
| comms | 2.53 | 2.4 | inbound-comms-sop.html |
| compliance | 1.11 | 1.1 | sop-compliance.html |
| os-bplan | 1.15 | 1.3 | os/business-plan-builder/sop.html |
| prospecting | 1.4 | 1.3 | sop-prospecting.html |
| sitemap | 1.17 | 1.2 | sop-sitemap.html |

Identical to yesterday. The pattern is structural: `pageVer` auto-bumps on every push,
`sopVer` is hand-bumped. Documentation lag, not a runtime risk. Not escalated, per prior policy.

**INFO — 14 pages carry no SOP at all** (`sopFile: ''`): money, wealth, income, ar-variable,
costs, transactions, operations, fintable, systemisation, os-team, content-machine, skills,
ai-brain, how-it-works. Yesterday's report listed 12; content-machine and ai-brain were omitted
there. The registry entries themselves are unchanged, so this is a reporting correction, not new
drift.

## 4. Health Check Results

| # | Check | Result |
|---|---|---|
| 4a | JS syntax — all 27 `js/*.js` parse clean (`node --check`) | **PASS** |
| 4b | Live deploy `index.html` | **PASS** (HTTP 200) |
| 4c | Live deploy `js/config.js` | **PASS** (HTTP 200) |
| 4d | Live deploy `js/dashboard.js` | **PASS** (HTTP 200) |
| 4e | Airtable API auth (PAT valid, base reachable) | **PASS** (HTTP 200) |
| 4f | Model-ID guard (`scripts/check-model-ids.sh`) | **PASS** (exit 0) |
| 4g | Sync-invariant suite (`npm run test:sync`) | **PASS — 61/61 in 38.0s** |
| — | Badge count consistency | N/A — needs an authenticated browser session |
| — | `window.PAT` health | N/A — same |
| — | Stale-data timestamp | N/A — same |
| — | localStorage dismissal integrity | N/A — same |

The four browser checks are auth-gated (the dashboard needs an interactive Airtable-token login)
and cannot run in a headless scheduled context. Standing limitation, not a new failure.

## 5. Actions Taken

### Auto-fixed
No code changes, and no PR. Nothing dead to comment out; the rogue IDs are policy-excluded from
unattended fixes (§2).

`monitoring/reference-map.json` was refreshed against `js/config.js`. Three corrections, all to
the monitoring artefact, none to app code:
- Added `tblWtiyQikO2hOzeW`, flagged as a **comment-only** reference. It appears at
  [js/config.js:70](js/config.js:70) inside a warning that reads: the legacy Net Worth Statement
  table is orphaned, last written Jan 2026, read by nothing, and caused a false six-month gap
  report on 2026-07-21. It is documented so nobody reads it. It is not a live reference.
- Added `recl1UbR0LhffjWbg` (Insurance / Needs money-group sub-category,
  [js/config.js:273](js/config.js:273)).
- Dropped `recS1AiGq8oDEzmZD` and `recEvGF5Sr8R9p6tC`, no longer referenced in `config.js`.
- Corrected the stale `summary.fields` count (read 401, actual 403).

### Escalated to Human Review

**No new tasks this run.** Nothing crossed the bar: 0 dead references, 0 type mismatches, 0
health failures, 0 schema changes.

Carried over: Airtable task **recbnIfmyoN3Idzgw** — "Drift Monitor: [WARNING] sop.html section
12 documents reconciliation matching rules that no longer exist (says 33% tolerance, code uses
10%)". Status **To do**, due 2026-07-18, now **5 days overdue**. Re-escalating would create a
duplicate. It remains the oldest unaddressed drift item and the only one on a money path.

---
*Working tree clean at run time apart from this run's own `monitoring/` output; run executed
against `HEAD` = `29d94dc` after `git pull origin main` (already up to date).*
